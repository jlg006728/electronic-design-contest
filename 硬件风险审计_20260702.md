# 硬件风险审计 2026-07-02

本文件用于后续接手同学快速避坑。判断方法沿用这次 `PA2~PA6` 排查思路：**芯片支持不等于开发板排针可用；空载电压正常不等于带载稳定；代码变量变化不等于物理脚真的输出到模块。**

## 资料依据

- 本地记录：`项目记忆.md`、`过程记录.md`、`接线步骤.md`、`硬件接口图.md`、`知识库/调试复盘_判断失误记录.md`、`器件采购清单.md`。
- TI LP-MSPM0G3507 官方用户指南/原理图：<https://www.ti.com/lit/pdf/slau873>
- TI MSPM0G3507 官方数据手册：<https://www.ti.com/lit/ds/symlink/mspm0g3507.pdf>
- TI MSPM0 G-Series Hardware Development Guide：<https://www.ti.com/lit/pdf/slaae76>
- Toshiba TB6612FNG 官方数据手册：<https://toshiba.semicon-storage.com/info/docget.jsp?did=10660&prodName=TB6612FNG>
- OpenMV H7 官方文档：<https://docs.openmv.io/openmvcam/quickref/openmv-cam-h7.html>

## 高危问题：可能烧板、烧模块、无法烧录

### 1. `PA2~PA6` 不能再按普通排针 GPIO 使用

现象：
- GPIO 诊断程序变量在跑，但 PA3/PA4 排针测不到 3.3V。
- `3V3` 与 `PA2~PA6` 测量现象和普通 GPIO 不一样。

原因：
- 芯片层面 `PA2~PA6` 确实有 GPIO/IOMUX 功能。
- 但 LP-MSPM0G3507 板级原理图显示 `PA2-/PA3-/PA4-/PA5-/PA6-` 到 MCU 脚之间经过 `R9/R10/R11/R15/R17`，默认 `DNC`。
- MCU 端还承担关键时钟/校准功能：`PA2=ROSC`，`PA3/PA4=LFXT`，`PA5/PA6=HFXT`。

规避：
- 后续默认禁用 `PA2/PA3/PA4/PA5/PA6`。
- 不再把 `PA3/PA4` 作为舵机信号脚。
- 若必须使用，需要焊接对应隔离电阻并重新验证晶振/时钟影响，这对当前比赛不值得。

验证：
- 任何新候选 GPIO 先烧“常高/常低慢速诊断程序”，用万用表测物理排针，而不是直接接模块。

### 2. MSPM0 普通 GPIO 不能承受 5V 信号

现象：
- 模块能亮、能输出，但 MSPM0 输入脚异常、发热、读数锁死。

原因：
- MSPM0G3507 推荐工作电压是 `1.62V~3.6V`。
- 数据手册绝对最大值中，普通 IO 输入最高为 `VDD + 0.3V`，只有 5V tolerant open-drain 脚例外。
- `PA0/PA1` 是 5V tolerant open-drain；绝大多数当前用到的 `PBx/PA7/PA8/PA10/PA11/PA12/PA13/PA15` 不是 5V 容忍普通输入。

规避：
- TCRT5000、编码器、OpenMV 与 MSPM0 直连的信号必须是 3.3V 逻辑。
- TCRT5000/编码器如果 VCC 接 5V，DO/A/B 输出也可能到 5V，不能直接进普通 MSPM0 GPIO。
- 目前巡线和编码器继续优先用 3.3V 供电。

验证：
- 接 MCU 前先用万用表测模块信号线对 GND 的高电平：必须约 3.3V，不能是 5V。

### 3. `PA20` 不建议作为激光正式控制脚

现象：
- 激光接上后能亮，但 CCS 调试/烧录异常，或断开 USB 后行为变化。

原因：
- `PA20` 是 `SWCLK`，`PA19` 是 `SWDIO`，用于调试/烧录。
- SDK README 明确：如果应用要用 `PA20`，需断开 `J101 15:16`；如果用 `PA19`，需断开 `J101 13:14`。

规避：
- 不把 `PA19/PA20` 分配给普通外设，尤其不要给激光。
- 激光控制另选已经确认排针可达、无冲突的 GPIO。
- 两线激光模块不能直接由 GPIO 供电；只有带 `TTL/EN/S` 控制脚的模块才允许 GPIO 控制使能。

验证：
- 激光模块先确认脚位类型：`VCC/GND` 两线为电源模块，不是 TTL 控制模块。

### 4. OpenMV 供电不能乱叠加

现象：
- OpenMV 偶发重启、发热、USB 识别不稳定。

原因：
- OpenMV H7 的 `VIN` 是 `3.6V~5V` 输入；官方说明 `VIN/LiPo` 和 USB 同时存在时由 VIN/LiPo 供电。
- 官方警告：不要同时插 LiPo 和 VIN；如果从 `3.3V` 直接供电，不要同时接 VIN 或 USB，否则可能反灌损坏。

规避：
- 推荐：OpenMV 用 `VIN=5V` 或 USB 二选一作为主供电，所有 GND 共地。
- 不直接从 MSPM0 `3.3V` 给 OpenMV 供电。
- 调试时如果 OpenMV USB 接电脑，同时外接 VIN，要确认没有再接 LiPo。

验证：
- OpenMV 上电后先单独在 IDE 中跑脚本，再接 MSPM0 UART。

### 5. 电源带载能力必须实测，空载电压没有说服力

现象：
- MP1584 空载 5.6V/5.8V，但接舵机后掉到 3.7V~4.4V。
- 舵机只咔哒、OpenMV 重启、MSPM0 发烫或整车复位。

原因：
- 舵机启动/堵转瞬间电流很大，面包板、杜邦线、虚焊、小降压模块都会造成压降。
- 用户现场已经验证：空载输出正常不代表带单舵机正常。

规避：
- 任何 5V 电源都按三步测：空载、接单舵机红黑线、舵机动作瞬间。
- 单舵机动作时 5V 不应明显跌破约 4.7V；双舵机+OpenMV 前必须再测一次。
- TB6612 稳压板 5V 目前可作为单舵机测试电源；是否能长期带双舵机+OpenMV，仍要带载验证。

验证：
- 万用表表笔直接压在模块输出端子上测，不只测面包板远端。

### 6. TB6612 输入悬空会默认无动作

现象：
- 电机不转，但方向脚看似接了，程序也在跑。

原因：
- Toshiba 数据手册显示 `PWMA/PWMB/AIN/BIN/STBY` 输入内部有下拉；`STBY=L` 进入 standby。
- `PWMA/PWMB` 没有有效高电平/PWM 时，电机等效停。

规避：
- 电机固定动作验证必须同时满足：`STBY=HIGH`，`PWM=HIGH/PWM`，方向脚一高一低。
- 不能把 `AIN1=AIN2=HIGH` 当作转动状态，那是 short brake。

验证：
- 手动硬件验证仍是黄金标准：`STBY/PWMA/PWMB` 拉 3.3V，单通道方向一高一低。

### 7. 电机反电动势和供电噪声会造成复位/误判

现象：
- 电机启动、急停、直角弯时 MCU 复位、OpenMV 重启、红外读数乱跳。

原因：
- TB6612 数据手册提醒电机反电动势可能把电机电源和输出脚推到异常条件。
- 官方典型应用图要求电源去耦电容尽量靠近 IC。

规避：
- TB6612 `VM-GND` 近端并 100uF~1000uF 电解 + 0.1uF 陶瓷。
- 舵机/OpenMV 5V 也要近端并电容。
- 功率地和信号地最终共地，但走线尽量星形汇合，避免电机电流流过 OpenMV/红外地线。

验证：
- 电机急加速时测 MSPM0 3.3V、OpenMV VIN、舵机 5V 是否瞬间塌陷。

### 8. 正负接反或 7.4V 误入 GPIO/3V3 是最高风险

现象：
- 芯片发烫、USB 断连、模块冒热、万用表读数异常。

原因：
- TB6612、MSPM0、OpenMV 都有明确的最大电压限制；Toshiba 数据手册也强调正负接反会导致超过绝对最大额定值。

规避：
- 每次大改线断电操作。
- 上电前测 `7.4V-GND`、`5V-GND`、`3.3V-GND` 是否短路。
- 7.4V 只进 TB6612 `VM` 或降压模块输入，不进 MSPM0、OpenMV GPIO、传感器信号脚。

验证：
- 先不插 USB，只接电池测稳压输出；再插 USB；最后接信号线。

## 中危问题：功能不通、调试严重走弯路

### 9. 当前 `empty.c` 不是循迹正式代码

现象：
- 烧录后车不循迹，只动舵机或电机被禁用。

原因：
- 当前活动工程 `C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c` 是临时 PA12 单舵机测试版。
- 稳定循迹基线在 `C:\Users\1\Desktop\电赛\ccs_project\line_sensor_test\line_follow_sharp_turn_stable_baseline.c`。

规避：
- 每次烧录前看文件头部注释。
- 凡是新生成代码，都保存日期时间命名文件，不只覆盖 `empty.c`。

### 10. `PA12` 舵机成功不代表最终云台方案成立

现象：
- 单舵机能转，但恢复电机后冲突。

原因：
- `PA12` 是 TB6612 `PWMA`，正式循迹必须占用。

规避：
- `PA12` 只用于证明舵机、电源、信号线没问题。
- 最终双舵机要重新找两个可达、无冲突 GPIO；先常高/常低测，再写 PWM。

### 11. 文档中仍可能有旧 PA3/PA4 残留

现象：
- 接手同学按旧图把舵机接回 PA3/PA4，舵机不动。

原因：
- 早期方案只看芯片 IOMUX，没有核对 LaunchPad 原理图。

规避：
- 以后以本文件和 `项目记忆.md` 最新条目为准。
- 遇到旧表格写 `PA3/PA4 -> 舵机`，直接视为废弃方案。

### 12. OpenMV UART0 会和 XDS110 backchannel 冲突

现象：
- MSPM0 和 OpenMV 串口没数据，或调试串口和 OpenMV 同时乱。

原因：
- `PA10/PA11` 可通过 `J21/J22` 接 XDS110 backchannel，也可接 BoosterPack 排针。
- SDK README 说明：BoosterPack 外部 UART 需要 `J21 2:3`、`J22 2:3`；XDS110 虚拟串口为 `1:2`。

规避：
- OpenMV 联调时：`J21/J22` 切到外部 BoosterPack，或拔掉与 XDS110 冲突的跳帽。
- 不同时使用 UART0 printf 和 OpenMV 通信。

验证：
- 先只让 OpenMV 自发固定帧，用逻辑上最简单的 MSPM0 接收程序验证。

### 13. OpenMV P4/P5 方向容易写反

正确接线：
- `MSPM0 PA10 / UART0_TX -> OpenMV P5 / UART3_RX`
- `MSPM0 PA11 / UART0_RX <- OpenMV P4 / UART3_TX`

原因：
- OpenMV 官方文档：UART3 的 `TX=P4`、`RX=P5`。

规避：
- 永远按 `TX -> RX` 交叉核对，不按“P4/P5 顺序”记忆。

### 14. `PA0/PA1` 不是普通推挽输出

现象：
- 程序输出 HIGH，但外部测不到高电平。

原因：
- `PA0/PA1` 是 5V tolerant open-drain，只有低侧 NMOS；输出高需要外部/跳帽上拉。
- TI 硬件指南说明 open-drain 要外部上拉才能输出高。

规避：
- 不把 `PA0/PA1` 当普通舵机/LED/使能输出。
- 若后续加 MPU6050，`PA0/PA1` 反而适合做 I2C0，但要确认 `J19/J20` 上拉选择、`J4` LED 连接状态。

### 15. MPU6050 接 PB2/PB3 会和电机方向冲突

现象：
- 加了 MPU6050 后电机方向异常，或 I2C 没数据。

原因：
- 当前 `PB2/PB3` 已用于 TB6612 `BIN1/BIN2`。

规避：
- 短期不接 MPU6050。
- 若必须接，优先重新评估 `PA0/PA1` I2C0，而不是占用 `PB2/PB3`。

### 16. `PA18/S1` 可能触发 BSL

现象：
- 按键或上电时板子不运行用户程序，像“烧录没成功”。

原因：
- LP-MSPM0G3507 用户指南中 `J8/S1` 是 BSL invoke button，关联 `PA18`。

规避：
- 外接启动/模式按键继续使用 `PB11/PB8`，不要用 `PA18`。

### 17. 参考代码不能直接照搬引脚和电机参数

现象：
- 编译能过但电机方向反、PWM 反、编码器距离不对。

原因：
- 老师参考代码面向另一套底盘/灰度/电机驱动。
- 当前车已经实测：TIMG0 compare 与逻辑占空比反向，编码器左右方向也不同。

规避：
- 参考代码只借状态机思想，不照搬 `config.h`。
- 所有引脚必须过“当前整车占用表 + LaunchPad 原理图 + 常高/常低诊断”三重检查。

### 18. 中文路径会导致 CCS/SysConfig 找不到文件

现象：
- SysConfig 报 `empty.syscfg does not exist`，路径里中文显示乱码。

原因：
- Windows/CCS/SysConfig 路径编码问题。

规避：
- CCS 工程继续放在英文路径：`C:\Users\1\Desktop\ELECTRIC\ele-car`。
- 版本备份可以放中文项目目录，但实际编译工程尽量英文路径。

## 低危但高频误判问题

### 19. CCS Debug 停在 `main()` 不是程序没跑

规避：
- 点调试后还要点绿色运行三角形。
- 真车离线不能 Watch，代码应提供 LED 状态码或固定动作现象。

### 20. PWM 用万用表测不到稳定 3.3V 不等于没信号

规避：
- 万用表测 PWM 只会看到平均电压。
- 判断舵机信号脚是否可达，先用“常高/常低 GPIO”程序，不用 PWM 程序。

### 21. 红外黑白都读 0 不一定是模块坏

可能原因：
- 模块没供电。
- 电位器阈值没调好。
- 供电电压不对。
- DO 输出逻辑与代码取反不一致。
- 传感器离地高度太高/太低。

规避：
- 逐个模块万用表测 `DO-GND`，先不接 MCU。
- 7 个模块阈值调到同一逻辑：当前目标为黑线 HIGH、白底 LOW。

### 22. 数字 TCRT5000 对直线稳定性有天然限制

现象：
- 直线段细小左右折线，出弯后中心锁定不够稳。

原因：
- 7 路数字 DO 是离散跳变，不是连续灰度。

规避：
- 当前先通过算法压抖；若后续打靶要求更高，考虑切到 AO 模拟采样，但这会重新分配 ADC 引脚和代码。

### 23. 机械安装会伪装成算法问题

检查项：
- 7 个红外是否在同一直线，S4 是否正对车体中心。
- 传感器距地是否约 3mm~5mm。
- 万向轮是否偏斜或卡滞。
- 两轮轮胎是否打滑、直径是否差异大。
- 云台舵机是否顶到机械限位。

### 24. 电池电压要分空载和带载

规避：
- 2S 电池空载 `7.4V` 只能说明没完全没电。
- 电机/舵机动作时跌落才决定能否稳定运行。
- 现场若看到电池端电压从几伏快速掉到 1V 以下，先怀疑保护板动作、短路或接触电阻，不继续反复上电。

### 25. 面包板和杜邦线在舵机/电机电流下不可靠

规避：
- 电机、舵机、电源主线尽量不用面包板承载大电流。
- 供电线用较短、较粗、可靠焊接或端子连接。
- 面包板只做低电流信号分配。

## 后续接线前的固定检查流程

1. 查芯片数据手册：该脚是否支持目标功能、是否 5V tolerant、是否 open-drain。
2. 查 LaunchPad 用户指南/原理图：该脚是否真正接到可插排针，是否经过 `DNC`、跳帽、板载 LED/按键/晶振。
3. 查当前整车占用表：是否已经被电机、编码器、巡线、UART、按键占用。
4. 烧常高/常低程序：万用表确认物理排针能输出 0V/3.3V。
5. 不接负载测信号，再接模块信号，最后接完整电源。
6. 带载测电压：电机启动、舵机动作、OpenMV 运行时都要测。

## 当前禁止/谨慎引脚表

| 引脚 | 结论 | 原因 |
|---|---|---|
| `PA2` | 禁用 | ROSC，LaunchPad 侧默认 DNC/校准电阻相关 |
| `PA3` | 禁用 | LFXIN，默认 DNC，旧舵机方案作废 |
| `PA4` | 禁用 | LFXOUT，默认 DNC，旧舵机方案作废 |
| `PA5` | 禁用 | HFXIN，默认 DNC/晶振 |
| `PA6` | 禁用 | HFXOUT，默认 DNC/晶振 |
| `PA12` | 临时可用，不作最终舵机 | 正式为 TB6612 `PWMA` |
| `PA13` | 不作舵机 | 正式为 TB6612 `PWMB` |
| `PA18` | 不作外部按键 | BSL invoke / 板载 S1 |
| `PA19` | 禁用普通外设 | SWDIO 调试脚 |
| `PA20` | 禁用普通外设 | SWCLK 调试脚，旧激光方案需作废 |
| `PA0/PA1` | 谨慎使用 | 5V tolerant open-drain，需要上拉；适合 I2C，不适合普通推挽输出 |
| `PB2/PB3` | 不接 I2C/MPU6050 | 当前是 TB6612 右电机方向脚 |
| `PB4/PB5` | 不接 UART1 | 当前分别是 TB6612 `STBY`、巡线 S1 |

## 当前优先下一步

舵机已能正常工作，下一步不要急着接 OpenMV/激光。建议先做一版“正式舵机候选脚扫描”：

1. 列出所有未占用且疑似排针可达的候选脚。
2. 每个脚输出 3 秒 HIGH、1 秒 LOW。
3. 用万用表逐个确认物理排针能否到 3.3V。
4. 确认两个正式舵机脚后，再写双舵机小幅 1400/1500/1600us 测试。

这样能避免再次出现“代码配置没错，但 LaunchPad 物理排针根本没连到 MCU”的坑。
