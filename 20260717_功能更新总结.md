# 20260717 功能更新总结

本文把最近一次仓库更新按功能模块整理成一份可直接查看的总结，方便后续接手、回看和归档。

## 已完成的功能

### 1. 八路灰度循迹

- 完成了 `YB-MYX05-V1.0` 八路灰度板的复用扫描。
- 使用 `AD0/AD1/AD2` 选择 `X1~X8`，`OUT` 作为统一输出。
- 采用白底基准加变化检测的方式，不再依赖单个模块的固定黑白极性。
- 这条线的最终稳定版本是 `ccs_project/line_sensor_test/20260715_204132_v53_gray8_start_boost_line_follow.c`。

### 2. 起步、循迹和停车策略

- 加入了起步增力，解决低速起步时静摩擦过大的问题。
- 保留了普通循迹和急弯处理，适合不同曲率的赛道段。
- 加入了丢线找回和停车保护，避免长时间失控。
- 这一部分对应的是当前可直接使用的 V53 底盘版本。

### 3. OpenMV 靶心识别 + 激光瞄准

- 把旧的红色 blob 追踪改成了同心圆靶心识别思路。
- 优先识别最小有效红色圆环，找不到时再用霍夫圆结构作为 fallback。
- MSPM0 端保留原 UART 协议，只调整瞄准参数，让云台在靶心附近小步进、慢抖动。
- 相关文件是 `ccs_project/openmv_test/20260708_openmv_uart_stream_ring_center_v1.py` 和 `ccs_project/openmv_test/20260708_ring_center_laser_aim_readme.md`。

### 4. 任务点流程

- 做过 A 点起步桥接。
- 做过 B 点停车后的静态瞄准。
- 做过 C/D 点提示和回 A 停车的流程版本。
- 这些流程主要用于把赛道任务拆成可调试的阶段，而不是一次性全开。

### 5. 编码器与闭环调试

- 完成了编码器方向确认。
- 确认了 PWM compare 和实际有效占空比的反向关系。
- 迭代过桥接段的差速、PI 和 PID 控制方案。
- 这些版本主要用于无线桥接和直线保持，不是当前 V53 的默认运行模式。

### 6. 供电与外设安全

- 修正了目标板独立 3.3V 供电方式，改为 `LM2596 -> J11/3V3`。
- 明确了 `J101` 跳帽在外部供电和 USB 调试同时存在时的处理方法。
- 把低电平触发蜂鸣器的空闲电平改成 `PB11=HIGH`，避免上电误响。

## 当前可直接使用的内容

- 当前仓库里最稳定、最适合直接上车验证的版本是 V53 八路灰度循迹。
- 当前活动工程内容与 V53 一致。
- 当前 V53 只启用八路灰度循迹、起步增力和停车保护。
- OpenMV、双舵机、编码器闭环和任务点流程在 V53 中先停用，作为历史已验证功能保留。

## 建议配套查看的文件

- `README.md`
- `lwx7月16晨必看.md`
- `接线步骤.md`
- `过程记录.md`
- `项目记忆.md`
- `ccs_project/line_sensor_test/20260715_204132_v53_gray8_start_boost_line_follow.c`
- `ccs_project/openmv_test/20260708_openmv_uart_stream_ring_center_v1.py`

## 一句话总结

这次更新的核心结果是把整车的“八路灰度循迹 + 起步增力 + 停车保护”真正跑通了，同时把 OpenMV 靶心瞄准、任务点流程、编码器闭环和供电/蜂鸣器的关键问题都整理成了可回看的版本链。
