# 校赛小车研制路线

## 0. 当前起点

已验证能力：

- TB6612 + 两电机可控。
- 编码器 A/B 已接通并验证过方向。
- 7 路 TCRT5000 数字巡线可用。
- 当前稳定循迹基线：`C:\Users\1\Desktop\电赛\ccs_project\line_sensor_test\line_follow_sharp_turn_stable_baseline.c`。
- 该基线已实测可通过圆角弯、S 弯、直角弯。

保留原则：

- 不直接覆盖稳定基线。
- 不整块移植老师工程。
- 每次只新增一个可验证模块。

## 1. 赛题拆解

基础分优先级：

1. 自动巡迹一圈回 A 停车，声光提示，30 s 内。
2. 静止定点瞄准，5 s 内指向靶心。
3. A -> B -> C -> D -> A 联动，B 点停车瞄准，所有关键点声光提示，40 s 内。
4. 按键或串口设置运行模式/参数。

发挥部分放后面：

1. 行驶中动态瞄准。
2. 静止绘制图案。
3. 四圈连续运行。
4. 工程完善度。

## 2. 参考代码怎么用

### 可借鉴

- `25E题底盘开源\ApplicationLayer\Task.c`
  - 关键点/直角判断。
  - 过点后用编码器距离做保护。
  - 强制转向找回黑线。
  - 圈数/关键点计数。
- `25E题底盘开源\FunctionalModule\Key.c`
  - 短按/长按触发不同功能。
- `25E题底盘开源\FunctionalModule\Buzzer.c`
  - 事件提示。
- `24H题第四问开源\ApplicationLayer\Task.c`
  - 不同路段切换不同速度/控制策略。
  - 启动保护、入弯保护。

### 不能直接搬

- 老师 `GraySensor.c` 是 8 路模拟复用 ADC，我们是 7 路数字 DO。
- 老师 `MotorsCtrl.c` 是四电机闭环，我们是两电机 TB6612。
- 老师编码器距离参数依赖他们的轮径、线数和车体结构，阈值必须我们自己标定。
- 老师工程的 SysConfig/引脚定义和我们的接线不同，不能直接复制 `ti_msp_dl_config.*`。

## 3. 我们的阶段路线

### 阶段 1：人机交互入口

目标：

- 小车上电后不自动跑。
- 按 START 后开始循迹。
- 再按 START 或任务完成后停车。
- MODE 键选择功能：循迹一圈、B 点停车联动、定点瞄准、调试模式。

接线：

| 功能 | MSPM0 引脚 | 接法 |
|---|---|---|
| START | `PB11` | 按键一端接 `PB11`，另一端接 GND，代码内部上拉 |
| MODE | `PB8` | 按键一端接 `PB8`，另一端接 GND，代码内部上拉 |
| 状态 LED | `PA15` | `PA15 -> 330Ω -> LED 长脚`，LED 短脚接 GND |

验证：

- Watch `g_start_pressed`、`g_mode_pressed`、`g_run_enabled`、`g_mode`。
- 按键按下时变量变化。
- 不按 START 车不动。
- 按 START 后保持原循迹效果。

### 阶段 2：自动巡迹一圈停车

目标：

- 从 A 点启动。
- 跑完整一圈后回到 A 点停车。
- 停车声光提示。

实现思路：

- 保留现有 `line_follow_update()`。
- 新增 `g_keypoint_count` 和 `g_lap_count`。
- 用“特殊传感器图案 + 编码器距离保护”判断 A/B/C/D，而不是只靠时间。
- 第一版先用编码器总距离作为粗略停车条件，避免过早引入复杂拐点状态机。

需要标定：

- 一圈编码器边沿数。
- A->B、B->C、C->D、D->A 的大致边沿数。
- 关键点附近传感器图案。

### 阶段 3：关键点联动

目标：

- A 出发。
- 到 B 停车并触发瞄准动作。
- 继续到 C/D/A，每个关键点声光提示。

实现思路：

- 参考 `25E` 的 `RightAngleCount/CircleCount` 思路，但变量改名为 `g_keypoint`。
- 过点后加保护距离，避免同一个点被连续计数。
- B 点停车后进入 `TASK_AIM_STATIC`，瞄准完成再恢复循迹。

### 阶段 4：静止瞄准模块

目标：

- 不跑车，只测试云台/激光/视觉。
- 5 s 内让执行器末端指向靶心。

接线按已有文件：

- 舵机 X 信号：`PA3`
- 舵机 Y 信号：`PA4`
- OpenMV UART：`PA10/TX -> OpenMV RX`，`PA11/RX <- OpenMV TX`
- OpenMV 和舵机使用独立 5 V 供电，并与 MSPM0 共地。

验证顺序：

1. 单舵机中位/左右摆动。
2. 双舵机中位。
3. OpenMV 单独识别靶心。
4. OpenMV 通过 UART 发 `dx,dy`。
5. MSPM0 按 `dx,dy` 调整舵机。

### 阶段 5：联动与发挥

顺序：

1. B 点停车后静止瞄准。
2. A/B/C/D/A 全流程。
3. 四圈连续运行。
4. 动态瞄准。
5. 绘图。

## 4. 当前下一步

下一步只做阶段 1：

```text
接 START/MODE 两个按键
烧录“按键启动版循迹”
确认按键变量和原循迹效果都正常
```

暂时不要接云台、OpenMV、激光。

原因：

- 题目要求支持按键/串口模式设置。
- 后续 B 点停车、定点瞄准、四圈运行都需要一个可靠的任务入口。
- 这一步不会改变传感器、电机、TB6612 的现有接线，风险最低。

