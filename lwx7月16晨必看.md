lwx，你要先阅读理解所有实物接线！

# 7月16日早晨接手必读

本文是2026年7月15日全部关键对话、实物接线、代码迭代、故障诊断和下一步操作的集中交接。不要先烧录代码，也不要凭旧印象接线。先核对第1节的供电和引脚，再看代码版本。

## 0. 当前状态

- 当前最新源码：`ccs_project/line_sensor_test/20260715_204132_v53_gray8_start_boost_line_follow.c`。
- 当前本机CCS活动文件：`C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`，内容与V53一致。
- V53已执行clean build，编译、链接通过且零警告；Codex未自动烧录，烧录由队员手动完成。
- 新八路灰度板隔离测试已经通过，用户明确反馈“灰度传感器效果非常好”。
- V53在USB/CCS连接时可运行；拔掉USB后车辆不动，同时X6常亮。已经确认这是目标板独立3.3V供电问题，不是灰度板损坏，也不是代码依赖CCS。
- 已带来LM2596，下一步必须先把它空载调成3.30V，再给LaunchPad J11/3V3供电。
- LM2596接入后的独立上电结果尚未反馈，这是7月16日早晨第一项实测。

## 1. 当前权威实物接线

所有插拔、改线和调压必须断电。万用表黑表笔必须确实插在COM端并连接公共GND。

### 1.1 电源总结构

```text
2S电池7.4V+ -> 总开关 -> TB6612电源输入/VM
2S电池7.4V- -> TB6612 GND -> 系统公共GND

2S电池7.4V+ -> LM2596 IN+
2S电池7.4V- -> LM2596 IN-
LM2596空载调到3.30V
LM2596 OUT+ -> LaunchPad J11/3V3
LM2596 OUT- -> LaunchPad J11/GND

TB6612稳定5V OUT -> 八路灰度板5V
独立稳定5V支路 -> OpenMV VIN、两个舵机红线
LaunchPad 3.3V -> 低电平触发蜂鸣器VCC
所有GND共地，但不同稳压器的正极不能并联
```

必须遵守：

- 不再使用 `TB6612 5V OUT -> LaunchPad J10/5V` 作为脱离USB后的MCU供电方案。
- 不再使用曾掉到约2.6V的TB6612 3V3 OUT给LaunchPad供电。
- LM2596未调压前绝对不能连接LaunchPad。
- LM2596接板后，`J11/3V3-GND` 必须实测为约3.25V～3.35V。
- 灰度板仍然使用5V，不能接LM2596的3.3V输出。

### 1.2 J101隔离跳帽

LM2596外部3.3V接J11时：

```text
拔掉J101中的3V3跳帽
保留J101中的GND、NRST、SWDIO、SWCLK
```

这样USB调试时，XDS110的3.3V不会与LM2596的3.3V并联。若拆掉外部3.3V、改回纯USB供电，才装回J101的3V3跳帽。

TI官方依据：LP-MSPM0G3507用户指南SLAU873D第2.4/2.5节。USB通过XDS110和J101给目标侧3.3V；外部电源从J11直接供给目标侧，必须满足MSPM0的1.62V～3.6V工作范围。

### 1.3 八路灰度板

模块：亚博/Yahboom `YB-MYX05-V1.0`，无MCU，六针接口按PCB丝印为：

```text
5V  AD2  AD1  AD0  OUT  GND
```

正式接线：

```text
灰度板5V  -> TB6612稳定5V OUT
灰度板AD2 -> MSPM0 PB7
灰度板AD1 -> MSPM0 PB6
灰度板AD0 -> MSPM0 PB5
灰度板OUT -> MSPM0 PA7
灰度板GND -> 公共GND
```

选通关系：

```text
AD2 AD1 AD0 = 000 -> X1
AD2 AD1 AD0 = 001 -> X2
AD2 AD1 AD0 = 010 -> X3
AD2 AD1 AD0 = 011 -> X4
AD2 AD1 AD0 = 100 -> X5
AD2 AD1 AD0 = 101 -> X6
AD2 AD1 AD0 = 110 -> X7
AD2 AD1 AD0 = 111 -> X8
```

安装定义：X1为车头最左，X8为车头最右，X4/X5中间对准车体中心。原七个独立TCRT5000正式退出，PA8/PB9/PB10释放。

### 1.4 TB6612与电机

```text
PB0  -> AIN1
PB1  -> AIN2
PB2  -> BIN1
PB3  -> BIN2
PB4  -> STBY
PA12 -> PWMA，左电机PWM
PA13 -> PWMB，右电机PWM
GND  -> TB6612 GND

TB6612 A通道/左侧6P口 -> 左电机
TB6612 B通道/右侧6P口 -> 右电机
```

当前PWM写入规则不是直接compare，而是：

```text
compare = 1600 - logical_duty_ticks
```

### 1.5 编码器

```text
左编码器E1A -> PB12
左编码器E1B -> PB13
右编码器E2A -> PB14
右编码器E2B -> PB15
```

PB14还可在LaunchPad的J12 QEI接口找到。当前V53尚未使用编码器闭环。

### 1.6 OpenMV和双舵机

当前V53禁用OpenMV和舵机控制，但正式硬件接线保持：

```text
OpenMV P4/TX -> PA11/MSPM0 RX
OpenMV P5/RX <- PA10/MSPM0 TX
OpenMV VIN   -> 独立稳定5V
OpenMV GND   -> 公共GND

水平舵机信号 -> PA14
俯仰舵机信号 -> PA17
舵机红线     -> 独立稳定5V
舵机棕/黑线  -> 公共GND
```

PA2～PA6继续禁用；PA20是SWCLK，也不作为普通外设脚。

### 1.7 低电平触发蜂鸣器

新蜂鸣器是三针低电平触发模块，丝印为 `VCC/I/O/GND`，板上已有驱动三极管和输入电阻。

```text
VCC -> LaunchPad 3.3V
I/O -> PB11
GND -> 公共GND
可选：I/O与3.3V之间加10kΩ上拉
```

逻辑：

```text
PB11 = LOW  -> 响
PB11 = HIGH -> 关闭
```

旧的“两针蜂鸣器+330Ω直驱”接法作废。V51之后所有安全初始化都必须让PB11保持HIGH。

### 1.8 状态LED

```text
PA15 -> 330Ω -> LED长脚
LED短脚 -> GND
```

PA15外接LED是判断目标MCU是否独立启动的重要依据。拔掉J101的3V3跳帽、没有USB时，XDS110区域的板载灯不亮不代表目标MCU没运行。

## 2. 7月15日对话与处理记录

以下按实际发生顺序整理，不包含编译工具的冗长输出。

### 2.1 新八路灰度板到货，要求汇总全部接线

用户：最终买了亚博八路灰度板，要求把它加入整车接线并重新输出全部接线。

处理：

- 根据实物照片确认接口顺序为 `5V/AD2/AD1/AD0/OUT/GND`。
- 复用原来已验证的巡迹GPIO：`AD0->PB5`、`AD1->PB6`、`AD2->PB7`、`OUT->PA7`。
- 确认这是三位地址选通、单一数字OUT的复用模块，不是八根DO，也不是连续模拟AO。
- 原七路独立模块退出；PA8/PB9/PB10释放。
- 更新根目录 `接线步骤.md`，并纠正PB11同时写成按键与蜂鸣器的旧冲突。

### 2.2 开始灰度板隔离测试，生成V50

用户：现在来测试灰度传感器。

处理：

- 生成 `20260715_152540_v50_gray8_mux_motor_off_test.c`。
- 电机、STBY、PWM、舵机、OpenMV和蜂鸣器全部禁用。
- 每通道等待40us，采样9次，至少5次高判为高。
- 上电时要求X1～X8全部位于白底；前50帧约1秒自动建立白底基准。
- 使用 `当前原始mask XOR 白底基准mask` 得到变化mask，不预设黑高或黑低。
- PA15：标定慢闪；基准完成后，任一通道相对白底变化则常亮；基准不一致时快闪。
- clean build通过，首轮一个未使用函数警告被删除，最终零警告，未自动烧录。

关键Watch：

```text
g_firmware_version = 50
g_gray_test_state
g_gray_raw_high_mask
g_gray_white_baseline_mask
g_gray_changed_from_white_mask
g_gray_unstable_mask
g_gray_x1 ... g_gray_x8
g_gray_x1_high_count ... g_gray_x8_high_count
```

### 2.3 PB14位置疑问

用户：找不到PB14，随后自行找到。

补充结论：PB14用于右编码器A相，在LaunchPad J12 QEI接口也可找到。它与新灰度板无关，新灰度板只用PB5/PB6/PB7/PA7。

### 2.4 更换为低电平触发蜂鸣器，生成V51

用户提供三针蜂鸣器照片，确认模块标注“低电平触发”。

处理：

- 发现V50把PB11强制为LOW，对新模块会变成持续鸣叫，不是安全关闭。
- 正式接法改为 `VCC->3.3V`、`I/O->PB11`、`GND->公共GND`。
- 控制逻辑改为LOW响、HIGH关闭；建议I/O加10kΩ上拉。
- 生成 `20260715_171632_v51_gray8_mux_motor_off_low_active_buzzer_safe.c`。
- V51只把PB11空闲电平改成HIGH，灰度测试逻辑不变。
- clean build通过且零警告，未自动烧录。

### 2.5 灰度传感器测试成功，生成V52低速循迹

用户明确反馈：灰度传感器效果非常好，进入下一步。

处理：

- 生成 `20260715_202553_v52_gray8_low_speed_line_follow.c`。
- 只启用八路灰度、TB6612和电机；OpenMV、舵机、编码器闭环和任务点继续禁用。
- 沿用历史已验证的PWM反向compare、普通修正、急弯和丢线找回结构。
- V52首测基础PWM为左500/右470，目标是低速安全联调。
- 白底标定成功后低速直行找线；连续2帧确认黑线后进入循迹。
- 接受1～6路有效黑线，7～8路作为异常；桥接约5秒未见线停车；循迹后连续丢线约2秒停车；总测试约30秒停车。
- clean build通过且零警告，未自动烧录。

### 2.6 V52车辆完全不动，生成V53启动增力版

用户：小车完全不动，怀疑速度太慢，随后要求直接修改代码。

分析：500/1600和470/1600只有约31%和29%逻辑占空比，对当前负重可能低于静摩擦起转阈值。

处理：

- 生成最新 `20260715_204132_v53_gray8_start_boost_line_follow.c`。
- 启动前20帧约0.4秒使用左760/右720突破静摩擦。
- 随后恢复历史已验证的左620/右590。
- PWM最小/最大恢复430/850。
- 如果0.4秒内提前捕获黑线，立即退出启动增力，避免高速冲弯。
- 其他灰度、状态机和保护逻辑保持不变。
- clean build通过且零警告，未自动烧录。

V53 Watch：

```text
g_firmware_version = 53
g_start_boost_active
g_left_pwm_ticks
g_right_pwm_ticks
g_phase
g_stop_reason
g_gray_line_mask
g_gray_line_error
g_turn_mode
```

### 2.7 只有连接CCS并按Run才运动，X6异常

用户反馈：

- 板子连接电脑并在CCS按Run后车辆运动。
- 拔掉电脑连接线、使用整车电源重新上电，车辆始终不动。
- 最新程序下X6一直亮。
- 回退V51灰度测试时，在USB调试条件下灰度板无异常。
- 进一步确认：USB连接时X6正常，拔掉USB后X6立即常亮。

代码核查结论：

- V53没有任何“必须连接CCS才运行”的条件。
- 唯一启动门禁是白底八路必须一致。
- X6异常会导致 `g_gray_baseline_uniform=0`、`g_stop_reason=2`，程序按设计禁止启动。

硬件根因：

- USB连接时，XDS110经J101给目标MCU提供稳定3.3V。
- USB拔掉后，目标MCU没有可靠3.3V，PB5/PB6/PB7地址线失去驱动或电平不稳。
- 灰度复用器因此选通异常，表现为X6常亮。
- USB插入后电源和地址线恢复，所以程序和X6同时正常。

### 2.8 外部供电入口纠正

此前文档写过 `TB6612 5V OUT -> LaunchPad 5V`，这个判断已经正式作废并记录为失误。

官方核查：

- J101隔离块包含GND、5V、3V3、UART、NRST、SWDIO、SWCLK、BSL。
- J10是5V电源排针。
- J11是3V3外部电源排针。
- 官方第2.5节明确外部电源从J11直接供给目标侧，并满足MSPM0 1.62V～3.6V范围。

因此，外部稳定3.3V必须接J11；5V不能直接接MCU的3V3域。

### 2.9 为什么旧红外阶段没有明显暴露问题

用户提出三个问题：旧红外为什么不掉压、开发板为什么不能5V供电、欠压为什么造成漂移。

结论：

- 旧TCRT5000在3.3V下LED电流较小，而且当时USB经常接着，XDS110实际承担了目标3.3V供电。
- 之前已经测到TB6612 3V3约2.6V，那其实就是掉压，只是当时没有与供电路径准确关联。
- 新板包含8组白光发射、接收、比较器、复用器和稳压电路，约85mA的5V负载暴露了原有供电余量问题。
- 旧模块每路独立DO；新板依赖MCU的AD0/AD1/AD2地址线，MCU欠压时新板会更明显地选错通道。
- MSPM0本体工作范围只有1.62V～3.6V；USB 5V可用是因为板上电路把它降到3.3V，不代表5V能直接进入MCU。
- 欠压会改变LED亮度、比较器阈值、GPIO高电平和电机起转阈值；左右电机静摩擦不同，因此会产生固定偏转、左右摆动或完全不启动。

### 2.10 带来LM2596，确定最终控制电源方案

用户：带来一块LM2596。

处理：

- LM2596专门从7.4V降到3.30V，只给LaunchPad J11/3V3和3.3V小负载供电。
- 灰度板继续使用TB6612稳定5V。
- OpenMV和双舵机继续使用独立稳定5V。
- TB6612的3V3 OUT不再接LaunchPad。
- LM2596必须先空载调压，再断电接板；带载后复测3.25V～3.35V。
- 建议J11附近并联100uF和0.1uF，但电容不能替代稳压器。

## 3. 今日代码版本关系

```text
V49  旧七路独立红外最终诊断版
  |
  +-> V50  新八路灰度，电机禁用，自动白底基准
        |
        +-> V51  低电平蜂鸣器安全空闲HIGH
              |
              +-> V52  新八路灰度 + 低速500/470电机循迹
                    |
                    +-> V53  启动760/720约0.4秒，正常620/590
```

不要覆盖这些时间戳文件。需要烧录时，将目标版本复制到自己的CCS工程 `empty.c`，编译后由人工烧录。

## 4. 7月16日早晨第一轮操作

### 4.1 先调LM2596

LaunchPad保持断开：

```text
电池7.4V+ -> LM2596 IN+
电池7.4V- -> LM2596 IN-
万用表测OUT+与OUT-
调到3.30V
```

多圈电位器可能需要转十几圈。不要猜方向，每次观察电压。

### 4.2 接入LaunchPad

全部断电：

```text
拔掉J101的3V3跳帽
LM2596 OUT+ -> J11/3V3
LM2596 OUT- -> J11/GND
```

电池上电、USB不接，测：

```text
LM2596 OUT+-OUT-       = 3.25V～3.35V
LaunchPad J11 3V3-GND = 3.25V～3.35V
```

如果不是这个范围，立即断电，不烧录、不跑车。

### 4.3 独立启动验证

- 保持USB断开。
- 按RST。
- PA15应先闪烁进行白底标定。
- X6必须与其他通道一样恢复正常。
- 标定成功后V53才允许电机启动。

### 4.4 先架空再落地

- 第一轮车轮架空，确认启动增力和左右差速方向。
- 第二轮把黑线分别放在X1/X2和X7/X8下，确认两侧产生相反修正。
- 第三轮才落地跑短赛道。

## 5. V53状态含义

```text
g_phase = 1  白底标定
g_phase = 8  低速/启动增力直行寻找黑线
g_phase = 2  正常循迹
g_phase = 3  约30秒测试完成
g_phase = 4  保护停车

g_stop_reason = 1  正常测试结束
g_stop_reason = 2  白底八路不一致
g_stop_reason = 3  约5秒未碰到黑线
g_stop_reason = 4  循迹后连续丢线约2秒
```

PA15灯语义：

```text
标定慢闪
标定成功常亮约0.8秒
桥接阶段熄灭
循迹阶段常亮
丢线找回阶段闪烁
停止后周期短闪
```

## 6. 绝对不要重复的错误

- 不要用板载灯是否亮判断目标MCU供电，先测J11/3V3，再看PA15外接状态LED。
- 不要把5V直接接J11/3V3或任何GPIO。
- 不要同时保留J101 3V3跳帽和LM2596外部3.3V。
- 不要让LM2596、TB6612 3V3和USB 3V3互相并联。
- 不要在未空载调压前连接LM2596输出。
- 不要把低电平触发蜂鸣器的PB11默认拉低；LOW是响，HIGH才是关闭。
- 不要再使用旧七路红外的PA8/PB9/PB10读取代码控制新灰度板。
- 不要看到X6常亮就先换传感器；USB插拔能改变X6状态时，优先查MCU供电和地址线。
- 不要因为车不动就只提高PWM；先判断PA15灯序、`g_phase`和`g_stop_reason`。

## 7. 相关文件

- `接线步骤.md`：当前权威接线。
- `项目记忆.md`：稳定事实、当前版本和长期约束。
- `过程记录.md`：完整时间线和每轮修改。
- `知识库/调试复盘_判断失误记录.md`：所有已确认判断错误及纠正原则。
- `ccs_project/line_sensor_test/20260715_152540_v50_gray8_mux_motor_off_test.c`。
- `ccs_project/line_sensor_test/20260715_171632_v51_gray8_mux_motor_off_low_active_buzzer_safe.c`。
- `ccs_project/line_sensor_test/20260715_202553_v52_gray8_low_speed_line_follow.c`。
- `ccs_project/line_sensor_test/20260715_204132_v53_gray8_start_boost_line_follow.c`。
- TI官方用户指南：https://www.ti.com/lit/pdf/slau873
