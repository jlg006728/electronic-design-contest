# 循迹调试交接重点阅读

面向对象：接替小车循迹调试的同学。

更新时间：2026-07-02 21:25

## 一句话当前状态

小车已经能跑基础循迹、S 弯、圆角弯和直角弯，当前最新待实车复测的问题是：

- 直角弯处偶发“没有识别到转弯，直接冲出赛道”。
- 小车被拿起后重新放回线上，可能因为旧丢线方向记忆而原地打转。

最新代码已经针对这两个逻辑风险做了修正，但还需要继续实车测试。

## 必读顺序

1. `交接说明_20260702_小车循迹.md`
   - 先建立项目入口、当前代码、硬件接线和下一步。
2. 本文件：`循迹调试交接_重点阅读.md`
   - 专门看循迹调试脉络。
3. `过程记录.md`
   - 按时间看现场调试过程，尤其 2026-07-02 16:00 之后的循迹调参记录。
4. `知识库/调试复盘_判断失误记录.md`
   - 看之前哪些判断走偏了，避免重复踩坑。
5. `ccs_project/line_sensor_test/`
   - 所有循迹历史代码版本都在这里，按时间戳倒序查。
6. `ccs_project/ele-car_active_project/empty.c`
   - 当前从实际 CCS 烧录工程复制进仓库的活动版本。

## 当前应该烧录哪一版

优先烧录：

```text
ccs_project/line_sensor_test/20260702_210925_line_follow_wide_turn_lost_timeout.c
```

同一份代码也已经复制到：

```text
ccs_project/ele-car_active_project/empty.c
```

本机当前实际工程原路径是：

```text
C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c
```

如果在另一台电脑继续，建议新建 MSPM0G3507 empty project，然后把上面的 `20260702_210925...c` 复制成工程的 `empty.c`。

## 当前循迹硬件接线

TB6612：

```text
PB0  -> AIN1
PB1  -> AIN2
PB2  -> BIN1
PB3  -> BIN2
PB4  -> STBY
PA12 -> PWMA / TIMG0_CCP0
PA13 -> PWMB / TIMG0_CCP1
```

7 路红外，从车头左到右：

```text
S1 -> PB5
S2 -> PB6
S3 -> PB7
S4 -> PA7
S5 -> PA8
S6 -> PB9
S7 -> PB10
```

不要再用这些旧红外输入脚：

```text
PA2 / PA5 / PA6
```

原因：之前现场测试发现这些脚对 3.3 V 触碰无响应或表现异常，换到 PB5/PB6/PB7 后问题解决。

## 循迹版本速查

`ccs_project/line_sensor_test/` 中关键版本如下：

| 文件 | 意义 |
|---|---|
| `line_sensor_read_test.c` | 最早的 7 路红外数字输入读取测试 |
| `line_sensor_read_test_remap_pb5_pb6_pb7.c` | 避开 PA2/PA5/PA6 后的重映射读取测试 |
| `line_position_error_test_remap_pb5_pb6_pb7.c` | 验证红外权重误差和传感器顺序 |
| `line_follow_sharp_turn_stable_baseline.c` | 早期 21:43 急弯策略基线，曾能过直角弯 |
| `20260702_191813_restore_pre_sharp_turn_for_test.c` | 简单 P 控制版本：直线很稳，但直角弯能力不足 |
| `20260702_191058_line_follow_center_first_v2_video_review.c` | 中心优先/状态机版本：转弯能力较强，但直线抖动更明显 |
| `20260702_203620_line_follow_simple_p_minimal_right_angle_assist.c` | 回到简单 P 主干，只加最小直角弯辅助 |
| `20260702_205214_line_follow_video_tuned_nonlinear_turn.c` | 根据视频观察加入非线性转弯补偿 |
| `20260702_210925_line_follow_wide_turn_lost_timeout.c` | 当前最新版本：处理宽黑线误判和长丢线复放打转 |

中间的 `20260702_16xxxx`、`20260702_17xxxx`、`20260702_20xxxx` 文件是参数探索过程，不建议直接作为最终版本，但保留用于追溯。

## 当前最新代码的核心逻辑

最新版本保持“简单 P 循迹”为主干，避免复杂状态机反复切换导致直线抖动。

关键补丁：

1. 宽黑线/直角弯检测
   - 如果 4 个及以上红外同时检测到黑线，不能简单用 `sum / count` 平均误差。
   - 因为直角弯或宽黑线可能让多个传感器同时触发，平均值反而接近 0，旧代码会误判为居中直行。
   - 新代码用最近可靠转向方向强制进入急弯处理。

2. 丢线恢复加时间上限
   - 短时完全丢线时，允许按最后方向找回。
   - 但完全丢线超过约 0.7 s 后，清空旧误差和转向记忆，改为直行搜索。
   - 目的：防止拿起小车再放回线上后继续沿旧方向原地打转。

3. 不再单纯依赖 Watch
   - 实车脱离 USB 后无法 Watch。
   - 后续判断要以实车现象、LED、视频、万用表为主。

## 现场对话浓缩记录

这些不是完整逐字聊天记录，而是把对话中真正影响代码和硬件判断的内容整理成可接续记录。完整过程已写入 `过程记录.md` 和 `知识库/调试复盘_判断失误记录.md`。

### 红外与接口排查

- 最初 7 路红外中前三路不响应，后四路正常。
- 排查后认为不是红外模块本身，而是旧接口 `PA2/PA5/PA6` 不可靠。
- 换成 `PB5/PB6/PB7 + PA7/PA8 + PB9/PB10` 后，7 路红外读取符合要求。

### 直线与转弯矛盾

- 简单 P 版本直线最好，但直角弯过不去。
- 复杂状态机版本转弯更强，但直线和出弯后更容易左右摇摆，不利于后续打靶。
- 最终策略：保留简单 P 主干，只对明确场景做局部补丁。

### 视频分析结论

- 用户提供实车视频后，观察到主要失败不是普通直线持续蛇形，而是部分直角弯转入太晚、半径过大、偶发冲出。
- 因此不再优先加直线状态机，而是强化直角弯识别和短时丢线处理。

### 最新问题

- 用户反馈：有时转弯时直接冲出赛道，没能识别到转弯。
- 用户反馈：拿起重新放线上后开始原地打转。
- 代码检查后确认两个根因：
  - 多红外同时触发时平均误差可能接近 0，导致直角弯误判为居中。
  - `lost_recovery` 旧逻辑无时间上限，旧方向记忆会污染复放后的动作。

## 下次测试看什么

烧录 `20260702_210925_line_follow_wide_turn_lost_timeout.c` 后，优先观察：

1. 直角弯处是否还会完全不转、直接冲出。
2. 宽黑线/转弯处是否转向方向正确。
3. 拿起小车超过 1 秒再放回线上，是否不再原地打转。
4. 直线是否仍保持接近 `20260702_191813_restore_pre_sharp_turn_for_test.c` 的稳定性。

如果宽黑线处转向方向反了，只改：

```c
#define DEFAULT_TURN_SIGN 1
```

改为：

```c
#define DEFAULT_TURN_SIGN -1
```

如果仍然冲出直角弯，优先只调这些窄参数：

```c
WIDE_TURN_ERROR
LINE_SHARP_KP_DIVISOR
LINE_SHARP_CORR_LIMIT
SHARP_LEFT_BASE_TICKS
SHARP_RIGHT_BASE_TICKS
```

不要第一时间重写状态机。

## 不要重复踩的坑

- 不要把“车看起来停了”直接等同于代码调用了 `motors_stop()`；低 PWM、地面摩擦、电机死区也会让车看起来停。
- 不要只根据 Watch 判断真实循迹；接 USB 时无法复现地面完整动态。
- 不要反复覆盖 `empty.c` 而不保存时间戳版本。
- 不要把所有直角弯失败都归因于转弯力度不够；先看是否是传感器组合被平均算法抵消。
- 不要轻易回到复杂状态机；之前多轮尝试表明它很容易改善某个弯，却让直线/出弯姿态变差。

## GitHub 提交说明

循迹相关过程文件和历史版本已经专门纳入提交：

- `过程记录.md`
- `知识库/调试复盘_判断失误记录.md`
- `交接说明_20260702_小车循迹.md`
- `循迹调试交接_重点阅读.md`
- `ccs_project/line_sensor_test/`
- `ccs_project/ele-car_active_project/`
- `video_analysis/`

接手同学优先看这些，不要只看最终 `empty.c`。

