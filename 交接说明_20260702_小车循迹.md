# 交接说明：小车循迹与电赛项目当前状态

更新时间：2026-07-02 21:15

## 先读哪些文件

1. `过程记录.md`
   - 按时间记录了硬件接线、传感器排查、循迹算法多轮版本和现场现象。
2. `知识库/调试复盘_判断失误记录.md`
   - 记录每次判断失误和后续避免方式，接手时非常重要。
3. `接线步骤.md`
   - 当前硬件接线参考。
4. `硬件接口图.md`
   - MSPM0、TB6612、红外循迹、OpenMV、舵机等接口归档。
5. `ccs_project/line_sensor_test/`
   - 循迹算法历史版本，每个重要版本都按时间戳保存。
6. `ccs_project/ele-car_active_project/`
   - 从当前实际烧录用 CCS 工程 `C:\Users\1\Desktop\ELECTRIC\ele-car` 复制进仓库的工程骨架。

## 当前最重要的代码版本

当前活动烧录代码：

- 外部 CCS 工程：`C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`
- 仓库内副本：`ccs_project/ele-car_active_project/empty.c`
- 时间戳备份：`ccs_project/line_sensor_test/20260702_210925_line_follow_wide_turn_lost_timeout.c`

这版是在简单 P 循迹主干上加入两处局部补丁：

- 4 个及以上红外同时检测到黑线时，认为可能进入宽黑线/直角弯，不再用平均误差接近 0 来判断居中直行。
- 完全丢线后只允许约 0.7 s 按最后方向找回；超过后清空旧误差和转向记忆，改为直行搜索，避免拿起再放回线上后原地打转。

## 当前硬件接线

主控：LP-MSPM0G3507。

TB6612：

- `PB0 -> AIN1`
- `PB1 -> AIN2`
- `PB2 -> BIN1`
- `PB3 -> BIN2`
- `PB4 -> STBY`
- `PA12 -> PWMA / TIMG0_CCP0`
- `PA13 -> PWMB / TIMG0_CCP1`

红外循迹 7 路，从车头左到右：

- `S1 -> PB5`
- `S2 -> PB6`
- `S3 -> PB7`
- `S4 -> PA7`
- `S5 -> PA8`
- `S6 -> PB9`
- `S7 -> PB10`

已知不要再用的旧红外引脚：

- `PA2`
- `PA5`
- `PA6`

## 当前循迹状态

已经实现：

- 电机、TB6612、编码器基础验证。
- 7 路红外数字循迹输入验证。
- 小车可完成基本循迹、圆角弯、S 弯、直角弯的多轮调参。
- 最近问题集中在：
  - 直角弯处偶发冲出赛道。
  - 拿起重放后可能因旧丢线状态原地打转。

最新版本已经针对这两个问题做了代码级修正，但还需要实车复测。

## 烧录与编译

CCS 项目当前在外部路径：

```text
C:\Users\1\Desktop\ELECTRIC\ele-car
```

仓库内保留了复制版：

```text
ccs_project/ele-car_active_project
```

如果换电脑接手：

1. 安装 TI CCS、MSPM0 SDK、SysConfig。
2. 优先尝试在 CCS 中导入 `ccs_project/ele-car_active_project`。
3. 如果路径或 SDK 报错，可新建 MSPM0G3507 empty project，再把 `ccs_project/line_sensor_test/20260702_210925_line_follow_wide_turn_lost_timeout.c` 内容复制为工程的 `empty.c`。

当前本机编译命令：

```powershell
& 'C:\ti\ccs2050\ccs\utils\bin\gmake.exe' -k -j 24 all -r -O
```

工作目录：

```text
C:\Users\1\Desktop\ELECTRIC\ele-car\Debug
```

## 下一个人应该怎么继续

1. 烧录最新版本 `20260702_210925_line_follow_wide_turn_lost_timeout.c`。
2. 实车看两个现象：
   - 直角弯是否还会完全没识别就直冲。
   - 拿起小车超过 1 秒再放回线上，是否不再原地打转。
3. 如果宽黑线处转向方向反了，只改：

```c
#define DEFAULT_TURN_SIGN 1
```

改成：

```c
#define DEFAULT_TURN_SIGN -1
```

4. 如果还冲出直角弯，不要先重写状态机。优先只调这几个窄参数：
   - `WIDE_TURN_ERROR`
   - `LINE_SHARP_KP_DIVISOR`
   - `LINE_SHARP_CORR_LIMIT`
   - `SHARP_LEFT_BASE_TICKS`
   - `SHARP_RIGHT_BASE_TICKS`

## 注意事项

- 不要只看 Watch 判断实车循迹，因为连接电脑时无法完全复现真实地面运动。
- 后续版本必须继续按时间戳保存，不要覆盖历史版本。
- 复杂状态机之前试过多轮，容易让直线更抖；当前策略是保留简单 P 主干，只对明确问题加局部补丁。
- 舵机供电问题尚未完全解决，之前现象是 MP1584 接入舵机后输出电压从约 5.8 V 掉到 3.7-4.4 V，疑似供电能力/连接问题，不要把它误判成 PWM 程序问题。

