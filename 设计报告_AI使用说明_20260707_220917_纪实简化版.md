# 项目 AI 使用说明

本项目使用 Codex 作为工程辅助工具。AI 没有直接参与小车运行，也没有在比赛过程中提供在线导航；小车的循迹、舵机控制、OpenMV 通信和声光提示均运行在 MSPM0、OpenMV 与本地硬件上。我们的使用方式更接近“结对调试”：队员把接线、万用表读数、视频现象、代码版本和现场结果告诉 Codex，Codex 负责整理假设、生成隔离测试程序、修改代码、保留历史版本，并在编译通过后等待队员手动烧录和实车验证。

## 一、AI 参与方式

| 环节 | 队员完成的工作 | Codex 完成的工作 |
|---|---|---|
| 需求拆解 | 阅读题目、确定比赛目标和现场策略 | 将任务拆成电机、循迹、OpenMV、双舵机、蜂鸣器、任务点状态机等模块 |
| 硬件排查 | 接线、焊接、测电压、观察实车现象 | 给出万用表测量顺序、判断可疑模块、生成隔离测试代码 |
| 代码迭代 | 手动烧录、实车试跑、描述偏左/偏右/打转/卡死等现象 | 修改 `empty.c`，保存时间戳版本，编译验证 |
| 版本管理 | 根据现场效果决定回退或继续 | 记录过程文件、备份有效版本、整理错误判断 |
| 报告整理 | 确认真实过程和最终状态 | 根据过程记录生成说明文字 |

本地文件统计显示，项目调试过程中保存了大量过程版本：`ccs_project` 下共有约 194 个 C 文件，其中 152 个为时间戳版本；另有 13 个 OpenMV/Python 脚本、72 个 readme 说明文件。代码版本不是一次生成完成的，而是在现场反馈中不断回退、比较和重写。

## 二、典型问题与解决过程

| 问题 | 解决方式 | 谁推动了关键转折 |
|---|---|---|
| 电机最初不转 | 先测 STBY、PWM、AIN/BIN、电机电源，再确认 TB6612 供电和 7.4V 电池 | Codex 提供排查顺序，队员用万用表确认 |
| PA2~PA6 等引脚异常 | 结合板卡资料和实测，改用 PB5/PB6/PB7/PA7/PA8/PB9/PB10 等可用引脚 | Codex 负责查资料和重映射，队员实测确认 |
| 循迹直线抖动、直角弯冲出 | 从普通比例修正逐步加入急弯策略、丢线找回、中心偏置和速度限制 | 双方共同完成，队员通过视频和实车现象不断纠偏 |
| 状态机版本直线不稳 | 回退到早期直线较稳的普通循迹版本，再只补直角弯能力 | 队员指出“应该回到基础方案”，Codex 重新合并 |
| OpenMV 串口看似卡死 | 最终发现 P4 接触不良，不是算法本身 | 队员现场发现硬件接触问题，Codex 记录并调整后续判断 |
| 舵机卡兹声、OpenMV 掉电 | 先怀疑电源，后发现部分来自机械结构过紧 | 队员通过拆装和听声确认，Codex 将机械因素纳入排查 |
| 蜂鸣器不响 | 写 PB11 隔离测试程序，最后确认是正负极接反 | Codex 写测试程序，队员发现极性问题 |
| 比赛场地中间可能无线 | 提出编码器桥接和红外重捕线方案，但现场时间不足以完全验证 | Codex 给出算法方案，队员判断先回到全线版本保稳定 |

这些过程说明，AI 的价值不在于“一次性给出正确答案”，而在于把复杂问题拆开、让每一步有可测对象。很多关键判断来自队员现场观察，例如万用表读数、线松动、接触不良、机械卡滞、蜂鸣器极性等；Codex 的作用是把这些反馈快速转化为下一版可测试代码。

## 三、硬件不足与算法补偿

项目中出现过多类硬件限制：部分引脚不可用、传感器接线松动、电源带载下降、舵机有效角度受机械结构限制、OpenMV 远距离识别不稳定、比赛场地线型与预期不同。我们没有把这些问题简单归结为“硬件坏了”，而是尽量通过算法和结构化调试补偿：

- 对红外循迹，使用 7 路传感器加权误差，而不是只看单个传感器。
- 对直角弯，加入急弯模式，使大误差时内侧轮明显降速，避免普通比例控制冲出赛道。
- 对传感器中心不在车身几何中心的问题，加入 `LINE_CENTER_OFFSET` 修正。
- 对 OpenMV 目标丢失，加入保持、搜索和赛段预偏转策略。
- 对任务点，使用状态机区分“循迹”和“作业提示”，避免所有逻辑混在一个循环里。
- 对蜂鸣器和传感器，优先写隔离测试程序，先确认硬件再合并进总程序。

这些补偿不能完全替代硬件精度，但使现有器件在有限时间内尽可能可用。

## 四、项目独立性

AI 只参与开发阶段。最终程序烧录到 MSPM0 和 OpenMV 后，小车不依赖联网 AI，也不需要电脑实时控制。Codex 不能直接观察真实电压、摩擦、螺丝松紧和接触状态，因此所有结论都必须由队员用实物验证。我们也明确规定：调试后期 Codex 只负责生成和编译代码，烧录由队员手动完成，避免误烧错误版本。

## 五、当前已实现与未完成内容

当前已实现：

- TB6612 双电机驱动与基础 PWM 调速。
- 7 路红外循迹，包含直线、圆弧、S 弯和直角弯策略。
- OpenMV 与 MSPM0 UART 通信。
- PA14/PA17 双舵机云台控制与红色目标追踪。
- PB11 蜂鸣器声光提示。
- 集成版本中可实现 B 点停车，C/D/A 点声光提示。
- 过程版本、接线说明、复盘记录和 AI 使用说明已保留。

仍未完全实现或仍需现场验证：

- 无黑线桥接路段的编码器闭环直行与重捕线策略。
- 任务点距离的编码器精确标定。
- 行进中动态打靶的稳定性。
- OpenMV 在不同光照、距离和靶面角度下的鲁棒识别。
- 比赛最终流程的一键启动、完整多圈稳定运行。

这些未完成项已经有方案：编码器用于距离和直线桥接，OpenMV 继续优化阈值与搜索策略，任务状态机继续细化 B/C/D/A 点逻辑。主要缺少的是现场调试时间，而不是从零开始重新设计。

## 六、总结

本项目中 AI 的使用是透明、可追溯、可验证的。队员负责真实硬件、现场判断和最终取舍；Codex 负责快速形成代码版本、排查清单和过程记录。项目的核心控制仍由我们设计、测试和确认，AI 是提高调试效率的工具，而不是替代独立完成项目的主体。
