# 2021 F题智能送药小车：可行性与实施路线

日期：2026-07-21

依据：

- `2021_F题_智能送药小车.pdf`
- 历史会话承接记录 `019f22a9-f880-7062-950c-25b5e741f86b`
- 当前活动源码 `C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`
- 当前归档基线 `ccs_project\integration_test\20260721_201820_v87_curve_state_split_gap_trim.c`
- `项目记忆.md`、`过程记录.md`、器件采购与接线文档

## 1. 结论

1. 现有一套小车的主硬件足以继续冲击全部单车基本要求，即近端、中部、远端送药并返回，共50分；但不是把V87换一张赛道就能完成，需要新增视觉导航、数字识别、装卸检测和拓扑状态机。
2. 当前最值得保留的是底盘、电机驱动、10ms控制框架、滤波/迟滞/斜率限制思想、OpenMV UART经验和已有编码器代码资产。
3. 当前V87追踪黑线，F题使用红线，且黑色数字边框距离红线约2cm。八路红外灰度无法可靠按颜色区分红线和黑色数字，因此不能直接作为F题主导航方案。
4. 推荐用OpenMV固定斜向下同时完成红线、路口和数字卡识别；MSPM0负责运动、编码器、载药检测、灯光和任务状态机。激光和二维云台在本题中应移除。
5. 现有器材不能完成双车发挥部分：只有一套完整小车，且没有已确认的无线链路和第二套数字识别系统。先完成单车50分，不要现在分散到双车。

## 2. 赛题功能与当前能力

| 赛题功能 | 当前资源 | 当前状态 | 判断 |
|---|---|---|---|
| 差速行驶、转向、停车 | R3X、TB6612、MSPM0 | 已有大量实车调试和V87基线 | 可直接复用底层驱动 |
| 黑线循迹 | 八路数字灰度、V87 | 已完成三态右弯控制和V85平滑出弯 | 只复用控制思想，不直接用于红线 |
| 红线循迹 | OpenMV H7、既有红色检测脚本 | 有红色blob识别基础，没有地面红线算法 | 硬件足够，需新算法和固定安装 |
| 1-8号起始识别 | OpenMV H7 | 没有数字识别代码；只有红色/黑框代码 | 可实现，需模板识别和数据集 |
| 行驶中识别随机房号 | OpenMV H7 | 未实现 | 可实现，需透视校正、分区识别和防抖 |
| 路口寻径 | MSPM0状态机经验 | 当前状态机针对固定黑线赛道和右弯 | 需新建F题拓扑导航层 |
| 200g药品承载 | R3X底盘、电池、电机 | 机械上有实现基础，尚无本题负载测试 | 需实际负载与制动测试 |
| 自动检测装药/卸药 | 无对应传感器或代码 | 缺失 | 必须新增微动开关/压力开关/FSR |
| 门口5cm区域停车 | 编码器硬件、视觉 | 当前V87未启用编码器，无门口识别 | 可实现，必须重新启用编码器并标定 |
| 红灯/绿灯 | 有LED和电阻采购记录 | 当前只有PA15单状态灯逻辑 | 基本硬件已有，需增加独立输出和状态表 |
| 黄色暂停灯 | 未确认黄色LED | 未实现 | 仅发挥部分需要 |
| 自动返航 | 差速底盘、状态机 | 无F题路径栈和掉头动作 | 可实现，需记录出程转向并逆序返回 |
| 双车协同 | 只有一套完整小车 | 无第二车、无无线模块 | 当前不可实现 |

## 3. 推荐系统方案

```text
固定斜向下 OpenMV
  ├─ 红线偏差/航向/路口分支
  ├─ 黑色数字框定位、透视校正、1-8模板识别
  └─ 门口虚线或红线终点检测
              │ UART + CRC + 帧序号
              v
MSPM0G3507
  ├─ 任务状态机和医院拓扑
  ├─ 编码器直行、转向、距离和精准停车
  ├─ TB6612电机控制
  ├─ 药盒微动开关消抖
  └─ 红/绿灯和蜂鸣器
```
MK
### 3.1 OpenMV输出建议

每帧向MSPM0发送：

- `line_valid`
- `line_offset`
- `line_angle`
- `junction_bits`：左、直、右是否存在红线
- `digit_id`：0表示无可靠数字，1-8表示房号
- `digit_confidence`
- `door_marker`
- `frame_sequence`
- `crc`

数字识别不必全程运行。普通行驶只跑红线；进入由编码器预测的号码区或路口区后降速，短时启用数字识别，降低OpenMV负载。

### 3.2 数字识别建议

不用先做神经网络。题目数字字模固定，优先采用：

1. 找黑色矩形边框。
2. 四角排序并做透视校正。
3. 裁掉边框，二值化数字区域。
4. 归一化到固定尺寸。
5. 与1-8模板计算像素相似度或轮廓特征。
6. 多帧投票，只接受高置信度且连续一致的结果。

只有模板法在真实两套字模、自然光和顶灯条件下不能达标时，再考虑小型分类模型。

### 3.3 MSPM0状态机建议

```text
BOOT
  -> READ_TARGET
  -> WAIT_LOAD
  -> OUTBOUND
  -> APPROACH_ROOM
  -> ROOM_STOP_RED
  -> WAIT_UNLOAD
  -> TURNAROUND
  -> RETURN
  -> HOME_STOP_GREEN
  -> DONE
```

任何红/绿/黄灯点亮之前必须先把电机命令清零，并确认短时间内编码器速度为零。灯亮期间禁止车辆运动。

## 4. 需要补齐的物品

### P0：完成单车基本要求必须有

| 物品 | 用途 | 当前情况 |
|---|---|---|
| 官方`数字字模.pdf`及两套打印卡 | 训练、标定和正式模拟 | 当前提供目录中缺失 |
| 白色亚光底、1.5-1.8cm红胶带、黑胶带 | 制作真实光学环境 | 红胶带和亚光底未确认 |
| 微动开关或压力开关1只 | 检测有药/无药 | 缺失；优先微动开关 |
| 弹性药盒托盘和限位结构 | 让约200g药品稳定压下开关 | 缺失 |
| OpenMV固定俯视/斜视支架 | 保持视野和标定不漂移 | 需改装现有结构 |
| 红、绿LED及330Ω电阻 | 送达和返药房指示 | 采购记录显示已有，实物需清点 |

微动开关比HX711更适合这次模拟：只需可靠区分有药和无药，接线简单、响应快、软件风险低。若药盒形状变化很大，再升级到FSR或称重传感器。

### P1：提高可靠性

- 备用微动开关、备用LED和电阻。
- 可调相机支架，但标定完成后必须锁紧。
- 独立5V稳压与足量去耦，给OpenMV供电。
- 小型保险丝或自恢复保险丝，降低此前跨电源域故障风险。

### P2：只为双车发挥部分

- 第二套底盘、编码器电机、TB6612、MSPM0、OpenMV、电池、稳压、药盒检测和状态灯。
- 两个无线收发模块及稳定供电。
- 黄色LED至少两只。
- 第二套完整数字识别和导航软件，不应依赖第一车替它识别。

## 5. 不应直接沿用的内容

- 不把V87的八路灰度误差直接当作红线误差。
- 不把V87的右弯三状态逻辑改名后硬套医院路口；F题需要左、直、右和掉头四种动作。
- 不再投入已损坏MPU。编码器加视觉重捕线足够完成本题。
- 不保留激光、感光纸和二维瞄准云台；它们增加高度、重量、供电波动和相机姿态变化。
- 不在现有V87文件上直接开发。新建F题分支和独立目录，保留V87作为可回退基线。

## 6. 一步一步实施流程

每一步都设验收门槛。未通过门槛时不进入下一步。

### 第0步：冻结基线并测量机械边界

操作：

1. 保留当前V87源码、编译产物和哈希，不覆盖。
2. 清点一套车的主控、底盘、编码器、OpenMV、电源、LED、开关和线材。
3. 测量最终预估外形，必须小于`25cm x 20cm x 25cm`。
4. 将200g配重放在计划药盒位置，检查起步、制动、轮胎打滑和重心。
5. 测量小车原地旋转的实际扫掠直径。

验收：200g下能稳定起步和刹停；尺寸合规；扫掠直径小于28cm时才允许在30cm门口区域原地掉头，否则改为退回60cm路口再掉头。

### 第1步：制作最小真实测试场

先不要做整张医院地图，只做：

- 1m红线直道。
- 一个十字/T形路口。
- 一个30cm深门口和黑白虚线边界。
- 红线旁2cm放一张黑色数字卡。

验收：线宽、数字卡尺寸和相对距离与题目一致；白底为亚光，能在自然光和顶灯下测试。

### 第2步：完成传感器路线决策

操作：

1. 固定OpenMV临时角度，采集白底、红线、黑线、黑色数字卡和虚线门口图像。
2. 在自然光、顶灯、两种电池电压下各采集至少100帧。
3. 验证红线阈值是否会吸附到黑色数字或墙线。
4. 八路灰度仅在5V/3.3V接口安全已重新证明时做对照测试，禁止使用旧短路板或把5V OUT直连PA7。

验收：OpenMV能够稳定把红线与黑色数字/墙线分开。默认确认OpenMV为主导航，灰度不再成为关键路径。

### 第3步：机械和电气改装

操作：

1. 移除激光和二维云台，将OpenMV固定斜向下，锁紧相机姿态。
2. 安装药盒托盘和微动开关，200g放入时可靠闭合，取走后可靠释放。
3. 恢复左右编码器接线；当前历史引脚为`PB12/PB13`和`PB14/PB15`。
4. 增加独立红、绿LED。可优先审计已释放的`PA8/PB9/PB10`，最终引脚以完整冲突检查为准。
5. OpenMV继续使用稳定5V；MSPM0使用稳定3.3V；全系统共地，禁止带电插拔。

验收：静态运行10分钟无电压抬升、模块发热或复位；相机和药盒不超尺寸；开关连续装卸50次无误触发。

### 第4步：分别完成四个单元测试固件

依次测试，不立即集成：

1. 编码器计数、方向和每毫米脉冲标定。
2. 微动开关消抖和有药/无药状态。
3. 红绿灯真值表，确保灯亮时PWM为0。
4. OpenMV UART帧、帧序号、CRC和超时停车。

验收：每项连续运行至少5分钟；拔掉OpenMV或产生坏帧时车辆只能停车，不能保留旧转向命令继续跑。

### 第5步：只做药房起始数字识别

操作：

1. 用官方两套字模采集1-8号在不同角度和照明下的图像。
2. 完成黑框定位、透视校正、模板比较和多帧投票。
3. 识别成功后锁存目标号码；低置信度时继续等待，不能猜测。
4. 识别阶段车辆必须静止。

验收：每个数字至少20次展示，至少19次正确，且错误数字不能被高置信度接受；任何一次识别时间小于20s。

### 第6步：只做红线闭环循迹

操作：

1. OpenMV下方ROI提取红线中心和方向，输出`line_offset`与`line_angle`。
2. MSPM0用偏移和角度组合成转向修正。
3. 复用V87的中位数抑噪、状态迟滞、斜率限制、跨零保护和安全停车思想，但重新标定参数。
4. 黑色数字卡、黑墙线和门口虚线放在真实位置，验证不会抢占红线。

验收：200g负载下直线、左转、右转各连续10次不碰黑墙；红线丢失后在限定距离内停车；黑色数字卡不会改变主导航目标。

### 第7步：建立带编码器的运动原语

只实现以下函数，不写完整任务：

- `follow_line_until_junction()`
- `turn_left_and_reacquire()`
- `go_straight_through_junction()`
- `turn_right_and_reacquire()`
- `enter_door_and_stop()`
- `turn_around()`
- `reverse_straight_to_junction()`

转弯用编码器限制最大角度/距离，视觉负责重捕红线。门口停车用“视觉标记或红线终点 + 编码器低速补偿”双条件。

验收：左转、直行、右转和掉头各20次至少19次成功；车头投影进入门口区域且停车误差控制在约2cm内；全程不压黑墙线。

### 第8步：实现医院拓扑和随机房号映射

操作：

1. 把地图建成节点，不用自由空间规划：药房、近端路口、中部路口、顶部中央路口、左右远端路口、各房门。
2. 1/2号位置固定，直接写入地图。
3. 到中部号码区时读取左右两张卡；目标在其中则转入，否则继续。
4. 到顶部中央号码区时读取四张远端卡，确定先向左还是向右，再在外侧路口选择上/下房门。
5. 出程每次动作压入路径栈；返程逆序取出并做反向动作，不重新猜路径。

验收：在电机架空或低速模式下更换3-8号位置，所有目标都能生成唯一合法路径；任何号码缺失或冲突时停车报警，不默认选路。

### 第9步：集成完整装卸任务状态机

流程：

1. 复位后静止识别目标号。
2. 等待药品放入并消抖确认。
3. 自动起步送药。
4. 到房门后完全停车，点亮红灯。
5. 等待药品取走并消抖确认。
6. 熄灭红灯，掉头并按路径栈返航。
7. 回到药房完全停车，点亮绿灯。

验收：不按额外按键、不人工干预；空载不会提前出发；送达前红灯不亮；卸载前不返回；运动时所有指示灯熄灭。

### 第10步：先拿近端房间

1号固定左转，2号固定右转，不启用随机号码寻径。

验收：1号和2号各连续10次，200g负载下送达和返回都小于18s，给正式20s限制留2s余量；10次均无碰墙、越线和停车越界。

### 第11步：再做中部房间

启用中部两张随机号码卡识别和路口决策。

验收：覆盖目标位于左/右以及目标不在中部三种情况；至少20组随机摆放无误选，送达和返回都小于18s。

### 第12步：最后做远端房间

启用顶部四张卡识别、左右远端路口和多次转向。返程只使用已记录路径栈。

验收：至少30组3-8号随机布局；所有六个随机位置都被覆盖；无误选房间；送达和返回都小于18s。

### 第13步：正式可靠性回归

必须覆盖：

- 自然光、顶灯、混合光。
- 满电、中等电量和允许的最低电压。
- 200g药品位置前后偏移。
- 两套数字卡、轻微旋转和褶皱。
- 一次复位后完整任务，不连接CCS。
- OpenMV丢帧、数字拒识、红线短时丢失和药品开关抖动。

最终门槛：连续30次完整单车任务无人工干预、无碰墙、无错误亮灯、无误选房间；时间统计使用P95而不是只看最快一次。

### 第14步：单车稳定后才考虑双车

双车应复制已经验证的单车软硬件，不使用不同架构。无线协议至少包含：

- 消息类型、任务号、序号、CRC。
- `READY`、`START`、`PAUSE_REACHED`、`UNLOADED`、`CONTINUE`、`ARRIVED`。
- ACK、重发和超时停车。

发挥部分（1）还要验证暂停点与车1的红线路径距离不小于70cm；任何黄灯亮时车2必须完全停止。双车不得同走廊错车或超车。

## 7. 实施优先级

1. 先完成红线视觉和数字模板识别的可行性验证。
2. 完成装卸检测、编码器和门口停车。
3. 完成近端，锁定12分能力。
4. 完成中部，增加18分能力。
5. 完成远端，补齐单车50分。
6. 只有单车连续30次稳定后，才采购和复制第二车冲击发挥部分。

## 8. 三人分工建议

- 队员A：OpenMV红线、路口、数字识别和采图数据。
- 队员B：MSPM0编码器、电机运动原语、UART和状态机。
- 队员C：药盒/微动开关、赛道制作、尺寸与负载、自动化测试记录。

每天合并前只交换已通过单元门槛的接口，不在同一份`empty.c`中并行手改。
