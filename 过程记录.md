# 过程记录

## 2026-07-25 V105 视频诊断启动

- supplemental recall：V104 保留25 cm首路口武装、`JUNCTION_TRIGGER_ACTIVE_MIN=4`、单帧宽线确认；用户反馈两段实车视频显示小车提前左转，要求逐帧核对蓝灯和车体状态。
- 已读取视频分析技能的工作流、模板分析、项目记录和视觉 QA 要求；未对原视频进行剪辑或覆盖。
- 已建立临时副本 `codex_two_video_diag\video1.mp4`、`video2.mp4`，用于 OpenCV 全帧分析；两段源视频分别约426帧和380帧，约30 fps。
- 初步结论：视频可见蓝灯多为中央区域且不存在可持续的4路证据，但10 ms MCU采样与33.3 ms视频采样不同步，单次瞬态不能由视频排除；V104的25 cm门控过早，是当前首要软件风险。
- 下一步：完成视觉子任务复核，新增V105源码契约测试，生成只读备份并进行源码/Host/极简循迹/TI语法回归。

## 2026-07-08 靶纸照片复核与 OpenMV 靶心识别优化

- 用户提供现场照片：靶纸贴在墙面低位，小车在白色赛道板上循迹，OpenMV/云台位于车体上方，任务目标从“追红色目标”明确变为“激光笔持续瞄准同心圆靶心”。
- 判断：旧 OpenMV 脚本 `20260707_144221_openmv_uart_stream_qvga_far_stable_v2.py` 使用红色 blob 中心，面对同心圆靶纸会误追圆环局部或顶部红色胶带；需要输出圆心而不是红色面积中心。
- 本次动作：新增独立 OpenMV 靶心版脚本与说明，不覆盖既有可运行版本；MSPM0 端协议保持兼容，仅建议/提供小死区瞄准参数版本。
- 生成文件：`ccs_project/openmv_test/20260708_openmv_uart_stream_ring_center_v1.py`、`ccs_project/integration_test/20260708_120000_v23_ring_center_laser_aim.c`、`ccs_project/openmv_test/20260708_ring_center_laser_aim_readme.md`。
- V23 瞄准端新增 `LASER_CALIB_X_PIXELS/Y_PIXELS`，用于补偿激光笔与 OpenMV 镜头不完全同轴造成的固定偏差，默认均为 0。
- 验证：OpenMV 脚本已执行 `python -m py_compile` 语法检查通过；MSPM0 V23 文件未覆盖活动工程，未自动烧录。

## 2026-07-08 OpenMV 左侧红色光影误检修复

- 用户反馈：即使非常靠近墙面，OpenMV 仍未识别靶心，IDE 帧缓冲中绿色框落在左下角红色光影区域。
- 复核截图：同心圆在当前曝光下偏灰，红色饱和度低；左侧墙面/地面交界处光影反而更符合红色阈值，因此单纯红色 blob 策略会稳定选错。
- 修复策略：`20260708_openmv_uart_stream_ring_center_v1.py` 改为优先使用霍夫圆/同心圆结构检测；默认关闭红色 blob 回退 `ENABLE_RED_BLOB_FALLBACK=False`，避免误瞄红色光影。
- 同步收紧 blob 回退条件：更大的最小目标宽高、更严格长宽比、最大密度限制和更低的高密度小红块拒绝阈值。
- 验证：已重新执行 `python -m py_compile ccs_project/openmv_test/20260708_openmv_uart_stream_ring_center_v1.py`，语法检查通过；现场效果需在 OpenMV IDE 中观察黄色圆和绿色十字。

## 2026-07-08 OpenMV 改为优先识别最小红色圆环

- 用户追问：为什么不识别视线范围内最小的红色圆形，再定位圆心。
- 判断：该思路更适合当前靶纸；内圈红色圆环与靶心同心，且比外圈受纸张边缘、透视和墙面光影影响更小。
- 修复：`20260708_openmv_uart_stream_ring_center_v1.py` 新增 `ENABLE_SMALL_RED_RING_PRIMARY=True`、`find_small_red_ring_target()`，优先筛选宽高适中、近似正圆、非高密度、非顶部胶带的最小红色圆环。
- 调整：放宽 `RED_THRESHOLDS` 的 `a_min` 以覆盖偏灰红棕圆环；保留霍夫同心圆作为第二级 fallback；红色 blob 泛化回退仍默认关闭。
- 验证：已执行 `python -m py_compile ccs_project/openmv_test/20260708_openmv_uart_stream_ring_center_v1.py`，语法检查通过；现场应先观察最小内圈附近是否出现黄色圆与绿色十字。

## 2026-07-06 21:48 巡迹 B 点瞄准 + C/D 提示 + A 停车 V2

- 用户反馈：B 点停车不错，第一圈能找到靶子，后续 AB 段 OpenMV 背对靶子暂不影响基本要求，允许进入下一步。
- 实现目标：补齐基本要求链路：A 出发，B 停车瞄准，继续巡迹，C/D 点 LED 提示，回 A 停车。
- 修改当前活动工程：`C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`。
- 备份：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260706_214843_before_keypoint_prompt_empty.c`。
- 时间戳源码：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260706_214843_line_follow_b_aim_cda_prompt_v2.c`。
- 说明文件：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260706_214843_line_follow_b_aim_cda_prompt_v2_readme.md`。
- 新增参数：`MISSION_C_PROMPT_SAMPLE=360`、`MISSION_D_PROMPT_SAMPLE=540`、`MISSION_A_FINISH_SAMPLE=720`、`MISSION_POINT_PROMPT_FRAMES=40`。
- 新增状态变量：`g_mission_c_done`、`g_mission_d_done`、`g_mission_a_done`、`g_mission_last_point`、`g_mission_prompt_frames`。
- 当前关键点定位仍使用 20ms 控制周期计数，后续现场逐点微调。
- 验证：CCS `gmake all` 编译通过；未自动烧录。

## 2026-07-06 20:57 巡迹到 B 停车静态瞄准 V1

- 用户确认：不优先做整圈动态 OpenMV 跟随，开始下一步基本要求链路。
- 实现目标：小车从 A 出发循迹，到估计 B 点停车；停车后 OpenMV + PA14/PA17 云台静态瞄准；瞄准稳定或超时后继续巡迹。
- 修改当前活动工程：`C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`。
- 备份：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260706_205751_before_mission_b_stop_aim_empty.c`。
- 时间戳源码：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260706_205751_line_follow_b_stop_static_aim_v1.c`。
- 说明文件：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260706_205751_line_follow_b_stop_static_aim_v1_readme.md`。
- 新增参数：`MISSION_B_STOP_SAMPLE=180`、`MISSION_AIM_MAX_FRAMES=250`、`MISSION_AIM_STABLE_FRAMES=20`。
- 新增状态变量：`g_mission_phase`、`g_mission_b_done`、`g_mission_aim_success`、`g_mission_aim_frames`、`g_mission_aim_stable_frames`。
- 说明：B 点位置目前用 20ms 控制周期计数估计，第一轮现场重点调 `MISSION_B_STOP_SAMPLE`。
- 验证：CCS `gmake all` 编译通过；未自动烧录。

## 2026-07-06 20:42 回退急弯温和化版本

- 用户要求：回退。
- 操作：将当前活动工程 `C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c` 恢复为 `20260706_201926_stable_line_openmv_servo_open_loop_balance_fix.c`。
- 回退前备份：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260706_204209_before_revert_softer_sharp_turn_empty.c`。
- 回退后时间戳源码：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260706_204209_revert_to_open_loop_balance_fix.c`。
- 说明文件：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260706_204209_revert_to_open_loop_balance_fix_readme.md`。
- 恢复参数：基础/急弯 PWM 为 `403/403`；急弯参数恢复为 `LINE_SHARP_ERROR=1800`、`LINE_SHARP_KP_DIVISOR=8`、`LINE_SHARP_CORR_LIMIT=380`。
- 验证：刷新 `empty.c` 时间戳后 CCS `gmake all` 重新编译通过；未自动烧录。

## 2026-07-06 20:36 右偏后朝左修正过度：降低急弯强度

- 用户反馈：小车偏离黑线向右后，朝左边的修正太过度，并询问当前参数是否对称。
- 复查：左右基础 PWM 和普通修正对称；当前更可疑的是低速档 `403` 下急弯修正上限仍为 `380`，一侧轮可能被压到接近停转，导致回拉过头。
- 修改当前活动工程：`C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`。
- 备份：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260706_203612_before_softer_sharp_turn_empty.c`。
- 时间戳源码：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260706_203612_stable_line_openmv_servo_softer_sharp_turn.c`。
- 说明文件：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260706_203612_stable_line_openmv_servo_softer_sharp_turn_readme.md`。
- 改动：`LINE_SHARP_ERROR 1800 -> 2200`、`LINE_SHARP_KP_DIVISOR 8 -> 10`、`LINE_SHARP_CORR_LIMIT 380 -> 260`。
- 意图：中等偏差继续用普通循迹；真正大偏差才进入急弯；急弯回拉更温和。
- 验证：CCS `gmake all` 编译通过；未自动烧录。

## 2026-07-06 20:19 回正后右前方走：开环左右轮基础平衡修正

- 用户判断：小车每次回正之后都会往右前方走，再回正；这更像每次回正后左轮仍比右轮快。
- 复核当前代码：上一版基础 PWM 为 `408/398`，当 `g_line_correction=0` 时左轮仍比右轮快，确实会形成“回正后继续右偏，再被循迹拉回”的周期性现象。
- 修正策略：撤掉上一版右偏回正增强，改为修根因，即左右基础 PWM 相等。
- 修改当前活动工程：`C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`。
- 备份：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260706_201926_before_open_loop_balance_fix_empty.c`。
- 时间戳源码：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260706_201926_stable_line_openmv_servo_open_loop_balance_fix.c`。
- 说明文件：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260706_201926_stable_line_openmv_servo_open_loop_balance_fix_readme.md`。
- 改动：普通/急弯基础 PWM 从 `408/398` 改为 `403/403`，平均速度保持 `403`；删除 `LINE_RIGHT_OVERSHOOT_BOOST_*` 和 `boost_right_overshoot_correction()`。
- 验证：CCS `gmake all` 编译通过；未自动烧录。

## 2026-07-06 20:12 视频分析后修正右侧偏出再回正

- 用户上传视频并指出：小车明显向右多偏出一点再回正。
- 视频判断：现象不是全程基础右偏，而是右侧误差出现后回正滞后；继续改左右基础 PWM 容易破坏直线基础平衡。
- 修改策略：在当前稳定 baseline + OpenMV/舵机 + `2/3` 速度 + 右偏微调版本上，增加负方向修正的 1.2 倍补偿。
- 修改当前活动工程：`C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`。
- 备份：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260706_201232_before_right_overshoot_fix_empty.c`。
- 时间戳源码：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260706_201232_stable_line_openmv_servo_right_overshoot_fix.c`。
- 说明文件：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260706_201232_stable_line_openmv_servo_right_overshoot_fix_readme.md`。
- 新增参数：`LINE_RIGHT_OVERSHOOT_BOOST_NUM=6`、`LINE_RIGHT_OVERSHOOT_BOOST_DEN=5`。
- 接入位置：普通循迹修正和急弯/丢线找回修正；当前只增强负修正方向。
- 风险记录：若现场现象反而更严重，说明右偏对应的误差方向判断反了，下一版应改为增强正修正方向。
- 验证：CCS `gmake all` 编译通过；未自动烧录。

## 2026-07-06 20:02 `2/3` 速度版固定偏右微调

- 用户反馈：当前稳定 baseline + OpenMV/舵机 + `2/3` 速度版仍有一点默认偏右。
- 判断：当前 `413/393` 继承 baseline 左高右低差值，当前电池/负重下差值偏大。
- 修改当前活动工程：`C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`。
- 备份：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260706_200241_before_right_bias_trim_empty.c`。
- 时间戳源码：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260706_200241_stable_line_openmv_servo_speed_2of3_right_bias_trim.c`。
- 说明文件：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260706_200241_stable_line_openmv_servo_speed_2of3_right_bias_trim_readme.md`。
- 改动：普通/急弯基础 PWM 从 `413/393` 改到 `408/398`，平均速度仍为 `403`，左右差值从 `20` 降到 `10`。
- 验证：CCS `gmake all` 编译通过；未自动烧录。

## 2026-07-06 19:54 稳定循迹 baseline + OpenMV/舵机版本降速到 2/3

- 用户要求：在刚才基于 `line_follow_sharp_turn_stable_baseline.c` 加 OpenMV/舵机的版本上，将速度降低为原来的 `2/3`。
- 修改当前活动工程：`C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`。
- 备份：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260706_195455_before_baseline_openmv_speed_2of3_empty.c`。
- 时间戳源码：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260706_195455_stable_line_baseline_plus_openmv_servo_speed_2of3.c`。
- 说明文件：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260706_195455_stable_line_baseline_plus_openmv_servo_speed_2of3_readme.md`。
- 改动：`620/590 -> 413/393`，`PWM_MIN 430 -> 287`，`PWM_MAX 850 -> 567`，急弯 PWM 同步降到 `413/393`、`PWM_SHARP_MAX=567`。
- 保留：baseline 循迹 `20/150`、急弯 `1800/8/380`、`LINE_CENTER_OFFSET=0`、OpenMV/舵机逻辑。
- 验证：CCS `gmake all` 编译通过；未自动烧录。

## 2026-07-06 19:51 基于稳定循迹 baseline 加 OpenMV/舵机

- 用户要求：以 `C:\Users\1\Desktop\电赛\ccs_project\line_sensor_test\line_follow_sharp_turn_stable_baseline.c` 为基础，加上 OpenMV 和舵机。
- 实现策略：保留当前集成版主循环和 OpenMV/PA14/PA17 双舵机逻辑，因为舵机需要 20ms 软件 PWM 帧；将循迹核心参数/误差中心恢复为用户指定的稳定 baseline。
- 修改当前活动工程：`C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`。
- 备份：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260706_195125_before_stable_baseline_plus_openmv_empty.c`。
- 时间戳源码：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260706_195125_stable_line_baseline_plus_openmv_servo.c`。
- 说明文件：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260706_195125_stable_line_baseline_plus_openmv_servo_readme.md`。
- 恢复循迹基线：`PWM_LEFT_BASE_TICKS=620`、`PWM_RIGHT_BASE_TICKS=590`、`PWM_MIN_TICKS=430`、`PWM_MAX_TICKS=850`、`LINE_KP_DIVISOR=20`、`LINE_CORR_LIMIT=150`、`LINE_CENTER_OFFSET=0`、急弯 `1800/8/380`。
- 保留集成内容：OpenMV UART0、PA14/PA17 双舵机、V9 丢目标搜索/边缘保护、用户调过的舵机参数。
- 说明：运行时间保持集成版约 90 秒；没有使用 baseline 原始约 12 秒测试时长。
- 验证：CCS `gmake all` 编译通过；未自动烧录。

## 2026-07-06 19:46 V12：速度按 3/5 缩放

- 用户反馈：V11 后车速仍太快，要求“减慢 2/5”。
- 执行解释：按字面理解为保留当前速度的 `3/5`，即 PWM 降低 40%。
- 修改当前活动工程：`C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`。
- 备份：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260706_194607_before_v12_slow_3of5_empty.c`。
- 时间戳源码：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260706_194607_line_follow_openmv_servo_v12_slow_3of5.c`。
- 说明文件：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260706_194607_line_follow_openmv_servo_v12_slow_3of5_readme.md`。
- 改动：`PWM_LEFT_BASE_TICKS=357`、`PWM_RIGHT_BASE_TICKS=369`、`PWM_MIN_TICKS=258`、`PWM_MAX_TICKS=510`、`PWM_SHARP_LEFT_BASE_TICKS=363`、`PWM_SHARP_RIGHT_BASE_TICKS=363`、`PWM_SHARP_MAX_TICKS=510`。
- 风险：该档位可能因负重导致地面起步无力；若发生，优先加短启动助推而不是整体提速。
- 验证：CCS `gmake all` 编译通过；未自动烧录。

## 2026-07-06 19:42 V11：固定偏右微调

- 用户反馈：V10 低速版小车有一点固定偏右。
- 判断：固定偏右优先按左右轮基础 PWM 不平衡处理；暂不改 `LINE_CENTER_OFFSET`、急弯策略或 OpenMV。
- 修改当前活动工程：`C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`。
- 备份：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260706_194219_before_v11_right_bias_empty.c`。
- 时间戳源码：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260706_194219_line_follow_openmv_servo_v11_right_bias_trim.c`。
- 说明文件：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260706_194219_line_follow_openmv_servo_v11_right_bias_trim_readme.md`。
- 改动：`PWM_LEFT_BASE_TICKS 605 -> 595`、`PWM_RIGHT_BASE_TICKS 605 -> 615`，平均速度仍为 `605`。
- 验证：CCS `gmake all` 编译通过；未自动烧录。

## 2026-07-06 19:36 V10：电池电压提高后恢复早期速度档

- 用户反馈：电池电压提高后，小车速度过快，要求恢复成最开始的速度。
- 操作原则：只调整电机 PWM 速度档，不回退 V9 OpenMV 丢目标搜索逻辑，不修改 `LINE_CENTER_OFFSET=500`。
- 修改当前活动工程：`C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`。
- 备份：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260706_193630_before_restore_initial_speed_empty.c`。
- 时间戳源码：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260706_193630_line_follow_openmv_servo_v10_initial_speed.c`。
- 说明文件：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260706_193630_line_follow_openmv_servo_v10_initial_speed_readme.md`。
- 改动：`PWM_LEFT_BASE_TICKS=605`、`PWM_RIGHT_BASE_TICKS=605`、`PWM_LEFT_BOOST_TICKS=605`、`PWM_RIGHT_BOOST_TICKS=605`、`PWM_MIN_TICKS=430`、`PWM_MAX_TICKS=850`、`START_BOOST_SAMPLES=0`。
- 判断：早期循迹基线原始速度为 `620/590`，平均约 `605`；本版使用 `605/605` 保留当前左右平衡，避免重新引入固定偏转。
- 验证：CCS `gmake all` 编译通过；未自动烧录。

## 2026-07-06 16:35 高扭矩合并 V9：OpenMV 丢目标搜索与边缘保护

- 用户反馈：`LINE_CENTER_OFFSET` 相关参数不用继续改，并上传运行视频；视频中可看到车辆过弯后 OpenMV 已经丧失目标。
- 判断：当前问题不应继续优先调 S4/S5 居中，而应处理视觉闭环。旧逻辑存在三个问题：目标丢失后只保持最后姿态；边缘半截目标仍会被追踪；`OpenMV link lost` 分支仍会慢慢回 HOME，可能在 P4/P5 瞬断时造成云台摇摆。
- 修改当前活动工程：`C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`。
- 保存 V9 源码：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260706_163540_line_follow_openmv_servo_high_torque_v9_lost_target_search.c`。
- 保存说明：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260706_163540_line_follow_openmv_servo_high_torque_v9_lost_target_search_readme.md`。
- 新 OpenMV 脚本：`C:\Users\1\Desktop\电赛\ccs_project\openmv_test\20260706_163540_openmv_uart_stream_edge_guard_rot180.py`。
- 保留底盘参数：`LINE_CENTER_OFFSET=500`、`LINE_KP_DIVISOR=16`、`LINE_CORR_LIMIT=190`，急弯/高扭矩参数保持 V8。
- 同步用户截图舵机参数：`SERVO_X_HOME_US=1600`、`SERVO_Y_HOME_US=1500`、`SERVO_X_MIN_US=350`、`SERVO_X_MAX_US=2650`、`SERVO_Y_MIN_US=500`、`SERVO_Y_MAX_US=2500`。
- 新增 `g_aim_state`：`8=边缘目标拒绝`、`9=丢目标搜索`、`10=底盘急弯/丢线期间保持云台`。
- 行为：目标丢失后先短暂保持；若底盘仍在急弯/丢线，则继续保持；底盘恢复普通循迹且仍无目标时，水平舵机按上次目标方向有限扫描找回目标；断链不再自动回 HOME。
- 验证：CCS `gmake all` 编译通过；OpenMV 脚本 `python -m py_compile` 通过；未自动烧录。

## 2026-07-03 20:44 OpenMV + 双舵机静态闭环瞄准版本

- 用户要求：OpenMV 测试通过后，继续写“OpenMV 目标识别 + 双舵机自动瞄准”的功能代码。
- 本次生成 MSPM0 静态瞄准代码：
  - `C:\Users\1\Desktop\电赛\ccs_project\openmv_test\20260703_204436_mspm0_openmv_servo_static_aim.c`
- 本次生成说明文档：
  - `C:\Users\1\Desktop\电赛\ccs_project\openmv_test\20260703_204436_static_aim_readme.md`
- 当前活动工程已替换：
  - `C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`
- 已在 `C:\Users\1\Desktop\ELECTRIC\ele-car\Debug` 下 clean build 通过，生成新的 `ele-car.out`。
- 使用的 OpenMV 端脚本仍是：
  - `C:\Users\1\Desktop\电赛\ccs_project\openmv_test\20260703_162934_openmv_full_selftest.py`
- 控制逻辑：
  - MSPM0 周期发送 `C` 命令。
  - OpenMV 回传红色目标 `cx/cy/pixels/fps/l_mean`。
  - MSPM0 用 `cx-160` 控制 `PA14` 水平舵机，用 `cy-120` 控制 `PA17` 俯仰舵机。
  - 设有中心死区：X 方向 12 像素，Y 方向 10 像素。
  - 每次最大舵机调整为 `8us`，舵机范围限制在 `1250us~1750us`，第一版以安全温和为主。
  - 目标丢失一段时间后，云台慢慢回中。
- 为避免舵机 PWM 阻塞 UART，车端改为 `UART0_IRQHandler` RX 中断解析 OpenMV 帧。
- 安全状态：
  - `PB4/STBY` 保持 LOW，直流电机不运行。
  - 只输出 `PA14/PA17` 舵机 PWM。
- 若现场发现追踪方向反了：
  - 左右反：改 `AIM_X_PULSE_SIGN`。
  - 上下反：改 `AIM_Y_PULSE_SIGN`。

## 2026-07-03 OpenMV 自检通过后的下一步

- 用户反馈：OpenMV 测试没有问题。
- 当前已确认模块：
  - 循迹：稳定基线可跑，圆角弯、S 弯、直角弯均已通过。
  - 双舵机：`PA14/PA17` 已可正常驱动二维云台。
  - OpenMV：本体取图/脚本运行/视觉测试正常。
- 下一步不建议直接整车联调，应先做“OpenMV + 双舵机闭环瞄准”静态测试：
  - OpenMV 输出目标 `cx/cy`。
  - MSPM0 根据 `cx/cy` 偏差调 `PA14/PA17` 舵机。
  - 目标是让画面中心自动追到红色目标/靶心附近。
- 该阶段先不让电机运行，`PB4/STBY` 保持低电平，避免小车移动干扰视觉闭环。

## 2026-07-03 16:29 OpenMV 完整自检代码生成

- 用户确认：PA14/PA17 双舵机已经可以正常工作，进入 OpenMV 功能验证阶段。
- 本次目标：先隔离 OpenMV 模块，不驱动电机、不驱动舵机动作，只验证：
  - OpenMV 本体：摄像头初始化、画面刷新、帧率、亮度统计、红色目标识别。
  - OpenMV 与 MSPM0：`PA10/PA11` 上的 UART0 双向通信、帧头帧尾、校验和、坐标数据解析。
- 已生成 OpenMV 端脚本：
  - `C:\Users\1\Desktop\电赛\ccs_project\openmv_test\20260703_162934_openmv_full_selftest.py`
- 已生成 MSPM0 端 CCS 测试代码：
  - `C:\Users\1\Desktop\电赛\ccs_project\openmv_test\20260703_162934_mspm0_openmv_full_selftest.c`
- 已生成测试说明：
  - `C:\Users\1\Desktop\电赛\ccs_project\openmv_test\20260703_162934_openmv_full_selftest_readme.md`
- 当前活动工程已替换：
  - `C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`
- 已在 `C:\Users\1\Desktop\ELECTRIC\ele-car\Debug` 下 clean build 通过，生成新的 `ele-car.out`。
- 接线规则：
  - `OpenMV P4 / UART3_TX -> MSPM0 PA11 / UART0_RX`
  - `OpenMV P5 / UART3_RX <- MSPM0 PA10 / UART0_TX`
  - `OpenMV GND -> MSPM0 GND`
  - 联调前拔掉 LaunchPad `J21/J22` backchannel UART 跳线帽，避免 XDS110 虚拟串口干扰 UART0。
- 当前 MSPM0 代码保护动作：
  - `PB4/STBY` 拉低，`PB0/PB1/PB2/PB3` 全部拉低，电机禁用。
  - `PA14/PA17` 舵机信号输出低电平，不产生舵机 PWM。

## 2026-07-03 16:20 循迹验证正常后再次恢复双舵机测试

- 用户反馈：恢复循迹基线后运行没有问题，说明板子、烧录、供电和程序运行链路正常。
- 当前活动工程 `C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c` 已再次恢复为 `PA14/PA17` 双舵机联动测试版。
- 新保存版本：
  - `C:\Users\1\Desktop\电赛\ccs_project\servo_test\20260703_162014_restore_dual_servo_after_line_ok.c`
- 已在 `C:\Users\1\Desktop\ELECTRIC\ele-car\Debug` 下 clean build 通过，生成新的 `ele-car.out`。
- 当前判断：如果这版烧录后 `PA15 LED` 仍不翻转或两个舵机不动，优先排查 CCS 是否 Resume 运行、`PA14/PA17` 实际排针位置、舵机信号线是否接到正确颜色线，而不是怀疑板子整体损坏。

## 2026-07-03 16:18 恢复循迹基线用于板子运行性测试

- 用户反馈：确认双舵机程序已烧录，但 `PA15 LED` 不闪；希望先烧回循迹代码验证板子/程序运行状态。
- 当前活动工程 `C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c` 已从双舵机测试版恢复为稳定循迹基线：
  - 来源：`C:\Users\1\Desktop\电赛\ccs_project\line_sensor_test\line_follow_sharp_turn_stable_baseline.c`
  - 新保存版本：`C:\Users\1\Desktop\电赛\ccs_project\line_sensor_test\20260703_161814_restore_line_follow_stable_for_board_check.c`
- 已在 `C:\Users\1\Desktop\ELECTRIC\ele-car\Debug` 下 clean build 通过，生成新的 `ele-car.out`。
- 注意：该代码会重新控制 TB6612：
  - `PB0/PB1/PB2/PB3/PB4`
  - `PA12/PA13`
  - 若巡线传感器检测到黑线，会驱动电机运行约 12 秒；测试时必须车轮悬空或断开电机 VM。
- 该版本启动现象：
  - 上电后 `PA15 LED` 先闪 6 次。
  - 如果没有检测到黑线，进入停止状态并持续短闪。
  - 如果检测到黑线，进入循迹运行。

## 2026-07-03 15:42 恢复双舵机联动测试

- 用户确认此前异常测量来自万用表黑表笔未插好，供电/电压问题解除。
- 当前活动工程 `C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c` 已从 `PA14/PA17` GPIO 电平诊断版恢复为双舵机联动测试版。
- 恢复来源：
  - `C:\Users\1\Desktop\电赛\ccs_project\servo_test\20260703_135829_dual_servo_pa14_pa17_linkage_test.c`
- 新保存的恢复版本：
  - `C:\Users\1\Desktop\电赛\ccs_project\servo_test\20260703_154200_restore_dual_servo_pa14_pa17_linkage_test.c`
- 已在 `C:\Users\1\Desktop\ELECTRIC\ele-car\Debug` 下 clean build 通过，生成新的 `ele-car.out`。
- 当前测试接线仍为：
  - `PA14 -> X/水平舵机信号`
  - `PA17 -> Y/俯仰舵机信号`
  - `TB6612 5V/GND -> 两个舵机红线/黑棕线`
  - `MSPM0 GND -> TB6612 GND`

## 2026-07-03 14:09 PA14/PA17 双舵机不动后的物理电平诊断

- 用户反馈：按 `PA14/PA17` 双舵机联动测试接线后，两个舵机都不动。
- 判断：两个舵机已经分别用 `PA12` 验证过正常，因此优先怀疑 `PA14/PA17` 物理排针没有输出到舵机信号线、排针位置找错、或这两个候选脚仍不适合当前板级接线；不优先怀疑舵机本体。
- 当前活动工程 `C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c` 已替换为 `PA14/PA17` 慢速 GPIO 电平诊断程序：
  - phase 1：`PA14=HIGH`，`PA17=LOW`，约 3 秒。
  - phase 2：两者 LOW，约 1 秒。
  - phase 3：`PA14=LOW`，`PA17=HIGH`，约 3 秒。
  - phase 4：两者 LOW，约 1 秒。
- 测试要求：先拔掉两个舵机的信号线，只用万用表测 `PA14-GND` 和 `PA17-GND`，看是否能稳定切换到约 3.3V。
- 时间戳源码已保存：
  - `C:\Users\1\Desktop\电赛\ccs_project\servo_test\20260703_140916_pa14_pa17_gpio_level_diagnostic.c`
- 已在 `C:\Users\1\Desktop\ELECTRIC\ele-car\Debug` 下 clean build 通过，生成新的 `ele-car.out`。

## 2026-07-03 13:58 双舵机正式候选脚联动测试

- 用户确认：两个 MG90S 舵机单独测试均正常。
- 目标：摆脱 `PA12` 临时测试脚，找两个不与当前整车冲突的正式候选信号脚，做双舵机联动测试。
- 候选脚选择：
  - `PA14`：SDK/LaunchPad 资料显示在 `J28_7`，当前未被电机、循迹、编码器、UART、按键、SWD 占用。
  - `PA17`：SDK/LaunchPad 资料显示在 `J3_28`，当前未被电机、循迹、编码器、UART、按键、SWD 占用。
  - 避开了 `PA2~PA6`、`PA12/PA13`、`PA18`、`PA19/PA20`、`PA0/PA1` 等风险脚。
- 当前活动工程 `C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c` 已替换为双舵机软件 PWM 测试：
  - `PA14 -> X/水平舵机信号`
  - `PA17 -> Y/俯仰舵机信号`
  - `PB4/STBY` 保持 LOW，`PB0/PB1/PB2/PB3` 全部 LOW，电机禁用。
  - 动作序列：`1500/1500us -> 1350/1650us -> 1500/1500us -> 1650/1350us` 循环。
- 时间戳源码已保存：
  - `C:\Users\1\Desktop\电赛\ccs_project\servo_test\20260703_135829_dual_servo_pa14_pa17_linkage_test.c`
- 已在 `C:\Users\1\Desktop\ELECTRIC\ele-car\Debug` 下 clean build 通过，生成新的 `ele-car.out`。

## 2026-07-02 23:46 硬件风险审计与文档清理

- 用户确认：临时 `PA12` 单舵机测试中舵机已经可以正常工作。
- 当前结论：舵机本体、单舵机 5V 供电和 3.3V 信号幅度基本可用；但 `PA12` 正式被 TB6612 `PWMA` 占用，只能作为临时验证脚。
- 按用户要求，参考 `PA2~PA6` 排查方式，对本地项目文件和官方资料进行了硬件风险审计：
  - 本地文件：`项目记忆.md`、`过程记录.md`、`接线步骤.md`、`硬件接口图.md`、`知识库/调试复盘_判断失误记录.md`、`器件采购清单.md`。
  - TI 官方资料：`LP-MSPM0G3507` 用户指南/原理图、`MSPM0G3507` 数据手册、MSPM0 硬件开发指南。
  - 外设官方资料：Toshiba `TB6612FNG` 数据手册、OpenMV H7 官方文档。
- 新增审计文档：
  - `C:\Users\1\Desktop\电赛\硬件风险审计_20260702.md`
- 关键高危结论：
  - `PA2~PA6` 禁用；`PA3/PA4` 舵机方案作废。
  - `PA20` 是 `SWCLK`，不再推荐作为激光控制脚。
  - MSPM0 普通 GPIO 不耐 5V；TCRT5000/编码器信号必须保持 3.3V 逻辑。
  - OpenMV 使用 `PA10/PA11` UART0 时要处理 `J21/J22` 与 XDS110 backchannel 冲突。
  - `PA0/PA1` 是 5V tolerant open-drain，需要上拉；不适合当普通推挽输出，但可作为 I2C0 候选。
  - 当前 `empty.c` 是舵机临时测试版，不是循迹正式代码。
- 已同步修正：
  - `项目记忆.md`：删除旧 `PA3/PA4` 舵机记忆，记录 `PA12` 仅为临时测试脚。
  - `接线步骤.md`：舵机信号改为待定，激光正式控制脚不再写 `PA20`。
  - `硬件接口图.md`：清理旧 `PA3/PA4` 舵机、`PA20` 激光、最终引脚表、测试步骤中的危险残留。

## 2026-07-02 恢复循迹稳定测试代码

- 用户决定暂时离开舵机问题，重新测试循迹模块。
- 已将当前 CCS 工程 `C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c` 从舵机临时测试程序恢复为稳定循迹基线代码：`C:\Users\1\Desktop\电赛\ccs_project\line_sensor_test\line_follow_sharp_turn_stable_baseline.c`。
- 该版本为此前实测可通过圆角弯、S 弯和直角弯的版本，使用 7 路红外映射 `PB5/PB6/PB7/PA7/PA8/PB9/PB10`，电机 PWM 使用 `PA12/PA13`。
- 已在 `C:\Users\1\Desktop\ELECTRIC\ele-car\Debug` 下执行 clean build 并编译通过，生成新的 `ele-car.out`。
- 用户反馈该版本不是想要的“解决直角弯之前”版本，当前车体体感更慢。已将 `empty.c` 改为“直角弯急弯策略加入前”的普通循迹重构版：保留已校正的方向 `left = base - correction`、`right = base + correction`，移除 `LINE_SHARP_*` 和急弯/丢线找回分支，`g_turn_mode` 固定为 0。
- 新备份源码：`C:\Users\1\Desktop\电赛\ccs_project\line_sensor_test\line_follow_pre_sharp_turn_reconstructed.c`。已重新编译通过。
- 用户进一步明确：实际要恢复的是 21:43 急弯策略版，即“圆角弯/S 弯可过，直角弯加入急弯策略”的版本。已再次恢复 `C:\Users\1\Desktop\电赛\ccs_project\line_sensor_test\line_follow_sharp_turn_stable_baseline.c` 到 `C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`。
- 当前 `empty.c` 关键特征：`LINE_SHARP_ERROR=1800`，`LINE_SHARP_KP_DIVISOR=8`，`LINE_SHARP_CORR_LIMIT=380`，`g_turn_mode=0/1/2`，急弯分支使用 `left = base - correction`、`right = base + correction`，并允许一侧轮低于普通最小 PWM。已 clean 后重新编译通过。

## 2026-07-02 TB6612 扩展板供电资料修正

- 用户提供电机驱动板资料图，标注该板电源输入为 4.5V~15V，并带稳压电路；对外提供带保护的 5V 5A 供电接口，以及 3.3V 0.5A 供电接口。
- 结论修正：该板不是普通裸 TB6612 模块，其 5V/3.3V 引脚按资料属于电源输出口，可作为外设供电候选。
- 对 MSPM0 供电的安全策略：可以考虑用该板 5V 输出接 MSPM0 LaunchPad 的 5V 输入/5V 电源脚，并共地；严禁把 5V 接到 MSPM0 3V3 或任意 GPIO。首次使用前必须先空载测 5V，再在电机运行/启动时测 5V 是否稳定。

## 2026-06-29 PA3/PA4 舵机信号脚最小化排查

- 当前现象：舵机测试程序运行时 `g_step/g_servo_x_us/g_servo_y_us/g_frame_count` 都在变化，说明程序主循环确实在运行；但断开舵机信号后，裸测 PA3 对 GND 约为 0V。
- 判断收窄：暂不再优先怀疑“程序没运行、烧错板、Watch 没变化”等方向；下一步只验证 PA3/PA4 作为普通 GPIO 输出时，物理排针是否能被拉到 3.3V。
- 已将 `C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c` 改为 PA3/PA4 慢速高低电平诊断程序，并备份到 `C:\Users\1\Desktop\电赛\ccs_project\servo_test\mspm0_pa3_pa4_gpio_level_diagnostic.c`。
- 测试规则：断开舵机信号线，只保留板子 USB；Watch `g_phase/g_pa3_expected/g_pa4_expected/g_cycle_count`。`g_phase=1` 时 PA3 应约 3.3V，`g_phase=3` 时 PA4 应约 3.3V，`g_phase=2/4` 时两者应约 0V。
- 用户实测：`g_phase=1` 时 PA3 为 0V，`g_phase=3` 时 PA4 也为 0V。
- 重新核对 TI LP-MSPM0G3507 用户指南：40-pin BoosterPack 排针图中列出 PA10/PA11/PA12/PA13/PA15 等，未把 PA3/PA4列为常用 BoosterPack 排针信号。结论修正为：PA3/PA4 是芯片引脚，但不适合作为当前杜邦线直接接入的舵机信号脚。
- 已将 `C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c` 改为 PA10/PA11 慢速 GPIO 电平诊断程序，并备份到 `C:\Users\1\Desktop\电赛\ccs_project\servo_test\mspm0_pa10_pa11_gpio_level_diagnostic.c`。
- 用户继续实测：PA10/PA11 也无示数，随手测 PA24 约 1.66V。该 1.66V 不代表程序输出，可能是悬空/模拟滤波/测到非当前输出点。
- 已将当前工程改为 PA12/PA13/PA24 三路慢速 GPIO 电平诊断程序，并备份到 `C:\Users\1\Desktop\电赛\ccs_project\servo_test\mspm0_known_pin_gpio_level_diagnostic_pa12_pa13_pa24.c`。PA12/PA13 是此前电机 PWM 已验证可用的硬证据引脚，用于判断程序运行和测量位置是否可靠。
- 用户实测：PA12 对 GND 可到 3.3V，PA13 对 GND 也有有效示数；PA24 一直约 1.66V。结论：程序运行、万用表测量方法和 PA12/PA13 物理排针均可靠；PA24 当前不作为舵机输出脚。
- 已将当前工程改为 PA12/PA13 临时舵机软件 PWM 测试程序，并备份到 `C:\Users\1\Desktop\电赛\ccs_project\servo_test\mspm0_servo_gpio_pwm_test_pa12_pa13_temporary.c`。注意 PA12/PA13 同时是 TB6612 PWMA/PWMB，只用于临时验证舵机动作，正式整合时需换到不与电机冲突的可达排针。
- 用户反馈：耳朵贴近舵机能听到微弱“咔擦”声，且一下一下有间断，但舵机始终不转。判断为舵机已收到某种控制脉冲，优先排查供电电流不足、机构/舵盘顶住、两舵机同时动作负载过大、极限脉宽过激。
- 已将当前工程改为单舵机 PA12 温和测试程序，只输出 1400/1500/1600 us 小幅动作，并只驱动一个舵机；备份到 `C:\Users\1\Desktop\电赛\ccs_project\servo_test\mspm0_servo_single_gentle_pa12_test.c`。
- 用户实测：单舵机温和测试时，舵机红黑供电在动作瞬间只有约 3.5~4.3V，且下方舵机完全没有声音。结论：当前舵机不转的主因转为供电侧，代码和 PA12 信号不是首要嫌疑。下一步检查 MP1584 输入电压、空载输出、电源线/焊点接触、舵机红黑只接电时是否仍掉压，以及舵机是否机械卡住。
- 进一步实测：拔掉舵机白色信号线，仅接舵机红黑电源，红黑电压仍在约 4V 波动、最高不超过 4.4V；MP1584 输入端仍有约 7.7V；云台机械可手动转动；MP1584 空载 OUT+/OUT- 为 5.6V，但接上舵机电源线后 OUT+/OUT- 变为约 3.7~4.3V。结论：舵机不转已定位为 5V 电源带载能力/输出接触/舵机电源支路异常，非 PWM 代码主因。
- 用户补充：曾观察到电池供电正负极电压差从约 2V 逐渐掉到 0.8V 并继续下降，但几分钟前单独测电池正负极又是约 7.4V。判断：若该测点为带载电池输出，则可能存在电池保护板过流/短路保护、供电链路接触电阻过大、或者负载侧异常拉低；需通过断开负载、只接 MP1584、只接单个舵机红黑三步隔离。
- 用户发现 MSPM0 单片机芯片明显发烫。安全结论：立即断开电池和 USB，不再继续带载试舵机。舵机堵转电流正常应由电池/MP1584/舵机回路承担，不应让 MSPM0 芯片明显发热；若 MCU 发烫，优先怀疑 5V/5.6V 误接到 MSPM0 3V3/5V/GPIO、舵机电源回流经过 GPIO 保护结构、共地/接线错误、或 PA12/PA13 与原 TB6612 连接未隔离造成反灌。
- 电池充好后复测：MP1584 空载输出约 5.8V，但输出端一连接舵机，OUT+/OUT- 仍下降到约 3.7~4.4V 波动。结论：电池电量不足基本排除；问题集中在 MP1584 输出带载能力、输出接线/焊点/面包板接触电阻、舵机红黑线接法或舵机本体异常。

## 2026-06-08 巡线输入端口重映射

- 现象：7 路 TCRT5000 依次经过黑线时，S1/S2/S3 无反应，S4/S5/S6/S7 能从 0 切到 1。
- 排查：对调红外模块后，故障仍固定在前三个位置；说明红外模块本体不是首要嫌疑。
- 进一步验证：原先 `PA2/PA5/PA6` 直接碰 3.3V 仍读不到有效变化，改用 `PB5/PB6/PB7` 后 S1/S2/S3 正常。
- 结论：`PA2/PA5/PA6` 暂列为不可用/禁用；当前巡线接线改为 `PB5/PB6/PB7/PA7/PA8/PB9/PB10`。
- 补充判断：用户已更换新 MSPM0 板子，故“原板 GPIO 输入保护结构被早期高压损伤”这一路径排除。剩余重点为旧三路的排针位置、板卡丝印/原理图映射、面包板/杜邦线链路。
- 当前工程代码：`C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c` 已使用重映射版本，编译通过。
- 下一步：做巡线传感器标定与位置误差计算，再接入电机 PWM 做低速循迹测试。

## 2026-06-08 巡线位置误差测试代码

- 已将当前工程 `C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c` 更新为 7 路红外位置误差测试版本。
- 备份源码：`C:\Users\1\Desktop\电赛\ccs_project\line_sensor_test\line_position_error_test_remap_pb5_pb6_pb7.c`。
- 该版本只读传感器，不驱动电机；计算 `g_line_error`、`g_line_active_count`、`g_line_valid`、`g_line_raw_mask`。
- 已在 `C:\Users\1\Desktop\ELECTRIC\ele-car\Debug` 下用 CCS gmake 编译通过。

## 2026-06-08 低速循迹 5 秒试跑代码

- 用户确认 7 路红外位置误差全部符合预期。
- 已将当前工程 `C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c` 更新为低速循迹 5 秒试跑版本。
- 备份源码：`C:\Users\1\Desktop\电赛\ccs_project\line_sensor_test\line_follow_low_speed_5s_test.c`。
- 运行策略：上电后闪灯约 3 秒，随后循迹低速运行 5 秒并自动停车；若连续约 100 ms 丢线则提前停车。
- 关键参数：`PWM_LEFT_BASE_TICKS=620`，`PWM_RIGHT_BASE_TICKS=590`，`LINE_KP_DIVISOR=16`，`LINE_CORR_LIMIT=150`。
- 已在 `C:\Users\1\Desktop\ELECTRIC\ele-car\Debug` 下用 CCS gmake 编译通过。

## 2026-06-08 低速循迹容错版

- 用户反馈上一版刚偏离就停车，看不出车辆真实偏向。
- 已将当前工程 `C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c` 改为 12 秒试跑版本。
- 调整参数：`RUN_SAMPLES=600`，运行约 12 秒；`LOST_STOP_SAMPLES=100`，连续约 2 秒丢线才停车；`LINE_KP_DIVISOR=20`，修正强度略降低。
- 已在 `C:\Users\1\Desktop\ELECTRIC\ele-car\Debug` 下用 CCS gmake 编译通过。

## 2026-06-08 循迹修正方向校正

- 用户反馈：S3 在黑线上启动时车往右转，S4 或 S5 在黑线上启动时车往左转。
- 判断：S3/S5 的现象说明巡线修正方向与实际物理左右轮相反；S4 仍左转说明基础 PWM 还存在偏置。
- 已将 `line_follow_update()` 中 PWM 修正方向反过来：
  - 原：`left = base + correction`，`right = base - correction`
  - 新：`left = base - correction`，`right = base + correction`
- 已编译通过。下一次重点观察：S3 启动应往左，S5 启动应往右；若 S4 仍左偏，再单独调基础 PWM。

## 2026-06-08 直角弯策略

- 用户反馈：圆角弯和 S 弯均可通过，直角弯容易冲出线。
- 判断：普通比例循迹适合圆弧/S 弯，但直角弯需要急弯策略；原 `PWM_MIN_TICKS=430` 限制导致内侧轮无法足够慢，转向半径过大。
- 已在当前工程 `empty.c` 增加急弯模式：
  - `LINE_SHARP_ERROR=1800`
  - `LINE_SHARP_KP_DIVISOR=8`
  - `LINE_SHARP_CORR_LIMIT=380`
  - `g_turn_mode`: `0=normal`，`1=sharp`，`2=lost-line recovery`
- 急弯模式下允许一侧 PWM 低于普通最小值，甚至接近停转；普通路段仍保留温和修正。
- 已在 `C:\Users\1\Desktop\ELECTRIC\ele-car\Debug` 下用 CCS gmake 编译通过。

## 2026-06-08 急弯加速感修正

- 用户反馈：直角弯能过，但一转弯感觉车会加速。
- 原因：急弯模式同时降低内侧轮、提高外侧轮，外侧 PWM 可能被推到 `PWM_MAX_TICKS=850`，体感像冲出去。
- 已修改当前工程 `empty.c`：急弯模式只降低内侧轮，外侧轮保持基础 PWM，不再额外加速。
- 已在 `C:\Users\1\Desktop\ELECTRIC\ele-car\Debug` 下用 CCS gmake 编译通过。

## 2026-06-08 急弯策略回退

- 用户实测：去掉外侧轮加速后，直角弯偏出赛道更多。
- 结论：当前车体/轮距/速度下，直角弯需要外侧轮参与加速，只降低内侧轮不足以产生足够转向。
- 已将当前工程 `empty.c` 改回急弯模式：`left = base - correction`，`right = base + correction`。
- 后续若加速感太强，优先降低整体基础速度或降低 `PWM_MAX_TICKS`，不再完全取消外侧轮加速。
- 已在 `C:\Users\1\Desktop\ELECTRIC\ele-car\Debug` 下用 CCS gmake 编译通过。

## 2026-06-08 版本确认：21:43 急弯策略版

- 用户明确要求回到“圆角弯/S 弯可过，直角弯加入急弯策略”的 21:43 版本。
- 已确认当前 `C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c` 保留该版本核心逻辑：
  - `LINE_SHARP_ERROR=1800`
  - `LINE_SHARP_KP_DIVISOR=8`
  - `LINE_SHARP_CORR_LIMIT=380`
  - `g_turn_mode` 用于区分普通/急弯/丢线找回
  - 急弯分支使用 `left = base - correction`、`right = base + correction`
- 当前代码不是“只降低内侧轮、外侧轮保持基础 PWM”的版本。

## 2026-06-08 稳定循迹基线固化

- 用户确认当前版本已经没有问题。
- 已保存稳定基线源码：`C:\Users\1\Desktop\电赛\ccs_project\line_sensor_test\line_follow_sharp_turn_stable_baseline.c`。
- 当前能力：圆角弯、S 弯、直角弯均可通过。
- 下一步建议：先把“硬编码 12 秒自动跑”改成“按键启动/停止 + 模式/圈数选择”，再接 OpenMV/舵机/激光。

## 2026-06-29 校赛题目与参考代码接入

- 新任务：读取校赛小车赛道题目 PDF，并结合两份老师参考代码，在现有小车基础上制定一步一步研制路线。
- 当前基线：`C:\Users\1\Desktop\电赛\ccs_project\line_sensor_test\line_follow_sharp_turn_stable_baseline.c` 已验证可过圆角弯、S 弯、直角弯。
- 工作原则：先分析赛题要求和参考代码结构，不直接覆盖 `C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`；后续只把适配后的模块逐步合入。
- 下一步动作：抽取 PDF 题目要求，解压并审查 `25E题底盘开源.zip`、`24H题第四问开源.zip` 的工程结构、引脚定义、核心算法。

## 2026-06-29 校赛题目与参考代码适配结论

- 已确认题目 PDF 共 3 页，赛题为“巡迹作业一体平台”。
- 基础要求核心拆分：
  - A 点出发自动循迹一圈并回到 A 点停车，声光提示，30 s 内。
  - 指定位置静止 5 s 内完成云台/机械臂对靶心指向。
  - A 点出发，依次到 B/C/D/A，每个关键点声光提示；B 点需要停车完成对靶作业；总时长 40 s 内。
  - 通过按键或串口支持模式/参数设置和模块独立调试。
- 参考代码 `25E题底盘开源` 的可借鉴部分：
  - `ApplicationLayer/Task.c` 的直角/关键点状态机思想：检测特殊传感器图案后，先用编码器距离保护，再强制转向，重新找回中线，并计数圈数/关键点。
  - `FunctionalModule/Key.c` 的短按/长按任务入口思想：短按设置圈数或模式，长按启动。
  - `FunctionalModule/Buzzer.c` 的事件提示思想：启动、过点、停车均用蜂鸣/LED 提示。
- 参考代码不能直接移植的部分：
  - `GraySensor.c` 面向 8 路模拟复用灰度传感器，且黑线逻辑与我们 7 路 TCRT5000 数字 DO 不同。
  - `MotorsCtrl.c` 面向四电机闭环底盘；我们目前是两电机 TB6612，且 PWM compare 极性已经实测为反向。
  - `Encoder.c` 的距离单位和车轮参数写死在老师车体上，只能借思想，不能照搬阈值。
- `24H题第四问开源` 主要提供“IMU + 灰度 + 编码器”的路段状态机参考，没有直接可用的视觉/云台模块。
- 当前路线：保留已验证循迹算法，先新增“按键启动/模式选择/声光提示”，再新增“关键点/圈数状态机”，最后接云台/OpenMV/激光作业模块。

## 2026-06-29 二维云台舵机测试

- 用户决定暂不接 OpenMV，先验证二维云台两个 MG90S 舵机能否转动。
- 当前工程 `C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c` 已改为舵机扫动测试版。
- 备份源码：`C:\Users\1\Desktop\电赛\ccs_project\servo_test\mspm0_servo_sweep_test.c`。
- 接线目标：
  - X 轴舵机信号 -> `PA3 / TIMG8_CCP0`
  - Y 轴舵机信号 -> `PA4 / TIMG8_CCP1`
  - 舵机红线 -> 外部 5V
  - 舵机棕/黑线 -> 外部 GND
  - MSPM0 GND -> 外部 GND
- 程序动作：
  - `g_step=0`：双舵机中位 1500 us。
  - `g_step=1/2`：X 轴 1000/2000 us。
  - `g_step=4/5`：Y 轴 1000/2000 us。
- 已在 `C:\Users\1\Desktop\ELECTRIC\ele-car\Debug` 下编译通过。

## 2026-06-29 OpenMV UART 引脚方向修正

- 之前文档中曾把 OpenMV `P4/P5` 的 TX/RX 方向写反。
- 已按 OpenMV 官方 UART3 引脚说明修正为：
  - `OpenMV P4 = UART3 TX`
  - `OpenMV P5 = UART3 RX`
  - `MSPM0 PA10 TX -> OpenMV P5 RX`
  - `MSPM0 PA11 RX <- OpenMV P4 TX`
- 已更新 `接线步骤.md`、`硬件接口图.md`、`知识库/视觉模块/OpenMV_MSPM0_串口通信协议.md`。

## 2026-07-02 16:24 循迹急弯版丢线不停与小幅提速

- 用户指出当前循迹代码存在问题：小车偏离黑线太多后会自动停止；如果正在转弯但暂时检测不到黑线，应继续按最后一次转弯方向找线，而不是停车。
- 当前工程 `C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c` 已在 21:43 急弯策略版基础上修改：
  - 保留 `LINE_SHARP_ERROR=1800`、`LINE_SHARP_KP_DIVISOR=8`、`LINE_SHARP_CORR_LIMIT=380`。
  - 基础速度从 `PWM_LEFT_BASE_TICKS=620`、`PWM_RIGHT_BASE_TICKS=590` 提高到 `650/620`。
  - 启动等待从约 3 s 改为约 1 s：`blink(5U, 50U, 150U)`。
  - 自动运行时间从约 12 s 改为约 90 s：`RUN_SAMPLES=4500`，循环周期约 20 ms。
  - 删除运行过程中 `g_line_lost_count` 达阈值后停车的分支；丢线时继续进入 `g_turn_mode=2`，用 `g_line_last_error` 维持最后方向找线。
- 已在 `C:\Users\1\Desktop\ELECTRIC\ele-car\Debug` 下编译通过，生成新的 `ele-car.out`。
- 按用户要求，后续每次生成代码都额外保存带日期时间的源码版本，不只覆盖 `empty.c`。本次版本保存为：
  - `C:\Users\1\Desktop\电赛\ccs_project\line_sensor_test\20260702_162412_line_follow_sharp_turn_no_lost_stop_faster_90s.c`

## 2026-07-02 16:29 急弯提速与丢线渐进加速找回

- 用户要求：小车转弯速度继续提高；如果转弯时丢失黑线，丢失时间越长，转弯速度越快，但速度必须有上限；重新找到黑线后自动恢复原来的直行速度。
- 当前工程 `C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c` 已修改：
  - 直行基础速度保持 `PWM_LEFT_BASE_TICKS=650`、`PWM_RIGHT_BASE_TICKS=620` 不变。
  - 急弯更积极：`LINE_SHARP_KP_DIVISOR=8 -> 7`，`LINE_SHARP_CORR_LIMIT=380 -> 430`。
  - 转弯最大 PWM 上限提高：`PWM_MAX_TICKS=850 -> 900`，主要影响急弯/丢线找回时外侧轮上限。
  - 新增 `g_last_turn_sign` 记忆最后一次明显转向方向，避免丢线且误差回到 0 时无法判断找线方向。
  - 新增 `g_lost_turn_boost`：丢线后每约 100 ms 增加 25 ticks 修正，最大增加 150 ticks；总丢线修正限制为 `LINE_LOST_CORR_LIMIT=580`。
  - 一旦重新检测到黑线，`g_line_lost_count` 清零，`g_lost_turn_boost` 归 0，控制回到普通/急弯循迹速度。
- 已在 `C:\Users\1\Desktop\ELECTRIC\ele-car\Debug` 下编译通过。
- 本次时间戳源码保存为：
  - `C:\Users\1\Desktop\电赛\ccs_project\line_sensor_test\20260702_162952_line_follow_sharp_turn_progressive_lost_boost.c`

## 2026-07-02 16:37 丢线找回最低轮速保护

- 用户反馈：小车远离黑线后仍像是自动停止。
- 核对代码后确认：运行中没有 `g_line_lost_count` 触发停车的分支；真正的代码停车点只有初始化、启动前无黑线、90 s 到时。
- 新判断：丢线找回时修正量可能把内侧轮压到几十 ticks，地面摩擦/电机死区会让车辆表现得像停住，而不是继续转弯找线。
- 当前工程 `C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c` 已优化：
  - 新增 `LOST_TURN_INNER_MIN_TICKS=220`。
  - 新增 `LOST_TURN_OUTER_MIN_TICKS=760`。
  - 丢线找回模式 `g_turn_mode=2` 下，按 `g_line_correction` 正负区分内侧轮/外侧轮，分别应用最低轮速保护。
  - 保留最高上限 `PWM_MAX_TICKS=900`，避免丢线越久无限加速。
- 已在 `C:\Users\1\Desktop\ELECTRIC\ele-car\Debug` 下编译通过。
- 本次时间戳源码保存为：
  - `C:\Users\1\Desktop\电赛\ccs_project\line_sensor_test\20260702_163704_line_follow_lost_recovery_min_wheel_speed.c`

## 2026-07-02 16:44 回退最低外侧轮速并修改启动策略

- 用户反馈：`20260702_163704` 版本直线稳定性变差，且仍有转弯像停住的问题。
- 已回退该版本中过激的丢线找回外侧轮固定最低速度策略：
  - 删除 `LOST_TURN_OUTER_MIN_TICKS=760`。
  - 不再强制外侧轮跳到固定高 PWM。
  - 保留更温和的内侧轮地板：`LOST_TURN_INNER_FLOOR_TICKS=260`，只防止内侧轮低到近似不转。
- 按用户要求，启动策略改为：
  - RST 后约 1 s 启动。
  - 启动瞬间如果没有检测到黑线，不再 `g_phase=4` 停车。
  - 默认先直线行驶；遇到黑线后自动进入循迹。
- 按用户要求，小车运行速度整体提高约 10%：
  - `PWM_LEFT_BASE_TICKS=650 -> 715`
  - `PWM_RIGHT_BASE_TICKS=620 -> 682`
  - `PWM_MAX_TICKS=900 -> 990`
- 90 s 自动停车逻辑仍保留：`RUN_SAMPLES=4500`。
- 已在 `C:\Users\1\Desktop\ELECTRIC\ele-car\Debug` 下编译通过。
- 本次时间戳源码保存为：
  - `C:\Users\1\Desktop\电赛\ccs_project\line_sensor_test\20260702_164417_line_follow_start_straight_speed10_gentle_lost_floor.c`

## 2026-07-02 16:57 无 Watch 条件下的循迹诊断版

- 用户指出：小车连接电脑时才能 Watch，但连接电脑时无法真实模拟地面循迹转弯，因此不能依赖 Watch 判断问题，必须通过现场现象判断。
- 当前工程 `C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c` 已改为无 Watch 诊断版：
  - 保留启动没线先直走找线策略。
  - 保留基础速度 `PWM_LEFT_BASE_TICKS=715`、`PWM_RIGHT_BASE_TICKS=682`。
  - 新增 `PWM_SLEW_STEP_TICKS=90`，运行中 PWM 每 20 ms 最多变化 90 ticks，降低转弯/启动瞬间电流突变，减少电源压降复位风险。
  - 运行循环中每 20 ms 调用 `motors_forward()`，反复重拉 `STBY` 和方向脚，避免驱动板 STBY/方向脚瞬时失能后无法恢复。
  - 新增板载 LED 状态码：
    - 上电/复位：约 1 s 快闪 5 次。
    - 正常循迹：约 1 s 短闪一次。
    - 急弯：更快短闪。
    - 丢线找回/启动直走找线：快速闪烁。
    - 90 s 正常结束：进入原来的停止后慢闪。
- 现场判断规则：
  - 如果车辆“停住”同时 LED 重新出现上电快闪，优先判断为电源压降/MCU 复位。
  - 如果车辆“停住”但 LED 仍按运行状态闪烁，说明程序仍活着，应查电机供电、TB6612、机械卡滞或 PWM 死区。
  - 如果 LED 快速闪烁且车在找线，说明传感器未检测到黑线或处于丢线找回。
- 已在 `C:\Users\1\Desktop\ELECTRIC\ele-car\Debug` 下编译通过。
- 本次时间戳源码保存为：
  - `C:\Users\1\Desktop\电赛\ccs_project\line_sensor_test\20260702_165700_line_follow_no_watch_diagnostics_motor_refresh_pwm_slew.c`

## 2026-07-02 17:07 直线抖动抑制版

- 用户反馈：其他问题基本解决，剩余问题是直线行驶时车头左右扭动明显，希望尽量减少直线扭头。
- 判断：当前 7 路数字红外在中线附近会出现 S3/S4/S5 小范围跳变，普通循迹把这些小误差立刻转成左右 PWM 差，导致直线段左右抖动。
- 当前工程 `C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c` 已只修改普通循迹分支，急弯和丢线找回分支保持强控制：
  - `LINE_KP_DIVISOR=20 -> 26`，普通路段修正更温和。
  - `LINE_CORR_LIMIT=150 -> 120`，限制普通路段最大扭头幅度。
  - 新增 `LINE_NORMAL_DEADBAND=350`，普通路段误差绝对值小于等于 350 时不修正。
  - 新增 `LINE_NORMAL_FILTER_DIVISOR=4` 和 `g_line_filtered_error`，普通路段误差采用低通滤波，减小传感器跳变引起的方向抖动。
  - 急弯/丢线分支进入时清空普通滤波记忆，避免急弯大误差带回直线段造成回正抖动。
- 现场预期：
  - 直线段车头左右摆动应明显减小。
  - 如果入弯变慢或贴线能力下降，优先微调普通参数：`LINE_KP_DIVISOR 26 -> 23` 或 `LINE_NORMAL_DEADBAND 350 -> 250`。
- 已在 `C:\Users\1\Desktop\ELECTRIC\ele-car\Debug` 下编译通过。
- 本次时间戳源码保存为：
  - `C:\Users\1\Desktop\电赛\ccs_project\line_sensor_test\20260702_170717_line_follow_smoother_straight_normal_filter.c`

## 2026-07-02 17:17 最终出弯稳定优化版

- 用户进一步判断：直线抖动根因可能不是直线段本身，而是转弯出来后车身姿态没有对齐直线，导致后续一直走折线。
- 用户确认可以牺牲三项：出弯后速度、急弯后灵敏度、极限过弯速度。
- 当前工程 `C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c` 已加入出弯稳定状态机：
  - `g_turn_mode=0`：普通循迹。
  - `g_turn_mode=1`：急弯。
  - `g_turn_mode=2`：丢线找回。
  - `g_turn_mode=3`：出弯稳定。
- 进入出弯稳定条件：
  - 上一个周期为急弯或丢线找回；
  - 当前重新检测到黑线；
  - 当前误差没有达到急弯阈值。
- 出弯稳定持续约 30 个控制周期，即约 0.6 s：
  - `EXIT_SETTLE_SAMPLES=30`
  - 基础速度降为 `EXIT_LEFT_BASE_TICKS=600`、`EXIT_RIGHT_BASE_TICKS=572`。
  - 修正灵敏度降低：`EXIT_KP_DIVISOR=40`。
  - 最大修正限制为 `EXIT_CORR_LIMIT=80`。
  - 误差滤波更慢：`EXIT_FILTER_DIVISOR=5`。
  - 死区为 `EXIT_DEADBAND=250`。
- 若出弯稳定期间再次进入急弯或丢线，立即退出稳定状态，重新进入急弯/丢线找回，避免因为稳定状态错过救线。
- LED 状态补充：
  - `g_turn_mode=3` 出弯稳定时使用中速闪烁，区别于普通慢闪、急弯较快闪、丢线快速闪。
- 已在 `C:\Users\1\Desktop\ELECTRIC\ele-car\Debug` 下编译通过。
- 本次时间戳源码保存为：
  - `C:\Users\1\Desktop\电赛\ccs_project\line_sensor_test\20260702_171734_line_follow_final_exit_turn_settle.c`

## 2026-07-02 17:28 中心优先状态机大改版

- 用户提出新的核心要求：后续要打靶，转弯后必须让中心红外 S4 精准对准黑线；为了稳定性可以牺牲速度、转弯灵敏度和极限过弯能力。
- 控制目标从“尽快救回黑线”改为“中心锁定后才允许高速直行”。
- 当前工程 `C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c` 已重写循迹状态机：
  - `g_turn_mode=0`：高速直行，只在 S4 稳定压线后进入。
  - `g_turn_mode=1`：线上慢速转弯，黑线仍被至少一路红外检测到时使用。
  - `g_turn_mode=2`：完全丢线加速找回，只有 7 路全部无黑线且已有方向记忆时才进入。
  - `g_turn_mode=3`：中心锁定，S4 已看到黑线但稳定时间还不够。
  - `g_turn_mode=4`：中速对线，看到黑线但还没到中心锁定条件。
- 高速直行门槛：
  - `S4=1`。
  - `abs(line_error)<=500`。
  - 连续满足 `CENTER_STABLE_SAMPLES=14` 个周期，即约 280 ms。
- 线上慢速转弯条件：
  - 只要还能看到黑线，但边缘传感器 S1/S2/S6/S7 触发，或 `abs(error)>=1400`，进入慢速转弯。
  - 不再因为“大误差但仍在线上”进入加速救线。
- 完全丢线加速条件：
  - `g_line_valid=0`，即 7 路都没看到黑线。
  - 曾经见过黑线 `g_has_seen_line=1`。
  - 有最后方向记忆 `g_last_turn_sign!=0`。
  - 连续丢线不少于 `LOST_ACCEL_CONFIRM_SAMPLES=3` 个周期。
- 启动没线仍保持直走找线，不立即转圈。
- 已在 `C:\Users\1\Desktop\ELECTRIC\ele-car\Debug` 下编译通过，并确认当前 `empty.c` 与备份 SHA256 一致。
- 本次时间戳源码保存为：
  - `C:\Users\1\Desktop\电赛\ccs_project\line_sensor_test\20260702_172827_line_follow_center_first_state_machine.c`

## 2026-07-02 19:10 视频复盘后的中心优先 v2

- 用户切换 API key 后提供真实跑车视频 `fb1ee43ddaa3a75f061e0c188d4a8bb1.mp4`，要求结合全部历史会话分析当前小车仍不满意的原因。
- 已用 ffmpeg 抽帧：
  - 总览拼图：`C:\Users\1\Desktop\电赛\video_analysis\fb1ee43ddaa3a75f061e0c188d4a8bb1\contact_sheet.jpg`
  - 2fps 拼图：`C:\Users\1\Desktop\电赛\video_analysis\fb1ee43ddaa3a75f061e0c188d4a8bb1\fps2\contact_fps2.jpg`
  - 8~16s、20~28s 两段局部拼图。
- 视频观察结论：
  - 小车能跑完整圈，主要问题不是丢线或停车。
  - 右侧斜角/出弯后，车身带明显斜角进入下边直线；随后在直线段继续折线追线。
  - 当前控制仍偏向“看到中心后较快恢复速度”，中心锁定门槛对打靶任务仍不够严格。
  - 线上偏线时，尤其 S3/S5 级别偏移，应该更早进入慢速对线/慢速转弯，不应等到外侧传感器或大误差才降速。
- 当前工程 `C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c` 已改为中心优先 v2：
  - 线上对线速度：`ALIGN_LEFT/RIGHT 620/592 -> 580/553`。
  - 线上慢转速度：`TURN_LEFT/RIGHT 560/535 -> 520/496`。
  - 中心锁定速度：`CENTER_LEFT/RIGHT 540/515 -> 500/475`。
  - 启动没线直走找线速度：`SEARCH_LEFT/RIGHT 600/572 -> 560/535`。
  - PWM 斜坡：`PWM_SLEW_STEP_TICKS 90 -> 60`，进一步减少车头摆动和电流冲击。
  - 线上慢转触发阈值：`ONLINE_TURN_ERROR 1400 -> 700`，S3/S5 级别偏移就更早降速转。
  - 中心稳定误差阈值：`CENTER_ERROR_LIMIT 500 -> 250`。
  - 中心稳定时间：`CENTER_STABLE_SAMPLES 14 -> 35`，约 0.7s 后才允许高速。
  - 中心稳定条件新增：边缘传感器 S1/S2/S6/S7 任一触发时，即使 S4 亮，也不算真正中心稳定。
- 已在 `C:\Users\1\Desktop\ELECTRIC\ele-car\Debug` 下编译通过，并确认当前 `empty.c` 与备份 SHA256 一致。
- 本次时间戳源码保存为：
  - `C:\Users\1\Desktop\电赛\ccs_project\line_sensor_test\20260702_191058_line_follow_center_first_v2_video_review.c`
## 2026-07-02 19:18 回退到直角弯优化前版本测试

- 用户要求先回到“下午最开始、直角弯还不能顺利通过前”的版本进行对比测试。
- 已确认当前工程 `C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c` 与备份 `C:\Users\1\Desktop\电赛\ccs_project\line_sensor_test\line_follow_pre_sharp_turn_reconstructed.c` 的 SHA256 完全一致。
- 当前恢复版本特征：
  - 无 `LINE_SHARP_ERROR`、`LINE_SHARP_KP_DIVISOR`、`LINE_SHARP_CORR_LIMIT` 急弯策略。
  - 普通循迹参数为 `PWM_LEFT_BASE_TICKS=620`、`PWM_RIGHT_BASE_TICKS=590`、`LINE_KP_DIVISOR=20`、`LINE_CORR_LIMIT=150`。
  - 运行时长为 `RUN_SAMPLES=600`，约 12 s。
  - 丢线保护仍为 `LOST_STOP_SAMPLES=100`，约 2 s 连续检测不到黑线后停车。
- 已按“每次代码留时间戳”的要求保存当前恢复版本：
  - `C:\Users\1\Desktop\电赛\ccs_project\line_sensor_test\20260702_191813_restore_pre_sharp_turn_for_test.c`
- 已在 `C:\Users\1\Desktop\ELECTRIC\ele-car\Debug` 下执行 clean 后重新编译通过，确认 `empty.c` 被实际重新编译进 `ele-car.out`。

## 2026-07-02 19:30 191813 直线稳定版与 191058 转弯版的合并策略

- 用户现场反馈：
  - `20260702_191813_restore_pre_sharp_turn_for_test.c` 直线明显更好，但部分弯转不过。
  - `20260702_191058_line_follow_center_first_v2_video_review.c` 转弯能力更强，但直线抖动/折线更大，不利于后续打靶。
- 重新核对当前 `empty.c` 时发现它已经不是纯 191813，而是一个未作为正式目标确认的 `Combined line-following` 中间态，仍保留了 191058 的五状态机骨架。
- 本次决定不在该中间态上继续堆补丁，而是重新生成一个新的正式时间戳版本：
  - 正常直线优先继承 191813 的简单 P 控制手感。
  - 急弯和完全丢线救回继承 21:43 急弯策略，而不是 191058 的五档中心锁定状态机。
  - 只加入一个轻量弯后稳定窗，避免弯后马上高速折线；稳定窗结束后回到 191813 的直线控制。
  - 保留此前已确定的比赛流程要求：RST 后约 1 s 启动、启动没线时先直走找线、运行中丢线不自动停车、约 90 s 自动结束。

## 2026-07-02 20:09 保守混合版生成并编译通过

- 已将当前工程 `C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c` 改为保守混合版。
- 设计取舍：
  - 正常有线且不是大弯时，恢复 191813 的直线控制手感：`PWM_LEFT_BASE_TICKS=620`、`PWM_RIGHT_BASE_TICKS=590`、`LINE_KP_DIVISOR=20`、`LINE_CORR_LIMIT=150`。
  - 急弯仅在 `abs(error)>=1800` 或外侧 `S1/S7` 触发时进入，避免 S3/S5 轻微偏线就切换状态。
  - 完全丢线且已经有转向记忆时，按最后方向继续转；若刚处于转弯/弯后稳定，则不等待 3 个周期，立即救弯。
  - 丢线救弯有温和渐进加速：每 5 个控制周期增加 15 ticks，最多 90 ticks，防止过强救弯造成直线抖动。
  - 急弯或丢线救回后进入约 0.56 s 的弯后稳定窗：`SETTLE_SAMPLES=28`，用较低速度 `560/533` 和较弱修正让车身对齐后再恢复正常直线。
  - 不再使用 191058 的五档中心锁定/中速对线/高速切换状态机，降低状态频繁切换导致的折线。
- 保留比赛流程：
  - RST 后约 1 s 启动。
  - 启动时没看到黑线，先直走找线。
  - 运行中不因丢线计数自动停车。
  - `RUN_SAMPLES=4500`，约 90 s 后自动停车。
- 已在 `C:\Users\1\Desktop\ELECTRIC\ele-car\Debug` 下编译通过。
- 时间戳源码已保存：
  - `C:\Users\1\Desktop\电赛\ccs_project\line_sensor_test\20260702_200901_line_follow_hybrid_prestraight_sharp_settle.c`

## 2026-07-02 20:14 关闭丢线渐进加速并加长出弯稳定窗

- 用户反馈：转完回到直线后仍左右摇摆，怀疑“转弯速度加快”功能可能影响出弯稳定。
- 当前判断：渐进加速只在完全丢线救回分支生效，不影响仍能看到线的普通急弯；但它可能放大出弯角速度，因此先做最小对照实验。
- 已修改当前工程 `C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`：
  - `LOST_TURN_BOOST_TICKS=15 -> 0`
  - `LOST_TURN_BOOST_MAX_TICKS=90 -> 0`
  - `SETTLE_SAMPLES=28 -> 45`，弯后稳定窗从约 0.56 s 增加到约 0.9 s。
  - `SETTLE_KP_DIVISOR=22 -> 28`，出弯修正变柔。
  - `SETTLE_CORR_LIMIT=120 -> 90`，出弯最大扭头幅度降低。
- 目的：保留急弯能力，先验证“完全丢线后的渐进加速”是否是弯后左右摇摆的放大因素。
- 已在 `C:\Users\1\Desktop\ELECTRIC\ele-car\Debug` 下编译通过。
- 时间戳源码已保存：
  - `C:\Users\1\Desktop\电赛\ccs_project\line_sensor_test\20260702_201444_line_follow_hybrid_no_lost_boost_longer_settle.c`

## 2026-07-02 20:19 回退丢线加速实验

- 用户现场测试后判断：关闭丢线渐进加速、加长出弯稳定窗并没有解决“转完回到直线后左右摇摆”的主要问题。
- 已回退该实验参数，恢复到 20:09 保守混合版：
  - `LOST_TURN_BOOST_TICKS=15`
  - `LOST_TURN_BOOST_MAX_TICKS=90`
  - `SETTLE_SAMPLES=28`
  - `SETTLE_KP_DIVISOR=22`
  - `SETTLE_CORR_LIMIT=120`
- 已在 `C:\Users\1\Desktop\ELECTRIC\ele-car\Debug` 下编译通过。
- 时间戳源码已保存：
  - `C:\Users\1\Desktop\电赛\ccs_project\line_sensor_test\20260702_201932_line_follow_hybrid_rollback_lost_boost_experiment.c`
- 新判断方向：
  - 摇摆更可能不是“完全丢线后渐进加速”导致，而是正常直线/出弯后普通 P 控制对离散红外跳变响应过快。
  - 后续应考虑只在“中心附近、没有外侧传感器触发、误差小或误差符号来回翻转”时，对普通修正进行衰减/限速，而不影响急弯和丢线救回。

## 2026-07-02 20:22 普通直线段加入修正阻尼

- 用户提出思路：直线段重复不断修正可能是“矫正过度”，希望在合适条件下让弯道/方向矫正度逐渐变小甚至衰减，同时不影响其他场景。
- 当前实现只作用于 `TURN_MODE_NORMAL` 普通直线段，不影响：
  - 急弯 `TURN_MODE_SHARP`
  - 丢线救回 `TURN_MODE_LOST`
  - 启动/搜索 `TURN_MODE_SEARCH`
  - 弯后稳定 `TURN_MODE_SETTLE`
- 新增参数：
  - `NORMAL_DAMP_ERROR_LIMIT=1000`
  - `NORMAL_DAMP_CENTER_ERROR=450`
  - `NORMAL_DAMP_STEP_TICKS=20`
  - `NORMAL_SIGN_FLIP_DIVISOR=3`
- 新增可观察变量：
  - `g_normal_target_correction`
  - `g_normal_damped_correction`
  - `g_last_normal_error`
  - `g_normal_damping_active`
- 控制逻辑：
  - 普通段先按旧版 P 控制算 `target_correction`。
  - 如果中心传感器 S4 看到线、外侧 S1/S7 没触发、误差足够小，则目标修正衰减为 0。
  - 如果误差在中心附近左右反号，则目标修正除以 3，再用每周期最多 20 ticks 的速度靠近，避免立刻反向猛打。
  - 如果误差持续同向变大或离开中心附近，则恢复普通 P 控制，不延迟救线。
- 已在 `C:\Users\1\Desktop\ELECTRIC\ele-car\Debug` 下编译通过。
- 时间戳源码已保存：
  - `C:\Users\1\Desktop\电赛\ccs_project\line_sensor_test\20260702_202255_line_follow_hybrid_normal_damping.c`

## 2026-07-02 20:29 急弯触发确认与出弯冷却

- 用户现场测试后反馈：普通直线段修正阻尼影响不大，并提出新假设：当前状态机可能没有明确进入直线状态，而是出弯后反复被判定为转弯。
- 代码核对结论：
  - 旧版 `20260702_191813_restore_pre_sharp_turn_for_test.c` 直线好，是因为没有状态机，始终用 `error/20`、限幅 `150` 的温和 P 控制。
  - 当前混合版中，`S1/S7` 外侧传感器瞬间触发即可进入急弯，并重置 `g_turn_settle_count`，可能造成“急弯 -> 稳定 -> 急弯 -> 稳定”的状态反复。
- 本次修改：
  - 新增 `SHARP_EDGE_CONFIRM_SAMPLES=2`，S1/S7 连续 2 个控制周期触发才允许进入急弯。
  - 新增 `SHARP_ERROR_CONFIRM_SAMPLES=2`，大误差连续 2 个控制周期才允许进入急弯。
  - 新增 `SHARP_REENTRY_COOLDOWN_SAMPLES=18`，急弯刚退出后约 0.36 s 内阻止再次由可见线触发急弯。
  - 连续处于真实急弯时不会被冷却打断；完全丢线救回 `TURN_MODE_LOST` 仍优先，不受冷却期影响。
  - 新增可观察变量：`g_sharp_edge_count`、`g_sharp_error_count`、`g_sharp_cooldown_count`、`g_sharp_raw_request`、`g_sharp_confirmed`、`g_sharp_blocked`。
- 测试判断：
  - 如果出弯后直线蛇形明显减小，说明主要问题是急弯状态被 S1/S7 瞬时触发反复拉起。
  - 如果直角弯变差，优先把 `SHARP_EDGE_CONFIRM_SAMPLES=2` 改回更灵敏，或把 `SHARP_REENTRY_COOLDOWN_SAMPLES=18` 降低。
- 已在 `C:\Users\1\Desktop\ELECTRIC\ele-car\Debug` 下编译通过。
- 时间戳源码已保存：
  - `C:\Users\1\Desktop\电赛\ccs_project\line_sensor_test\20260702_202941_line_follow_sharp_confirm_cooldown.c`

## 2026-07-02 20:36 回到普通 P 循迹并只加最小直角弯补偿

- 用户提出：状态机方式可能并不可行，应回退到普通方式，在最基础方案上只优化直角急弯问题。
- 本次重新从 `20260702_191813_restore_pre_sharp_turn_for_test.c` 生成，而不是继续删改当前复杂状态机版本。
- 保留普通版直线稳定内核：
  - `PWM_LEFT_BASE_TICKS=620`
  - `PWM_RIGHT_BASE_TICKS=590`
  - `LINE_KP_DIVISOR=20`
  - `LINE_CORR_LIMIT=150`
  - 普通有线且非急弯时始终使用 `error/20` 的简单 P 控制。
- 只加入最小直角弯补偿：
  - `LINE_SHARP_ERROR=2000`
  - `LINE_SHARP_KP_DIVISOR=8`
  - `LINE_SHARP_CORR_LIMIT=380`
  - 当 `S1/S7` 外侧触发、或 `abs(error)>=2000`、或完全丢线但已有最后误差方向时，短时使用强差速。
  - 无出弯稳定窗、无中心锁定、无冷却、无普通直线阻尼、无状态保持。
- 保留比赛流程：
  - RST 后约 1 s 启动。
  - 启动没线时先直走找线。
  - 运行中不因丢线计数自动停车。
  - `RUN_SAMPLES=4500`，约 90 s 自动停车。
- 测试判断：
  - 如果直线重新变稳，说明前几版主要问题确实来自状态机反复切换。
  - 如果直角弯仍不过，优先只增强最小补偿参数，不重新引入复杂状态机：`LINE_SHARP_ERROR 2000 -> 1800` 或 `LINE_SHARP_CORR_LIMIT 380 -> 430`。
  - 如果直线又开始被 S1/S7 瞬时触发影响，再考虑只对 `S1/S7` 做 2 次确认，而不要引入出弯状态。
- 已在 `C:\Users\1\Desktop\ELECTRIC\ele-car\Debug` 下编译通过。
- 时间戳源码已保存：
  - `C:\Users\1\Desktop\电赛\ccs_project\line_sensor_test\20260702_203620_line_follow_simple_p_minimal_right_angle_assist.c`

## 2026-07-02 20:52 视频复盘后加入非线性转弯补偿

- 用户提供实车视频 `757377267a4edf9d61f6155f8d630793.mp4`，要求仔细分析并尽量一次修正到位。
- 已抽帧：
  - `C:\Users\1\Desktop\电赛\video_analysis\757377267a4edf9d61f6155f8d630793\contact_1fps.jpg`
  - `C:\Users\1\Desktop\电赛\video_analysis\757377267a4edf9d61f6155f8d630793\contact_3fps.jpg`
  - `C:\Users\1\Desktop\电赛\video_analysis\757377267a4edf9d61f6155f8d630793\segment_0__2fps.jpg`
  - `C:\Users\1\Desktop\电赛\video_analysis\757377267a4edf9d61f6155f8d630793\segment_10__2fps.jpg`
  - `C:\Users\1\Desktop\电赛\video_analysis\757377267a4edf9d61f6155f8d630793\segment_20__2fps.jpg`
  - `C:\Users\1\Desktop\电赛\video_analysis\757377267a4edf9d61f6155f8d630793\segment_30__2fps.jpg`
- 视频观察：
  - 小车整体能跑完多圈，普通直线没有持续严重蛇形。
  - 主要问题集中在上方直角弯：进入直角弯时转向开始偏晚，转弯半径偏大，车会贴外侧甚至略冲出，再绕回来。
  - 底部尖角/斜角相对能跟住，说明不应再优先调直线状态机。
- 当前判断：
  - 不需要重新引入复杂状态机。
  - 需要在普通 P 基础上加入非线性转弯补偿：中心附近仍用旧版温和 P；中等偏差提前加强；明确急弯时低速强差速，减少前冲。
- 已修改当前工程 `C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`：
  - 新增中等偏差补偿：
    - `LINE_MEDIUM_ERROR=1500`
    - `LINE_MEDIUM_KP_DIVISOR=12`
    - `LINE_MEDIUM_CORR_LIMIT=260`
  - 增强急弯：
    - `LINE_SHARP_ERROR=2000 -> 1800`
    - `LINE_SHARP_KP_DIVISOR=8 -> 7`
    - `LINE_SHARP_CORR_LIMIT=380 -> 430`
  - 急弯时整体降速并限制外轮最高速度：
    - `SHARP_LEFT_BASE_TICKS=580`
    - `SHARP_RIGHT_BASE_TICKS=552`
    - `SHARP_MAX_TICKS=780`
    - `SHARP_INNER_FLOOR_TICKS=160`
  - 普通中心附近仍保留：
    - `PWM_LEFT_BASE_TICKS=620`
    - `PWM_RIGHT_BASE_TICKS=590`
    - `LINE_KP_DIVISOR=20`
    - `LINE_CORR_LIMIT=150`
- 已在 `C:\Users\1\Desktop\ELECTRIC\ele-car\Debug` 下编译通过。
- 时间戳源码已保存：
  - `C:\Users\1\Desktop\电赛\ccs_project\line_sensor_test\20260702_205214_line_follow_video_tuned_nonlinear_turn.c`









## 2026-07-02 19:52 直线+转弯融合版：STRAIGHT_FAST 退出迟滞

- 诊断 v2 (191058) 直线抖动根因：
  - center_seen 条件严格：S4 ON + 无边传感器 + |error|<=250
  - STRAIGHT_FAST 速度下 (715/682)，微小扰动触发 S1/S7 闪烁 → center_seen 掉 → g_center_stable_count 重置 → 退出 STRAIGHT_FAST → 降速 → 速度突变加剧更多振荡
  - FAST_DEADBAND=420 过宽允许大幅漂移，FAST_KP_DIVISOR=34 修正过弱，FAST_FILTER_DIVISOR=5 引入滞后
- 诊断 pre-sharp-turn (191813) 转弯失败根因：
  - 单模式无降速，弯道 base speed 太高 (620/590)
  - 无丢线渐进增压恢复
  - PWM_MIN_TICKS=430 内侧轮减速不足
  - 丢线 2s 即停车
- 融合策略：
  1. **STRAIGHT_FAST 退出迟滞**：已在 STRAIGHT_FAST 时，需连续 FAST_EXIT_DEBOUNCE=8 个采样非 center 才退出，防止边传感器瞬态闪烁导致模式跳变
  2. **STRAIGHT_FAST 参数回靠 pre-sharp-turn**：
     - FAST_KP_DIVISOR: 34→22
     - FAST_CORR_LIMIT: 90→120
     - FAST_DEADBAND: 420→200
     - FAST_FILTER_DIVISOR: 5→8 (更轻的 EMA)
  3. **CENTER_STABLE_SAMPLES**: 35→25 (更快进入直线快速)
  4. **PWM_SLEW_STEP_TICKS**: 60→80 (更灵敏的 PWM 响应)
  5. **CENTER_ERROR_LIMIT**: 250→300 (略微放宽中心判定)
  6. **CENTER_KP_DIVISOR**: 50→40 (更快收敛到中心)
- 保留 v2 的 LOST_ACCEL 渐进增压、LINE_TURN 提前降速、inner_floor 等所有转弯逻辑
- 新增变量：g_fast_exit_count
- 源码：C:\Users\1\Desktop\电赛\ccs_project\line_sensor_test\20260702_195230_line_follow_straight_hysteresis.c
- 已编译通过，ele-car.out 生成于 2026-07-02 19:53

## 2026-07-02 21:09 宽黑线急弯识别与丢线超时防打转

- 用户反馈：当前代码有时在转弯处直接冲出赛道，像是没有识别到转弯；把车拿起来重新放回线上后，又可能原地打转。
- 逐段检查 `empty.c` 后确认两个真实逻辑风险：
  - 直角弯/宽黑线区域可能 4 个以上红外同时检测到黑线，`sum / count` 的平均误差会接近 0，原代码会误判为“居中直行”，导致转弯识别失败并直冲出去。
  - 完全丢线时原逻辑一直沿用 `g_line_last_error`，`lost_recovery` 没有时间上限，拿起小车再放回线附近时可能继续按旧方向强转，表现为原地打转。
- 本次修改仍保持简单 P 控制主干，不重新引入复杂状态机：
  - 新增 `WIDE_LINE_ACTIVE_COUNT=4`，当 4 个及以上红外同时触发时，认为进入宽黑线/直角弯候选区。
  - 新增 `g_wide_line_detected`、`g_last_turn_sign`、`g_turn_memory_valid`，宽黑线时不再直接相信平均误差 0，而是按最近可靠转向方向强制进入急弯。
  - 新增 `LOST_TURN_MAX_SAMPLES=35`，完全丢线后最多约 0.7 s 按最后方向找回；超过后清空旧误差和转向记忆，改为直行搜索，避免长时间原地打转。
  - `g_turn_memory_valid` 初始有效，用于直线很正、第一次遇到宽黑线时仍能按默认方向兜底；但长时间完全丢线后会被清零，避免复放后旧方向污染。
- 已在 `C:\Users\1\Desktop\ELECTRIC\ele-car\Debug` 下编译通过。
- 当前活动代码：`C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`
- 时间戳备份：`C:\Users\1\Desktop\电赛\ccs_project\line_sensor_test\20260702_210925_line_follow_wide_turn_lost_timeout.c`
## 2026-07-02 21:15 GitHub 交接上传准备

- 用户要求：把历史记录、历史版本、所有文件上传到 GitHub 仓库，保证另一个人可以直接根据记录接续工作。
- 已确认当前仓库：
  - 本地路径：`C:\Users\1\Desktop\电赛`
  - remote：`https://github.com/jlg006728/electronic-design-contest.git`
  - 当前分支：`master`
- 发现并处理的交接风险：
  - 当前真正烧录的 CCS 工程在仓库外：`C:\Users\1\Desktop\ELECTRIC\ele-car`
  - 因此已复制一份工程骨架到仓库：`ccs_project/ele-car_active_project/`，排除 `Debug`/`Release` 编译产物。
  - `参考代码/.../25E题底盘国一开源/` 内存在嵌套 `.git`，不应作为坏 submodule 上传；已在 `.gitignore` 增加 `**/.git/`，保留源码文件但排除嵌套 Git 元数据。
  - Git remote 原本配置了本地代理 `127.0.0.1:7897`，该代理当前不可用；后续 push 需使用临时取消 proxy 的 git 命令。
- 新增交接入口文件：
  - `交接说明_20260702_小车循迹.md`
  - `ccs_project/ele-car_active_project/README_当前烧录工程.md`

## 2026-07-02 21:25 循迹专项交接强调

- 用户补充要求：特别强调循迹相关的过程性文件与对话记录，因为另一个同学会接替循迹调试。
- 已新增专项文件：`循迹调试交接_重点阅读.md`
- 该文件专门整理：
  - 当前循迹状态和最新待测版本。
  - 当前硬件接线。
  - `ccs_project/line_sensor_test/` 中关键历史版本的意义。
  - 现场对话中影响代码判断的浓缩记录。
  - 下一个同学继续调参时应该优先观察什么、不要重复踩哪些坑。
- 说明：仓库无法自动包含 Codex 客户端的完整逐字聊天界面，但关键对话结论已经整理进 `过程记录.md`、`知识库/调试复盘_判断失误记录.md` 和 `循迹调试交接_重点阅读.md`。
## 2026-07-02 21:27 舵机单路测试：TB6612 板载 5V 仅作供电

- 用户要求继续舵机测试，并希望“舵机使用现在的 TB6612 驱动”。
- 关键澄清：
  - 舵机不能接 TB6612 的 `AOUT/BOUT` 电机输出，TB6612 H 桥只能驱动直流电机。
  - 本次测试中 TB6612 只作为临时 `5V/GND` 供电来源，舵机控制信号仍由 MSPM0 输出。
- 测试接线：
  - `TB6612 5V -> 舵机红线`
  - `TB6612 GND -> 舵机黑/棕线`
  - `MSPM0 GND -> TB6612 GND`
  - `MSPM0 PA12 -> 舵机白/黄/橙信号线`
- 为避免车轮参与测试，程序将：
  - `PB4/STBY` 拉低；
  - `PB0/PB1/PB2/PB3` 全部拉低；
  - `PA13` 作为未用 PWM 线拉低。
- 只接一个空载舵机先测，动作范围采用较温和的 `1300us/1500us/1700us`。
- 当前活动工程 `C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c` 已临时替换为舵机测试程序，并在 `C:\Users\1\Desktop\ELECTRIC\ele-car\Debug` 下编译通过。
- 时间戳源码已保存：
  - `C:\Users\1\Desktop\电赛\ccs_project\servo_test\20260702_212704_servo_tb6612_5v_single_pa12.c`
- 现场测试注意：
  - 舵机红黑线电压动作时应尽量保持在 4.7V 以上；若明显掉到 4V 左右或板子复位，说明 TB6612 板载 5V 供电能力不足。
  - 万用表测 `PA12-GND` 只会看到 PWM 平均电压，可能约 0.2V，不应期待稳定 3.3V。

## 2026-07-02 21:34 舵机测试引脚从 PA12 改为 PA3

- 用户指出：`PA12` 已经被 TB6612 `PWMA` 占用，不能作为正式舵机测试信号脚。
- 判断修正：
  - 上一版 `PA12` 只能算“临时独立舵机测试”，不适合在当前整车接线基础上继续使用。
  - 当前整车预留舵机信号脚应使用 `PA3/PA4`，其中先用 `PA3` 单路测试一个舵机。
- 已生成新测试程序：
  - `C:\Users\1\Desktop\电赛\ccs_project\servo_test\20260702_213422_servo_tb6612_5v_single_pa3.c`
- 当前活动工程 `C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c` 已替换为 PA3 版，并编译通过。
- 新接线：
  - `TB6612 5V -> 舵机红线`
  - `TB6612 GND -> 舵机黑/棕线`
  - `MSPM0 GND -> TB6612 GND`
  - `MSPM0 PA3 -> 舵机白/黄/橙信号线`
- 仍然保持 `PB4/STBY` 拉低，避免测试舵机时直流电机动作。

## 2026-07-02 21:41 舵机不转但红黑线稳定 5V：转入 PA3/PA4 信号脚诊断

- 用户反馈：舵机仍然不转，但舵机红黑线电压稳定 5V。
- 判断：
  - 供电问题暂时降级，不再优先怀疑电压掉落。
  - 下一步应验证 `PA3` 信号脚是否真的输出到了舵机信号线，而不是继续盲目改舵机 PWM 参数。
- 已将当前活动工程 `C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c` 临时替换为 `PA3/PA4` 慢速高低电平诊断程序。
- 诊断程序行为：
  - `PA3` 高电平约 3 秒，`PA4` 低；
  - 两者低电平约 1 秒；
  - `PA4` 高电平约 3 秒，`PA3` 低；
  - 两者低电平约 1 秒；
  - 循环。
- 已 clean 后重新编译通过，避免 Make 因复制文件保留旧时间戳而误判 `ele-car.out` 已是最新。
- 时间戳源码已保存：
  - `C:\Users\1\Desktop\电赛\ccs_project\servo_test\20260702_214139_servo_pa3_pa4_gpio_level_diagnostic.c`
- 现场测试要求：
  - 先拔掉舵机信号线，只测 `PA3-GND` 和 `PA4-GND`；
  - 如果 `PA3` 能在 3.3V/0V 间切换，再切回舵机 PWM 程序；
  - 如果 `PA3` 不变而 `PA4` 正常，可临时改用 `PA4`；
  - 如果 `PA3/PA4` 都不变但 LED 在闪，优先怀疑物理针脚找错或这些排针未实际接到目标引脚。

## 2026-07-02 21:56 PA3 测量现象矛盾：改为 PA3 常高诊断

- 用户反馈：
  - `红表笔 PA3 / 黑表笔 GND = 0V`
  - `红表笔 3V3 / 黑表笔 PA3 = 0V`
- 逻辑判断：
  - 这两个读数不能在同一时刻同时为真。
  - 如果 PA3 是 0V，则 `3V3-PA3` 应约为 3.3V。
  - 如果 `3V3-PA3` 是 0V，则 PA3 应约为 3.3V。
  - 可能原因包括：测量发生在 PA3 高低切换的不同相位、表笔/档位/接触点问题、PA3 物理针脚找错、杜邦线或面包板行未导通。
- 为去掉相位干扰，已将当前活动工程改成 PA3 常高诊断：
  - `PA3` 永远输出 HIGH；
  - `PA4` 永远输出 LOW；
  - `PA15 LED` 常亮；
  - `PB4/STBY` 仍保持 LOW，电机禁用。
- 已 clean 后重新编译通过。
- 时间戳源码已保存：
  - `C:\Users\1\Desktop\电赛\ccs_project\servo_test\20260702_215630_pa3_constant_high_pa4_low_diagnostic.c`
- 预期万用表读数：
  - `红 PA3 / 黑 GND`：约 3.3V
  - `红 3V3 / 黑 PA3`：约 0V
  - `红 PA4 / 黑 GND`：约 0V
  - `红 3V3 / 黑 PA4`：约 3.3V
## 2026-07-02 22:13 PA2~PA7 官方资料核查：PA2~PA6 为 LaunchPad 板级隔离关键脚

- 用户确认：`PA3` 一直测不到有效输出；继续测 `3V3` 与 `PA2/PA3/PA4/PA5/PA6/PA7` 发现这一组引脚现象特殊，并明确排除焊接问题和万用表测量问题。
- 已联网核查 TI 官方资料：
  - `MSPM0G3507` 芯片数据手册的 pin attributes 表显示：`PA2~PA7` 芯片层面都有 Standard GPIO 能力，其中 `PA2=ROSC`，`PA3=LFXIN`，`PA4=LFXOUT`，`PA5=HFXIN`，`PA6=HFXOUT`，`PA7` 是普通标准 GPIO/CLK_OUT 等功能。
  - `LP-MSPM0G3507` LaunchPad 用户指南/原理图显示：`PA2-/PA3-/PA4-/PA5-/PA6-` 到 MCU 真实引脚之间分别通过 `R9/R10/R11/R15/R17` 隔离，且这些电阻标注为 `DNC`，默认不焊接。
  - 同一原理图还显示：`PA2_ROSC` 通过 `R8=100k 0.1%` 接地；`PA3/PA4` 接 32.768kHz 低频晶振；`PA5/PA6` 接 40MHz 高频晶振。
- 结论修正：
  - `PA2~PA6` 不是芯片不能做 GPIO，而是这块 LaunchPad 默认没有把对应低侧排针直接连到 MCU，且 MCU 端还承担时钟/校准功能。
  - `PA3/PA4` 不再作为舵机信号脚使用；之前“PA3/PA4 预留给舵机”的方案作废。
  - `PA7` 不属于同一类 DNC 隔离晶振脚；如果 PA7 现场异常，应单独核查是否找错排针、是否与当前红外 S4 接线/代码占用冲突，而不要和 PA2~PA6 混为一类。
- 下一步：舵机信号脚重新选用已经从 BoosterPack 排针验证可输出的脚位，优先避开 `PA2~PA6`、`PA12/PA13`、当前循迹和电机占用脚。
## 2026-07-02 22:20 临时改用 PA12 验证单舵机动作

- 用户要求先不做完整引脚可用性分析，直接找一个确认可用的引脚再次测试舵机。
- 决策：临时使用 `PA12` 作为单舵机信号脚。
  - 理由：`PA12` 已在电机 PWM 测试中被确认是 LaunchPad 排针可达、能实际输出的引脚。
  - 风险：`PA12` 正式用途是 TB6612 `PWMA`，不能作为最终舵机方案。
  - 临时保护：本测试程序把 `PB4/STBY` 拉低，并把 `PB0/PB1/PB2/PB3` 方向脚全部拉低，避免直流电机参与动作。
- 当前活动工程已替换：
  - `C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`
- 时间戳备份：
  - `C:\Users\1\Desktop\电赛\ccs_project\servo_test\20260702_222000_servo_single_pa12_confirmed_pin_test.c`
- 程序行为：
  - 单舵机 50Hz 软件 PWM。
  - 脉宽顺序为 `1500us -> 1300us -> 1500us -> 1700us`，每个位置保持约 1.2 秒。
  - `PA15` LED 每切换一次目标位置翻转一次。
- 已在 `C:\Users\1\Desktop\ELECTRIC\ele-car\Debug` clean 后重新编译通过，生成 `ele-car.out`。
- 现场测试接线：
  - `TB6612 5V -> 舵机红线`
  - `TB6612 GND -> 舵机黑/棕线`
  - `MSPM0 GND -> TB6612 GND`
  - `MSPM0 PA12 -> 舵机白/黄/橙信号线`
- 判断标准：
  - 若舵机能摆动：证明舵机本体和 5V 供电在单舵机场景下基本可用，下一步再找不与电机冲突的正式舵机脚。
  - 若舵机仍不动但 LED 在翻转、红黑仍稳定 5V：优先怀疑舵机信号线颜色/接触、舵机本体损坏、舵机需要更高信号幅度或机构仍卡滞。

## 2026-07-03 16:23 双舵机 PA14/PA17 程序卡在第一帧的修正

- 现场现象：用户确认程序已编译并烧录成功，程序开始运行后 `PA15 LED` 从熄灭变为常亮，但两个舵机都不转。
- 判断修正：
  - 这不是“没烧进去”，因为 LED 状态已经被程序改变。
  - 当前双舵机程序第一步让 X/Y 两个舵机都处于 `1500us` 中位，旧版 `servo_output_one_frame()` 会在 `x_us == y_us` 时执行 `delay_us(second_us - first_us)`，也就是 `delay_us(0)`。
  - `delay_us(0)` 进一步调用 `delay_cycles(0)`，在当前 TI 环境下可能卡住或行为异常；这会导致 LED 只翻转一次后常亮，舵机信号也停在异常状态。
- 已修正：
  - `delay_us(0)` 直接返回。
  - `x_us == y_us` 时两个舵机信号同时拉高、延时同一脉宽、再同时拉低，避免进入零延时路径。
- 当前活动工程：
  - `C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`
- 时间戳备份：
  - `C:\Users\1\Desktop\电赛\ccs_project\servo_test\20260703_162351_dual_servo_pa14_pa17_no_zero_delay.c`
- 已在 `C:\Users\1\Desktop\ELECTRIC\ele-car\Debug` clean 后重新编译通过，生成新的 `ele-car.out`。
- 现场预期：
  - 烧录后 `PA15 LED` 不应一直常亮，而应约每 1.4 秒翻转一次。
  - 两个舵机应按 `中心 -> X左/Y上 -> 中心 -> X右/Y下` 循环轻微摆动。
## 2026-07-03 21:22 OpenMV 云台上电回正与 HOME 校准模式

- 用户要求：每次上电都让 OpenMV 云台初始化回正；当前机械方位已被手动调整到认为正确的位置，希望程序读出并固化。
- 关键澄清：当前三线舵机没有位置反馈，MSPM0 不能从 PA14/PA17 读出当前机械角度；只能通过 PWM 脉宽校准 HOME 位置。如果舵机当前由程序控制，则可从 Watch 读取 `g_servo_x_us/g_servo_y_us`；如果是断电手扭到位，则不能反读。
- 代码修改：
  - 新增 `SERVO_X_HOME_US/SERVO_Y_HOME_US`，当前默认 `1500/1500us`。
  - 新增 `g_servo_x_home_us/g_servo_y_home_us`，可在 CCS Watch 中现场调整。
  - 新增 `servo_hold_home_on_startup()`，上电后先输出 HOME 约 1.5 秒，再进入 OpenMV 追踪。
  - 丢目标/断联后的慢回中改为回到 HOME，而不是固定回到 1500us。
  - 新增 `g_servo_calibrate_enable`：置 1 时暂停追踪，持续输出 HOME，方便 Watch 调参。
- 当前活动工程已更新：`C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`
- 时间戳源码：`C:\Users\1\Desktop\电赛\ccs_project\openmv_test\20260703_212219_mspm0_openmv_servo_home_calibration.c`
- 测试说明：`C:\Users\1\Desktop\电赛\ccs_project\openmv_test\20260703_212219_servo_home_calibration_readme.md`
- 已在 `C:\Users\1\Desktop\ELECTRIC\ele-car\Debug` clean build 通过。
## 2026-07-03 21:31 OpenMV 倒装图像修正与舵机范围放宽

- 用户反馈：当前 OpenMV 物理倒置，读出的图像也是倒置的，舵机方位也倒置；Watch 截图显示 `g_servo_y_home_us=1250`、`g_servo_y_us=1250`，已经触及旧版 Y 轴最小限位。
- 判断：
  - 舵机俯仰不能继续到位，首先是旧保护范围 `1250..1750us` 卡住，不代表舵机坏。
  - 图像倒置应该优先在 OpenMV 脚本里修正，而不是只在 MSPM0 里反向追踪；否则 IDE 预览和后续打靶坐标仍然倒，调试会混乱。
- 修改：
  - MSPM0 当前 `empty.c` 保留 `SERVO_X_HOME_US=1500U`，将 `SERVO_Y_HOME_US` 改为现场已调到的 `1250U`。
  - MSPM0 舵机范围由 `1250..1750us` 放宽为 `900..2100us`，便于继续 HOME 校准。
  - 新增 OpenMV 脚本 `20260703_213123_openmv_full_selftest_rot180.py`，开启 `sensor.set_hmirror(True)` 和 `sensor.set_vflip(True)`，等效 180 度旋转图像。
- 验证：
  - MSPM0 工程 `C:\Users\1\Desktop\ELECTRIC\ele-car\Debug` 已编译通过。
  - OpenMV Python 脚本已通过本地语法检查。
- 文件：
  - 当前烧录文件：`C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`
  - MSPM0 时间戳源码：`C:\Users\1\Desktop\电赛\ccs_project\openmv_test\20260703_213123_mspm0_openmv_servo_wide_range_rot180.c`
  - OpenMV 新脚本：`C:\Users\1\Desktop\电赛\ccs_project\openmv_test\20260703_213123_openmv_full_selftest_rot180.py`
  - 说明文档：`C:\Users\1\Desktop\电赛\ccs_project\openmv_test\20260703_213123_rot180_wide_servo_readme.md`
## 2026-07-03 21:39 舵机 Y 轴 HOME 从 750us 开始并放开校准下限

- 用户要求：`SERVO_Y_MIN_US` 先设置为 `750`，允许继续向下调整；X 方向初始已经正确，不要改。
- 修改：
  - `SERVO_X_HOME_US` 保持 `1500U`。
  - `SERVO_Y_HOME_US` 从 `1250U` 改为 `750U`，上电 HOME 直接从更低俯仰位置开始。
  - `SERVO_Y_MIN_US` 改为 `750U`，作为普通追踪阶段的 Y 轴保护下限。
  - 新增 `SERVO_Y_CALIB_MIN_US = 0U`，`g_servo_calibrate_enable=1` 的 HOME 校准阶段允许 `g_servo_y_home_us` 继续低于 `750`。
- 注意：如果舵机持续顶死或嗡鸣，应立即把 `g_servo_y_home_us` 往回调；当前放开是为了现场找机械 HOME，不代表最终正式运行也要完全无限位。
- 当前烧录文件：`C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`
- 时间戳源码：`C:\Users\1\Desktop\电赛\ccs_project\openmv_test\20260703_213950_mspm0_openmv_servo_y750_calib_unlimited.c`
- 已在 `C:\Users\1\Desktop\ELECTRIC\ele-car\Debug` 编译通过。
## 2026-07-03 21:43 云台 HOME 固化为 X=1500/Y=580，进入追踪方向测试

- 用户反馈：云台 HOME 已调好，Watch 截图显示：
  - `g_servo_x_home_us = 1500`
  - `g_servo_y_home_us = 580`
  - `g_servo_calibrate_enable = 1`
  - `g_aim_state = 5`
- 修改：
  - `SERVO_X_HOME_US` 保持 `1500U`。
  - `SERVO_Y_HOME_US` 固化为 `580U`。
  - `SERVO_Y_MIN_US` 改为 `500U`，避免正式追踪时从 HOME=580 被旧下限拉回，也给 Y 轴向下微调留余量。
  - `g_servo_calibrate_enable` 默认仍为 `0`，重新烧录/复位后将自动进入追踪模式；若现场 Watch 仍显示 1，是运行中手动改过，需改回 0。
- 当前烧录文件：`C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`
- 时间戳源码：`C:\Users\1\Desktop\电赛\ccs_project\openmv_test\20260703_214308_mspm0_openmv_servo_home_1500_580_track_test.c`
- 测试说明：`C:\Users\1\Desktop\电赛\ccs_project\openmv_test\20260703_214308_home_1500_580_track_test_readme.md`
- 已在 `C:\Users\1\Desktop\ELECTRIC\ele-car\Debug` 编译通过。
## 2026-07-03 21:48 追踪不动排查：正式版忽略校准开关

- 用户反馈：OpenMV 应该检测到了物体，但舵机始终没有变化，不能实现追踪。
- 代码复查后发现最可能的阻断点：
  - 上一轮校准 Watch 截图中 `g_servo_calibrate_enable=1`、`g_aim_state=5`。
  - 旧代码在 `g_servo_calibrate_enable != 0` 时会每帧强制 HOME，并跳过 `aiming_update_from_latest_color_frame()`。
  - 因此 OpenMV 可以检测到目标，但 MSPM0 仍会锁住舵机不追踪。
- 修改：
  - 保留 `g_servo_calibrate_enable` 变量以兼容 Watch。
  - 正式追踪版中如果该变量被误设为 1，程序自动清回 0，不再阻断追踪。
  - 主循环只要 `g_omv_color_frame_count` 变化，就调用 `aiming_update_from_latest_color_frame()`。
- 当前烧录文件：`C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`
- 时间戳源码：`C:\Users\1\Desktop\电赛\ccs_project\openmv_test\20260703_214842_mspm0_openmv_track_ignore_calib_switch.c`
- 说明文档：`C:\Users\1\Desktop\电赛\ccs_project\openmv_test\20260703_214842_track_ignore_calib_switch_readme.md`
- 已在 `C:\Users\1\Desktop\ELECTRIC\ele-car\Debug` 编译通过。
## 2026-07-03 21:59 OpenMV 主动串流版用于排查 MSPM0 Rx=0

- 用户反馈：只有一根 USB；OpenMV 程序已保存到板内、没有连接 USB；OpenMV 灯遇到红色物体会变化；MSPM0 Watch 中 `Tx` 一直增加但 `Rx` 一直为 0。
- 判断：
  - 这里的 `Tx` 对应 MSPM0 的 `g_omv_tx_requests`，说明 MSPM0 正在发 `'C'` 请求。
  - `Rx=0` 对应 `g_omv_rx_bytes=0`，说明 MSPM0 完全没有收到 OpenMV 发回的字节。
  - OpenMV 灯随红色变化只证明 OpenMV 本地视觉在运行，不证明 UART 回传链路通。
- 为隔离问题，新增 OpenMV 主动串流脚本：
  - `C:\Users\1\Desktop\电赛\ccs_project\openmv_test\20260703_215935_openmv_uart_stream_rot180.py`
  - 该脚本即使没有收到 MSPM0 命令，也会每约 `100ms` 通过 UART3 主动发送一帧颜色识别结果。
  - 这样可以将问题拆成：只要 `OpenMV P4 TX -> MSPM0 PA11 RX` 和共地正确，`g_omv_rx_bytes` 就应该增加。
- MSPM0 当前逻辑无需改变，只更新文件头脚本名并重新编译通过：
  - 当前烧录文件：`C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`
  - 时间戳源码：`C:\Users\1\Desktop\电赛\ccs_project\openmv_test\20260703_215935_mspm0_openmv_track_uart_stream.c`
- 说明文档：
  - `C:\Users\1\Desktop\电赛\ccs_project\openmv_test\20260703_215935_uart_stream_debug_readme.md`
## 2026-07-04 J21/J22 UART 跳帽结论修正：应移动到 BP 位置

- 用户反馈：使用 OpenMV 主动串流版后，MSPM0 的 Rx 仍无示数。
- 用户提供 LP-MSPM0G3507 板载 UART 跳帽照片。
- 联网核查 TI 官方资料/TI E2E 后修正前一判断：
  - J21/J22 不是简单拔掉。
  - J21/J22 应从 `XDS` 位置移动到 `BP` 位置，使 PA10/PA11 路由到外部 BoosterPack 排针。
  - 如果拔空，OpenMV 即使接到外部排针，MSPM0 也可能收不到数据。
- 已生成标注图：
  - `C:\Users\1\Desktop\uart_jumper_J21_J22_move_to_BP.jpg`
- 下一步：
  - 断电。
  - 将 J21/J22 两个黑色跳帽都移动到 `BP` 侧。
  - 重新上电运行 OpenMV 主动串流脚本和 MSPM0 当前 `empty.c`。
  - 观察 `g_omv_rx_bytes` 是否增加。
## 2026-07-04 13:59 OpenMV 追踪控制从慢速积分改为快速稳定版

- 用户反馈：
  - OpenMV/MSPM0 串口打通后，舵机可以正常转动。
  - 但固定红色目标时，舵机仍会持续移动，甚至明显偏出边界。
  - 舵机移动速度太慢，肯定跟不上后续行进间打靶。
- 诊断：
  - 当前 MSPM0 控制策略是每个颜色帧按 `dx/dy` 累加一个很小脉宽步长，近似慢速积分控制。
  - 如果方向有一轴反了，或目标坐标有噪声/误识别，累加控制会一路把舵机推到边界。
  - 因此不能只把 `AIM_MAX_STEP_US` 调大，必须同时增加死区、滤波、目标确认和小目标拒收。
- OpenMV 修改：
  - 新脚本：`C:\Users\1\Desktop\电赛\ccs_project\openmv_test\20260704_135927_openmv_uart_stream_fast_rot180.py`
  - 串流周期 `100ms -> 50ms`。
  - `MIN_RED_PIXELS/MIN_RED_AREA` 从 `12` 提高到 `30`。
- MSPM0 修改：
  - 当前烧录文件：`C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`
  - 时间戳源码：`C:\Users\1\Desktop\电赛\ccs_project\openmv_test\20260704_135927_mspm0_openmv_fast_stable_track.c`
  - `AIM_MAX_STEP_US = 45`
  - `AIM_X_DIVISOR/AIM_Y_DIVISOR = 3`
  - `AIM_DEAD_X_PIXELS/AIM_DEAD_Y_PIXELS = 20/16`
  - 增加 `g_aim_filtered_cx/g_aim_filtered_cy` 坐标低通滤波。
  - 增加 `AIM_TARGET_CONFIRM_FRAMES = 2`，连续两帧确认后才动作。
  - 增加 `AIM_MIN_PIXELS = 30`，小目标拒收。
  - `OPENMV_ENABLE_REQUESTS = 0`，MSPM0 不再主动请求，只接收 OpenMV 主动串流。
- 说明文档：
  - `C:\Users\1\Desktop\电赛\ccs_project\openmv_test\20260704_135927_fast_stable_track_readme.md`
- 验证：
  - MSPM0 工程已编译通过。
  - OpenMV 脚本已通过本地语法检查。
## 2026-07-04 15:24 舵机机械方向改变后重新进入 HOME 校准

- 用户反馈：刚才舵机到了机械临界值，现在舵机换了一个方向，需要重新执行初始位置调整。
- 判断：
  - 不能继续运行 OpenMV 追踪版。
  - 上一轮固化的 `X=1500/Y=580` 已经不适合当前机械方向，继续使用有再次顶死风险。
- 当前 `C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c` 已切换为纯 HOME 校准版：
  - `SERVO_CALIBRATION_MODE = 1`
  - `SERVO_X_HOME_US = 1500`
  - `SERVO_Y_HOME_US = 1500`
  - `g_servo_calibrate_enable = 1`
  - 主循环只执行 `servo_go_home_now()` 和 `servo_output_one_frame()`，不追踪 OpenMV。
  - `g_aim_state = 5` 表示校准模式。
  - X/Y 校准探索范围均为 `500..2500us`。
- 已编译通过。
- 时间戳源码：
  - `C:\Users\1\Desktop\电赛\ccs_project\openmv_test\20260704_152456_mspm0_servo_home_recalibration.c`
- 说明文件：
  - `C:\Users\1\Desktop\电赛\ccs_project\openmv_test\20260704_152456_servo_home_recalibration_readme.md`
- 下一步：
  - 烧录当前 `empty.c`。
  - 从 Watch 中调整 `g_servo_x_home_us/g_servo_y_home_us`，每次建议加减 20 或 50。
  - 找到新的安全初始方向后，记录最终 X/Y 数值，再生成新的追踪版。
## 2026-07-04 15:31 默认 HOME 可用，切回 OpenMV 快速追踪

- 用户反馈：默认初始位置已经可以，要求回到刚才的 OpenMV 追踪步骤。
- 修改：
  - 当前 `C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c` 从纯校准版切回快速稳定追踪版。
  - `SERVO_X_HOME_US = 1500U`
  - `SERVO_Y_HOME_US = 1500U`
  - `SERVO_CALIBRATION_MODE = 0U`
  - `g_servo_calibrate_enable = 0U`
  - 继续使用 OpenMV 主动快速串流脚本 `20260704_135927_openmv_uart_stream_fast_rot180.py`。
  - `OPENMV_ENABLE_REQUESTS = 0U`，MSPM0 不主动发请求，只接收 OpenMV 主动帧。
- 已编译通过。
- 时间戳源码：
  - `C:\Users\1\Desktop\电赛\ccs_project\openmv_test\20260704_153116_mspm0_openmv_fast_track_home_1500_1500.c`
- 说明文件：
  - `C:\Users\1\Desktop\电赛\ccs_project\openmv_test\20260704_153116_fast_track_home_1500_1500_readme.md`
- 下一步：
  - 烧录当前 `empty.c`。
  - 确认 OpenMV 仍运行 `20260704_135927_openmv_uart_stream_fast_rot180.py`。
  - 固定红色目标测试 X/Y 追踪方向。

## 2026-07-04 15:45 OpenMV 掉电原因排除，修正 X 轴追踪方向

- 现场结论：OpenMV 用着用着不亮灯/停止，不是脚本 watchdog 或程序异常，原因是当时没有连接电池，仅靠 USB 供电导致掉电。后续不要沿着软件重启方向继续排查该现象。
- 当前问题：视觉追踪时左右方向反了，上下方向看起来正常。
- 代码处理：只修改 MSPM0 端 X 轴舵机追踪符号，不改 Y 轴：`AIM_X_PULSE_SIGN` 从 `1` 改为 `(-1)`，`AIM_Y_PULSE_SIGN` 保持 `(-1)`。
- 当前烧录文件：`C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`
- 时间戳源码：`C:\Users\1\Desktop\电赛\ccs_project\openmv_test\20260704_154526_mspm0_openmv_fast_track_home_1500_1500_x_sign_fixed.c`
- OpenMV 脚本继续使用：`C:\Users\1\Desktop\电赛\ccs_project\openmv_test\20260704_135927_openmv_uart_stream_fast_rot180.py`
- 编译验证：`C:\Users\1\Desktop\ELECTRIC\ele-car\Debug` 执行 `gmake all`，结果 `ele-car.out is up to date`，无编译错误。
- 下一步测试：重新烧录当前 `empty.c`，用固定红色物体测试：物体在画面右侧时云台应向右追，物体在画面左侧时应向左追；上下方向暂时只观察，不先修改。

## 2026-07-04 16:04 OpenMV 双舵机追踪改为平滑快速版

- 用户更新现象：上下方向基本正常，主要问题变成目标稍快时跟随不足、舵机一卡一卡、运行中又出现停止；同时提出希望左右各 360 度。
- 判断：
  - “一卡一卡”主要来自旧版 MSPM0 每收到一帧 OpenMV 坐标就直接跳一次舵机脉宽，OpenMV 旧串流周期为 `50ms`，视觉上会有明显阶跃。
  - 快速目标跟随不足需要同时提高 OpenMV 坐标刷新率和 MSPM0 目标追踪速度，但不能只粗暴加大舵机步长，否则抖动和冲限位更严重。
  - 普通三线 PWM 舵机不能靠代码实现左右各 360 度绝对位置控制；若需要 360 度，需要连续旋转舵机、带反馈多圈/串口舵机，或电机+编码器云台。
- MSPM0 修改：
  - 当前烧录文件仍为 `C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`
  - 时间戳源码：`C:\Users\1\Desktop\电赛\ccs_project\openmv_test\20260704_160413_mspm0_openmv_smooth_fast_track_hold_loss.c`
  - 新增 `g_servo_x_goal_us/g_servo_y_goal_us`，视觉算法更新目标脉宽，实际输出 `g_servo_x_us/g_servo_y_us` 每个 20ms 舵机周期按 `SERVO_SLEW_STEP_US=32us` 平滑追向目标。
  - `AIM_MAX_STEP_US` 提高到 `70`，`AIM_X/Y_DIVISOR` 改为 `2`，死区略收小为 `14/12` 像素，小目标阈值改为 `25`。
  - 短时丢目标时用最后追踪方向做有限预测，`g_aim_state=8`；长时间丢目标保持当前位置，不再自动回中。
  - 新增 `g_tracking_stop_reason`：`0` 正常，`1` OpenMV 帧超时，`2` 未检测到目标，`3` 小目标被过滤。
- OpenMV 修改：
  - 新脚本：`C:\Users\1\Desktop\电赛\ccs_project\openmv_test\20260704_160413_openmv_uart_stream_smoother_fast_rot180.py`
  - 串流周期 `50ms -> 25ms`。
  - 红色目标筛选从 `30` 像素/面积降低到 `25`，blob 评分更偏向最大红色目标，减少快速运动时被中心小噪点抢目标。
- 说明文件：`C:\Users\1\Desktop\电赛\ccs_project\openmv_test\20260704_160413_smooth_fast_track_readme.md`
- 验证：
  - MSPM0 工程已编译通过。
  - OpenMV 脚本已通过 Python 语法检查。

## 2026-07-04 16:16 OpenMV 运行中关机进一步定位：水平舵机危险区保护

- 用户补充：OpenMV 掉电不是随机发生，而是常在水平舵机持续向左或向右追踪、距离初始位置不到 90 度时出现；舵机会先一卡一卡，随后 OpenMV 停止/关机。用户已尝试单独供电，现象仍存在。
- 判断：
  - 这不再像单纯静态电压不够，而更像水平舵机被视觉闭环推入某个高负载/内部电气边界区域，出现电流尖峰或地线噪声。
  - “外观看还没到 90 度”不等于舵机没到内部吃力区。普通 PWM 舵机的内部电气行程、云台结构负载、连杆角度都可能比肉眼看到的机械空间更早进入危险区。
- 代码修改：
  - 当前烧录文件：`C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`
  - 时间戳源码：`C:\Users\1\Desktop\电赛\ccs_project\openmv_test\20260704_161608_mspm0_openmv_yaw_soft_limit_guard.c`
  - 水平轴范围从 `900..2100us` 收窄为 `1050..1950us`。
  - 新增 `SERVO_X_LIMIT_MARGIN_US=35` 和 `SERVO_X_LIMIT_BACKOFF_US=70`。
  - 当 `g_servo_x_goal_us` 已接近水平安全边界且算法继续往外推时，不再硬顶边界，而是退回一点，并记录 `g_tracking_stop_reason=4`、`g_servo_x_limit_guard_count++`。
- OpenMV 脚本继续使用：`C:\Users\1\Desktop\电赛\ccs_project\openmv_test\20260704_160413_openmv_uart_stream_smoother_fast_rot180.py`
- 说明文件：`C:\Users\1\Desktop\电赛\ccs_project\openmv_test\20260704_161608_yaw_soft_limit_guard_readme.md`
- 验证：MSPM0 工程已重新编译通过。
- 下一步判据：
  - 如果这版不再关机，说明之前主要是水平舵机危险工作区触发的电源/地线瞬态问题。
  - 如果仍会关机，重点改接线：舵机电流回路不要经过 OpenMV/MSPM0 的 GND 细线，采用星形共地，并在舵机电源端加大电容。

## 2026-07-04 16:20 OpenMV 仍断电，生成冻结水平舵机诊断版

- 用户反馈：水平软限位保护版仍然断电。
- 判断：软件边界保护未能消除问题，需要进一步隔离“水平舵机运动”这一变量。
- 修改：
  - 当前烧录文件：`C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`
  - 时间戳源码：`C:\Users\1\Desktop\电赛\ccs_project\openmv_test\20260704_162030_mspm0_openmv_diag_freeze_yaw.c`
  - 设置 `DIAG_FREEZE_YAW_SERVO=1U`。
  - 水平舵机目标强制锁定到 HOME，不再参与左右追踪；OpenMV 识别、UART、俯仰舵机保留。
- 说明文件：`C:\Users\1\Desktop\电赛\ccs_project\openmv_test\20260704_162030_diag_freeze_yaw_readme.md`
- 验证：MSPM0 工程已编译通过。

## 2026-07-05 13:45 恢复双舵机 + OpenMV 平滑追踪测试代码

- 用户完成 OpenMV 供电检查：OpenMV 断开自身 USB 后，用 USB 给主板供电，小车能动，OpenMV 也亮灯。
- 用户要求重新给双舵机 + OpenMV 测试代码，用于复测是否还会断电。
- 操作：
  - 将活动工程 `C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c` 从 PA17 单引脚诊断版恢复为双舵机 OpenMV 平滑追踪版。
  - 恢复来源：`C:\Users\1\Desktop\电赛\ccs_project\openmv_test\20260704_160413_mspm0_openmv_smooth_fast_track_hold_loss.c`
  - 今日恢复存档：`C:\Users\1\Desktop\电赛\ccs_project\openmv_test\20260705_134545_restored_dual_servo_openmv_smooth_track.c`
  - OpenMV 端脚本继续使用：`C:\Users\1\Desktop\电赛\ccs_project\openmv_test\20260704_160413_openmv_uart_stream_smoother_fast_rot180.py`
- 注意：
  - 复制源码后因为原文件时间戳较旧，`gmake` 曾误判 up to date；已手动更新时间戳并重新编译，确认 `empty.c` 被重新编译链接。
  - 命令行烧录尝试失败，错误为 XDS110 `Error -260`。Windows 设备枚举中 XDS110 多项显示 `Unknown`，说明当前调试器连接/驱动状态异常。当前只确认代码已恢复并编译通过，尚未确认烧录进板子。

## 2026-07-05 13:51 纠正恢复版本：改为用户指定的 20260704_153116 HOME=1500/1500 版本

- 用户指出上一次恢复错版本，正确目标文件为：`C:\Users\1\Desktop\电赛\ccs_project\openmv_test\20260704_153116_mspm0_openmv_fast_track_home_1500_1500.c`
- 该版本特点：
  - `SERVO_X_HOME_US=1500U`
  - `SERVO_Y_HOME_US=1500U`
  - `SERVO_CALIBRATION_MODE=0U`
  - `OPENMV_ENABLE_REQUESTS=0U`
  - `AIM_X_PULSE_SIGN=1`
  - `AIM_Y_PULSE_SIGN=(-1)`
- 操作：
  - 已将该文件原样复制到当前活动工程 `C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`
  - 恢复前备份：`C:\Users\1\Desktop\电赛\ccs_project\openmv_test\20260705_135113_before_restore_153116_empty.c`
  - 今日恢复存档：`C:\Users\1\Desktop\电赛\ccs_project\openmv_test\20260705_135113_restored_153116_fast_track_home_1500_1500.c`
- 验证：
  - 已强制重新编译，避免时间戳误判。
  - 已通过 UniFlash/dslite 命令行烧录成功，输出 `Running... Success`。

## 2026-07-05 13:57 机械结构改变后切回双舵机 HOME 校准版

- 用户反馈：同学修改了舵机机械结构，需要重新调试初始位置。
- 操作：
  - 将当前活动工程 `C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c` 切回纯舵机 HOME 校准版。
  - 恢复来源：`C:\Users\1\Desktop\电赛\ccs_project\openmv_test\20260704_152456_mspm0_servo_home_recalibration.c`
  - 恢复前备份：`C:\Users\1\Desktop\电赛\ccs_project\openmv_test\20260705_135736_before_home_recalibration_empty.c`
  - 今日恢复存档：`C:\Users\1\Desktop\电赛\ccs_project\openmv_test\20260705_135736_restored_servo_home_recalibration.c`
- 当前校准版关键配置：
  - `SERVO_X_HOME_US=1500U`
  - `SERVO_Y_HOME_US=1500U`
  - `SERVO_CALIBRATION_MODE=1U`
  - `OPENMV_ENABLE_REQUESTS=0U`
  - 校准范围 `SERVO_X/Y_CALIB_MIN_US=500U` 到 `SERVO_X/Y_CALIB_MAX_US=2500U`
  - 运行时 `g_aim_state=5`
- 验证：
  - MSPM0 工程已编译通过。
  - 已通过 UniFlash/dslite 命令行烧录成功，输出 `Running... Success`。
- 下一步：在 CCS Watch/Expressions 中调 `g_servo_x_home_us` 和 `g_servo_y_home_us`，找到新机械结构的安全中位。

## 2026-07-05 14:08 新机械 HOME 固化并进入 OpenMV 联合测试

- 用户完成新机械结构 HOME 校准，截图显示：
  - `g_servo_x_home_us = 850`
  - `g_servo_y_home_us = 1500`
  - `g_aim_state = 5`
- 操作：
  - 基于用户指定的 `20260704_153116_mspm0_openmv_fast_track_home_1500_1500.c` 生成联合测试版。
  - 只固化 HOME，不额外改变追踪逻辑。
  - 当前活动工程：`C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`
  - 生成文件：`C:\Users\1\Desktop\电赛\ccs_project\openmv_test\20260705_140806_mspm0_openmv_joint_home_850_1500.c`
  - 恢复前备份：`C:\Users\1\Desktop\电赛\ccs_project\openmv_test\20260705_140806_before_openmv_joint_home_850_1500.c`
- 当前关键配置：
  - `SERVO_X_HOME_US=850U`
  - `SERVO_Y_HOME_US=1500U`
  - `SERVO_CALIBRATION_MODE=0U`
  - `OPENMV_ENABLE_REQUESTS=0U`
  - `AIM_X_PULSE_SIGN=1`
  - `AIM_Y_PULSE_SIGN=(-1)`
- 验证：
  - MSPM0 工程已编译通过。
  - 已通过 UniFlash/dslite 命令行烧录成功，输出 `Running... Success`。
- OpenMV 端脚本继续使用：`C:\Users\1\Desktop\电赛\ccs_project\openmv_test\20260704_160413_openmv_uart_stream_smoother_fast_rot180.py`

## 2026-07-05 14:13 OpenMV 联合测试修正 X 轴追踪方向

- 用户反馈：当前代码左右反了，上下没问题。
- 修改：
  - 只将 `AIM_X_PULSE_SIGN` 从 `1` 改为 `(-1)`。
  - `AIM_Y_PULSE_SIGN` 保持 `(-1)`，不改上下方向。
  - HOME 保持 `SERVO_X_HOME_US=850U`、`SERVO_Y_HOME_US=1500U`。
- 当前活动工程：`C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`
- 时间戳源码：`C:\Users\1\Desktop\电赛\ccs_project\openmv_test\20260705_141350_mspm0_openmv_joint_home_850_1500_x_sign_fixed.c`
- 验证：MSPM0 工程已编译通过。
- 按用户规则：未自动烧录，等待用户手动烧录。

## 2026-07-05 14:17 扩大双舵机运行范围并修正 X HOME 被夹问题

- 用户反馈：当前代码舵机旋转范围不对。机械结构允许水平以初始位置为基准左右各约 180 度，上下各约 90 度，需要优化代码范围。
- 复查发现：
  - 当前 `SERVO_X_HOME_US=850U`。
  - 旧运行下限仍为 `SERVO_X_MIN_US=900U`。
  - 因此正常追踪模式下 `clamp_home_position()` 会把用户校准的 X HOME=850 强行夹到 900，导致初始位置和范围都不对。
- 修改：
  - `SERVO_X_MIN_US` 从 `900U` 改为 `500U`
  - `SERVO_X_MAX_US` 从 `2100U` 改为 `2500U`
  - `SERVO_Y_MAX_US` 从 `2100U` 改为 `2500U`
  - `SERVO_Y_MIN_US` 保持 `500U`
  - 运行范围与校准范围保持一致：`500..2500us`
- 当前活动工程：`C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`
- 时间戳源码：`C:\Users\1\Desktop\电赛\ccs_project\openmv_test\20260705_141749_mspm0_openmv_joint_home_850_1500_wide_servo_range.c`
- 验证：MSPM0 工程已编译通过。
- 按用户规则：未自动烧录。

## 2026-07-05 后续调试烧录规则更新

- 用户明确要求：之后调试程序时，Codex 只需要验证代码可以编译即可。
- 烧录由用户手动完成。
- 后续默认流程：
  1. 修改/恢复代码。
  2. 保存时间戳版本。
  3. 编译验证。
  4. 输出当前 `empty.c` 路径、时间戳源码路径和测试说明。
  5. 不自动执行 dslite/UniFlash 烧录命令，除非用户明确再次要求。
- 判据：
  - 若本版不再断电，问题集中在水平舵机运动、水平舵机供电回路、线束拉扯或水平舵机自身。
  - 若本版仍断电，问题不在水平追踪角度，应转向 OpenMV 供电线、GND 走线、UART 线干扰、OpenMV 固定/线束受力或 OpenMV 板子自身。

## 2026-07-04 16:28 冻结水平舵机不再断电，生成水平慢扫诊断版

- 用户反馈：冻结水平舵机版本没有断电。
- 结论：OpenMV 自身、UART、俯仰舵机基本不是主因；断电与水平舵机运动强相关。
- 下一步诊断：生成水平舵机慢扫版，绕开视觉闭环，仅让水平舵机以小步长慢速运动，用来判断“水平舵机只要运动是否就会导致 OpenMV 掉电”。
- 当前烧录文件：`C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`
- 时间戳源码：`C:\Users\1\Desktop\电赛\ccs_project\openmv_test\20260704_162850_mspm0_openmv_diag_yaw_slow_sweep.c`
- 代码状态：
  - `DIAG_FREEZE_YAW_SERVO=0`
  - `DIAG_YAW_SWEEP_MODE=1`
  - 水平舵机在 `1300..1700us` 慢速扫描，步长 `3us/20ms`。
  - 俯仰舵机锁 HOME，OpenMV 继续运行，但视觉识别结果不参与控制。
- 说明文件：`C:\Users\1\Desktop\电赛\ccs_project\openmv_test\20260704_162850_diag_yaw_slow_sweep_readme.md`
- 验证：MSPM0 工程已编译通过。
- 判据：
  - 若慢扫仍断电，问题在水平舵机运动硬件链路：舵机本体、机构卡滞、线束拉扯、舵机电源/GND 回路或舵机噪声。
  - 若慢扫不再断电，问题更可能是正式视觉闭环中过快、频繁反向、持续单向追踪导致的动态负载，下一版应做限速限幅追踪。

## 2026-07-04 16:35 水平慢扫仍导致 OpenMV 断电，定位到水平舵机硬件链路

- 用户反馈：水平舵机慢扫诊断版仍会导致 OpenMV 断电。
- 额外现象：断电后水平舵机转动时没有“卡兹”声；OpenMV 未断电时水平舵机一直有该声音。
- 结论：
  - 视觉闭环、OpenMV 脚本、UART、俯仰舵机已基本排除。
  - 问题集中在水平舵机硬件链路：水平舵机本体异常/干扰大、水平云台机构负载过大、线束随水平轴运动被拉扯、舵机电源/GND 回路被舵机电流冲击影响 OpenMV。
  - “卡兹”声说明水平舵机上电受控时持续抖动或接近堵转/高负载状态。OpenMV 掉电后声音消失，说明掉电后舵机失去有效受控/力矩下降，而不是问题消失。
- 下一步不再优先改 OpenMV/追踪算法，改做硬件隔离：
  1. 水平舵机卸下舵盘/脱离云台负载，单独慢扫。
  2. 互换水平/俯仰两个舵机本体，判断故障跟随舵机还是跟随水平机构。
  3. 水平舵机红黑电源线独立走线到 5V 电源端，OpenMV 也独立走线到 5V 电源端，GND 星形共地，不让舵机电流经过 OpenMV 供电线。
  4. 舵机 5V/GND 近端加 470uF~1000uF 电解电容。

## 2026-07-04 17:32 PA14 单引脚扫描结果更新，生成 PA17 单引脚扫描版

- 用户反馈：
  - PA14 单引脚宽范围快速扫描时没有卡兹声。
  - 最开始不断电，但运行久了仍会断电。
  - 用户怀疑卡兹声来自竖直舵机。
- 判断：
  - PA14 单独扫描无卡兹声，说明之前“卡兹声来自水平轴”的判断需要修正。
  - “开始不断电、运行久了断电”不像瞬时软件方向错误，更像舵机持续动作后 5V 稳压模块/电源线/接插件/舵机本体升温，引发热保护或掉压。
  - 需要单独测试 PA17，确认卡兹声和掉电是否跟竖直舵机相关。
- 当前烧录文件：`C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`
- 时间戳源码：`C:\Users\1\Desktop\电赛\ccs_project\openmv_test\20260704_173227_mspm0_diag_single_pin_pa17_wide_fast_sweep_no_uart.c`
- 代码状态：
  - `DIAG_SINGLE_PIN_SWEEP_MODE=1`
  - `DIAG_SINGLE_PIN_IS_PA14=0`
  - 只给 `PA17` 输出 `1100..1900us`、`8us/20ms` 的单引脚扫描脉冲。
  - `PA14` 保持低电平，不初始化 OpenMV UART。
- 说明文件：`C:\Users\1\Desktop\电赛\ccs_project\openmv_test\20260704_173227_diag_single_pin_pa17_wide_fast_sweep_no_uart_readme.md`
- 验证：MSPM0 工程已编译通过。

## 2026-07-04 机械结构复核：PA17 卡兹声确认为竖直机构螺丝过紧

- 用户完成机械排查：PA17 竖直舵机扫描产生的卡兹声不是电流或程序问题，而是机械结构连接问题。
- 具体原因：竖直机构螺丝上得太紧，导致俯仰轴负载过大/摩擦过大。
- 处理结果：用户松开/调整螺丝后，卡兹声解决。
- 结论：
  - 之前“OpenMV 掉电 + 卡兹声”中，卡兹声至少主要来自机械结构摩擦。
  - 后续不要再把该声音直接等同于舵机电气过流或 OpenMV 程序错误。
- 下一步：
  1. 保持当前 PA17 单引脚扫描版，运行 1~2 分钟确认竖直轴不卡、不响、不发热、不掉电。
  2. 若稳定，再恢复 OpenMV 双舵机平滑追踪版。
  3. 恢复追踪后重点观察是否仍会长时间掉电；如果掉电消失，机械摩擦就是主因；如果仍掉电，再查 5V 稳压热保护/双舵机同时动作电流。
## 2026-07-05 14:57 OpenMV 疑似卡死/舵机不动诊断版本

- 当前现象：OpenMV 有时先表现为“识别到红色目标、灯颜色变化，但舵机不动”，随后可能出现 OpenMV 停止/卡死。判断上不能只凭 LED 认定 OpenMV 程序卡死，因为 LED 变化只能说明视觉循环某一段仍在跑，不能证明 MSPM0 已收到有效 UART 帧。
- 保持 MSPM0 当前诊断版 `C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c` 不变；该版本来自 `C:\Users\1\Desktop\电赛\ccs_project\openmv_test\20260705_145444_mspm0_openmv_joint_home_850_1500_link_diag.c`，已包含 `g_diag_stage` 等诊断变量。
- 新增 OpenMV 抗卡死主动串流脚本：`C:\Users\1\Desktop\电赛\ccs_project\openmv_test\20260705_145746_openmv_uart_stream_watchdog_no_print_rot180.py`。
- 新脚本改动：禁用脱机 `print`；主动约 25ms 发颜色帧；UART RX 清理加 16 字节上限；摄像头初始化失败时自动重试；Python 层异常自动重初始化摄像头；支持 WDT 看门狗；定期 `gc.collect()` 降低长时间运行内存碎片风险。
- 新增说明文件：`C:\Users\1\Desktop\电赛\ccs_project\openmv_test\20260705_145746_openmv_watchdog_stream_readme.md`。
- 验证结果：OpenMV 脚本 `python -m py_compile` 通过；CCS 当前工程 `gmake all` 通过，输出 `ele-car.out is up to date`。按用户要求，本次未自动烧录。
## 2026-07-05 OpenMV 卡死原因确认：P4 接触不良

- 现场最终确认：此前 OpenMV 使用过程中出现的“识别灯变化异常、舵机停止响应、疑似 OpenMV 卡死/断电”主要原因是 OpenMV `P4` 引脚接触不良。
- `P4` 是 OpenMV UART3_TX，连接到 MSPM0 `PA11/UART0_RX`，负责 OpenMV 向 MSPM0 回传识别结果。该线接触不良时，OpenMV 端视觉可能仍在运行，但 MSPM0 收不到稳定数据，表现会很像软件卡死或通信中断。
- 后续处理原则：OpenMV UART 线必须做机械固定/防松处理，尤其是 `P4 TX`、`P5 RX`、`GND`。若再次出现“灯还会变但舵机不动”，优先查 P4 接触和 `g_omv_color_frame_count` 是否继续增加，不要直接判断为程序问题。
## 2026-07-05 水平舵机左右范围不对称原因

- 现场现象：OpenMV 不再断电后，水平舵机向左转动范围明显小，向右转动范围过大，表现为不是“左 180 / 右 180”对称。
- 代码核对：当前 `C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c` 中 `SERVO_X_HOME_US=850U`，而运行限幅是 `SERVO_X_MIN_US=500U`、`SERVO_X_MAX_US=2500U`。
- 结论：以当前 HOME 为中心，左侧只有约 `350us` 空间，右侧有约 `1650us` 空间，必然导致转动范围严重不对称。这不是 OpenMV/P4 问题，也不是供电问题，而是水平舵机的“机械正前方”没有落在舵机电气中位附近。
- 后续建议：最终方案应让水平舵机在 `1500us` 附近时机械朝正前方，然后使用接近 `500..2500us` 的对称软件范围。若不调整机械结构，只能通过软件把右侧范围也限制到约 `1200us`，但会牺牲右侧可用视野，不能真正得到左右大范围对称。
## 2026-07-05 15:24 水平舵机改为 1500us 对称中位

- 已按现场决策修改当前 CCS 工程 `C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`：`SERVO_X_HOME_US` 从 `850U` 改为 `1500U`。
- 时间戳源码已保存：`C:\Users\1\Desktop\电赛\ccs_project\openmv_test\20260705_152451_mspm0_openmv_track_x_home_1500_symmetric.c`。
- 说明文件：`C:\Users\1\Desktop\电赛\ccs_project\openmv_test\20260705_152451_x_home_1500_symmetric_readme.md`。
- 保持水平舵机软件范围 `SERVO_X_MIN_US=500U`、`SERVO_X_MAX_US=2500U`，使 X 轴以 `1500us` 为中心左右余量对称。
- 后续现场需要在烧录后让水平舵机保持 `1500us`，松开水平摆臂/云台结构，将 OpenMV 镜头机械调整到正前方后再锁紧。
- 验证：仅执行 CCS `gmake all` 编译，通过；按用户要求未自动烧录。
## 2026-07-05 17:03 删除丢目标自动回中逻辑

- 已按现场要求临时删除“OpenMV 没识别到目标一段时间后，舵机慢慢返回 HOME”的逻辑。
- 当前 CCS 工程文件：`C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`。
- 时间戳源码：`C:\Users\1\Desktop\电赛\ccs_project\openmv_test\20260705_170304_mspm0_openmv_track_hold_when_target_lost.c`。
- 说明文件：`C:\Users\1\Desktop\电赛\ccs_project\openmv_test\20260705_170304_hold_when_target_lost_readme.md`。
- 现在行为：识别到目标时追踪；没识别到目标时 `g_aim_x_step_us/g_aim_y_step_us=0`，舵机保持最后位置；长时间没识别到目标只重置滤波 `g_aim_filter_ready=0`，不再调用 `recenter_servo_slowly()`。
- 保留上电回 HOME、X 轴 `1500us` 对称中位、OpenMV 链路诊断变量。
- 验证：CCS `gmake all` 编译通过；按用户要求未自动烧录。
## 2026-07-05 17:12 OpenMV 双舵机静态瞄准最终调参版

- 用户贴入并确认一份现场调参后的最终代码，已替换到当前 CCS 工程：`C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`。
- 时间戳源码已保存：`C:\Users\1\Desktop\电赛\ccs_project\openmv_test\20260705_171205_mspm0_openmv_static_aim_final_user_tuned.c`。
- 说明文件：`C:\Users\1\Desktop\电赛\ccs_project\openmv_test\20260705_171205_static_aim_final_user_tuned_readme.md`。
- 关键参数：`SERVO_X_HOME_US=1600U`、`SERVO_Y_HOME_US=1500U`、`SERVO_X_MIN_US=800U`、`SERVO_X_MAX_US=2200U`、`SERVO_Y_MIN_US=500U`、`SERVO_Y_MAX_US=2500U`；`AIM_X/Y_PULSE_SIGN=-1`；死区 `20/16`，最大单步 `45us`，滤波除数 `2`，确认帧数 `2`。
- 行为确认：电机禁用；上电回 HOME；OpenMV UART 回传红色目标坐标；PA14/PA17 双舵机追踪；丢目标后保持最后位置，不再慢慢回 HOME。
- 验证：CCS `gmake all` 编译通过；按用户要求未自动烧录。
## 2026-07-05 17:20 开始合并循迹与 OpenMV 双舵机瞄准

- 当前准备合并两个已验证基线：
  - 循迹基线：`C:\Users\1\Desktop\电赛\ccs_project\line_sensor_test\line_follow_sharp_turn_stable_baseline.c`。
  - 静态瞄准基线：`C:\Users\1\Desktop\电赛\ccs_project\openmv_test\20260705_171205_mspm0_openmv_static_aim_final_user_tuned.c`。
- 合并原则：第一版只验证“循迹和云台追踪能同时运行”，不加入打靶状态机，不加入发射控制，不改变已调定 OpenMV 追踪参数。
- 预计结构：电机 PWM 继续使用 `TIMG0 PA12/PA13` 硬件输出；主循环每约 20ms 输出一帧 PA14/PA17 舵机脉冲，同时更新一次 OpenMV 最新帧和循迹控制。
- 风险点：舵机 PWM 当前为软件延时输出，会让主循环天然以约 20ms 节拍运行；第一版需要实测循迹是否仍稳定，若抖动或转弯变差，再考虑把舵机 PWM 改成硬件定时器。
## 2026-07-05 17:23 循迹 + OpenMV 双舵机合并 V1 编译通过

- 已完成第一版合并代码，当前活动工程文件：`C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`。
- 时间戳源码：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260705_172354_line_follow_openmv_servo_integration_v1.c`。
- 说明文件：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260705_172354_line_follow_openmv_servo_integration_v1_readme.md`。
- 合并策略：以用户最终调定的 OpenMV 双舵机静态瞄准版为底，加入稳定循迹基线的红外读数、电机 PWM、急弯/丢线找回逻辑。电机 `PA12/PA13` 使用 `TIMG0` 硬件 PWM；舵机 `PA14/PA17` 仍使用软件 20ms 脉冲。
- 当前行为：上电闪灯并云台回 HOME；随后电机启动循迹；OpenMV UART 同时回传红色目标坐标；云台在循迹过程中继续追踪；运行约 90 秒停车；已经见线后连续丢线约 2 秒停车；启动时暂时无线则先直走找线。
- 风险记录：软件舵机 PWM 会让主循环约 20ms 更新一次循迹，可能影响急弯响应。若现场出现循迹变差、直角弯冲出、云台/电机互相影响，下一版优先考虑将舵机 PWM 改为硬件定时器或降低云台控制频率。
- 验证：CCS `gmake all` 编译通过；按用户要求未自动烧录。
## 2026-07-05 21:50 循迹 + OpenMV 双舵机高扭矩合并 V2

- 用户反馈：小车增加 OpenMV、云台、舵机和供电线束等负重后明显走不动，电机扭矩不足。
- 已以合并 V1 `20260705_172354_line_follow_openmv_servo_integration_v1.c` 为基底生成高扭矩 V2，并替换当前活动工程 `C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`。
- V2 前活动文件备份：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260705_215010_before_high_torque_v2_empty.c`。
- 时间戳源码：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260705_215010_line_follow_openmv_servo_high_torque_v2.c`。
- 说明文件：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260705_215010_line_follow_openmv_servo_high_torque_v2_readme.md`。
- PWM 改动：`PWM_LEFT_BASE_TICKS=720U`、`PWM_RIGHT_BASE_TICKS=690U`、`PWM_LEFT_BOOST_TICKS=900U`、`PWM_RIGHT_BOOST_TICKS=870U`、`PWM_MIN_TICKS=520U`、`PWM_MAX_TICKS=1000U`、`START_BOOST_SAMPLES=20U`。前约 400ms 使用启动助推基准 PWM，同时保留循迹修正。
- 新增观察变量：`g_start_boost_active`，用于确认是否处在启动助推阶段。
- 保留 OpenMV 双舵机最终调参、丢目标保持最后位置、90 秒运行时间、见线后连续丢线约 2 秒停车、启动无线先直走找线。
- 验证：CCS `gmake all` 编译通过；未自动烧录。
## 2026-07-05 21:55 高扭矩合并 V3：轻微加强转弯

- 用户反馈：高扭矩版本转弯偏慢，希望“稍微急一点点”。
- 已基于当前高扭矩 V2 修改当前活动工程 `C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`。
- 时间戳源码：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260705_215512_line_follow_openmv_servo_high_torque_v3_sharper_turn.c`。
- 说明文件：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260705_215512_line_follow_openmv_servo_high_torque_v3_sharper_turn_readme.md`。
- 改动仅限急弯参数：`LINE_SHARP_ERROR 1800 -> 1600`、`LINE_SHARP_KP_DIVISOR 8 -> 7`、`LINE_SHARP_CORR_LIMIT 380 -> 430`。
- 普通循迹参数 `LINE_KP_DIVISOR=20`、`LINE_CORR_LIMIT=150` 未改，避免直线抖动变大。
- 高扭矩基础 PWM、启动助推、OpenMV 双舵机参数均保持 V2。
- 验证：CCS `gmake all` 编译通过；未自动烧录。
## 2026-07-05 22:12 高扭矩合并 V4：急弯降速受控版

- 用户补充视频判断：V2 停止处不是单纯转弯太慢，而是最后应该继续往右转，但小车往左转了；V3 加强急弯后甚至放上去就原地打转。
- 诊断结论：V3 的问题来自“更早进入急弯 + 更强修正 + 高扭矩外侧轮”，会把错误方向/错误侧传感器读数迅速放大。不能继续靠加大急弯修正解决。
- 已基于 V2 恢复当前活动工程 `C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c` 并生成 V4。
- 时间戳源码：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260705_221203_line_follow_openmv_servo_high_torque_v4_controlled_turn.c`。
- 说明文件：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260705_221203_line_follow_openmv_servo_high_torque_v4_controlled_turn_readme.md`。
- V4 前活动文件备份：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260705_221203_before_controlled_turn_v4_empty.c`。
- V4 保留 V2 急弯参数：`LINE_SHARP_ERROR=1800`、`LINE_SHARP_KP_DIVISOR=8`、`LINE_SHARP_CORR_LIMIT=380`。
- V4 新增急弯低速参数：`PWM_SHARP_LEFT_BASE_TICKS=620U`、`PWM_SHARP_RIGHT_BASE_TICKS=590U`、`PWM_SHARP_MAX_TICKS=850U`。普通/起步仍保持高扭矩，急弯时降速并限制外侧轮，减少冲过弯后反向误判。
- 验证：CCS `gmake all` 编译通过且无警告；未自动烧录。
## 2026-07-05 22:22 高扭矩合并 V5：直线基础平衡

- 用户反馈：车辆放到直线段时，本应识别为直线，但启动后立即有右转倾向；远离黑线后又有左偏补偿感。
- 诊断：优先处理直线基础偏置。V4 的普通、启动助推和急弯低速基准都为 `LEFT > RIGHT`，在当前负重/供电下形成固定右转倾向；丢线后的左偏可能是后续找线补偿，暂不作为第一调参目标。
- 已修改当前活动工程 `C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`。
- 时间戳源码：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260705_222246_line_follow_openmv_servo_high_torque_v5_straight_balance.c`。
- 说明文件：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260705_222246_line_follow_openmv_servo_high_torque_v5_straight_balance_readme.md`。
- 改动：`PWM_LEFT_BASE_TICKS/PWM_RIGHT_BASE_TICKS` 从 `720/690` 改为 `705/705`；`PWM_LEFT_BOOST_TICKS/PWM_RIGHT_BOOST_TICKS` 从 `900/870` 改为 `880/880`；`PWM_SHARP_LEFT_BASE_TICKS/PWM_SHARP_RIGHT_BASE_TICKS` 从 `620/590` 改为 `605/605`。
- 保留 V4 急弯降速受控策略、OpenMV 双舵机参数、急弯触发参数。
- 验证：CCS `gmake all` 编译通过；未自动烧录。
## 2026-07-05 22:32 高扭矩合并 V6：红外输入下拉防悬空

- 用户澄清：之前“从左到右第 5 个传感器长期在黑线上”的现象实际是传感器线松了，不是循迹修正方向反。
- 已撤回“修正方向反”的判断，没有改动循迹方向。
- 当前活动工程 `C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c` 已在 V5 基础上做低风险输入防护。
- 时间戳源码：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260705_223215_line_follow_openmv_servo_high_torque_v6_sensor_pulldown.c`。
- 说明文件：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260705_223215_line_follow_openmv_servo_high_torque_v6_sensor_pulldown_readme.md`。
- 改动：7 路红外输入 `init_input()` 中 `DL_GPIO_RESISTOR_NONE` 改为 `DL_GPIO_RESISTOR_PULL_DOWN`，降低传感器 DO 线松动/断开时输入悬空乱跳成假黑线的概率。
- 保留 V5 左右基础 PWM 平衡、V4 急弯降速受控策略、OpenMV 双舵机参数。
- 备注：每次把车拿起再放回赛道，建议按 `RST`，否则 `g_line_last_error/g_line_lost_count/g_phase` 等状态会保留，现象不适合作为直线调参依据。
- 验证：CCS `gmake all` 编译通过；未自动烧录。
## 2026-07-06 15:58 高扭矩合并 V7：加强普通居中纠偏

- 用户反馈：小车前两圈可以正常循迹，但第三圈在后半圈直线某处偏出轨道并停车；前两圈仍主要由从左到右第 5 个传感器 `S5` 压在黑线上。
- 判断：车辆能识别赛道，但直线居中偏弱，长期贴着线的一侧跑，累计偏差或赛道某处胶带/反光变化后容易出轨。
- 已修改当前活动工程 `C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`。
- 时间戳源码：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260706_155859_line_follow_openmv_servo_high_torque_v7_center_correction.c`。
- 说明文件：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260706_155859_line_follow_openmv_servo_high_torque_v7_center_correction_readme.md`。
- 改动：仅普通直线/小弯纠偏加强，`LINE_KP_DIVISOR 20 -> 16`，`LINE_CORR_LIMIT 150 -> 190`；急弯、高扭矩、OpenMV 参数不变。
- 预期：当只有 `S5` 压线时，普通修正由约 `50 tick` 提高到约 `62 tick`，让车更主动向中间回正。
- 验证：CCS `gmake all` 编译通过；未自动烧录。
## 2026-07-06 16:08 高扭矩合并 V8：S4/S5 中心偏置

- 用户确认：车身中心感觉在 `S4` 和 `S5` 中间。
- 诊断：旧算法以 `S4=0` 为软件中心，但当前机械中心更接近 `S4/S5` 中间，因此应将目标中心设置为 `+500`。
- 已修改当前活动工程 `C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`。
- 时间戳源码：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260706_160854_line_follow_openmv_servo_high_torque_v8_center_offset.c`。
- 说明文件：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260706_160854_line_follow_openmv_servo_high_torque_v8_center_offset_readme.md`。
- 改动：新增 `LINE_CENTER_OFFSET=500`，并将 `g_line_error` 计算改为 `(sum / count) - LINE_CENTER_OFFSET`。这样 `S4+S5` 同时压线时误差为 0，单独 `S5` 压线误差从 `+1000` 降为 `+500`。
- 保留 V7 普通居中纠偏、V6 红外输入下拉、V5 左右 PWM 平衡、V4 急弯降速受控策略和 OpenMV 双舵机参数。
- 验证：CCS `gmake all` 编译通过；未自动烧录。

## 2026-07-06 22:04 B 点左预瞄 V3 与声光提示方案

- 用户反馈：B 点停车时没有找到靶子，水平云台向左转动幅度不够；OpenMV 稍远时抓取不稳定；同时开始考虑声光提示怎么做、声音器件怎么接线。
- 诊断：
  - 当前 B 点瞄准版原本停车后直接从当前云台姿态开始找靶，缺少 B 点“先朝靶区预瞄”的动作。
  - `aim_should_hold_for_chassis_turn()` 会在 `g_turn_mode=1/2` 时禁止丢目标搜索；如果 B 点停车前残留急弯/丢线状态，云台会在静态停车时仍不积极搜索目标。
  - OpenMV 远距离抓不稳不应简单放宽阈值。目标远时红色 blob 像素数变小，若直接降低 `MIN_RED_PIXELS/MIN_RED_AREA`，空场景误识别和灯乱闪会更明显。
  - 查 `器件采购清单.md` 和 `器件采购链接汇总.md`，已买/规划器件中有 LED、电阻、按键、开关、电容、电源模块等，但没有明确蜂鸣器。
- 已修改当前活动工程：`C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`。
- 时间戳源码：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260706_220410_line_follow_b_aim_left_preset_v3.c`。
- 说明文件：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260706_220410_line_follow_b_aim_left_preset_v3_readme.md`。
- 代码改动：
  - 新增 `MISSION_B_AIM_X_PRESET_US=2450U`、`MISSION_B_AIM_Y_PRESET_US=SERVO_Y_HOME_US`、`MISSION_B_AIM_SEARCH_DIR=1`。
  - B 点进入瞄准时先停车、清 `g_turn_mode/g_line_lost_count/g_line_correction`，并将水平舵机预置到左侧。
  - B 点进入瞄准时清除旧目标滤波、确认计数、搜索状态和丢目标计数。
  - `PHASE_MISSION_AIM` 阶段不再因为底盘转弯状态而阻止云台搜索。
  - `MISSION_AIM_MAX_FRAMES` 从 `250U` 提到 `350U`，`AIM_SEARCH_STEP_US` 从 `18` 提到 `22`。
- 声光提示结论：
  - 现有器件可先做 LED 光提示。
  - 如果要声音，需要新增有源蜂鸣器模块最省事；接线建议 `VCC -> 3.3V/5V(按蜂鸣器模块)`, `GND -> 公共 GND`, `SIG -> 空闲 GPIO`。
  - 若使用 5V 蜂鸣器或电流大于 GPIO 安全驱动能力，应加 NPN 三极管或小 MOSFET 驱动，不能直接让 MSPM0 引脚承受负载电流。
- 验证：CCS `gmake all` 编译通过；按用户规则未自动烧录。

## 2026-07-06 23:43 当前版本备份与 25E 参考代码学习

- 用户要求：结合当前代码和之后要完成的步骤，学习参考获奖代码并优化；开始前先把当前版本备份到桌面 `电赛备份`。
- 已创建备份目录：`C:\Users\1\Desktop\电赛备份\backup_20260706_234346_current_integration`。
- 备份内容：
  - 当前活动 `empty.c`
  - `20260706_220410_line_follow_b_aim_left_preset_v3.c`
  - `20260706_220410_line_follow_b_aim_left_preset_v3_readme.md`
  - `20260706_163540_openmv_uart_stream_edge_guard_rot180.py`
  - `项目记忆.md`
  - `过程记录.md`
  - `知识库\调试复盘_判断失误记录.md`
- 已阅读参考代码：
  - `25E题底盘国一开源\ApplicationLayer\Task.c`
  - `25E题底盘国一开源\FunctionalModule\Buzzer.c`
  - `25E题底盘国一开源\FunctionalModule\GraySensor.c`
  - `25E题底盘国一开源\FunctionalModule\MotorsCtrl.c`
  - `25E题底盘国一开源\user\main.c`
  - `25E题底盘国一开源\user\datatype.h`
- 学习结论文件：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260706_234346_reference_code_learning_optimization_notes.md`。
- 关键结论：
  - 参考代码最值得借鉴的是任务状态机、非阻塞蜂鸣器提示、按赛段切换速度，以及后续用编码器距离替代采样计数。
  - 参考代码的灰度传感器、四电机 PID、TIMG12/PA25 蜂鸣器配置不能直接搬到当前车。
  - 当前集成版最大的结构限制是 PA14/PA17 舵机 PWM 仍使用 20ms 阻塞软件脉冲；后续若要提高循迹/打靶性能，应考虑硬件定时器舵机 PWM。
- 本次没有修改当前 `empty.c`，只完成备份和学习记录。

## 2026-07-07 13:54 全程跟踪测试 V1

- 用户决定：蜂鸣器先不装；先调试全程 OpenMV/云台跟踪，再回头做 ABC 三点/任务点。
- 改前备份：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260707_135406_before_full_course_tracking_empty.c`。
- 已修改当前活动工程：`C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`。
- 时间戳源码：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260707_135432_full_course_tracking_only_v1.c`。
- 说明文件：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260707_135432_full_course_tracking_only_v1_readme.md`。
- 代码改动：
  - 新增 `MISSION_ENABLE_B_AIM=0U`。
  - 新增 `MISSION_ENABLE_POINT_PROMPTS=0U`。
  - 主循环中 B 点停车触发受 `MISSION_ENABLE_B_AIM` 控制。
  - C/D/A 点提示和最终 A 点任务受 `MISSION_ENABLE_POINT_PROMPTS` 控制。
- 本版行为：小车上电回 HOME 后直接循迹，OpenMV/PA14/PA17 全程追踪红色目标；不在 B 点停车，不做 C/D/A 提示；仍保留全局运行超时停车和连续丢线停车保护。
- 验证：CCS `gmake all` 编译通过；按用户规则未自动烧录。

## 2026-07-07 14:09 全程跟踪主动搜索 V2

- 用户反馈：询问全程跟踪 V1 的实现逻辑，并反馈 V1 程序失败。
- V1 真实逻辑说明：
  - V1 只关闭了 B 点停车和 C/D/A 任务点。
  - V1 没有重新设计“全程运动追踪”策略，仍沿用此前静态/半静态 OpenMV 追踪逻辑。
  - 原逻辑在底盘急弯/丢线找回时会禁止云台搜索，并且会拒绝靠近图像边缘的目标；这两个保护在全程追踪中可能导致目标离开视野后找回过慢或根本不找。
- 改前备份：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260707_140850_before_full_course_tracking_v2_empty.c`。
- 已修改当前活动工程：`C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`。
- 时间戳源码：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260707_140926_full_course_tracking_active_search_v2.c`。
- 说明文件：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260707_140926_full_course_tracking_active_search_v2_readme.md`。
- V2 改动：
  - `AIM_X_DIVISOR 3 -> 2`：水平追踪更灵敏。
  - `AIM_MAX_STEP_US 45 -> 60`：单帧最大舵机步长提高。
  - `AIM_SEARCH_START_FRAMES 14 -> 6`：丢目标后更早开始搜索。
  - `AIM_SEARCH_STEP_US 22 -> 35`：水平搜索更快。
  - 新增 `FULL_TRACK_SEARCH_DURING_TURN=1U`：底盘转弯时云台仍允许搜索。
  - 新增 `FULL_TRACK_ACCEPT_EDGE_TARGET=1U`：目标靠近画面边缘时不再直接拒绝追踪。
- 风险：V2 云台会比 V1 更主动，可能带来抖动或偶尔追边缘误目标；本版用于判断“V1 是否因搜索过保守失败”。
- 验证：CCS `gmake all` 编译通过；按用户规则未自动烧录。

## 2026-07-07 14:31 OpenMV VGA 远距离红色识别 V3

- 用户反馈：优先改 OpenMV 识别，定点稍微远一点都识别不到；随后要求参考老师给的代码。
- 参考代码检查：
  - 在 `C:\Users\1\Desktop\电赛\参考代码\校赛_2026-06-29` 中搜索 OpenMV/视觉/颜色/摄像头相关实现。
  - 老师参考工程没有直接可移植的 OpenMV Python 视觉识别代码；主要是底盘、灰度、编码器、任务状态机、蜂鸣器。
  - 可借鉴思想：先让传感器输入稳定，再进入上层任务判断；不要只靠单一脆弱阈值。
- MSPM0 改前备份：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260707_143104_before_openmv_vga_far_detect_empty.c`。
- 已修改当前活动工程：`C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`。
- MSPM0 时间戳源码：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260707_143104_full_course_tracking_openmv_vga_far_red_v3.c`。
- 说明文件：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260707_143104_full_course_tracking_openmv_vga_far_red_v3_readme.md`。
- 新 OpenMV 脚本：`C:\Users\1\Desktop\电赛\ccs_project\openmv_test\20260707_143104_openmv_uart_stream_vga_far_red_v1.py`。
- OpenMV 改动：
  - 分辨率从 `QVGA 320x240` 改为 `VGA 640x480`。
  - 红色阈值保留原阈值并新增一组更宽的远距离阈值。
  - 增加 blob 宽高、面积、像素数、密度过滤，降低空场景误识别风险。
  - 降低中心惩罚，避免远距离目标在边缘时被小噪声抢走。
  - 设置 `sensor.set_saturation(2)` 增强红色分离。
  - UART 主动发送间隔从 `25ms` 改为 `40ms`，适配 VGA 帧率。
- MSPM0 同步改动：
  - `IMG_WIDTH 320 -> 640`
  - `IMG_HEIGHT 240 -> 480`
  - `AIM_DEAD_X_PIXELS 20 -> 32`
  - `AIM_DEAD_Y_PIXELS 16 -> 24`
- 验证：
  - OpenMV 脚本 `python -m py_compile` 语法检查通过。
  - CCS `gmake all` 编译通过。
  - 按用户规则未自动烧录。
- 现场测试建议：
  - 先不跑车，直接用 OpenMV IDE 静态测试 A/B/BC/CD 几个距离与角度是否能稳定框住红色靶。
  - 静态识别稳定后，再烧录 MSPM0 当前 `empty.c` 做全程跟踪。

## 2026-07-07 14:42 回滚 VGA，生成 OpenMV QVGA 远距离稳定识别 V4

- 用户反馈：VGA 版本 OpenMV 一直亮红灯，小车无法追踪；插电脑观察时颜色异常且帧率很卡。
- 诊断：
  - 按当前 OpenMV 脚本逻辑，红灯用于异常恢复，不是目标识别提示。
  - 绿灯才表示检测到目标，蓝灯为心跳。
  - 红灯常亮 + 颜色异常 + 帧率卡，说明 VGA 版对当前 OpenMV/现场负载过重或颜色设置不合适，不能继续作为实车追踪版本。
- MSPM0 改前备份：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260707_144221_before_revert_openmv_qvga_far_stable_empty.c`。
- 已修改当前活动工程：`C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`。
- MSPM0 时间戳源码：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260707_144221_full_course_tracking_openmv_qvga_far_stable_v4.c`。
- 说明文件：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260707_144221_full_course_tracking_openmv_qvga_far_stable_v4_readme.md`。
- 新 OpenMV 脚本：`C:\Users\1\Desktop\电赛\ccs_project\openmv_test\20260707_144221_openmv_uart_stream_qvga_far_stable_v2.py`。
- OpenMV 改动：
  - 回到 `QVGA 320x240`。
  - 不再设置 `sensor.set_saturation(2)`。
  - 保留旧稳定红色阈值，同时新增一组温和放宽的远距离红色阈值。
  - 降低 `MIN_RED_PIXELS/MIN_RED_AREA` 到 `14`。
  - 增加 `MIN_RED_W/MIN_RED_H/MIN_RED_DENSITY_PERCENT` 过滤噪声。
  - 降低中心惩罚，远处偏边目标更容易保留。
- MSPM0 同步改动：
  - `IMG_WIDTH 640 -> 320`
  - `IMG_HEIGHT 480 -> 240`
  - `AIM_DEAD_X_PIXELS 32 -> 20`
  - `AIM_DEAD_Y_PIXELS 24 -> 16`
  - `AIM_MIN_PIXELS 30/14状态确认后设为 14U`
- 验证：
  - OpenMV 脚本 `python -m py_compile` 语法检查通过。
  - CCS `gmake all` 编译通过。
  - 按用户规则未自动烧录。
- 下一步测试：先用 OpenMV IDE 静态运行新 QVGA 脚本，确认红灯不常亮、蓝灯心跳、绿灯能随红色靶稳定亮；静态通过后再跑车。

## 2026-07-07 14:53 全程跟踪赛段预瞄偏置 V5

- 用户反馈：当前 OpenMV 已经能在较远距离识别目标；要求重新给小车跑一圈代码，并明确云台策略：BC 段应持续左偏，CD 段才开始往右回，DA 段持续右偏。
- 理解：这里的左偏/右偏指 OpenMV 云台水平预瞄，不是小车循迹偏线。
- MSPM0 改前备份：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260707_145140_before_segment_aim_bias_empty.c`。
- 已修改当前活动工程：`C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`。
- 时间戳源码：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260707_145322_full_course_segment_aim_bias_v5.c`。
- 说明文件：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260707_145322_full_course_segment_aim_bias_v5_readme.md`。
- 当前 OpenMV 脚本保持：`C:\Users\1\Desktop\电赛\ccs_project\openmv_test\20260707_144221_openmv_uart_stream_qvga_far_stable_v2.py`。
- 新增参数：
  - `COURSE_SEGMENT_AIM_ENABLE=1U`
  - `COURSE_SEGMENT_BC_LEFT_US=2450U`
  - `COURSE_SEGMENT_CD_RETURN_US=SERVO_X_HOME_US`
  - `COURSE_SEGMENT_DA_RIGHT_US=800U`
  - `COURSE_SEGMENT_AIM_STEP_US=35`
- 新增 Watch 变量：
  - `g_course_segment`
  - `g_segment_aim_x_us`
- 行为：
  - 识别到目标时仍按 OpenMV `cx/cy` 正常闭环追踪。
  - 丢目标时根据 `g_run_sample` 判断赛段：
    - AB：目标 X 为 HOME
    - BC：目标 X 为 `2450us`，持续左偏
    - CD：目标 X 为 HOME，开始向右回
    - DA：目标 X 为 `800us`，持续右偏
  - 每帧最多移动 `35us`，避免突然甩头。
- 风险/待调：
  - 赛段边界仍复用 `MISSION_B_STOP_SAMPLE/MISSION_C_PROMPT_SAMPLE/MISSION_D_PROMPT_SAMPLE`，不是编码器距离；若切换早晚不对，应优先调这些 sample 值。
- 验证：CCS `gmake all` 编译通过；按用户规则未自动烧录。
- 2026-07-07 15:15 补充召回：用户纠正全程预瞄逻辑。旧 V5 把 AB/BC/CD/DA 当成离散段处理，并在 DA 加右偏，这与赛道图和现场意图不一致。新目标：从 A 点出发后，A->B 过程中云台持续有左偏倾向，并保持左偏直到 BC 中点；从 BC 中点开始按圈进度缓慢向右回正，到下一次 A 点回到正前方。预偏转应有角度/脉宽限制，避免舵机扫到无意义极限。当前只改代码并编译验证，不自动烧录。
- 2026-07-07 15:16 全程 OpenMV 几何预偏转 V6：用户指出 V5 仍不符合图纸和现场思路，要求第一圈从 A 点开始，A->B 过程中云台持续左偏，并保持左偏直到 BC 中点；随后开始向右慢慢回正，直到下一次 A 点回到正前方。同时确认靶位按题意在 AB 段外侧约 50cm 处。已将 `C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c` 改为连续几何预偏转模型，保存为 `C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260707_151637_full_course_geometric_preaim_curve_v6.c`，说明文件为 `C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260707_151637_full_course_geometric_preaim_curve_v6_readme.md`。关键参数：`COURSE_LAP_SAMPLES=1280U`，`COURSE_BC_MID_SAMPLE=462U`，`COURSE_LEFT_TARGET_US=2550U`，`COURSE_PREAIM_STEP_US=35`。依据图纸 AB=100cm、R=40cm，A 到 BC 中点约占整圈 36.1%。OpenMV 稳定识别时仍闭环追踪；目标丢失/不可用时按几何预偏转方向搜索。CCS `gmake all` 编译通过，未自动烧录。
- 2026-07-07 15:24 补充召回：用户反馈 V6 仍不对，提出 ABCD 点需要强硬修正，并要求 ABCD 点分别停车 2s；同时要求检查小车行驶距离/电机控制是否需要优化。当前判断：现有代码没有使用编码器距离，`g_run_sample` 是基于 20ms 舵机帧的时间步估算，因此电压、负重、速度变化会导致点位漂移。本轮计划在当前 `empty.c` 上增加赛道关键点状态机：按图纸比例估算 B/C/D/下一次 A 的 sample 阈值，到点停车 2s，并在停车入口把云台强制打到该点对应的几何预瞄位置；停车期间 OpenMV 仍可继续识别并微调。
- 2026-07-07 15:28 关键点停车强制校正 V7：已在当前 `C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c` 增加 `MISSION_ENABLE_POINT_STOPS=1U`、`PHASE_POINT_HOLD=7U`、`COURSE_POINT_HOLD_FRAMES=100U`，并按图纸比例加入 B/C/D/下一次 A 点 sample：B=284、BC中点=462、C=640、D=924、A=1280。到点后电机停止约 2s，云台水平角强制设置为该点几何预瞄角；停车期间 OpenMV 请求、串口接收、舵机 PWM 和视觉闭环仍运行。上电起始 A 点 HOME 保持改为 100 帧，约 2s。时间戳源码：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260707_152852_course_point_stop_forced_aim_v7.c`；说明文件：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260707_152852_course_point_stop_forced_aim_v7_readme.md`。CCS `gmake all` 编译通过，未自动烧录。电机距离结论：当前代码未使用编码器，点位距离仍依赖 `g_run_sample` 时间估算；若要真正停准 ABCD，需要后续接入编码器计数替代时间估算。
- 2026-07-07 15:34 补充召回：用户确认按 5V 有源蜂鸣器方案处理，要求把此前预设的声光提示逻辑加入当前集成代码。目标：上电滴 1 声，B 点滴 2 声，瞄准成功滴 3 声，失败长响，C/D/A 用不同闪灯/鸣叫次数区分。实现原则：蜂鸣器用 MSPM0 GPIO 控制开关，建议外接三极管/MOSFET 驱动；代码侧使用 20ms 主循环帧的非阻塞蜂鸣状态机，避免长延时破坏舵机软件 PWM 和 OpenMV 串口处理。
- 2026-07-07 15:35 有源蜂鸣器提示 V8：已在当前 `C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c` 加入 PB11 蜂鸣器控制，PB11 对应 `IOMUX_PINCM28`，默认低电平关闭，高电平鸣叫。代码采用非阻塞蜂鸣状态机，每个 20ms 舵机帧更新一次，避免长延时影响舵机软件 PWM、OpenMV 串口和循迹。鸣叫规则：上电 HOME 1声，B点2声，OpenMV 瞄准稳定3声，C点4声，D点5声，A点1声，关键点停车2s内未锁定目标则长响约1s。时间戳源码：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260707_153507_course_point_stop_buzzer_v8.c`；说明文件：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260707_153507_course_point_stop_buzzer_v8_readme.md`。CCS `gmake all` 编译通过，未自动烧录。硬件建议仍是 PB11 通过 1kΩ 到三极管/MOSFET 基极/栅极，由 5V 给有源蜂鸣器供电，MSPM0 与 5V 电源共地。
- 2026-07-07 15:39 补充召回：用户实测 A->B 跑的距离比地图实际距离多约 2/5，指出当前 `g_run_sample` 点位距离完全不准，并要求两版代码：一版暂不使用编码器，把每一段距离改为原来的 `5/7`；另一版使用编码器做精确距离。判断：当前 V8 的 B/C/D/A sample 阈值按图纸比例计算没错，但把 20ms 时间步当距离导致车速/负重/电压变化后点位漂移。短期先做 `5/7` 缩放版用于快速验证；长期应使用编码器脉冲替代 `g_run_sample`。
- 2026-07-07 15:44 距离修正两版代码：按用户要求完成两版并均编译通过。V9 为不使用编码器的 `5/7` 缩放版，将 `COURSE_LAP_SAMPLES 1280->914`、B `284->203`、BC中点 `462->330`、C `640->457`、D `924->660`，文件：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260707_153950_course_point_stop_buzzer_scaled_5over7_v9.c`，说明：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260707_153950_course_point_stop_buzzer_scaled_5over7_v9_readme.md`。V10 为编码器距离版，加入 PB12/PB13/PB14/PB15 编码器中断，使用左右轮前进边沿平均值 `g_encoder_distance_edges` 触发 B/C/D/A，文件：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260707_154420_course_point_stop_buzzer_encoder_distance_v10.c`，说明：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260707_154420_course_point_stop_buzzer_encoder_distance_v10_readme.md`。当前活动 `empty.c` 已恢复为 V9 并重新 clean build；V10 已保存为独立文件。后续建议先实测 V9 验证 5/7 是否接近，再校准 V10 的 `ENCODER_EDGES_PER_CM`。
- 2026-07-07 15:50 补充召回：用户纠正全程预瞄策略：不是到 BC 中点后回正，而是应一直左偏到 CD 中点，随后再回正直到 A。当前优先使用 V9 的 5/7 非编码器版本，不切入编码器版。同时用户要求舵机左右旋转范围尽量达到 270°，当前体感只有 180°；代码侧应放宽水平舵机软件限幅和左偏目标，但需注意真实角度仍受舵机本体和机械安装限制，非法 `0us` 脉宽不能作为有效舵机范围。
- 2026-07-07 16:08 V11 左偏保持到 CD 中点：用户纠正策略后，已在当前 `C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c` 上修改，仍使用 5/7 非编码器版本。将回正起点从原 BC 中点改为 CD 中点，新增/替换为 `COURSE_LEFT_HOLD_SAMPLE=559U`，由当前 C=457、D=660 取中点得到。水平舵机软件范围由 `350..2650us` 放宽到 `300..2800us`，左偏目标 `COURSE_LEFT_TARGET_US=2750U`。编译通过，未自动烧录。时间戳源码：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260707_160803_scaled_5over7_left_until_cd_mid_servo_wide_v11.c`；说明文件：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260707_160803_scaled_5over7_left_until_cd_mid_servo_wide_v11_readme.md`。说明：扩大软件限幅只能保证代码不提前截断脉宽，不能突破舵机本体物理角度；`0us` 是非法/丢信号级脉宽，不能用作角度下限。
- 2026-07-07 16:14 V12 舵机安全回退：用户反馈 V11 水平舵机向左发生超过 270°偏转。原因判断：`SERVO_X_MAX_US=2800U`、`COURSE_LEFT_TARGET_US=2750U` 已超出当前舵机/云台机械安装的安全有效范围；MSPM0 只输出脉宽，无法知道真实角度，超出舵机内部标定区后可能继续过转。已将当前活动 `empty.c` 回退为安全范围：`SERVO_X_MIN_US=350U`、`SERVO_X_MAX_US=2650U`、`COURSE_LEFT_TARGET_US=2550U`，保留“左偏持续到 CD 中点”的逻辑。编译通过，未自动烧录。时间戳源码：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260707_161405_scaled_5over7_left_until_cd_mid_servo_safe_v12.c`。
- 2026-07-07 16:18 补充召回：用户反馈 V12 AB 段仍没有左偏，并质疑代码实际行为与描述不一致。需重点检查预偏转是否只是更新 `g_course_preaim_x_us` 变量，而没有在无目标/未锁定目标时真正驱动 `g_servo_x_us` 向左移动。当前假设：此前逻辑主要在 `aim_search_for_lost_target()` 中才执行预偏转，若 OpenMV 未连接/未产生新帧/尚未进入丢目标搜索，AB 段可能保持 HOME，导致“宏对了但舵机不动”。
- 2026-07-07 16:22 V13 主动预偏转修正：用户质疑 V12 AB 段没有左偏，并指出此前版本已经实现过预偏转。检查后确认 V12 只在主循环计算 `g_course_preaim_x_us`，没有主动改变 `g_servo_x_us`；实际步进逻辑仍只在 `aim_search_for_lost_target()` 内。已抽出旧逻辑为 `course_preaim_step_to_target()`，新增 `course_preaim_update_when_unlocked()`，主循环每帧调用：OpenMV 未稳定锁定时主动向预瞄角移动，稳定锁定后由视觉闭环接管。保留 V12 的安全舵机范围 `350..2650us` 和左偏到 CD 中点 `COURSE_LEFT_HOLD_SAMPLE=559U`。编译通过，未自动烧录。时间戳源码：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260707_162256_scaled_5over7_left_until_cd_mid_active_preaim_v13.c`；说明文件：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260707_162256_scaled_5over7_left_until_cd_mid_active_preaim_v13_readme.md`。
- 2026-07-07 16:45 V14 回到 V9 触发方式：用户明确确认要用“V9 的预偏转触发方式，只把回正临界点从 330 改为 559”的思路做 V14。已先备份当前 `empty.c` 为 `C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260707_164523_before_v14_current_empty_backup.c`，再从 V9 `C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260707_153950_course_point_stop_buzzer_scaled_5over7_v9.c` 恢复基底。V14 只改：`COURSE_BC_MID_SAMPLE=330U` 改名并改值为 `COURSE_LEFT_HOLD_SAMPLE=559U`，并替换 `course_preaim_phase_from_sample()`、`course_preaim_target_from_lap_sample()` 中的回正起点引用。保留 V9 的 `COURSE_PREAIM_STEP_US=35`、`COURSE_LEFT_TARGET_US=2550U`、安全舵机范围，以及 `aim_search_for_lost_target()` 内无目标时才步进预偏转的触发方式；没有引入 V13 的 `course_preaim_update_when_unlocked()`。当前活动源码：`C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`；时间戳源码：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260707_164523_v9_trigger_left_until_cd_mid_v14.c`；说明文件：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260707_164523_v9_trigger_left_until_cd_mid_v14_readme.md`。CCS `gmake all` 编译通过，未自动烧录。
- 2026-07-07 16:58 V15 距离缩短与右回加速：用户现场反馈 A 到 B 段舵机保持了良好左偏能力，BC 段继续左偏已经受到限制；要求 CD 中点以及 DA 部分向右回转速度变为 2 倍，并且整个程序 ABCD 点距离减小为当前的 `4/5`。已备份 V14 为 `C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260707_165804_before_v15_current_v14_backup.c`。V15 修改当前 `C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`：`COURSE_LAP_SAMPLES 914->731`，`COURSE_LEFT_HOLD_SAMPLE 559->447`，`COURSE_POINT_B/C/D_SAMPLE 203/457/660 -> 162/366/528`，旧任务提示点 `MISSION_B/C/D/A` 同步缩放为 `144/288/432/576`。新增 `COURSE_PREAIM_RETURN_STEP_US=70`，左偏仍用 `COURSE_PREAIM_STEP_US=35`，只有向右回正分支使用 70，避免破坏 AB 段手感。时间戳源码：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260707_165804_v15_shorter_distance_fast_return.c`；说明文件：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260707_165804_v15_shorter_distance_fast_return_readme.md`。CCS `gmake all` 编译通过，未自动烧录。
- 2026-07-07 17:11 V16 右回继续加速且不再硬推左偏：用户反馈舵机左转始终只有约 180 度且表现为卡住，未达到 270 度；右回速度仍不够快。判断：左转卡住属于舵机本体/机械结构有效行程限制，不能继续通过增大 `COURSE_LEFT_TARGET_US` 或 `SERVO_X_MAX_US` 硬顶，否则可能堵转、发热、电源压降并影响 OpenMV/MSPM0。V16 保持 `COURSE_LEFT_TARGET_US=2550U`、`SERVO_X_MIN_US=350U`、`SERVO_X_MAX_US=2650U` 不变，只将 `COURSE_PREAIM_RETURN_STEP_US 70->140`，左偏速度 `COURSE_PREAIM_STEP_US=35` 不变。时间戳源码：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260707_171101_v16_faster_right_return_no_left_limit_push.c`；说明文件：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260707_171101_v16_faster_right_return_no_left_limit_push_readme.md`。CCS `gmake all` 编译通过，未自动烧录。
- 2026-07-07 17:15 V17 扩大水平舵机左偏软件范围：用户纠正 V16 判断，确认舵机本体支持 270 度，左转不到 270 度是代码给的最大偏转范围限制。已基于 V16 备份并修改当前 `C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`：`SERVO_X_MAX_US 2650->3050`，`COURSE_LEFT_TARGET_US 2550->3000`；保持 `SERVO_X_HOME_US=1600U`、`SERVO_X_MIN_US=350U`、`COURSE_PREAIM_STEP_US=35`、`COURSE_PREAIM_RETURN_STEP_US=140` 不变。下限不改，避免接近此前 `0us` 造成舵机乱转。时间戳源码：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260707_171538_v17_expand_left_servo_range.c`；说明文件：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260707_171538_v17_expand_left_servo_range_readme.md`。CCS `gmake all` 编译通过，未自动烧录。
- 2026-07-07 17:24 回退 V17 并重新审视舵机策略：用户提出新的判断：舵机可能程序控制只能稳定达到约 180 度，但机械云台手动可无限旋转；询问能否在接近 180 度时“重置”再旋转 90 度，实现类似二次控制。已将当前活动 `empty.c` 回退到 V16 安全边界，时间戳源码为 `C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260707_172422_restore_v16_for_servo_strategy_review.c`，说明文件为 `C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260707_172422_restore_v16_for_servo_strategy_review_readme.md`。结论：普通三线位置舵机的 PWM 是绝对位置命令，不是相对位移命令；没有编码器/角度反馈/机械复位机构时，不能可靠实现“软件重置后继续相对转 90 度”。若是连续旋转舵机，则 PWM 控速度而非角度，可以定时转动但误差不可控，不适合精确打靶。下一步应优先写水平舵机类型识别/扫角测试程序，判断到底是位置舵机、连续旋转舵机，还是机械结构打滑/手动可转但输出轴不可绝对定位。
- 2026-07-07 17:34 V18 分段右侧预瞄曲线：用户提出替代策略，不再追求舵机连续左转 270，而是按赛段让云台在 B 后快速转向右侧视野。已基于回退后的 V16 安全边界修改当前 `C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`。行为：`0~B(162)` 保持左侧 `2550us`；B 后立刻切到右接近 180 的 `700us`，并在 `B~CD中点(447)` 线性恢复到右 90 的 `1100us`；`CD中点~D(528)` 再线性向右到 `700us`；`D~A(731)` 线性回 HOME `1600us`。新增 `COURSE_RIGHT_NEAR_TARGET_US=700U`、`COURSE_RIGHT_MID_TARGET_US=1100U`、`COURSE_PREAIM_FAST_RIGHT`、`COURSE_PREAIM_RIGHT_DEEPEN`；保留 `SERVO_X_MIN/MAX=350/2650` 和 `COURSE_LEFT_TARGET_US=2550`。关键修正：原 `clamp_course_preaim_x()` 只允许 HOME 到 LEFT，V18 改为允许 RIGHT_NEAR 到 LEFT，否则右侧预瞄目标会被夹回 HOME。时间戳源码：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260707_173433_v18_segmented_right_side_preaim.c`；说明文件：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260707_173433_v18_segmented_right_side_preaim_readme.md`。CCS `gmake all` 编译通过，未自动烧录。
- 2026-07-07 18:41 PB11 蜂鸣器隔离测试：用户反馈蜂鸣器一直不响。检查当前 V18 代码后确认软件中 PB11/`IOMUX_PINCM28` 已初始化为输出，且上电会触发 1 声，因此需要隔离硬件接线、蜂鸣器类型和 PB11 输出。已备份 V18 当前活动文件为 `C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260707_184130_before_buzzer_test_current_v18_backup.c`，并临时将当前 `C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c` 替换为蜂鸣器测试程序。时间戳源码：`C:\Users\1\Desktop\电赛\ccs_project\buzzer_test\20260707_184130_pb11_buzzer_isolation_test.c`；说明文件：`C:\Users\1\Desktop\电赛\ccs_project\buzzer_test\20260707_184130_pb11_buzzer_isolation_test_readme.md`。测试行为：PB11 高电平约 1 秒、PB11 约 2kHz 方波约 1 秒、PB11 低电平约 1 秒循环；LED 同步提示。电机、舵机、OpenMV 均关闭。CCS `gmake all` 编译通过，未自动烧录。
- 2026-07-07 19:05 V19 恢复动态追踪并保留蜂鸣器：用户确认蜂鸣器已响，真实原因是蜂鸣器正负极接反。已备份当前蜂鸣器测试版为 `C:\Users\1\Desktop\电赛\ccs_project\buzzer_test\20260707_190540_before_restore_current_buzzer_test_backup.c`，并将当前 `C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c` 恢复为 V18 动态追踪/分段右侧预瞄集成版。时间戳源码：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260707_190540_v19_restore_dynamic_tracking_with_buzzer.c`；说明文件：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260707_190540_v19_restore_dynamic_tracking_with_buzzer_readme.md`。蜂鸣器逻辑保留：上电 1 声、B 点 2 声、OpenMV 瞄准稳定 3 声、C 点 4 声、D 点 5 声、A 点 1 声、点位停车未锁定目标长响。注意：复制 V18 到 `empty.c` 后 `gmake` 初次提示 up to date，原因是复制保留旧时间戳；随后已执行 `gmake clean` + `gmake all`，确认重新编译通过，未自动烧录。
- 2026-07-07 19:12 场地图像显示中间直线无黑线：用户提供比赛场地图，黑线只有上下两段弧，中间直线连接段没有线。最初准备做 V20“丢线后开环直行桥接”，但用户立即指出关键问题：如何确保转弯结束后走的是直线并正好对上下一个弯道。经检查，当前 V19 没有编码器闭环，单靠固定 sample/时间直行无法保证直线和对准。因此已回退当前 `empty.c` 到 V19，并 clean build 通过；不要使用未完成的 `20260707_191256_v20_line_gap_bridge_no_center_line.c` 作为可烧录版本。正确方向应为“编码器直线闭环桥接 + 红外重捕线”：进入断线段时记录编码器，桥接期间用左右编码器差做直线纠偏，看到黑线稳定出现后退出桥接恢复普通循迹。
- 2026-07-07 19:22 编码器桥接方案继续被审视：用户进一步指出，即使编码器能保证出弯后走直线，也不能保证走的是正确那条直线。该质疑正确。编码器差速闭环只能保持进入桥接瞬间的车身航向；如果出弯姿态/位置本身错误，它会稳定地沿错误方向直行。因此新场地桥接状态机必须同时满足三类条件：1）只在已知赛道相位/预期断线窗口进入桥接；2）进入桥接前必须确认红外处于弧线末端边缘图案，尽量保证车身切线方向正确；3）桥接期间用编码器差速保持直线，退出时用红外稳定重捕线验证。已再次回退当前 `empty.c` 到 V19 并 clean build 通过；未完成的编码器桥接代码不得烧录。
- 2026-07-07 19:26 V21 最终版编码器门控桥接：用户要求不要继续回退，直接写最终版。已基于 V19 生成 `C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260707_192609_v21_final_encoder_gated_gap_bridge.c` 并替换当前 `C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`。V21 增加 PB12/PB13/PB14/PB15 编码器中断读取，新增无线路段桥接状态机：只有在 B/D 预期断线窗口附近、且最后线误差接近边缘时进入桥接；桥接期间用左右编码器差 `g_line_gap_left_forward - g_line_gap_right_forward` 纠正 PWM，`g_turn_mode=4`；红外重新稳定看到黑线 3 帧后退出桥接；超过 220 帧未重捕线则交给原丢线停车保护。保留 V19 动态追踪、V18 分段右侧预瞄和 PB11 蜂鸣器。说明文件：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260707_192609_v21_final_encoder_gated_gap_bridge_readme.md`。已 clean build 编译通过，未自动烧录。
- 2026-07-07 20:00 电机不动红外传感器静态测试：比赛场地 D->A 有线段测试时循迹失效并原地右转，用户要求生成电机不动的传感器测试程序。已备份当前集成版为 `C:\Users\1\Desktop\电赛\ccs_project\line_sensor_test\20260707_200036_before_sensor_static_test_current_backup.c`，并将当前 `C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c` 临时替换为静态红外测试程序。时间戳源码：`C:\Users\1\Desktop\电赛\ccs_project\line_sensor_test\20260707_200036_static_line_sensor_motor_off_test.c`；说明文件：`C:\Users\1\Desktop\电赛\ccs_project\line_sensor_test\20260707_200036_static_line_sensor_motor_off_test_readme.md`。该程序强制 PB0/PB1/PB2/PB3/PB4 全 LOW，PWM 不初始化，TB6612 STBY LOW，电机不会动；循环读取 S1~S7，并暴露 `g_raw_s1..g_raw_s7`、`g_s1..g_s7`、`g_line_raw_mask`、`g_line_mask`、`g_line_valid`、`g_line_error`。已 clean build 编译通过，未自动烧录。
- 2026-07-07 20:06 尝试命令行烧录传感器静态测试程序：用户明确要求尝试烧录。使用 DSLite 命令 `load --config MSPM0G3507.ccxml ele-car.out`，失败报错 `Error -260: An attempt to connect to the XDS110 failed`。随后用 `xdsdfu -e` 检查 XDS110 枚举，结果 `Found 0 devices`。结论：当前电脑没有识别到 XDS110 调试器，问题在 USB/调试器连接/驱动/板子上电，不是当前 `ele-car.out` 文件或代码编译问题。
- 2026-07-07 21:24 V22 按题目任务点调整：用户要求从 V19 开始，先假定赛道全程有黑线，不考虑中途断线；按题目要求只有 B 点停车，C/D/A 只是声光提示不停车。已从 `C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260707_190540_v19_restore_dynamic_tracking_with_buzzer.c` 恢复当前工程并修改。时间戳源码：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260707_212451_v22_b_stop_cda_prompt_full_line.c`；说明文件：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260707_212451_v22_b_stop_cda_prompt_full_line_readme.md`。主要改动：`MISSION_ENABLE_POINT_PROMPTS=1U`；C/D/A 提示 sample 改为 `COURSE_POINT_C/D/A_SAMPLE`；`mission_try_enter_point_hold()` 只允许 `g_point_stop_next_index==0` 的 B 点进入停车；B 停完后设置 `g_mission_b_done=1U` 和 `g_point_stop_next_index=1U`，后续 C/D/A 只提示不停车；A 点原来的最终停车逻辑移除。已 clean build 编译通过，未自动烧录。
- 2026-07-07 设计报告 AI 使用说明：用户提供校赛题目 PDF，要求撰写设计报告中的“项目 AI 使用说明”。已复制题目 PDF 到 `C:\Users\1\Desktop\电赛\资料提取\contest_track_problem.pdf` 和 `C:\Users\1\Desktop\contest_track_problem.pdf`，使用 `pdfplumber` 抽取题目文本，确认题目第 3 页要求说明 AI 工具、使用方式、帮助和局限性。已基于题目要求和本项目真实调试过程生成 Markdown 初稿：`C:\Users\1\Desktop\电赛\设计报告_AI使用说明.md`。内容覆盖 AI 工具使用概况、具体参与环节、人工验证方式、帮助、局限性和风险控制。

## 2026-07-07 21:45 恢复 OpenMV + 双舵机无电机测试程序

- 用户要求找回“只测试 OpenMV 与舵机、不包括电机”的历史程序。
- 选用历史最终手调版：`C:\Users\1\Desktop\电赛\ccs_project\openmv_test\20260705_171205_mspm0_openmv_static_aim_final_user_tuned.c`。
- 已备份恢复前活动文件：`C:\Users\1\Desktop\电赛\ccs_project\openmv_test\20260707_214537_before_restore_openmv_servo_no_motor_empty.c`。
- 已生成本次恢复时间戳副本：`C:\Users\1\Desktop\电赛\ccs_project\openmv_test\20260707_214537_restore_openmv_servo_no_motor_test.c`。
- 已替换当前 CCS 活动文件：`C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`。
- 程序行为：PA14/PA17 双舵机接收 OpenMV 串口目标坐标并静态追踪；电机禁用，PB4/STBY 保持 LOW。
- 已执行 `gmake clean` + `gmake all`，编译通过；按用户规则未自动烧录。

## 2026-07-07 22:09 设计报告 AI 使用说明纪实简化版

- 用户要求将“项目 AI 使用说明”进一步简化，并突出真实调试纪实：遇到的问题、AI 解决的问题、队员点拨后解决的问题、硬件问题与算法补偿、项目独立性、人机交互方式、版本数量和当前功能状态。
- 已备份旧稿：`C:\Users\1\Desktop\电赛\设计报告_AI使用说明_20260707_220812_旧稿备份.md`。
- 已重写主文件：`C:\Users\1\Desktop\电赛\设计报告_AI使用说明.md`。
- 已保存新稿时间戳副本：`C:\Users\1\Desktop\电赛\设计报告_AI使用说明_20260707_220917_纪实简化版.md`。
- 本地统计口径：`ccs_project` 下约 194 个 C 文件，其中 152 个时间戳 C 版本；13 个 OpenMV/Python 脚本；72 个 readme 说明文件。新版报告使用这些数据说明多轮打磨过程。
- 新版结构：AI 参与方式、典型问题与解决过程、硬件不足与算法补偿、项目独立性、当前已实现与未完成内容、总结。

## 2026-07-07 22:23 设计报告 AI 使用说明按反馈强化

- 用户在截图中指出三处需要加强：第三节需加粗写明队员提出的“180 度舵机无法连续转 270 度时的分段换边预瞄”思路；第六节需更有现场感，写出迭代版本、代码行数和调试投入；第五节需结合题目详细要求，把未完成项细化为可行方案，并加入 XDS110 调试器损坏导致未能及时在学校专用场地模拟的局限。
- 已核对题目 PDF：基本要求包括 30 s 自动巡迹、5 s 定点瞄准、B 点停车作业后 C/D/A 声光提示且一圈 40 s 内；发挥部分包括动态瞄准、同步绘制、四圈连续运行和工程完善度；说明中要求小车不得借助外部设备导航，并鼓励详细说明 AI 工具使用方式、帮助和局限。
- 已更新主文件：`C:\Users\1\Desktop\电赛\设计报告_AI使用说明.md`。
- 已保存时间戳副本：`C:\Users\1\Desktop\电赛\设计报告_AI使用说明_20260707_222354_按反馈强化版.md`。
- 已确认关键词存在：舵机分段换边预瞄、XDS110、152 个时间戳版本、四圈连续运行、同步绘制。

## 2026-07-07 22:27 生成设计报告 PDF

- 用户要求生成好看的 PDF 版本，命名为“北京邮电大学校赛-蒋立果-设计报告”。
- 输入 Markdown：`C:\Users\1\Desktop\电赛\设计报告_AI使用说明.md`。
- 计划输出 PDF：`C:\Users\1\Desktop\电赛\北京邮电大学校赛-蒋立果-设计报告.pdf`。
- 排版要求：中文字体正常、标题层级清晰、表格可读、重点加粗保留、页面渲染检查通过后交付。
- 已生成 PDF：`C:\Users\1\Desktop\电赛\北京邮电大学校赛-蒋立果-设计报告.pdf`。
- PDF 共 5 页，约 386 KB。
- 已用 PyMuPDF 渲染检查封面、第 1/2/3/4 页正文，输出位于 `C:\Users\1\Desktop\电赛\tmp_pdf_render\`；检查结果为中文正常、表格未越界、页眉页脚正常、加粗重点可见。

## 2026-07-09 OpenMV 黑框中心检测验证通过

- 背景：原先尝试用 OpenMV 识别红色圆环，但现场画面中红色圆环颜色很淡、线条很细，颜色阈值容易误检其它红色/反光区域，无法稳定完成目标定位。
- 新策略：放弃红色识别，改为利用比赛现场稳定存在的黑色外框。OpenMV 检测黑框后，取黑框四个角点平均值作为目标中心 `cx/cy`。
- 测试脚本：`ccs_project/openmv_test/20260709_black_frame_center_test.py`。
- 烧录状态：已写入 OpenMV `E:\main.py`，写入前备份为 `ccs_project/openmv_test/openmv_main_backup_20260709_before_black_frame.py`。
- 算法流程：
  - 摄像头保持 QVGA/RGB565，并按当前机械安装设置 `CAMERA_HMIRROR=True`、`CAMERA_VFLIP=True`。
  - 优先调用 `find_rects()` 检测矩形黑框；若矩形检测失败，则退回黑色 blob 检测。
  - 对候选目标进行贴边、尺寸、宽高比、黑色密度过滤。
  - 成功候选输出 `BOXDBG`，并在画面中标注中心点。
- 现场成功反馈样例：
  - `BOXDBG rects=0 blobs=4 ok=1 edge=2 size=1 ratio=0 dens=0 best=blob cx=131 cy=131 box=(69,85,125,92) p=2390 d=20% corners=(69,85)(194,85)(194,177)(69,177) fps=57.3`
  - `BOXDBG rects=0 blobs=4 ok=1 edge=2 size=1 ratio=0 dens=0 best=blob cx=141 cy=132 box=(79,86,124,92) p=2365 d=20% corners=(79,86)(203,86)(203,178)(79,178) fps=57.3`
  - 偶发失败样例：`BOXDBG rects=0 blobs=3 ok=0 edge=2 size=1 ratio=0 dens=0 best=none fps=57.4`
- 结论：
  - `find_rects()` 当前未稳定识别到矩形，`rects=0`；但备用黑色 blob 路线能够成功得到黑框中心。
  - 成功时 `best=blob`，中心点约在 `cx=131~141`、`cy=131~132`，帧率约 `57fps`，满足后续接入 MSPM0 云台控制的实时性。
  - 后续集成时可先使用 blob 中心作为 OpenMV 回传坐标；若需要进一步稳定，可继续优化黑色阈值、候选尺寸范围、或增加上一帧中心的时间滤波。

## 2026-07-09 OpenMV 黑框中心接入 UART 上报与自恢复主控

- 背景：用户要求回到“识别目标 + UART 上报 + watchdog/异常自恢复”的 OpenMV 主控结构，但用已验证可行的黑框检测替代原红色圆环/红色目标识别。
- 基线：`ccs_project/openmv_test/20260707_144221_openmv_uart_stream_qvga_far_stable_v2.py`，该版已有 UART3 主动流式上报、17 字节帧协议、LED 状态、watchdog、UART 输入限量 drain、异常后重初始化 sensor。
- 新脚本：`ccs_project/openmv_test/20260709_black_frame_uart_stream_watchdog.py`。
- 队员查阅 README：`ccs_project/openmv_test/20260709_black_frame_uart_stream_watchdog_readme.md`。
- 烧录状态：已写入 OpenMV `E:\main.py`，写入前备份为 `ccs_project/openmv_test/openmv_main_backup_20260709_before_black_frame_uart_stream.py`。
- 保留内容：
  - UART 帧协议不变：`0xAA 0x55 ... checksum 0x5B`。
  - `TYPE_COLOR=3` 和 `FLAG_TARGET_DETECTED` 语义不变，MSPM0 端可继续按原 OpenMV 目标帧解析。
  - `cx/cy` 字段继续表示视觉目标中心，未检测到目标时仍为 `-1/-1`。
  - watchdog、自恢复、蓝灯心跳、绿灯目标锁定提示均保留。
- 替换内容：
  - 原 `find_red_target()` 替换为 `find_black_frame_target()`。
  - 优先 `find_rects()` 检测黑色矩形框；若失败，则用黑色 blob 备用路线。
  - 成功候选取四角点平均中心作为 `cx/cy`；`pixels` 字段对 blob 为黑色像素数，对 rect 为候选框面积。
  - 调试打印改为 `OMV black-frame ... target=... cx=... cy=...`，当前 `ENABLE_DEBUG_PRINT=True`，便于现场接入 MSPM0 前观察。
- 校验：本地 Python 语法检查通过；烧录后源文件与 `E:\main.py` SHA256 一致。

## 2026-07-09 OpenMV 黑框检测泛化增强

- 现场反馈：黑框中心识别准确，基本没有误识别，但存在较频繁的单帧/短时漏检，需要提高泛化能力。
- 修改文件：`ccs_project/openmv_test/20260709_black_frame_uart_stream_watchdog.py`。
- 烧录状态：已写入 OpenMV `E:\main.py`，写入前备份为 `ccs_project/openmv_test/openmv_main_backup_20260709_before_black_frame_generalized.py`。
- 调整内容：
  - 黑色阈值新增更宽松范围 `(0, 70, -45, 45, -45, 45)`，用于覆盖光照变化下的浅黑/灰黑边框。
  - `RECT_THRESHOLD` 从 `9000` 降为 `6000`，提高 `find_rects()` 的召回率。
  - 黑色 blob 最小像素/面积从 `8/8` 降为 `4/4`，合并距离从 `20` 增至 `30`。
  - 框尺寸和宽高比范围放宽：最小框 `10x10`，最大框 `220x190`，宽高比允许 `0.35~2.85`。
  - 黑色密度范围放宽到 `1%~90%`。
  - 新增 `GROUP` 兜底：当普通 rect/blob 均失败时，尝试把多个非贴边黑块组合成一个整体候选框。
  - 新增 `TARGET_HOLD_FRAMES=8`：当前帧漏检时短时间沿用最近一次目标，降低 UART 目标闪断。
  - 调试打印增加 `hold=` 字段，用于区分实时检测和短时保持。
- 校验：本地 Python 语法检查通过；烧录后源文件与 `E:\main.py` SHA256 一致。
## 2026-07-08 20:23 V23 A 点无线起步桥接实现启动

- 用户要求实现“A 点从一开始就没有直线/红外完全看不到线”的完整任务方案。
- 实现基底按计划选用 V22：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260707_212451_v22_b_stop_cda_prompt_full_line.c`。
- 已备份当前活动 `empty.c`：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260708_202324_before_v23_current_empty_backup.c`。
- 新源码副本：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260708_202324_v23_a_start_gap_bridge_test_first.c`。
- 本轮默认 `START_GAP_TEST_ONLY=1U`，优先验证 A 点启动桥接和重捕线；不自动烧录。
- 已完成实现并替换当前 CCS 活动文件：`C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`。
- 当前活动文件与时间戳源码 SHA256 一致。
- 已生成说明文件：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260708_202324_v23_a_start_gap_bridge_test_first_readme.md`。
- 新增 PB12/PB13/PB14/PB15 编码器输入、中断计数、`PHASE_START_GAP_BRIDGE=8U` 启动桥接状态、`g_start_gap_*` Watch 变量和桥接超时停车 `g_stop_reason=6U`。
- 默认行为：上电后如果红外完全看不到线，则直接进入编码器差速直行桥接；连续看到黑线 4 帧后进入普通循迹。由于 `START_GAP_TEST_ONLY=1U`，本版暂不触发 B/C/D/A 任务点。
- 已执行 CCS `gmake clean` + `gmake all`，编译通过；未自动烧录。
## 2026-07-08 20:37 V24 A 点无线桥接首测修正

- 现场反馈：V23 烧录后，小车从 A 点无线起步时一直绕同一个顺时针圆圈，根本没有直行；手动让红外连续约 4 秒识别到黑线后，也没有稳定进入循迹，仍会偏出黑线。
- 诊断结论：V23 的桥接阶段在编码器方向/相序尚未复测前默认使用编码器差速闭环，风险过高；一旦左右任一编码器计数方向与假设相反，`left_forward - right_forward` 修正会把直行闭环变成持续转向。另一个问题是重捕线后直接跳入普通循迹，没有独立稳定期，车身姿态偏斜时容易刚见线又脱线。
- 已在当前活动文件 `C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c` 上修改，并 clean build 通过，未自动烧录。
- 新时间戳源码：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260708_203751_v24_a_start_gap_direct_bridge_reacquire.c`。
- 新说明文件：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260708_203751_v24_a_start_gap_direct_bridge_reacquire_readme.md`。
- 关键改动：新增 `START_GAP_USE_ENCODER_BALANCE=0U`，默认桥接阶段只按左右基准 PWM 直行，编码器仍计数但不参与控制；新增 `START_GAP_LEFT_TRIM_TICKS/START_GAP_RIGHT_TRIM_TICKS` 供现场修正桥接固定偏航；新增 `PHASE_START_GAP_ALIGN=9U` 和 `START_GAP_ALIGN_FRAMES=12U`，重捕黑线后先稳定约 12 帧再进入正式循迹。
- 现场提示：进入桥接蜂鸣 2 声，成功重捕黑线蜂鸣 3 声。首测仍保持 `START_GAP_TEST_ONLY=1U`，只验证 A 点无线桥接，不触发 B/C/D/A 任务点。
## 2026-07-08 20:54 V25 固定赛道混合控制：A 点无线桥接 + 捕线锁中心

- 用户反馈：V24 已不再顺时针画圆，但长期执行后仍有轻微向右偏趋势；同时小车没能实现重捕黑线后的普通循迹。用户进一步提出应考虑固定赛道定制思路。
- 判断：当前比赛场地和任务固定，不应继续只追求通用循迹；但也不应完全纯脚本开环，因为电池电压、地面摩擦、负重和人工摆车角度会造成累计误差。正确方向是“固定赛道状态机 + 红外/编码器反馈校正”的混合控制。
- 已备份 V25 修改前活动文件：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260708_205429_before_v25_current_v24_backup.c`。
- 已修改当前活动文件：`C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`。
- 新时间戳源码：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260708_205429_v25_fixed_course_start_gap_capture.c`。
- 新说明文件：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260708_205429_v25_fixed_course_start_gap_capture_readme.md`。
- 关键改动：A 点桥接加入轻微左修正 `START_GAP_LEFT_TRIM_TICKS=-8`、`START_GAP_RIGHT_TRIM_TICKS=8`；新增 `PHASE_START_GAP_CAPTURE=9U` 捕线阶段；从“看到任意黑线 2 帧后进入捕线”，到“中心锁定连续 5 帧后才进入普通循迹”。
- 捕线条件：`g_line_valid=1`、`abs(g_line_error)<=1200`、且 `S3/S4/S5` 至少一个压线。捕线阶段低速 PWM `370/370`，修正参数 `START_GAP_CAPTURE_KP_DIVISOR=10`、`START_GAP_CAPTURE_CORR_LIMIT=220`。
- 反馈方式：上电 1 声；进入 A 点无线桥接 2 声；中心锁定进入普通循迹 3 声；桥接超时长响。
- 已执行 CCS `gmake clean` + `gmake all`，编译通过；未自动烧录。
## 2026-07-08 21:07 V26 A 点桥接任意传感器见线即循迹

- 用户反馈：V25 桥接后车大致直行，但进入弯道时没有切换到循迹，仍然直接直走。用户指出程序应简化为“只要有一个传感器识别到黑线就立刻进入循迹模式”。
- 判断：V25 的中心锁定条件在该场景过于保守。它要求 `g_line_valid=1`、`abs(g_line_error)<=1200`、`S3/S4/S5` 至少一个压线并连续 5 帧，导致车辆在弯道入口虽已碰到黑线，但未达到中心锁定，继续桥接直走并冲过弯道。
- 已备份 V26 修改前活动文件：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260708_210707_before_v26_current_v25_backup.c`。
- 已修改当前活动文件：`C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`。
- 新时间戳源码：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260708_210707_v26_start_gap_any_sensor_reacquire.c`。
- 新说明文件：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260708_210707_v26_start_gap_any_sensor_reacquire_readme.md`。
- 关键改动：`START_GAP_REACQUIRE_FRAMES=1U`；`start_gap_bridge_update()` 中只要 `g_line_valid != 0U`，立即清桥接状态、调用 `line_follow_update()`、设置 `g_phase=PHASE_LINE_RUN` 并蜂鸣 3 声。
- V25 的 `PHASE_START_GAP_CAPTURE` 捕线函数暂时保留在代码中，但桥接入口不再进入该状态；当前有效路径为 `PHASE_START_GAP_BRIDGE -> PHASE_LINE_RUN`。
- 已执行 CCS `gmake clean` + `gmake all`，编译通过；未自动烧录。
## 2026-07-08 21:57 V27 状态切换强制停 2 秒调试版

- 用户确认红外电平：白线/白底为 0V，黑线为 3.3V，因此 `LINE_BLACK_IS_HIGH=1` 是正确的；此前“极性可能反了”的判断被排除。
- 用户要求：先分析原因，再写一个更明显的程序，使状态切换时小车强制原地停 2 秒再继续。
- 分析：如果无论是否在黑线上启动都只有上电 1 声，可能是上电初始采样误判、桥接/重捕蜂鸣太快听不清、或未烧录最新版本。需要把状态切换做成肉眼可见的停车事件。
- 已备份 V27 修改前活动文件：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260708_215716_before_v27_current_v26_backup.c`。
- 已修改当前活动文件：`C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`。
- 新时间戳源码：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260708_215716_v27_state_switch_hold_2s_debug.c`。
- 新说明文件：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260708_215716_v27_state_switch_hold_2s_debug_readme.md`。
- 新增 `state_switch_hold_then_resume()`：保存当前 PWM，`motors_stop()`，维持舵机 PWM 与蜂鸣更新 100 帧约 2 秒，然后恢复 PWM 并 `motors_forward()`。
- 新增状态提示：进入桥接停 2 秒并响 2 声；上电直接认为有线则停 2 秒并响 4 声；桥接重捕线进入循迹则停 2 秒并响 3 声。
- 新增 Watch：`g_state_switch_reason`、`g_state_switch_hold_event_count`、`g_state_switch_hold_frames_total`。
- 已执行 CCS `gmake clean` + `gmake all`，编译通过；未自动烧录。
## 2026-07-08 22:02 V28 状态切换调试版完善

- 用户简短要求“完善”。按当前上下文理解为：在 V27 基础上进一步整理状态切换调试版，使现象更清楚、代码路径更干净。
- 已备份 V28 修改前活动文件：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260708_220234_before_v28_current_v27_backup.c`。
- 已修改当前活动文件：`C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`。
- 新时间戳源码：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260708_220234_v28_state_switch_debug_cleanup.c`。
- 新说明文件：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260708_220234_v28_state_switch_debug_cleanup_readme.md`。
- 关键清理：删除 V25 遗留的 `PHASE_START_GAP_CAPTURE` 捕线状态、捕线参数、捕线变量和 `start_gap_capture_*` 函数，当前有效路径只保留 `PHASE_START_GAP_BRIDGE -> PHASE_LINE_RUN`。
- 调试增强：状态切换强制停车 2 秒期间 PA15 LED 闪烁；新增 `g_state_switch_hold_frames_left`。
- 启动诊断增强：新增 `g_startup_line_valid_snapshot`、`g_startup_line_raw_mask_snapshot`、`g_startup_line_active_count_snapshot`、`g_startup_line_error_snapshot`，用于记录上电瞬间红外读数。
- 已执行 CCS `gmake clean` + `gmake all`，编译通过；未自动烧录。
## 2026-07-08 22:15 V29 参考代码增强版：红外多采样 + 黑线锁存 + 编码器里程

- 用户要求：全方位学习/复制增强参考代码思路，同时保持当前硬件接口不变。
- 已阅读 25E 参考代码：`FunctionalModule\GraySensor.c`、`FunctionalModule\Encoder.c`、`FunctionalModule\MotorsCtrl.c`、`ApplicationLayer\Task.c`、`user\main.c`、`user\headfile.h`。
- 学习结论：参考代码核心不是纯循迹，而是 200Hz 定时任务中执行灰度多次采样、编码器距离积分、任务状态机和电机速度 PID；直角弯通过灰度触发 + 编码器距离 + 强制转向完成。
- 本次保持接口不变：TB6612、7 路红外、编码器、OpenMV、舵机、蜂鸣器所有引脚均未改动，当前活动文件仍为 `C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`。
- 已备份 V29 修改前活动文件：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260708_221546_before_v29_current_v28_backup.c`。另有一次手误生成的同内容备份 `20260708_222030_before_v29_current_v28_backup.c`，可忽略。
- 新时间戳源码：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260708_221546_v29_reference_enhanced_line_encoder.c`。
- 新说明文件：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260708_221546_v29_reference_enhanced_line_encoder_readme.md`。
- 红外增强：`read_line_sensors()` 改为连续采样 5 次，记录 `g_line_raw_high_last_mask/g_line_raw_high_or_mask/g_line_raw_high_and_mask`；当前黑线高电平，因此逻辑黑线采用 OR 结果，降低运动中漏采样概率。
- 黑线锁存：新增 `LINE_SEEN_LATCH_FRAMES=8U`、`g_line_seen_latched/g_line_seen_latch_frames/g_line_seen_event_count`；桥接阶段触发条件改为 `g_line_valid != 0 || g_line_seen_latched != 0`。
- 编码器里程：新增 `encoder_distance_update()` 和 `g_encoder_left_forward_total/g_encoder_right_forward_total/g_encoder_avg_forward_total/g_encoder_diff_forward_total`，沿用当前方向约定：左轮前进量取反，右轮前进量为正。
- 已执行 CCS `gmake clean` + `gmake all`，编译通过；未自动烧录。
## 2026-07-08 22:35 V30 参考代码架构底盘重写版

- 用户明确要求：不要再延续现有思路，直接以参考代码为基本，修改适应当前接口和机器，相当于重写全新代码。
- 执行策略：保留当前硬件接口不变，只重写底盘控制逻辑。OpenMV/PA14/PA17 舵机本版不控制，避免底盘问题继续被视觉/舵机逻辑干扰。
- 已备份 V30 修改前活动文件：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260708_223500_before_v30_current_v29_backup.c`。另有一次手误同内容备份 `20260708_223330_before_v30_current_v29_backup.c`，可忽略。
- 已重写当前活动文件：`C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`。
- 新时间戳源码：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260708_223500_v30_reference_style_chassis_rewrite.c`。
- 新说明文件：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260708_223500_v30_reference_style_chassis_rewrite_readme.md`。
- 新架构：`hardware_init()`、`line_sensor_update()`、`line_position_update()`、`encoder_distance_update()`、`line_follow_drive()`、`bridge_drive()`、`force_turn_drive()`，主循环按“编码器更新 -> 红外更新 -> 状态机 -> 电机输出”执行。
- 当前状态机：`PHASE_START_GAP_BRIDGE=8`、`PHASE_LINE_RUN=2`、`PHASE_FORCE_TURN=10`、`PHASE_STOP=4`。白底启动进入桥接；黑线启动直接循迹；桥接重捕线后进入循迹；丢线后按最后误差方向强制转向找线。
- 保持接口：TB6612、7 路红外、编码器、蜂鸣器、LED 引脚均保持不变；当前烧录入口仍为 `empty.c`。
- 已执行 CCS `gmake clean` + `gmake all`，首次出现一个 16 位计数与 `0xFFFFFFFF` 比较的警告，已修正为 `0xFFFF`；再次 clean build 编译通过且无警告；未自动烧录。
## 2026-07-09 00:22 V31 稳定底盘抢救验证版

- 背景：用户要求实现“第一步：先恢复一个稳定底盘版，确认红外能变、电机能跑”。此前 V30 参考代码式重写后，现场反馈所有红外 Watch 近似常量、上电后车不走，说明重写底层过于激进。
- 判断：V30 同时改动了底层读红外、多采样锁存、状态机、PWM 速度和参考代码架构，导致问题难以定位。第一步必须回到已验证底层，而不是继续堆任务逻辑。
- 已备份当前活动文件：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260709_002220_before_v31_stable_chassis_rescue_empty.c`。
- 已生成时间戳源码：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260709_002220_v31_stable_chassis_rescue.c`。
- 已替换当前 CCS 活动文件：`C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`。
- 本版范围：只包含 7 路红外、TB6612、电机、PA15 LED、PB11 有源蜂鸣器、A 点无黑线起步桥接、任意红外见黑线后立即切入普通循迹；暂不包含 OpenMV、舵机、ABCD 任务点、编码器距离层。
- 关键恢复点：
  - 红外输入恢复为 `DL_GPIO_RESISTOR_NONE`。
  - 红外读取恢复为单次读取，不使用 V30 的 OR/AND 多采样和黑线锁存。
  - STBY 初始化时先置高。
  - PWM 恢复到稳定基线：左 620、右 590、最小 430、最大 850。
  - 白底启动：上电 1 声后，进入桥接前滴 2 声并停车约 2 秒。
  - 桥接中任意传感器看到黑线：停车约 2 秒并滴 3 声，然后进入普通循迹。
  - 黑线启动：停车约 2 秒并滴 4 声，然后普通循迹。
- 验证：已执行 CCS `gmake clean` + `gmake all`，编译通过并生成 `ele-car.out`；未自动烧录。clean 阶段末尾 `Could Not Find ... empty.d` 为删除不存在旧依赖文件，不影响编译输出。
## 2026-07-09 18:45 V32 电机禁用红外软件阈值测试版

- 背景：用户反馈红外阈值不对，要求先禁用电机并修改红外阈值。
- 判断：当前 7 路红外接的是模块 `DO` 数字输出，真正的黑白阈值主要由红外模块板载比较器/电位器决定；MSPM0 代码无法改变硬件比较阈值，只能做软件多采样判定，缓解抖动或临界误判。
- 已备份 V31 活动文件：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260709_184549_before_v32_motor_off_line_threshold_empty.c`。
- 已生成 V32 时间戳源码：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260709_184549_v32_motor_off_line_threshold_test.c`。
- 已替换当前 CCS 活动文件：`C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`。
- 本版行为：电机永久禁用，PB4/STBY 初始化为低，PWM 始终为 0；主循环只读取红外并更新 Watch，PA15 LED 在任意红外判定为黑线时点亮。
- 红外软件阈值：`LINE_SAMPLE_COUNT=9U`，`LINE_BLACK_MIN_HIGH_SAMPLES=5U`，即每次更新连续采样 9 次，同一路至少 5 次为高电平才认为该路看到黑线。
- 新增 Watch：`g_line_raw_high_last_mask`、`g_line_raw_high_or_mask`、`g_line_raw_high_and_mask`、`g_s1_high_count` 到 `g_s7_high_count`。
- 调参规则：白底误判黑线则提高 `LINE_BLACK_MIN_HIGH_SAMPLES` 到 6 或 7；黑线识别不稳定则降低到 4 或 3。若某一路 `high_count` 在黑白上完全不变，则需要调模块电位器/检查该模块 DO 与接线，代码阈值无法修复。
- 验证：已执行 CCS `gmake clean` + `gmake all`，编译通过且无 C 代码警告；未自动烧录。
## 2026-07-09 18:58 V33 红外极性修正

- 现场反馈：V32 按 `LINE_BLACK_IS_HIGH=1` 测试时，白色亮、黑色灭。
- 判断：当前红外模块 DO 极性为“白底高电平、黑线低电平”，与此前程序假设相反。
- 已备份 V32：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260709_185819_before_v33_line_polarity_invert_empty.c`。
- 已生成 V33：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260709_185819_v33_motor_off_line_polarity_invert.c`。
- 已替换当前活动文件：`C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`。
- 唯一逻辑修改：`LINE_BLACK_IS_HIGH 1 -> 0`。当前软件判定为：9 次采样中高电平次数小于等于 4 次，判定为黑线。
- 电机仍禁用，PB4/STBY 低，PWM 为 0。
- 验证：CCS clean build + build 通过，无 C 代码警告；未自动烧录。
## 2026-07-09 19:29 V34 红外原始标准确认版

- 现场反馈：用户确认不应把红外显示逻辑反过来，后续应沿用硬件标准“白色亮、黑色灭”，只在后面的循迹程序里处理黑线为低电平。
- 已备份 V33：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260709_192943_before_v34_sensor_raw_standard_empty.c`。
- 已生成 V34：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260709_192943_v34_motor_off_sensor_raw_standard.c`。
- 已替换当前活动文件：`C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`。
- 当前约定：`g_s1~g_s7` 和 `g_line_raw_mask` 表示红外模块原始高电平结果，即白色为 1、黑线为 0；PA15 LED 跟随原始高电平，因此白色亮、黑色灭。
- 新增变量：`g_black_line_mask = (~g_line_raw_mask) & 0x7F`，供后续循迹算法使用，表示真正的黑线位置。
- 电机仍禁用，PB4/STBY 低，PWM 为 0。
- 验证：CCS clean build + build 通过，无 C 代码警告；未自动烧录。
## 2026-07-09 19:31 V35 启用电机并沿用白亮黑灭红外标准

- 用户要求：在 V34 红外原始标准确认后启用电机。
- 已备份当前活动文件：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260709_193152_before_v35_motor_enabled_empty.c`。
- 已生成 V35：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260709_193152_v35_motor_enabled_black_low_line_follow.c`。
- 已替换当前活动文件：`C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`。
- 关键约定：`g_s1~g_s7` 和 `g_line_raw_mask` 继续表示红外原始高电平，白色为 1、黑线为 0；循迹算法不再直接使用 `g_s1~g_s7`，而是使用 `g_black_line_mask = (~g_line_raw_mask) & 0x7F`。
- 电机重新启用：PB4/STBY 初始化为高，进入桥接/循迹时按 TB6612 正常输出。
- 红外采样：保留 9 次采样、至少 5 次高电平判定为白色；低电平侧通过 `g_black_line_mask` 作为黑线。
- 运行逻辑：白底/无线段启动时进入桥接，任意红外检测到黑线低电平后停车 2 秒并滴 3 声，然后进入普通循迹；黑线上启动则滴 4 声后进入普通循迹。
- 验证：CCS clean build + build 通过，无 C 代码警告；未自动烧录。
## 2026-07-09 20:04 V36 降低车速并修正白底启动左歪

- 现场反馈：V35 启用电机后车速太快；在白色地面启动时有固定左歪。
- 判断：白底启动阶段属于无黑线桥接开环直走，固定左歪通常意味着右轮有效速度偏快，因此应在桥接阶段相对降低右轮/提高左轮占比；同时整体降低普通循迹速度。
- 已备份 V35：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260709_200431_before_v36_slow_bridge_left_drift_empty.c`。
- 已生成 V36：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260709_200431_v36_slow_bridge_left_drift_fix.c`。
- 已替换当前活动文件：`C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`。
- 普通循迹 PWM：`PWM_LEFT_BASE_TICKS 620->540`，`PWM_RIGHT_BASE_TICKS 590->515`，`PWM_MIN_TICKS 430->380`，`PWM_MAX_TICKS 850->760`。
- 白底桥接 PWM：`START_GAP_LEFT_TICKS=560`，`START_GAP_RIGHT_TICKS=500`，用于抵消启动时固定左歪。
- 红外标准不变：`g_s1~g_s7` 白色为 1、黑线为 0；循迹继续使用 `g_black_line_mask`。
- 验证：CCS clean build + build 通过，无 C 代码警告；未自动烧录。
## 2026-07-09 20:13 V37 进一步降速并减小右偏

- 现场反馈：V36 相比之前不再明显左偏，但变成略微偏右；同时速度还需要再慢一点。
- 判断：V36 桥接左右差 `560/500` 对左偏修正略过头，导致右偏；同时整体 PWM 仍偏快。
- 已备份 V36：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260709_201351_before_v37_slower_reduce_right_bias_empty.c`。
- 已生成 V37：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260709_201351_v37_slower_reduce_right_bias.c`。
- 已替换当前活动文件：`C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`。
- 普通循迹 PWM：`540/515 -> 500/485`，`PWM_MIN_TICKS 380->350`，`PWM_MAX_TICKS 760->700`。
- 白底桥接 PWM：`560/500 -> 520/485`，降低速度并减小左右差，缓解右偏。
- 红外标准不变：`g_s1~g_s7` 白色为 1、黑线为 0；循迹继续使用 `g_black_line_mask`。
- 验证：CCS clean build + build 通过，无 C 代码警告；未自动烧录。
## 2026-07-09 20:23 V38 桥接重捕线后不停车，改善 AB 到 BC 衔接

- 现场反馈：V37 中 AB 段偏出范围已可接受，但目前仍衔接不上 BC 段。
- 判断：当前 AB 白底桥接到有线段时，一旦看到黑线会调用 `transition_hold(HOLD_REASON_REACQUIRE, 3U)` 原地停约 2 秒，再进入循迹。这在 BC 入口可能打断车辆姿态和速度连续性，使车停在边缘/斜角位置后难以重新接入弯道。
- 已备份 V37：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260709_202319_before_v38_no_stop_reacquire_empty.c`。
- 已生成 V38：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260709_202319_v38_no_stop_reacquire_bc_connect.c`。
- 已替换当前活动文件：`C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`。
- 唯一控制逻辑改动：桥接阶段 `g_line_valid != 0` 后，去掉重捕线停车/滴 3 声，直接 `g_phase=PHASE_LINE_RUN`、执行 `line_follow_update()` 并继续前进。
- 速度与桥接 PWM 沿用 V37：普通循迹 500/485，桥接 520/485。
- 红外标准不变：`g_s1~g_s7` 白色为 1、黑线为 0；循迹使用 `g_black_line_mask`。
- 验证：CCS clean build + build 通过，无 C 代码警告；未自动烧录。
## 2026-07-09 20:32 V39 半速验证与右偏修正

- 现场反馈：V38 桥接阶段仍有部分右偏；同时小车经过黑线时红外传感器没有熄灭。用户要求先把速度降为当前的 1/2 再尝试，并思考潜在原因。
- 判断：右偏说明当前桥接左右配比仍偏向左轮快/右轮慢；经过黑线未熄灭可能来自速度过快采样窗口错过、传感器实际没有压到黑线、传感器高度/角度不合适、模块电位器阈值未适配现场黑线、或现场黑线反射率与调试黑线不同。
- 已备份 V38：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260709_203216_before_v39_half_speed_right_bias_empty.c`。
- 已生成 V39：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260709_203216_v39_half_speed_right_bias_sensor_check.c`。
- 已替换当前活动文件：`C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`。
- 普通循迹 PWM 约降为 V38 的 1/2：`500/485 -> 250/242`，`PWM_MIN_TICKS 350->180`，`PWM_MAX_TICKS 700->350`。
- 白底桥接 PWM 约降为 1/2 并修正右偏：`520/485 -> 245/250`。
- 红外标准不变：`g_s1~g_s7` 白色为 1、黑线为 0；循迹继续使用 `g_black_line_mask`。
- 现场验证重点：低速经过黑线时观察红外模块自身灯、`g_sx_high_count` 是否下降、`g_black_line_mask` 是否出现对应位。若低速仍完全不变，优先调模块电位器/传感器高度/确认黑线是否实际经过红外阵列。
- 验证：CCS clean build + build 通过，无 C 代码警告；未自动烧录。

## 2026-07-09 20:53 V40 历史红外 + 编码器桥接 + OpenMV 舵机并行测试

- 用户要求一次性修改当前问题：桥接阶段加入编码器修偏；进入循迹用宽度为 3 的七路红外历史窗口；退出循迹看当前七路是否完全无黑线；同时把 OpenMV UART 和 PA14/PA17 舵机追踪加回，但暂时不写任务模块。
- 已备份 V39：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260709_205326_before_v40_history_encoder_openmv_line_test_empty.c`。
- 已替换活动工程：`C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`。
- 已保存时间戳源码：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260709_205326_v40_history_encoder_openmv_line_test.c`。
- 核心逻辑：`g_black_line_mask` 保留硬件反相后的原始黑线掩码；`g_line_detect_mask` 是过滤“全灭异常”后的可用黑线掩码；`g_line_history_or_mask` 是 3 帧窗口 OR 结果。
- 桥接进入循迹：`g_line_history_nonzero != 0`。
- 循迹退出桥接：当前帧 `g_line_detect_mask == 0`。
- 7 路全灭保护：`g_sensor_all_low_abnormal=1` 时不参与循迹，连续 `SENSOR_ALL_LOW_STOP_FRAMES` 帧触发 `STOP_REASON_SENSOR_ALL_LOW`，避免悬空被误判为居中黑线。
- 桥接编码器修偏：`correction = g_encoder_diff_forward_total / START_GAP_ENCODER_KP_DIV`，限幅 `START_GAP_ENCODER_LIMIT`；左轮前进量取反，右轮前进量为正。
- OpenMV/舵机：使用 UART0 PA10/PA11 收 OpenMV 帧，PA14/PA17 输出 20ms 软件舵机 PWM；视觉只驱动云台，不参与底盘导航。
- 编译验证：CCS clean build + build 通过，无 C 代码警告；未自动烧录。

## 2026-07-09 21:28 V41 接受全黑线 + 舵机上电自检摆动

- 用户反馈：V40 仍无法进入循迹，黑线上启动也不正确；舵机没有观察到动作。
- 判断：V40 将 7 路红外全灭作为异常过滤，导致现场真实黑线若让 7 路全灭，会被误杀为 `g_line_detect_mask=0`，无法进入循迹。
- 修改前备份：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260709_212854_before_v41_accept_all_black_servo_wiggle_empty.c`。
- 当前时间戳源码：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260709_212854_v41_accept_all_black_servo_wiggle_line_test.c`。
- 修改：`LINE_ENTER_MAX_ACTIVE` 从 6 改为 7；`g_sensor_all_low_abnormal` 仅保留 Watch 观察，不再把全灭强制清零，也不再触发停车。
- 修改：舵机上电 HOME 阶段加入轻微 X/Y 摆动后回 HOME，用于确认 PA14/PA17 舵机信号确实存在；追踪逻辑仍需 OpenMV 有效帧和有效目标。
- 风险：拿起小车时 7 路全灭会再次被当作黑线，测试时不要在运行状态拿起车；后续需要更可靠的离地/异常判据。
- 编译验证：CCS clean build + build 通过；随后按用户要求使用 DSLite 直接烧录。

## 2026-07-09 21:30 V41 烧录失败记录

- V41 编译已通过，输出文件为 `C:\Users\1\Desktop\ELECTRIC\ele-car\Debug\ele-car.out`。
- 使用 DSLite 烧录时失败：`Error -260 @ 0x0: An attempt to connect to the XDS110 failed`。
- 随后执行 `xdsdfu.exe -e`，结果 `Found 0 devices`；Windows 当前设备列表也未发现 XDS110/Texas/MSP 调试器。
- 判断：这是 PC 未枚举到 XDS110/USB 数据连接问题，不是 V41 编译或程序逻辑问题。需要重新插拔 USB、换数据线/接口、确认板子上电，待 XDS110 重新出现后再烧录。

## 2026-07-09 21:34 V42 禁用电机测试版

- 用户要求：禁用当前版本电机。
- 修改前备份：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260709_213435_before_v42_motor_disabled_empty.c`。
- 当前时间戳源码：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260709_213435_v42_motor_disabled_line_openmv_servo_test.c`。
- 修改：新增 `MOTOR_ENABLE=0U`，`PB4/STBY` 初始化为低电平；所有 `motors_forward()` 调用都会清 PWM、清方向脚并拉低 STBY。
- 保留：红外读取、三帧历史、编码器计数、OpenMV UART、PA14/PA17 舵机上电自检和追踪逻辑。
- 编译验证：CCS clean build + build 通过，生成 `C:\Users\1\Desktop\ELECTRIC\ele-car\Debug\ele-car.out`。
- 烧录状态：`xdsdfu.exe -e` 仍显示 `Found 0 devices`，电脑未识别 XDS110，因此未能烧录。

## 2026-07-09 21:47 V43 电机静默采样 + 编码器增量桥接修偏

- 用户补充：桥接偏向不是单轮内左右摆动，而是不同测试轮次会固定偏向不同方向；同时电机运行时红外常量，禁用电机时每个红外都能正确反应。
- 判断：不同轮次固定偏向不同方向，不适合继续用固定左右 PWM trim；更适合用编码器闭环抵消每次起步/摆放/摩擦差异。电机运行时红外常量更像电机供电/地噪声/PWM 干扰，不像 pin 接口冲突。
- 修改前备份：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260709_214736_before_v43_motor_quiet_sensor_sample_empty.c`。
- 当前时间戳源码：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260709_214736_v43_motor_quiet_sensor_delta_encoder_bridge.c`。
- 修改：重新启用电机，桥接基础 PWM 改为左右对称 `250/250`，不再做固定方向 trim。
- 修改：桥接编码器修偏从累计误差 `g_encoder_diff_forward_total` 改为每帧增量速度差 `g_start_gap_encoder_delta_diff`，避免累计误差过度追偿造成方向来回变化。
- 修改：主控制循环读红外前短暂 `set_pwm(0,0)`，延时 `SENSOR_QUIET_SAMPLE_US=1500us` 后采样，再恢复 PWM，用于验证电机噪声是否导致红外 DO 常量。
- 新增 Watch：`g_motor_quiet_sample_count`、`g_start_gap_left_forward_delta`、`g_start_gap_right_forward_delta`、`g_start_gap_encoder_delta_diff`。
- 编译验证：CCS clean build + build 通过，已生成 `C:\Users\1\Desktop\ELECTRIC\ele-car\Debug\ele-car.out`。
- 烧录状态：`xdsdfu.exe -e` 仍显示 `Found 0 devices`，电脑未识别 XDS110，因此未能烧录。
## 2026-07-09 22:06 V45 无线桥接编码器 PI 直行 + 入线转弯加强

- 用户反馈：V43/V44 思路下已经能成功识别黑线，但无线直行桥接阶段仍有右偏；进入循迹后的转弯幅度希望稍微加大。
- 外部资料结论：差速小车无黑线直行不能依赖左右 PWM 相同，应使用编码器反馈比较左右轮位移/速度；实际控制上以“每帧编码器增量差”压制瞬时偏航，以“累计里程差”消除残余偏差，并设置限幅防止过修。
- 当前活动文件：`C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`。
- 时间戳源码：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260709_220609_v45_encoder_pi_bridge_stronger_turn.c`。
- 桥接修改：
  - `START_GAP_LEFT_TRIM_TICKS=-8`，`START_GAP_RIGHT_TRIM_TICKS=8`，用于抵消本轮现场观察到的固定右偏。
  - `START_GAP_ENCODER_DELTA_KP_DIV=2`，以每帧左右编码器增量差做快速修正。
  - `START_GAP_ENCODER_DISTANCE_KI_DIV=64`，以累计左右前进距离差做慢速修正。
  - `START_GAP_ENCODER_LIMIT=95`，`START_GAP_ENCODER_SLEW_LIMIT=18`，限制修正幅度和单帧变化，避免桥接阶段突然打方向。
  - 新增 Watch：`g_start_gap_encoder_target_correction`、`g_start_gap_left_cmd_ticks`、`g_start_gap_right_cmd_ticks`。
- 循迹修改：
  - `LINE_KP_DIVISOR 18 -> 16`
  - `LINE_CORR_LIMIT 170 -> 190`
  - `LINE_SHARP_KP_DIVISOR 7 -> 6`
  - `LINE_SHARP_CORR_LIMIT 420 -> 450`
- 验证：已执行 CCS clean build + build，通过并生成 `C:\Users\1\Desktop\ELECTRIC\ele-car\Debug\ele-car.out`。本轮未自动烧录。
- 现场调参规则：
  - 若无线桥接仍右偏：先把 `START_GAP_LEFT_TRIM_TICKS=-12`、`START_GAP_RIGHT_TRIM_TICKS=12`。
  - 若桥接开始左右摆：先把 `START_GAP_ENCODER_LIMIT=80` 或 `START_GAP_ENCODER_SLEW_LIMIT=12`。
  - 若进入循迹后转弯仍不够：优先把 `LINE_SHARP_CORR_LIMIT=480`，不要先动传感器极性。
## 2026-07-09 22:22 V46 编码器 PID 桥接 + 黑线脉冲入口

- 用户反馈：V45 仍然循迹/桥接有右偏；同时红外传感器硬件有反应，但小车没有直接进入循迹。用户指出别人方案多为 PID，质疑前一版为什么只采用 PI。
- 复盘判断：
  - V45 对编码器桥接的命名不严谨。若以“左右累计距离差”为被控误差，则每帧左右编码器增量差本质上是 D 项，而不是普通 P 项；V45 实际更接近 PD + 静态 trim，但缺少真正的 I 项。
  - “红外灯有反应但软件没切入循迹”说明硬件 DO 可能已经短暂变化，但 V45 用 9 次采样多数判定生成 `g_line_detect_mask`，短暂压线会被滤掉，导致桥接状态没有看到可用黑线。
- 当前活动文件：`C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`。
- 时间戳源码：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260709_222223_v46_encoder_pid_pulse_line_entry.c`。
- 桥接 PID 修改：
  - `P = g_encoder_diff_forward_total / START_GAP_ENCODER_DISTANCE_KP_DIV`
  - `I = g_start_gap_encoder_integral_error / START_GAP_ENCODER_INTEGRAL_KI_DIV`
  - `D = g_start_gap_encoder_delta_diff / START_GAP_ENCODER_SPEED_KD_DIV`
  - 当前参数：`KP_DIV=48`，`KI_DIV=1800`，`KD_DIV=2`，`INTEGRAL_LIMIT=12000`，`OUTPUT_LIMIT=115`，`SLEW_LIMIT=18`。
  - 右偏补偿从 `-8/+8` 加强为 `-14/+14`。
- 入线判定修改：
  - 新增 `g_line_black_pulse_mask = (~g_line_raw_high_and_mask) & 0x7F`，表示 9 次采样里任意一次出现黑线低电平的传感器。
  - 新增 `g_line_entry_mask = g_line_detect_mask | g_line_black_pulse_mask`。
  - 三帧历史窗口改为记录 `g_line_entry_mask`，桥接阶段只要捕获到任意黑线脉冲就进入循迹。
  - 循迹退出条件从 `g_line_detect_mask == 0` 改为 `g_line_entry_mask == 0`，避免刚入线时因多数判定尚未稳定又立刻退出。
- 验证：已执行 CCS clean build + build，通过并生成 `C:\Users\1\Desktop\ELECTRIC\ele-car\Debug\ele-car.out`。本轮未自动烧录。
- 现场调参规则：
  - 若仍右偏：优先把 `START_GAP_LEFT_TRIM_TICKS=-18`、`START_GAP_RIGHT_TRIM_TICKS=18`。
  - 若桥接左右摆：把 `START_GAP_ENCODER_SPEED_KD_DIV=3` 或 `START_GAP_ENCODER_LIMIT=95`。
  - 若误入循迹：把 `LINE_ENTRY_USE_PULSE_MASK=0` 可临时关闭脉冲入口验证。
## 2026-07-09 22:33 V47 假入线修复 + 桥接右偏继续修正

- 用户反馈：V46 小车仍然偏右；仍未稳定进入真实循迹；蜂鸣器一直响，PA15 LED 跑起来后常亮。
- 诊断：
  - V46 中 `g_line_black_pulse_mask` 被直接 OR 进 `g_line_entry_mask`，导致白地运行时的瞬时低电平/电机噪声也可能触发入线。
  - LED 常亮不是“没进循迹”，而更可能是“过早进入假循迹”；蜂鸣器一直响可能来自桥接/假循迹之间反复切换触发提示。
- 当前活动文件：`C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`。
- 时间戳源码：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260709_223358_v47_confirmed_line_entry_pid_bridge.c`。
- 入线确认修改：
  - `g_line_detect_mask` 多数判定非零时立即进入循迹。
  - `g_line_black_pulse_mask` 不再直接进入循迹，只作为候选。
  - 候选脉冲需在最近 4 帧中至少出现 2 帧：`LINE_ENTRY_CANDIDATE_HISTORY_MASK=0x0F`，`LINE_ENTRY_CANDIDATE_CONFIRM_MIN=2U`。
  - 如果脉冲为 `0x7F` 且多数判定仍为 0，视为不可靠候选，避免白地噪声/悬空/干扰造成假入线。
  - 新增过渡帧：`LINE_ENTRY_CONTROL_FRAMES=3U`，刚进入循迹后前 3 帧可使用确认入口 mask，随后必须回到多数判定。
- 桥接右偏修改：
  - `START_GAP_LEFT_TRIM_TICKS -14 -> -20`
  - `START_GAP_RIGHT_TRIM_TICKS 14 -> 20`
  - `START_GAP_ENCODER_LIMIT 115 -> 130`
  - `START_GAP_ENCODER_SLEW_LIMIT 18 -> 14`
- 蜂鸣器修改：
  - 新增 `BUZZER_TRIGGER_COOLDOWN_FRAMES=25U`。
  - 同类蜂鸣在冷却期内不重复触发，避免状态抖动时一直响。
  - 不同类蜂鸣仍可覆盖，例如桥接 2 声后真正入线 3 声不会被吞掉。
- 新增/保留 Watch：
  - `g_line_candidate_history_mask`
  - `g_line_candidate_confirm_count`
  - `g_line_confirmed_entry_count`
  - `g_line_entry_control_frames_left`
  - `g_buzzer_cooldown_frames`
  - `g_buzzer_suppressed_count`
- 验证：已执行 CCS clean build + build，通过并生成 `C:\Users\1\Desktop\ELECTRIC\ele-car\Debug\ele-car.out`。本轮未自动烧录。
## 2026-07-09 22:44 V48 启动白底假黑过滤 + 入线/出线防抖

- 用户反馈：拿起小车上电时 LED 不常亮；放在白底上启动时 LED 默认常亮。说明 V47 仍会在白底上发生假入线，且问题不只是单次脉冲噪声，而是白底反射/传感器阈值导致某些通道被稳定读成黑线。
- 关键判断：
  - V47 防住了“单次脉冲假入线”，但没有防住“白底稳定假黑”。
  - 只要 `g_line_detect_mask != 0` 单帧有效，就仍可能在白底上直接进入 `PHASE_LINE_RUN`。
  - 出循迹也必须使用过滤后的有效检测，否则某个白底假黑传感器会让小车误以为一直在线上，导致不能出线。
- 当前活动文件：`C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`。
- 时间戳源码：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260709_224405_v48_startup_false_black_filter_debounced_line.c`。
- 新增启动假黑过滤：
  - 启动第一帧读取 `g_line_detect_mask`。
  - 若启动时检测到的黑线位数 `<= LINE_START_IGNORE_MAX_ACTIVE=2U`，将这些位记录为 `g_line_start_ignore_mask`。
  - 后续导航使用 `g_line_effective_detect_mask = g_line_detect_mask & ~g_line_start_ignore_mask`。
  - 如果启动时有效黑线位数 `>= LINE_START_DIRECT_MIN_ACTIVE=3U`，仍允许直接循迹，避免真正放在线上时完全不可用。
- 入线逻辑：
  - 候选来源改为 `g_line_effective_detect_mask` 或过滤后的脉冲候选。
  - 候选仍需最近 4 帧至少 2 帧确认。
  - `enter_line_run()` 保存入口 mask 时也过滤 `g_line_start_ignore_mask`。
- 出线逻辑：
  - 使用 `g_line_effective_detect_mask` 判断是否仍在线上。
  - 连续 `LINE_EXIT_CONFIRM_FRAMES=3U` 帧无有效黑线后才退出循迹回桥接。
  - 新增 Watch：`g_line_exit_missing_count`、`g_line_exit_confirmed_count`。
- 验证：已执行 CCS clean build + build，通过并生成 `C:\Users\1\Desktop\ELECTRIC\ele-car\Debug\ele-car.out`。本轮未自动烧录。
## 2026-07-10 现场补充召回：V48 静态三场景结果与下一步诊断

- 当前活动源码：`C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`，文件头为 V48；SHA256 为 `AD15B5E484BE0CEE79F107C671F75D23B4A55CBE4F60744C7B46D667F5E5540A`。
- 当前编译产物：`C:\Users\1\Desktop\ELECTRIC\ele-car\Debug\ele-car.out`，生成时间 2026-07-09 22:44:15。
- 现场三场景反馈：
  - 白底上电：7 路红外模块灯全亮，小车 LED 立即亮，车辆随后右偏运动。
  - 黑线上电：约 1~2 路红外模块灯熄灭，小车 LED 亮，但没有正确沿黑线循迹。
  - 悬空上电：7 路红外模块灯全灭，小车 LED 不亮，轮子转动。
- 关键矛盾：按 V48 的软件约定，白底全亮应得到 `g_black_line_mask=0`，不应进入循迹；悬空全灭应得到 `g_black_line_mask=0x7F`，反而可能直接进入循迹。现场 LED 现象与代码状态指示相反。
- 当前不能继续仅靠调整入线帧数或 PID。下一步必须确认：板上实际烧录版本、用户所指 LED 是否确为 PA15 测试灯、MCU Watch 中各层 mask/phase 的静态值、七路传感器物理左右顺序，以及左右编码器计数方向。
- 下一 intended action：先做电机架空/静态 Watch 三场景采样；依据结果判断是烧录/LED身份问题、传感器极性问题、引脚映射问题，还是状态机问题，再生成下一时间戳代码。
## 2026-07-10 补充召回：核对用户粘贴代码是否为当前 V48

- 用户提供附件：`C:\Users\1\.codex\attachments\7591a031-2ce8-47ae-b77e-f64109996b58\pasted-text.txt`，要求确认其内容是否等同于当前 CCS 活动源码。
- 对比目标：`C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`，当前预期版本为 V48。
- 本轮只做只读逐行/哈希比较，不修改活动代码，不烧录。
## 2026-07-10 20:53 V49 红外物理极性修正 + 悬空拒绝

- 用户提供 V48 三场景 Watch：
  - 白底：`g_line_raw_mask=16`、`g_black_line_mask=111`、`g_phase=2`、`g_line_error=-166`。
  - S4 压黑线：`g_line_raw_mask=24`、`g_black_line_mask=103`、`g_phase=2`。
  - 悬空：`g_line_raw_mask=127`、`g_black_line_mask=0`、`g_phase=8`。
- 结论：MCU 物理输入实际为白底低电平、黑线高电平、悬空全高；V48 的 `SENSOR_HIGH_IS_WHITE=1` 完全反向。白底因此被解释为 6 路黑线并进入假循迹，`g_line_error=-166` 导致右偏。
- 当前活动文件：`C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`。
- 时间戳源码：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260710_205311_v49_correct_sensor_polarity_suspend_reject.c`。
- V49 修改：
  - `SENSOR_HIGH_IS_WHITE 1 -> 0`；软件归一化结果仍保持 `g_s=1` 表示白色、`g_s=0` 表示黑线。
  - `LINE_START_IGNORE_MAX_ACTIVE 2 -> 1`，白底上稳定异常的 S5 可被启动过滤，但真实 S4+S5 两路黑线不会一起被忽略。
  - `LINE_START_DIRECT_MIN_ACTIVE 3 -> 2`，黑线上电检测到 2 路有效黑线时可直接循迹。
  - `LINE_ENTER_MAX_ACTIVE 7 -> 4`，实际黑线只接受 1~4 路；7 路不再作为有效黑线。
  - `SENSOR_ALL_LOW_STOP_FRAMES 0 -> 3`，悬空全高经归一化后形成 7 路黑线异常，连续 3 帧后停车。
  - 新增 Watch：`g_firmware_version=49`。
- 编码器数据当前全部为 0，PID 尚未获得反馈；但本轮右偏发生在错误的 `PHASE_LINE_RUN`，不能据此判断桥接 PID 方向。需先验证 V49 状态正确，再复测编码器。
- 用户逐个压 S1~S7 的后续截图数值完全一致，推测调试器暂停后移动黑线，变量没有刷新；该组截图暂不能确认物理左右顺序。
- 验证：CCS clean build + build 通过，生成 `C:\Users\1\Desktop\ELECTRIC\ele-car\Debug\ele-car.out`，未自动烧录。
## 2026-07-10 红外传感器阈值科学校准方法

- 当前硬件极性已由 Watch 确认：物理 DO 在白底为低、黑线为高、悬空可能全高；V49 软件归一化后 `g_s=1` 表示白色、`g_s=0` 表示黑线。
- 红外模块使用 DO 数字输出时，真正的光学阈值由模块电位器/比较器决定；代码中的 `LINE_WHITE_MIN_HIGH_SAMPLES=5/9` 只是时间去抖，不是光学阈值。
- 科学校准原则：固定比赛高度、角度、白底/黑线材料和现场光照；逐路调整电位器，使物理高电平采样计数在白底接近 0/9、黑线接近 9/9，远离 4~5 的临界区；随后在电机运行状态复测抗干扰。
- 当前 S5 在白底仍表现为物理高电平，优先单独校准 S5；若电位器全范围内仍无法实现白低黑高，应通过与正常模块对调来区分模块、接线和 MCU 引脚问题。
## 2026-07-10 独立红外模块与八路灰度阵列选型复盘

- 用户指出 7 个独立红外模块存在逐路电位器、接线、安装高度和阈值一致性问题，询问为何未采用八路灰度阵列。
- 技术结论：红外循迹与灰度检测通常都基于红外反射；关键差异不是光源，而是输出信息和集成方式。当前模块为单点比较器 DO 二值输出；更合适的八路灰度阵列应提供统一机械间距及模拟值或板载 MCU 处理后的多路灰度数据。
- 若后续更换，优先选择 3.3V 逻辑兼容、能输出每路连续灰度值、支持 I2C/UART 或已有可靠驱动的 8 路阵列；仅提供 8 路 DO 的廉价模块只能改善机械和布线，不能根除阈值问题。
- 八路阵列只解决有线循迹检测，赛道无线桥接仍需编码器/姿态闭环。
## 2026-07-10 补充召回：智能灰度阵列合规、舵机角度与安装工具选型

- 用户提供官方训练题页面：`https://res.nuedc-training.com.cn/topic/2025/topic_124.html`。
- 本轮目标：依据题目原文判断带板载 MCU 的智能灰度阵列是否合规；结合当前 OpenMV 双舵机云台判断 180°、270°、360°舵机的适用性；给出能在现有小车底盘上可靠打孔、定位和固定灰度阵列的工具与结构要求。
- 当前硬件背景：MSPM0G3507 主控、TB6612、电机编码器、OpenMV、PA14/PA17 双舵机；现有 7 个独立红外模块阈值和布线调试成本较高，正在评估一体化灰度阵列替换。
- 下一 intended action：读取题目网页原文和器件限制，优先采用官方条款作为合规依据；再输出采购规格、舵机选择与安装施工方案。
## 2026-07-10 2025 E题官方条款下的智能灰度、云台角度与安装结构结论

- 官方来源：https://res.nuedc-training.com.cn/topic/2025/topic_124.html，题目为 2025 E题“简易自行瞄准装置”。
- 官方关键条款：
  - 自动寻迹小车行驶必须采用 TI MSPM0 系列 MCU 控制，明确包括巡迹和电机控制。
  - 瞄准模块使用二维云台控制蓝紫激光方向。
  - 小车与瞄准模块总体轮廓不大于 25cm×15cm×25cm。
  - MSPM0 控制板需安装在装置表面并裸露 MCU 型号；小车控制板与瞄准模块使用独立电源开关并显示供电状态。
- 合规判断：题目未禁止传感器模块带辅助 MCU。智能灰度阵列可用于光电采集、通道校准和返回原始灰度值；巡迹误差计算、PID/状态机和左右电机 PWM 必须留在 MSPM0。为降低争议，不选择直接输出转向命令或电机控制量的“循迹控制板”。
- 采购优先级：3.3V 逻辑兼容、返回 8 路原始连续灰度值、I2C 优先、更新率至少 100Hz、支持环境光/白黑校准、具有明确 M2/M3 安装孔与尺寸图。只有 8 路 DO 的板只能改善机械布线，不能根除阈值问题。
- 舵机结论：
  - 180°位置舵机适合基础静态瞄准和有限前向视场，不适合全赛道多圈连续指向固定靶面。
  - 270°位置舵机扩大覆盖但仍有角度边界，且同脉宽范围内角分辨率更低。
  - 普通 360°连续旋转舵机是速度控制而非位置控制，不适合精确瞄准。
  - 全程连续瞄准更适合带绝对位置反馈的总线舵机/编码器旋转机构；多圈运行还需处理线缆扭转，必要时使用导电滑环。
- 安装结论：优先使用带长圆孔的可调支架固定阵列，不在传感器 PCB 上打孔。推荐迷你台钻或手捻钻、2/2.5/3mm 钻头、游标卡尺、直角尺、夹具、垫木、锉刀/去毛刺器、M2/M3 螺钉和尼龙隔柱。模块及支架必须计入 25×15×25cm 总体尺寸。
## 2026-07-10 MPU6050 与显示屏采购判断

- MPU6050 对全程有黑线的基础循迹不是必需；对校赛无线桥接段有价值，可使用 Z 轴角速度在数秒内保持车头方向，并在重新捕线后清零/重校准。它不能长期提供无漂移绝对航向，不能替代编码器和红外。
- 推荐顺序：先修通左右编码器，再加入 MPU6050，形成编码器距离闭环 + 陀螺仪航向闭环。若编码器仍为 0，单独增加 IMU 不能解决里程问题。
- 低成本采购可选 GY-521 MPU6050，必须以 3.3V 供电并确认 SDA/SCL 上拉不超过 3.3V；若追求更低噪声可选 ICM-42688-P，但驱动复杂度更高。
- 显示屏不是题目要求，官方只要求控制板/瞄准模块供电状态用发光管显示。显示屏仅用于模式、传感器值、故障码和电池电压调试。
- 推荐 0.96 英寸、128×64、SSD1306、I2C、4 针、3.3V 兼容 OLED；显示刷新限制在 5~10Hz，禁止在 20ms 控制环内整屏高频刷新。
## 2026-07-10 无 MCU 八路灰度阵列可行性结论

- 无 MCU 八路阵列完全可以完成高质量循迹，且合规边界最清晰：MSPM0 负责采样、校准、线位置计算、PID 和电机控制。
- 选型按输出方式排序：连续模拟 AO 或 RC 放电时间型最佳；只有 8 路 DO 比较器的阵列只能改善机械一致性和布线，仍存在硬件阈值问题。
- 当前 MSPM0 引脚较紧张且 PA2~PA6 不宜使用。若选 8 路 AO，可配 74HC4051 模拟多路复用器，以 1 路 ADC + 3 路选择线采集；若选 RC 阵列，可复用当前 7 路传感器 GPIO 并新增 PB8。
- 推荐阵列需满足 3.3V、连续数据、明确安装孔、约 7~10mm 通道间距和至少 100Hz 整体更新率。
## 2026-07-10 补充召回：京东八路灰度商品页选型核查

- 用户已在 Edge 打开京东商品：https://item.jd.com/10200846846557.html。
- 本轮核查目标：确认商品是否为无 MCU 连续灰度阵列，检查 AO/DO/RC 输出、供电与逻辑电压、通道间距、板宽、安装孔、接口文档和能否适配 MSPM0 + 74HC4051。
- 判定标准：优先 8 路连续 AO 或 RC；只有 8 路 DO 的型号不作为最终推荐；模块及支架需满足整车 25cm×15cm×25cm 外形约束。

## 2026-07-10 补充召回：水平带反馈连续旋转机构 + 导电滑环采购

- 用户要求直接给出水平方向连续旋转、带角度反馈且可配导电滑环的明确商品方案。
- 已知项目约束：MSPM0G3507；OpenMV 使用 PA10/PA11 UART0；现有水平舵机信号为 PA14、俯仰舵机信号为 PA17；当前普通 MG90S 水平轴只有约 180°且不能反读角度。
- 本轮优先保持现有 PWM 控制架构，避免为了总线舵机额外占用 UART 和增加半双工收发硬件；同时要求反馈电平不超过 MSPM0 ADC 的 3.3V。
- 候选核心型号：飞特 `FB5317M-360`（PWM 连续旋转、0~3.3V/0~360°模拟角度反馈）与 `STS3215`（TTL 总线、12 位磁编码、多圈位置控制）。
- 滑环选型重点：过孔式、孔径能容纳 25T 舵盘/转轴、至少 6 路且优先 12 路保留独立电源/地与信号余量；帽式滑环不适合直接同轴套在现有舵机输出轴周围。
- 下一 intended action：结合在售价格、供电、控制改造量和机械孔径，确定唯一推荐 SKU，并记录购买链接、接线约束与不可误选项。

## 2026-07-10 水平连续旋转机构最终采购结论

- 唯一推荐水平舵机：飞特 `FB5317M-360`。官方 1688 商品 `https://m.1688.com/offer/1014220906516.html`，2026-07-10 页面价 ¥88、库存 9970。
- 官方规格核验：4~8.4V；17.5kg·cm@7.4V；60RPM；钢齿；25T/5.9mm 输出；机械无限位；500~2000µs 连续旋转；1500±20µs 停止；反馈为 0~3.3V 对应 0~360°。规格页：`https://www.feetech.cn/17kg-digital-magnetic-code-continuous-rotation-analog-feedback-actuator.html`。
- 选择该型号而不选普通 FS90R/DS3235 等 360°舵机的原因：普通连续舵机只能控方向/速度，不能回读角度；`FB5317M-360` 有独立绝对角反馈。
- 选择该型号而不选 `ST3215-HS/STS3215` 的项目性原因：可保留 PA14 PWM，只新增一路 ADC；总线舵机需半双工 UART、适配板、协议移植和新引脚，而 OpenMV 已占 PA10/PA11 UART0。水平轴无重力负载，可由 MSPM0 外环补偿静态偏差；后续需处理 0°/360°环形误差、PWM 死区和 ADC 滤波。
- 唯一推荐滑环：`SRC022A-12`，22mm 带法兰、12 路、每路 2A、最高约 300RPM。国内购买页：`https://www.taobao.com/list/item/677639840287.htm`；规格参考：`https://www.adafruit.com/product/1196`。
- 机械结构：滑环居中，舵机偏置；两个 `6805-2RS`（25×37×7mm）轴承承担转台载荷；25T 铝舵盘连接 1:1 GT2 同步轮。禁止让滑环承重，禁止使用 3:1 减速，否则舵机单圈绝对反馈无法在掉电后判断平台所处扇区。
- 12 路建议分配：3 路并联 5V、3 路并联 GND、OpenMV TX/RX、俯仰舵机 PWM、激光使能、2 路备用。
- 供电：新增独立 5~6V/5A UBEC；舵机堵转电流约 2.2A，不能继续依赖当前带载已掉压的弱 MP1584/TB6612 板载 5V。舵机端并联 1000µF，反馈 ADC 前加 1kΩ + 100nF 低通。

## 2026-07-14 补充召回：八路灰度板到货并整合接线

- 用户最终购买并收到亚博/Yahboom `YB-MYX05-V1.0` 无 MCU 八路灰度巡线板，实物图片确认六针接口为 `5V/AD2/AD1/AD0/OUT/GND`，板上 `X1~X8` 通过三位地址复用到单一数字 `OUT`。
- 本轮目标：废止原 7 个独立 TCRT5000 的正式接线，给新灰度板分配无冲突引脚，并重新输出当前整车全部接线。
- 当前活动 V49 仍按 7 路独立 GPIO 读取，不能直接驱动新板；下一步必须先写电机禁用的八通道复用扫描程序。
- 新板引脚分配：`AD0->PB5`、`AD1->PB6`、`AD2->PB7`、`OUT->PA7`；复用原已验证可用巡迹引脚，释放 `PA8/PB9/PB10`。
- 电气约束：灰度板接稳定 5V；首次连接 `OUT` 前测最高电平，若接近 5V 则用 10kΩ/20kΩ分压到 PA7；所有模块共地。
- 已更新 `C:\Users\1\Desktop\电赛\接线步骤.md`，并修正旧文档中 `PB11` 同时分配给 START 按键和蜂鸣器的冲突：当前 `PB11` 只用于蜂鸣器，按键暂不接。

## 2026-07-15 补充召回：开始八路灰度板隔离测试

- 用户已按新接线准备测试 `YB-MYX05-V1.0` 八路复用灰度板。
- 本轮先不恢复循迹、电机、编码器闭环、OpenMV UART 或舵机控制；所有电机方向脚、STBY、PA12/PA13 PWM、PA14/PA17 舵机信号和 PB11 蜂鸣器均强制为低。
- 测试程序使用 `PB5/PB6/PB7 -> AD0/AD1/AD2`，`PA7 <- OUT`，依次扫描 X1~X8；每通道多次采样并输出原始高电平 mask、各通道高电平计数、稳定性和相对白底基准变化 mask。
- 上电前要求八个探头全部放在白底；程序先采集约 1 秒白底基准。基准完成后，PA15 LED 在任一通道相对白底发生变化时常亮。
- 代码保存时间戳版本并替换活动 `empty.c`，只编译验证，不自动烧录，不生成 readme。

### V50 实现结果

- 当前活动文件：`C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`。
- 时间戳源码：`C:\Users\1\Desktop\电赛\ccs_project\line_sensor_test\20260715_152540_v50_gray8_mux_motor_off_test.c`。
- 修改前备份：`C:\Users\1\Desktop\电赛\ccs_project\line_sensor_test\20260715_152540_before_v50_current_empty_backup.c`。
- `g_firmware_version=50`；状态码：0=上电等待，1=白底标定，2=均匀白底基准完成，3=白底基准八路不一致。
- 每个通道等待 40us 后采样 9 次，至少 5 次高电平判为高；`g_gray_unstable_mask` 标记一次扫描中不是全高/全低的通道。
- 白底基准完成后使用 `当前原始mask XOR 白底基准mask` 得到 `g_gray_changed_from_white_mask`，因此测试不依赖模块究竟是黑高还是黑低。
- PA15：标定阶段慢闪；均匀基准完成后，任一通道相对白底变化则常亮；基准不一致时快闪。
- 已执行 CCS `gmake clean` + `gmake -j 8 all`，编译和链接通过且零警告；按用户规则未自动烧录。

## 2026-07-15 低电平触发蜂鸣器模块替换

- 用户找到三针低电平触发有源蜂鸣器模块，实物丝印接口为 `VCC/I/O/GND`，板上自带驱动三极管和输入电阻。
- 正式接线改为 `VCC->LaunchPad 3.3V`、`I/O->PB11`、`GND->公共GND`；建议 `I/O` 到 3.3V 增加 10kΩ 上拉，防止复位期间 GPIO 高阻造成短促误响。
- 控制极性改为 `PB11 LOW=响`、`PB11 HIGH=关闭`。旧的两针蜂鸣器直驱/330Ω接法作废。
- 发现 V50 灰度隔离测试将 PB11 强制为 LOW；对新模块这不是“安全关闭”，而是持续鸣叫。因此生成 V51，将蜂鸣器空闲输出改为 HIGH，灰度扫描逻辑不变。
- V51 时间戳源码：`C:\Users\1\Desktop\电赛\ccs_project\line_sensor_test\20260715_171632_v51_gray8_mux_motor_off_low_active_buzzer_safe.c`。
- 已替换活动 `C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`，执行 clean build，编译和链接通过且零警告；未自动烧录。

## 2026-07-15 补充召回：八路灰度板进入低速电机循迹联调

- 用户确认新八路灰度传感器效果非常好，X1~X8复用扫描、白底基准和黑线变化检测已通过，准备进入下一阶段。
- 本轮只集成八路灰度、TB6612和电机；OpenMV UART、双舵机、任务点、激光与编码器闭环继续禁用，低电平蜂鸣器保持 PB11=HIGH 静音。
- 方案：沿用已验证的 `line_follow_sharp_turn_stable_baseline.c` 电机方向、PWM反向compare规则、普通修正和急弯/丢线找回结构，把七路直接GPIO输入替换为八路地址复用扫描与白底XOR黑线mask。
- 首测降低基础PWM，白底标定成功后先低速直行接近黑线，连续确认2帧后进入循迹；丢线按最后非零误差找回最多约2秒，测试约30秒自动停车。
- 生成V52时间戳源码、替换活动empty.c并只做clean build，不自动烧录，不生成readme。

### V52 实现结果

- 当前活动文件：`C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`。
- 时间戳源码：`C:\Users\1\Desktop\电赛\ccs_project\line_sensor_test\20260715_202553_v52_gray8_low_speed_line_follow.c`。
- `g_firmware_version=52`；基础PWM为左500/右470，低于历史稳定版620/590，同时保留30 ticks左右差值。
- 白底标定不一致时不启动；桥接连续2帧确认黑线后进入循迹；接受1~6路有效黑线，7~8路作为异常；连续丢线100帧或桥接250帧未见线停车；总测试1500帧后停车。
- 普通修正使用 `LINE_KP_DIVISOR=22`、限幅130；大误差/丢线找回使用急弯除数8、限幅340。电机PWM仍遵循 `compare=1600-duty`。
- PA15状态：标定慢闪，标定完成常亮0.8秒，桥接熄灭，循迹常亮，丢线找回闪烁，停止后周期短闪。PB11始终HIGH，低电平触发蜂鸣器保持静音。
- 已执行clean build，编译链接通过且零警告；按用户规则未自动烧录。

## 2026-07-15 补充召回：V52电机完全不动，增加启动增力

- 用户反馈V52上电后小车完全不动。V52基础PWM左500/右470，仅约31%/29%占空比，对当前负重和静摩擦可能低于起转阈值。
- 本轮按用户要求直接修改代码，保持灰度扫描、白底标定、状态机、入线确认和停车保护不变。
- V53计划：桥接启动前20帧约0.4秒使用左760/右720突破静摩擦，随后使用历史已验证的左620/右590基础PWM；PWM最小值恢复430、最大值恢复850。
- 生成时间戳源码、替换活动empty.c、clean build验证，不自动烧录，不生成readme。

### V53 实现结果

- 当前活动文件：`C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`。
- 时间戳源码：`C:\Users\1\Desktop\电赛\ccs_project\line_sensor_test\20260715_204132_v53_gray8_start_boost_line_follow.c`。
- `g_firmware_version=53`；`g_start_boost_active=1`表示760/720启动增力，20帧后自动变为0并切换620/590；若提前捕获黑线，也立即退出增力。
- 灰度扫描、白底标定、两帧入线确认、急弯、丢线找回、运行时间和停车保护均未改变。
- 已执行clean build，编译链接通过且零警告；未自动烧录。

## 2026-07-15 独立上电不运行与X6常亮联合诊断

- 用户反馈：V53只有连接电脑、进入CCS并按Run后小车才运动；拔掉USB后重新用整车电源上电始终不动。进一步确认：USB连接时X6恢复正常，拔掉USB后X6立即常亮；V51灰度隔离程序在USB调试条件下无异常。
- 代码核查：V53不存在调试器/Watch依赖；唯一启动门禁是白底八路必须一致。X6异常会导致 `g_gray_baseline_uniform=0`、`g_stop_reason=2`，程序按设计禁止电机启动。
- TI官方 `SLAU873D` 第2.4/2.5节确认：USB供电由XDS110经J101向目标侧提供3.3V；外部电源应通过 `J11` 直接提供符合1.62~3.6V范围的3.3V。J10只是5V电源排针，不能继续假定接入J10就一定能给目标MCU建立3.3V。
- 根因判断：拔掉USB后目标MCU没有可靠3.3V，PB5/PB6/PB7地址线失去驱动或电平不稳，灰度复用器选通异常，X6常亮；USB插入后XDS110恢复目标3.3V和GPIO驱动，所以两个症状同时消失。
- 正式修正：TB6612稳定3V3 OUT接LaunchPad J11/3V3，所有GND共地；若外部3.3V和USB同时存在，拔掉J101中的3V3跳帽，保留调试信号。接线前必须验证3V3带载为约3.25~3.35V。
- 当前不修改V53控制代码；先修正供电并验证独立上电PA15启动灯序和X6状态。

## 2026-07-15 LM2596加入控制电源

- 用户带来一块LM2596降压模块。当前最合适用途是从2S电池7.4V直接降至稳定3.30V，专门给LaunchPad J11/3V3供电。
- 正式接线：电池正负接LM2596 IN+/IN-；空载调到3.30V后，OUT+接J11/3V3、OUT-接J11/GND；所有系统GND共地。
- TB6612 3V3 OUT不再连接LaunchPad；灰度板继续使用TB6612稳定5V；OpenMV和舵机继续走独立5V支路。
- 使用LM2596外部3.3V时，J101的3V3跳帽保持拔下；USB调试只保留GND/NRST/SWDIO/SWCLK连接。
- 调压必须先空载进行，接板后复测J11为3.25~3.35V；禁止把未调节的LM2596输出直接接MCU。

## 2026-07-15 GitHub晨间交接归档

- 用户要求把最近更新全部提交GitHub，并把当天所有关键对话转存为仓库根目录最显眼的 `lwx7月16晨必看.md`。
- 文档第一行严格为：`lwx，你要先阅读理解所有实物接线！`
- 文档集中记录当前权威实物接线、V50~V53代码关系、低电平蜂鸣器、USB/XDS110与J11外部3.3V供电根因、LM2596接法、Watch变量、明早操作步骤和禁止重复的错误。
- 本次提交范围按用户“最近更新全部提交”要求，包含此前尚未纳入Git的V23~V49过程版本、OpenMV脚本、V50~V53、接线/记忆/过程/复盘文档和晨间交接文件。

## 2026-07-16 补充召回：LM2596 独立供电首次验证

- 已知当前活动程序为 V53，USB/CCS 条件下已编译并运行过；当前阶段不先修改控制代码。
- 既定独立供电方案：7.4V 电池接 LM2596 输入，LM2596 空载调至 3.30V，OUT+ 接 LaunchPad J11/3V3，OUT- 接 J11/GND；使用外部 3.3V 时拔下 J101 的 3V3 跳帽。
- 用户最新现象：不插 USB 时，板载指示灯不亮，PA15 测试 LED 也不闪。
- 判断边界：J101 的 3V3 跳帽拔下后，XDS110 调试器区域指示灯不亮可以正常；PA15 不闪说明目标 MCU 未正常获得 3.3V、被复位压住，或程序未启动。
- 下一步只测电源链路，不改循迹参数：依次测 LM2596 OUT+/OUT-、LaunchPad J11/3V3-GND、NRST-GND，并检查 J101 3V3 跳帽状态与 LM2596 极性。

## 2026-07-16 NRST 钳位与灰度板 5V 断供确认

- 用户实测：无 USB 独立供电时，保留 J101 的 NRST 跳帽则 PA15 不闪、电机不转；拔掉 NRST 跳帽后 PA15 开始闪烁，电机立即运行。
- 这证明当前硬件上，未供电的 XDS110 侧通过 NRST 路径把目标 MCU 复位线拉低或钳位，目标芯片此前一直停在复位状态。
- 独立运行接法改为：J101 的 3V3 与 NRST 均拔掉；LM2596 3.30V 继续供给 J11/3V3。需要下载/调试时接回 NRST/SWDIO/SWCLK，J101 的 3V3 仍保持断开。
- 同时发现灰度传感器不亮。原因是灰度板 5V/GND 接在开发板排针上，而当前开发板只有 J11 外部 3.3V；无 USB 时开发板 5V 母线没有电源，3.3V 不会反向升压生成 5V。
- 下一步恢复灰度板独立稳定 5V：优先使用已验证的 TB6612 5V OUT，仅给灰度板等小负载；所有电源 GND 共地。接线前测 5V OUT-GND，应稳定接近 5V。

## 2026-07-16 独立运行与烧录流程简化方案

- 为避免每次运行都移动跳帽，拟固定拔下 J101 的 `3V3` 与 `NRST`，永久保留 `GND、SWDIO、SWCLK`。
- 目标板在烧录和脱机运行期间都由 LM2596 3.30V 经 J11 供电；USB 仅作为 XDS110 数据/调试连接，不再承担目标板供电。
- 当前代码未占用 SWD 调试资源；先验证 NRST 永久断开时 CCS 是否仍能正常连接、下载和运行。若个别情况下 CCS 连接失败，再单独增加一个 NRST 开关/跳线作为备用，而不恢复 3V3 并联。

## 2026-07-16 补充召回：V53 独立供电后的首轮动态反馈

- 独立供电链路已能让目标程序和电机运行；当前活动逻辑仍以 V53 八路灰度循迹为基底。
- 用户最新反馈：车辆速度过快，要求约减半；白底标定后车辆固定向左转并绕圆；遇到黑线后轨迹会发生变化，说明灰度输入已影响控制，但尚不能稳定沿线循迹。
- 本轮先核查 V53 的 PWM 最小限幅、左右基础 PWM、启动增力、灰度 X1~X8 位序、误差符号与左右轮修正方向，再生成新的时间戳版本；只编译，不自动烧录。

## 2026-07-16 V54 实现结果

- 全面检查 V53 后确认：灰度权重为 `X1=-3500 ... X8=+3500`，即正误差代表黑线在车体右侧；但原控制使用 `left=base-correction`、`right=base+correction`，会让车辆向左转，修正方向与物理需求相反。这解释了“识别黑线后轨迹变化但不能循迹”。
- 生成 `20260716_151953_v54_gray8_half_speed_encoder_straight_corrected_steering.c`，并同步到活动 `empty.c`。
- 普通循迹 PWM 改为左右310，正常修正限幅100；急弯修正限幅210，允许内侧轮降至较低速度、外侧轮最高约520，仍低于旧版直行速度。
- 白底桥接加入编码器 PID：前进计数继续采用左轮取反、右轮为正；累计路程差、积分差和每帧速度差共同生成限幅且带斜率限制的左右命令。
- 编码器接口保持 `PB12/PB13` 左 A/B、`PB14/PB15` 右 A/B；与灰度地址 `PB5/PB6/PB7`、灰度 OUT `PA7` 无冲突。
- 执行 CCS clean build 和最终增量 build，编译、链接均通过且无警告；按既定规则未烧录。

## 2026-07-16 补充召回：V54 仅小幅提速

- 用户要求在 V54 基础上只把速度稍微提高，禁止改动灰度判断、循迹修正方向、编码器闭环和状态机。
- 本轮按约 10% 提高普通行驶与短时启动增力 PWM，生成独立时间戳版本并只编译、不烧录。

### V55 实现结果

- `g_firmware_version=55`；普通左右 PWM 从310改为340，启动增力从440改为480，启动增力时长仍为8帧。
- 未修改任何 PID 参数、灰度权重、循迹方向、急弯限幅、编码器接口或状态切换条件。
- 时间戳源码与活动 `empty.c` 已同步，CCS build通过且零警告；未烧录。

## 2026-07-16 补充召回：V54/V55 低于实车起转阈值

- 用户现场确认 V54 与 V55 均无法驱动车辆，最后确认能运行的是 V53，但 V53 速度过快。
- 本轮严格只调整速度，按 V53 与 V55 的逐侧 PWM 均值生成下一版：普通左480/右465，启动左620/右600；以当前能运行的 V53 控制结构为准，其余灰度循迹和状态机保持不变。

### V56 实际基底核对与实现

- 编辑前核对发现活动 `empty.c` 已由用户恢复为原始 V53，而不是 V55；因此V56从当前能运行的V53直接复制，未包含V54/V55新增的编码器桥接和循迹符号改动。
- 仅修改速度参数：普通左620/右590改为左480/右465；启动左760/右720改为左620/右600；PWM范围由430~850改为295~705。
- 其余常量、控制函数、灰度扫描与状态机均保持V53原样。CCS build通过且零警告，未烧录。

## 2026-07-16 补充召回：V53/V55 低速侧 2/3 加权

- 用户反馈V56实车速度体感仍与快速版接近，要求尝试“V33/V55 2/3速度均值”。
- 文件核对表明仓库V33是电机禁用的红外极性测试版，没有有效行驶PWM，因此按用户实际意图解释为V53与V55。
- 为继续降速但避开V55不能起转的问题，本轮采用 `新值=(V53+2*V55)/3`，即V55低速参数占2/3、V53可运行参数占1/3；只修改速度。

### V57 实现结果

- 普通PWM：左433、右423；启动增力：左573、右560；PWM下限250、上限657。
- 除版本号和上述速度参数外，源码与V56/V53控制结构一致；CCS build通过且零警告，未烧录。

## 2026-07-16 补充召回：V57 循迹方向相反且修正过强

- 用户实车确认V57能够运动，但碰到黑线后的转向修正完全相反，同时修正幅度明显过大。
- 根因与此前V53审查一致：正误差代表黑线位于右侧，旧公式却降低左轮、提高右轮，导致车辆向错误方向转动。
- 下一版保持V57全部速度参数不变，只反转循迹差速符号，并将普通/急弯修正强度降低约一半。

### V58 实现结果

- 速度参数完整保持V57：普通左433/右423，启动左573/右560，PWM范围250~657。
- 普通循迹改为 `left=base+correction`、`right=base-correction`；急弯和丢线找回分支同步反转，避免只修普通分支造成模式切换时方向再次相反。
- 普通修正强度降低为除数36、限幅70；急弯/丢线降低为除数14、限幅180。
- 与V57的源码差异仅包含版本号、四个修正参数和两处分支的左右符号；CCS build通过且零警告，未烧录。

## 2026-07-17 补充召回：V58 出弯后持续右转

- 用户确认V58弯道循迹已经基本正常，但出弯后不能恢复直线，会继续偏右并形成圆圈。
- 代码层面存在两个独立诱因：普通基础PWM仍为左433/右423，本身带有右转偏置；丢线找回会持续使用弯道最后一次非零误差，右弯后短暂丢线时会继续强制右转。
- 下一版保持有效黑线状态下的普通/急弯参数不变，只处理直行基础对称、中心重捕后清除转弯记忆，以及丢线时间增长后的转向衰减。

### V59 实现结果

- 左右普通基础PWM统一为428，启动增力统一为566，均取原左右平均值，不改变整体速度等级。
- 新增中心重捕确认：mask包含X4/X5且绝对误差不超过600，连续2帧后将最后非零误差清零。
- 新增仅作用于完全丢线状态的分级限幅：0~10帧180、11~25帧100、25帧后45；有效线弯道参数保持V58原值。
- 新增Watch变量 `g_line_center_confirm_count` 与 `g_line_lost_correction_limit`，供架空或短距离静态调试使用。
- CCS build通过且零警告，未烧录。

## 2026-07-17 V59 现场复测仍持续右转

- 用户反馈V59没有解决出弯后右转圆圈问题。
- V59已经消除固定左右PWM差，并对丢线找回进行了分级衰减；现象仍不变，当前不能继续仅凭“右转”判断是机械差速、有效线误差，还是丢线找回。
- 下一步利用现有PA15灯语义做无需Watch的分流诊断：右转圆圈时PA15常亮表示灰度仍被判定为有效线；PA15约100ms节拍闪烁表示已经完全丢线并处于 `g_turn_mode=2`。
- 在得到该现象前不修改弯道参数，避免破坏已经正常的弯道循迹。

## 2026-07-17 MPU6050 用于出弯航向稳定的可行性

- 用户确认手头有MPU6050，询问能否解决出弯后持续右转问题。
- 结论：MPU6050的Z轴陀螺仪可以直接测量车身偏航角速度，适合做出弯角速度抑制、短时航向保持和无线直线桥接辅助；不能替代灰度传感器判断赛道位置，也不能单独提供长期无漂移绝对航向。
- 推荐融合结构：弯道继续由灰度控制；中心重捕后记录当前短时航向；直线阶段使用灰度位置误差+陀螺仪航向/角速度修正；出弯短暂丢线时优先保持航向，不再沿最后弯道误差持续转圈。
- 实施前先做独立MPU6050测试：验证WHO_AM_I、静止零偏、Z轴旋转方向和电机运行时噪声，再接入底盘控制。

## 2026-07-17 MPU6050 实物确认

- 用户提供模块正反面照片，确认是常见MPU-6050/GY-521类八针板，丝印依次为 `VCC/GND/SCL/SDA/XDA/XCL/AD0/INT`。
- 照片可见板载稳压器和SCL/SDA附近两颗 `103` 上拉电阻。为避免任何5V上拉风险，首次测试仍采用模块VCC接3.3V，并确保LaunchPad J19/J20不把I2C总线上拉到5V。
- 推荐I2C0接口：`PA0 -> SDA`、`PA1 -> SCL`。禁止使用PB2/PB3，因为它们已连接TB6612的BIN1/BIN2。
- 首测仅连接VCC、GND、SDA、SCL和AD0；AD0接GND固定地址0x68，XDA/XCL/INT悬空。

## 2026-07-17 MPU6050 安装位置与独立测试实施

- 用户照片显示MPU6050模块平面基本与车身底板平行，Z轴方向适合测量车辆水平偏航；当前只在一侧固定，静态测试可用，正式跑车前需增加第二固定点或硬质双面胶，避免线束和底盘振动带动模块摆动。
- 本轮生成电机禁用的MPU6050测试程序：I2C0使用PA0/PA1，读取WHO_AM_I，唤醒传感器，配置Z轴陀螺仪，静止标定零偏，并持续输出Z轴原始值、角速度和积分航向到Watch变量。
- 程序必须在I2C断线/NACK时超时返回，禁止因模块未接好而永久卡死；PA15显示通信/标定状态。只编译，不自动烧录。

### V60 实现结果

- 生成 `20260717_111641_v60_mpu6050_motor_off_test.c` 并替换活动 `empty.c`。
- 直接使用DriverLib初始化I2C0/PA0/PA1，无需占用PB2/PB3，也无需修改项目SysConfig；读写函数均带超时和FIFO恢复。
- 状态：`g_mpu_state=0`初始化、1身份通过、2标定、3运行、4错误；通信正常时 `g_mpu_who_am_i=0x68`、`g_mpu_link_ok=1`。
- Watch变量包含三轴原始陀螺仪、Z轴零偏、校正值、mdps角速度、mdeg积分航向、采样计数和I2C错误计数。
- PA15：身份/配置失败时100ms快闪；标定期间交替闪；正常运行时每秒短亮约100ms。
- 执行CCS clean build，编译和链接通过且零警告；未烧录。

## 2026-07-17 V60 首次WHO_AM_I实测

- Watch结果：`g_mpu_state=4`、`g_mpu_error=2`、`g_mpu_link_ok=0`、`g_mpu_who_am_i=112(0x70)`、`g_i2c_error_count=0`。
- 结论：PA0/PA1 I2C通信和接线已经成功，失败仅来自程序将WHO_AM_I严格限定为0x68；0x70对应MPU6500兼容芯片身份值，模块板丝印与实际芯片型号不完全一致。
- 下一版接受0x68和0x70两种身份，并增加设备类型Watch变量；MPU6500的本次所用电源、陀螺仪输出和量程配置寄存器与MPU6050兼容，无需改变接线。

### V61 实现结果

- 身份判断改为接受0x68和0x70，0x70设置 `g_mpu_device_type=2` 后继续执行复位、配置、标定和连续采样。
- 接线与跳帽均不改；CCS build通过且零警告，未烧录。

## 2026-07-17 V61 静止录屏验证

- 用户提供约13.8秒Watch录屏。观察到标定阶段 `g_mpu_state=2`，随后稳定进入3；`g_mpu_error=0`、`g_mpu_link_ok=1`、WHO_AM_I=112、I2C错误计数始终为0。
- Z轴零偏稳定为约55 raw；静止校正值约-4~+5 raw，对应角速度约-61~+76 mdps。
- 积分航向约从0漂到121 mdeg，即十余秒约0.12度，静止性能足够用于短时出弯航向保持。
- 录屏未出现明显手动旋转，尚不能确定模块当前安装方向下“左转/右转”对应的Z轴正负号；下一步需分别手动旋转约90度并观察峰值与积分符号。

## 2026-07-17 V61 左右旋转录屏验证

- 用户按“向左、回正、向右、再回正”手动转动车辆并提供约13.9秒录屏。
- 当前安装方向下：左转Z轴角速度为正，右转为负。左转录得约+47.4deg/s并积分到约+82deg；回正后约+8deg；右转峰值约-66.7deg/s并到约-56deg；再次回正后约+6.9deg。
- 全程 `g_mpu_state=3`、`g_mpu_error=0`、`g_mpu_link_ok=1`、`g_i2c_error_count=0`，角速度方向、量级和总线稳定性均满足融合控制条件。
- 下一版以V59循迹为基底：有效弯道保持灰度控制；中心重捕后的短窗口加入Z轴角速度阻尼和航向保持；长时间丢线后用陀螺仪抑制持续画圆。

### V62 实现结果

- 从时间戳V59恢复灰度循迹基底，加入V61已验证的I2C0/MPU6050-MPU6500兼容驱动、500样本Z轴零偏标定和20ms采样。
- 融合控制：有效急弯禁用MPU修正；中心锁定时启用角速度阻尼；从转弯/丢线回到中心后记录当前航向并保持30帧；丢线超过10帧后启用角速度阻尼。
- Z轴角速度修正除数1500、限幅50；短时航向误差除数1000、限幅30；MPU总修正限幅65，属于保守首测参数。
- 新增Watch：`g_mpu_link_ok`、`g_gyro_z_mdps`、`g_yaw_mdeg`、`g_turn_exit_hold_count`、`g_turn_exit_heading_target_mdeg`、`g_gyro_rate_correction`、`g_gyro_heading_correction`、`g_mpu_control_correction`、`g_total_control_correction`。
- MPU不可用时所有陀螺仪修正自动归零并继续V59控制。执行CCS clean build，编译链接通过且零警告；未烧录。

## 2026-07-17 队友三项建议与赛道视频复核

- 视频和现场观察呈对称故障：右弯丢线后持续右转，左弯丢线后持续左转。该现象直接指向“沿最后非零误差继续同向找线”的丢线策略，而不是单侧电机偏差。
- 建议1中“保存连续3帧灰度历史”有效；但仅按最后X5/X6或X3/X4永久反打不充分，因为同一末端mask既可能是出弯过冲，也可能是急弯转向不足。推荐只在连续两帧同侧、随后全灭的瞬间触发短时反向制动，并由MPU角速度接近0决定结束。
- 建议2“弯道角度提高8%”当前不采用。有效线弯道已基本正常，故障发生在全灭后；提高弯道增益可能加大过冲和丢线概率。仅当后续证据表明黑线持续向X7/X8或X1/X2外移且未进入丢线记忆时再单独提高。
- 建议3“MPU作用不确定”已被实测排除：总线零错误、左正右负、角速度和积分量级正常。MPU不能判断赛道位置，但适合结束反向制动和抑制丢线后的持续自转。
- 下一版V63：保留V62有效线弯道参数，新增三帧mask历史、全灭瞬间的对称反向制动状态、MPU角速度归零退出和最大时长保护。

### V63 实现结果

- 新增三帧 `g_line_mask_history_0/1/2` 与误差历史；全灭第一帧只在前两帧同属右侧0x70或左侧0x0E区域时触发，避免普通噪声全灭反复反打。
- 右侧历史触发修正-50，左侧历史触发修正+50；这是与之前同向找回相反的短时制动，不是长期反向循迹。
- MPU正常时，至少制动3帧并在对应方向角速度回到约3deg/s内停止；最长15帧保护。无MPU时固定8帧后停止。
- 停止制动后清零 `g_line_last_nonzero_error`、记录当前航向并启动V62的30帧短时保持；重新看到黑线则立即退出制动并恢复灰度控制。
- 新增Watch：`g_line_mask_history_0/1/2`、`g_turn_exit_brake_active`、`g_turn_exit_brake_direction`、`g_turn_exit_brake_frame_count`、`g_turn_exit_brake_trigger_count`、`g_turn_exit_brake_stop_reason`。
- CCS build通过且零警告，未烧录。

## 2026-07-17 补充召回：V63反向制动有效但过强

- 用户确认V63明显改善出弯持续转圈，证明“三帧历史+短时反向制动+MPU退出”方向有效；当前剩余问题是出弯回正修正略过大。
- 调参纪律改为单变量法：本轮只降低 `TURN_EXIT_BRAKE_CORR_TICKS`，不同时修改触发mask、最短/最长帧数、MPU角速度停止阈值、弯道增益或基础速度。
- 首次把制动力50降到35（降低30%）；后续依据“仍过冲/回正不足/持续过久”分别决定调整力度、停止阈值或时长，禁止混调。

## 2026-07-17 X6再次常亮且车辆不启动

- 用户中止V64命令行烧录，并反馈当前无论重新烧录与否，灰度X6再次常亮且车辆不动。
- 该组合症状优先归类为硬件/供电/复位/灰度地址线问题，不继续修改循迹参数。
- 八路复用地址中X6对应 `AD2 AD1 AD0=101`；程序一轮扫描结束应停在X8的111。若PB6->AD1断开或被拉低，原本111会变成101，恰好表现为X6长期被选中。
- 排查顺序：J11 3V3、NRST、灰度模块5V、PA15灯序、PB5/PB6/PB7静态电平、PB4/STBY。重点检查近期安装MPU时是否拉松PB6/AD1或公共GND杜邦线。

### V64 实现与后续调参规则

- V64仅修改版本号和 `TURN_EXIT_BRAKE_CORR_TICKS: 50 -> 35`，源码差异已限定在单一控制维度；CCS build通过且零警告，未烧录。
- 同一电量、同一起点分别至少测试3次左弯和3次右弯；记录出弯是否过冲到反方向、是否仍沿原方向转、恢复直线所需时间。
- 若35仍明显反向过冲：下一步只改35->28。若35不足、仍沿原弯方向：只改35->42。若角度合适但制动拖得太久：保持35，下一轮才调整MPU停止阈值或最短帧数。
- 弯道增益、历史触发mask、基础速度和航向保持参数在完成制动力标定前全部冻结。
## 2026-07-17 补充召回：X6 运行中常亮并导致底盘禁止启动

- 当前活动源码为 V64：`C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`，已编译通过；用户已取消命令行烧录，因此实车固件版本仍以用户最后一次手动烧录为准。
- 最新现象：车辆运行一段时间后 X6 再次常亮，重新烧录无效，随后小车不动。当前先停止调参和烧录，按供电、复位、灰度复用选通线、TB6612 STBY 的顺序排查。
- 代码确认：灰度地址为 `PB5/PB6/PB7 -> AD0/AD1/AD2`，X6 地址为 `101`。若 `PB6 -> AD1` 断路或被拉低，程序选择 X8 的 `111` 会在模块端退化为 X6 的 `101`。
- V64 上电会采集 50 轮白底基线；若八路不统一，设置 `STOP_REASON_BASELINE_INVALID`、拉低 STBY 并永久停车。因此“X6 异常 + 电机不动”可由同一硬件故障链解释，重新烧录不能排除该问题。
- 下一步只进行无 USB、整车供电状态下的电压和连续性测量，不修改控制代码。
## 2026-07-17 X6 与底盘不启动拆分定位

- 用户实测 `NRST-GND` 约为 0.8V，PA15 完全不闪，说明目标 MCU 被压在复位区；程序未运行，TB6612 STBY 不会拉高，因此底盘不动。
- X6 在轻微提高灰度板高度后恢复正常，说明本次 X6 常亮主要来自安装高度/光学阈值，而不是固件或 PB6 地址线故障。
- 处理顺序：断电后移除 J101 的 NRST 跳线帽，保持外部 LM2596 供电方案下的 J101 3V3 跳线帽断开；重新上电后分别测目标侧 NRST 与 J11/3V3，均应约为 3.3V，再观察 PA15。
## 2026-07-17 NRST恢复后 PA15 仍为低电平

- 最新实测：目标侧 `NRST-GND=3.3V`、`J11/3V3-GND=3.3V`，但 PA15 始终为 0V，底盘不运行。
- V64 启动顺序确认：`SYSCFG_DL_init -> 输出初始化 -> 灰度初始化 -> PWM初始化 -> I2C初始化 -> MPU初始化/校准 -> PA15启动提示`。因此 PA15 不亮可能是固件未执行，也可能是启动阶段卡在 MPU/I2C，不能继续归因于复位电压。
- 下一步采用无代码改动的最小隔离：断电拔除 MPU 的 SDA/SCL/VCC/GND，重新独立上电观察 PA15；若恢复，定位到 MPU供电/接线/I2C总线，若仍不恢复，再完全隔离 XDS110 的 SWDIO/SWCLK 并检查固件执行。
## 2026-07-17 CCS 截图确认固件已越过 MPU 失败分支

- 用户完全隔离 MPU、NRST、SWDIO、SWCLK 后 PA15 仍为0；重新接入调试器截图中 `g_mpu_state=4`、`g_mpu_error=1`、`g_mpu_link_ok=0`、`g_i2c_error_count=1`。
- 这些值证明 CPU 已执行过 MPU 初始化失败和 V59 降级分支，排除“程序永久卡死在 MPU/I2C 初始化循环”的判断。
- CCS 当前显示暂停按钮，代表目标正在运行；普通 Watch 在运行态不保证实时刷新。下一步必须运行数秒后点击暂停，读取 Call Stack、PC所在行以及 `g_phase/g_stop_reason/g_gray_baseline_* /g_led_state/g_run_frame/PWM`，区分基线保护、运行完成、HardFault和PA15物理输出故障。
## 2026-07-17 CCS 双工程错位与调试配置修复

- 工作区同时存在两套独立 CCS 工程。当前 V64 位于 `C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`，产物 `Debug\ele-car.out` 更新时间为2026-07-17；可正常点击调试的另一项实际对应 `empty_LP_MSPM0G3507_nortos_ticlang\main.c`，其源码是 V48，产物最后更新时间为2026-07-10。
- 两工程的 `MSPM0G3507.ccxml` 完全相同，且两份 `.out` 均存在；因此图二能调试只证明旧V48工程有效，不能证明当前V64已加载。图一问题定位到 `ele-car` 的 CCS 调试配置/工作区缓存，而不是XDS110或芯片连接。
- 已备份原 `.theia/launch.json` 为 `launch_20260717_144445_backup.json`，并新增 `ele-car-current-clean` 调试入口，仍明确绑定 `projectInfo.name=ele-car` 和 `resourceId=/ele-car`，不覆盖原配置或任一源码。
- 后续必须确认调试下拉框选择 `ele-car-current-clean (ELECTRIC)`；不可选择旧的 `empty_LP_MSPM0G3507_nortos_ticlang` 作为当前版本。
## 2026-07-17 新调试入口现场确认

- 用户截图确认已选择 `ele-car-current-clean (ELECTRIC)`，调试工具栏显示暂停/复位/停止按钮，说明新会话已成功启动，原“点击ele-car无反应”问题已由新入口绕过。
- Watch 中 `g_mpu_state=4`、`g_mpu_error=1` 等V64专有符号可正常读取，证明当前加载的是 `ele-car.out`，不是旧V48。
- `g_firmware_version` 显示539000832且地址为0，静态检查发现该未引用全局变量未进入最终map/symbol表；539000832即`0x20208000`栈末地址，不是固件版本。后续不得用该Watch判断映像，除非代码显式保留/引用该变量。
## 2026-07-17 旧V48能响能转而当前V64不启动的代码原因

- 用户切回 `empty_LP_MSPM0G3507_nortos_ticlang` 后蜂鸣器能响且电机能转，证明蜂鸣器、TB6612、电机和基本下载链路可用。
- 静态对比确认：旧工程是2026-07-10的V48，`main()`上电调用`buzzer_start_beeps(1U)`，随后无论直接入线还是桥接都会调用`motors_forward()`；STBY初始化也先置HIGH。它没有V64的八路灰度白底一致性硬门禁。
- 当前V64明确将低触发蜂鸣器保持HIGH静音；先`motors_stop()`，采集50轮灰度基线，只有baseline mask为`0x00`或`0xFF`才继续。任何混合基线都会设置`g_phase=4,g_stop_reason=2`并永久关闭STBY。
- 结合X6刚刚因安装高度出现异常，当前最可能故障是V64基线一致性保护被触发，而不是ele-car工程不能下载。后续应将判据从“八路电平必须完全相同”改为“每一路自身基线必须稳定”，允许混合基线并对少量异常通道降级，同时恢复明确蜂鸣状态提示。
## 2026-07-17 V65 灰度降级标定与启动诊断

- 实施前发现活动 `ELECTRIC\ele-car\empty.c` 在14:56被替换为历史V62且哈希完全一致。为保留已经实车验证更好的三帧历史+MPU出弯制动，本版明确以历史V64为基底生成新文件，不覆盖V62/V63/V64。
- 当前活动源码：`C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`；时间戳源码：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260717_150653_v65_gray_baseline_degraded_boot_diag.c`；两者SHA256一致。
- V65改动：PA15在MPU/灰度/PWM初始化前闪3次，第一次闪烁同步低有效蜂鸣1声；增加`g_boot_stage`。灰度50轮标定改为逐通道稳定性，单路高票数<=5或>=45视为稳定。
- 启动准入：至少7路稳定，且X4/X5必须同时稳定；允许白底baseline mask混合，不再要求只能`0x00/0xFF`。唯一不稳定通道从运行mask中屏蔽；全部可用通道同时变化显式判异常。
- 蜂鸣语义：上电1声；基线完全正常2声；可降级使用3声；不可用长响约0.5秒并停车。`g_boot_stage=8`表示电机已使能，`0xE1`表示基线不可用。
- 已执行clean build和修正后的增量build，编译链接通过且零警告；最终map/nm确认版本、boot stage和全部baseline诊断变量均进入`ele-car.out`。独立代码审查初次发现六通道降级安全风险，已收紧至7路+中心双通道并复审通过。
- 按用户长期规则未自动烧录，未生成readme。
## 2026-07-17 灰度板经GPIO反向灌入3.3V与LM2596的硬件根因

- 用户现场确认：灰度传感器支路电流可沿信号线倒灌开发板3.3V电源轨，并继续到LM2596输出端。
- 根因路径：灰度板使用5V供电，`OUT`直接连接MSPM0的PA7；TI MSPM0G3507数据手册将PA7列为Standard GPIO，而普通GPIO输入绝对最大值仅为`VDD+0.3V`，仅特定ODIO为5V容忍且fail-safe。灰度OUT若接近5V，会通过PA7内部钳位结构向VDD注入电流；MCU 3.3V掉电或启动较慢时形成明显的寄生供电。
- `PB5/PB6/PB7 -> AD0/AD1/AD2`也跨越3.3V/5V电源域。模块内部若存在5V上拉，或两侧上电顺序不同，同样可能通过地址线注入电流。LM2596简易模块通常不提供输出反向电流隔离，所以其输出端出现被外部抬高的电压并不异常。
- 该链路可统一解释此前无USB时NRST约0.8V、PA15不工作、X6固定、插USB后恢复等“半上电”现象。倒灌本身不等于灰度板已经损坏，但长期无串联限流地钳位会同时威胁灰度板输出级、PA7和MCU电源域。
- 正式修复前禁止将灰度OUT直接重接PA7。优先方案：OUT使用5V到3.3V电平转换或电阻分压；AD0/1/2增加正式电平转换或至少串联限流；确保两路电源同一总开关、公共地并避免灰度5V单独上电。
## 2026-07-17 TURN_EXIT_BRAKE_CORR_TICKS降至0后仍过修正

- 用户提供的V64实测版本已将`TURN_EXIT_BRAKE_CORR_TICKS`从50逐步降至0，但出弯仍有轻微反向过修正。
- 代码核查：该参数为0时，`turn_exit_brake_handle()`仍会占用至少3帧并输出左右等PWM；结束后无条件设置`g_turn_exit_hold_count=TURN_EXIT_HOLD_FRAMES=30`，随后MPU角速度+航向闭环最多继续约600ms，修正限幅仍可达到`GYRO_TOTAL_CORR_LIMIT=65`。
- 结论：负值会翻转制动方向，变成沿原弯道继续转，不是“比0更弱”。下一轮应保持该值为0，单独把`TURN_EXIT_HOLD_FRAMES`从30降至0做归因测试；若过修正消失，再从5~10帧逐步恢复，而不是同时调整MPU增益。

## 2026-07-17 V66 出弯后向右修正约5度

- 用户要求在当前实车修正基础上，出弯时再向右修正约5度。为保留现场参数，本版从用户提供且已把`TURN_EXIT_BRAKE_CORR_TICKS`调到0的V64生成，没有从V65启动诊断分支继续。
- 新增目标航向闭环：前级出弯制动正常达到角速度阈值后，记录`target=current_yaw-5000mdeg`；正航向定义为左转，因此减5度对应向右。控制误差为`current-target`，通过限幅比例差速使左轮加速、右轮减速。
- 参数为：目标5000mdeg、容差500mdeg、Kp除数200、最小修正6 ticks、最大修正35 ticks、最长30帧。目标达到或超时后恢复原丢线控制，避免长期锁在右转状态。
- 安全审查最初发现三处高风险：制动超时也会启动右修正、右修正会覆盖已经重捕的黑线、MPU不可用时会以零修正占用恢复过程。三处均已修复：只允许正常制动结束触发；任意有效黑线同帧取消并交还灰度循迹；MPU失败直接回退原V64线控。
- 最终文件：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260717_164118_v66_v64_turn_exit_right_bias_5deg.c`，并同步到`C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`；两者SHA256为`9E1406C824A8667B9E634E642E10656E9067CFC819A0EE1E3B56A5AC0CB2A416`。
- clean/incremental build通过且零警告，`ele-car.map`确认右修正函数和全部调试变量已链接；未自动烧录。
- 实车调参只改一个量：右修正过大先把目标`5000 -> 3000mdeg`；角度合适但动作过猛则把限幅`35 -> 25`；未到目标就超时才考虑把30帧增至40帧。

## 2026-07-17 V66右打转回归与V67归因版本

- 现场反馈：加入出弯后5度右偏的V66开始持续向右打转。无法在架空环境复现完整赛道，但版本差异明确集中在新增右偏状态，因此先进行最小化回退，不继续盲调角度和增益。
- 代码审计发现两条可能叠加的右转链：V66最多执行30帧固定方向右偏；若前级制动超时，旧的`g_line_last_nonzero_error`仍会进入后续丢线找回，可能继续沿原右转方向控制。
- V67设置`TURN_EXIT_RIGHT_BIAS_ENABLE=0`，同时把旧误差清零移动到制动“正常结束或超时结束”的公共路径。该版本用于验证右打转是否由V66新增状态及旧误差续转共同造成。
- 独立复审确认：关闭宏后没有右偏启动路径；hold和旧误差均被清零；灰度重捕可在同一帧恢复线控。复审发现的唯一问题是版本标识仍为V66，已改为`FIRMWARE_VERSION=67`。
- 当前活动文件和时间戳文件SHA256一致：`BDBDEC55C5DD1569FCE2FF53ED42D6AF55CC597AFBD00245D7BCACE90D26E88E`。最终增量编译、链接通过且零警告，未自动烧录。

## 2026-07-17 V67右偏复盘与V68三帧确认实现

- 用户补充关键对照：截图中的V64在出弯后能走直线，仅存在出弯偏早、叠加修正后略偏左；V67则开始向右。代码差异证明V67关闭固定5度右偏时也清除了V64的30帧MPU航向保持，使底盘自身右偏重新暴露。
- 决策：采用“延后出弯判定”而非把`TURN_EXIT_BRAKE_CORR_TICKS`调为负值。该参数已为0，负值只会反转方向；真正仍在工作的修正来自后续航向保持，应先只改变判定时机。
- 不能把原条件从`g_line_lost_count==1`机械改成`==3`：第3个全灭帧到来时，两帧mask历史已经被0覆盖，无法判断此前从左侧还是右侧出弯。
- V68实现候选锁存：首个全灭帧读取`history_1/history_2`并保存方向，随后使用独立计数器累计；连续3帧全灭才确认。若中间重捕任意黑线或出现7~8路同时变化异常，候选立即取消。
- 初次复审发现通用`g_gray_line_valid==0`同时包含0路和大于6路异常，不能等同于“全灭”；已改为严格使用`g_gray_changed_count==0`，不再复用`g_line_lost_count`。最终复审无问题。
- 当前文件：`C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`；时间戳文件：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260717_172944_v68_v64_three_frame_turn_exit_confirm.c`；最终SHA256为`03DFF3ABAC57979DBE40FFB00F30D845FC69E69725954F0DC7A4402CE609C9D7`。编译链接通过且零警告，未自动烧录。

## 2026-07-17 V69 直线极轻微右偏单变量修正

- V68现场表现已收敛到唯一问题：所谓直线存在极轻微右偏。按照小步调参原则，不再修改出弯确认、MPU增益或保持时间。
- 当前差速公式中正correction为左轮加速、右轮减速，对应向右；抵消右偏需要负correction。因此基础PWM由`428/428`改为`427/429`，等效-1 tick且平均速度不变。
- 当前文件与时间戳版本`20260717_174411_v69_v68_minimal_straight_left_trim.c`哈希一致，SHA256为`4C090C1D33CEA5751DFFAD06AA5307E99248F5BDA43086CCC7271E99427D20F7`。编译链接通过且零警告，独立复审通过，未自动烧录。

## 2026-07-17 用户手调V69达到基本循迹后的稳定性瓶颈

- 用户提交最终手调代码，SHA256为`1B787E32D9D9A0AC1441428BDF46017A8ACDA7120E20FDE28D40E1C93F43002C`。相对生成版继续调整为基础PWM425/429、启动563/566、出弯连续全灭5帧、MPU航向保持28帧，制动差速仍为0。
- 实车已能基本完成循迹，出弯后也基本保持直线；剩余随机失败来自出弯触发时刻：同一固定帧阈值受速度、电压、灰度边缘抖动和车辆入弯姿态影响，早一帧/晚一帧会留下不同航向角，累计到下一弯道后偏离。
- 技术判断：出弯早晚本质是航向问题，优先使用MPU局部转角闭环，而非继续增加全灭帧数。编码器适合测`(left+right)/2`局部路程和赛道段落，不适合单独决定车头方向；左右轮里程差可作为陀螺仪的交叉校验。
- 推荐状态机：灰度确认入弯后记录`yaw_entry`并清零本段编码器；弯中灰度主控；全灭只产生候选。仅当局部转角进入该弯标定窗口且编码器里程进入允许窗口时确认出弯；角度不足则继续沿最后弯向，角度过量则立即反向收敛；确认后保持标定目标航向，重新捕获中心线后结束该段并重置局部基准。
- 每个弯道单独标定目标角和里程窗口，来源为多次成功运行的中位数；不使用从上电开始的无限累计yaw或总编码器里程，避免三圈以上漂移累加。

## 2026-07-17 V70标定日志实现

- 读取赛题PDF并核对几何：A-D和B-C为对称半圆，两段理论航向变化均为180度；直线/虚线段长度约100cm。几何目标明确，但实车传感器安装偏置、车身中心到前置灰度板距离和编码器尺度仍需现场数据修正。
- 先将用户附件原样备份为`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260717_205654_v69_user_final_tuned_input.c`，SHA256与附件一致，然后以它替换活动`empty.c`作为V70唯一基底。
- 恢复PB12/PB13左编码器、PB14/PB15右编码器的双沿A相中断计数；本版不计算编码器PID、不修改任何电机输出，只在主循环原20ms节拍读取原子快照。
- 新增16项RAM日志。首次连续2帧看到黑线时建立弯道入口；出弯候选首帧记录yaw和局部左右里程；候选取消时累计噪声次数；连续5帧全灭确认后记录最终局部yaw和里程，并等待下一段黑线建立新入口。
- 初次独立审查发现严重问题：沿用旧版本`GPIOB_IRQHandler`，但当前MSPM0G3507启动文件的GPIOB IRQ1入口实际为`GROUP1_IRQHandler`；若不修正，首个编码器边沿会进入`Default_Handler`并冻结车辆。已改名并重新链接，map确认`GROUP1_IRQHandler=0x1821`、`Default_Handler=0x13e3`，两者已分离。
- 最终复审确认PB12~PB15无现有冲突、方向判定与状态清除正确、日志数组无越界、候选时序不写回控制量。V70时间戳文件与活动源码SHA256均为`AFB2CA5C878EADE10E13E00C2602CD534E5EDC8B4E801447BFF45A7532C88CCB`；编译链接通过，未烧录。

## 2026-07-17 V70实车失败与V71紧急回退

- 用户烧录V70后确认：出发初始正常，但加速行驶逐渐轻微右偏；进入第一弯时已经错过弯道起点；第一次出弯转角过大并偏右；之后直行右偏明显，连一圈都无法完成。V70作为采集版反而破坏原控制，不能继续采四圈数据。
- V70相对手调V69未改PWM和控制参数，主要运行时差异是PB12/PB14双边沿GPIO中断及日志。当前控制大量使用“帧数×假定20ms”：启动20帧、全灭确认5帧、航向保持28帧；MPU yaw积分同样固定按20ms计算，不读取真实计时器。高频编码器ISR会延长每帧真实时间，使按帧动作持续更久，并导致gyro积分时间尺度偏小，现象与实车一致。
- 立即停止在V70上调参。活动`empty.c`从用户原始手调备份`20260717_205654_v69_user_final_tuned_input.c`恢复，只将注释与固件版本改为V71；no-index diff确认仅两行差异。
- V71时间戳文件：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260717_215527_v71_restore_user_v69_no_encoder_logger.c`，SHA256为`FFA15450DCEBE00DE1A281AF3A2DFEEBAF3BDFED3757D8BDFF4E10DC18C9540B`。clean build通过，复审确认V70编码器、日志和中断代码全部移除，未烧录。
- 后续角度标定不得再次启用高频软件GPIO编码器ISR。优先方案是先做仅MPU的低开销事件日志；编码器改用硬件QEI/计数器，或先实测中断负载并把主控制迁移到硬件定时器固定周期后再接入。

## 2026-07-17 V71中心判定与弯道单侧修正审计

- 用户观察到车辆在弯道中主要表现为先向左越过黑线、随后再向右扳回，询问是否存在固定右偏及其对出弯的影响。
- 代码核对：X1~X8权重为-3500到+3500；X4+X5对应-500/+500，平均为0。连续2帧中心后清零最后非零误差。正常线控correction为0，但基础PWM425/429本身等效轻微左偏，并非左右轮相同。
- 更关键的是`center_locked`无条件启用`mpu_compute_control_correction(false)`。安装方向中右转Z角速度为负，角速度项也为负，经`left=base+corr,right=base-corr`转换后产生左向修正，因此在半圆弯道X4/X5居中时会抑制正确的右转角速度。
- 结果是典型无曲率前馈P控制：居中时沿切线走，黑线逐渐落到右侧X5/X6后，正误差才产生右转修正；回到中心后阻尼再次抑制转弯。车辆长期只在黑线一侧摆动，不需要出现对称的“修正过度后反向回正”。
- 出弯前若最后有效误差为正，连续全灭确认的前4帧仍进入`sharp_turn`并沿正误差继续右转，第5帧才启动`BRAKE_CORR=0`的出弯状态。故当前5帧参数不仅是滤波时间，也直接增加出弯右转量。
- 下一轮优化重点不应再调基础PWM：应区分直线和弯道，弯道中心时关闭直线角速度阻尼并加入受限曲率前馈；全灭阶段由局部目标角决定是否继续转，不再固定保持最后误差5帧。

## 2026-07-17 V72固定赛道半圆状态机实现

- 按赛题几何和用户实车现象开始大改：不再把X4/X5居中等同于直线，也不再把出弯瞬间当前yaw当正确航向。固定A-B-C-D-A方向的两段半圆均按顺时针180度处理。
- 新增curve/gap状态：起步桥接重捕线后进入curve并记录入口yaw；弯道有效线期间右转前馈从0按2 ticks/帧平滑增加到12；curve期间禁止中心gyro damping和长丢线gyro damping，避免抑制合法右转。
- 出弯候选仍要求严格全灭5帧，但增加局部角门控。角度不足165度时保留小幅右弯前馈；达到165度后把旧误差和前馈从本帧控制中清除，防止5帧防抖继续造成过量右转。出弯完成后目标固定为`entry_yaw-180000mdeg`。
- gap状态持续使用MPU角速度+航向闭环，不受原28帧结束限制；进入下一弯前至少经过25帧且灰度中心稳定，再连续确认2帧。确认前即使擦到杂线，也只保持航向，不让灰度误差参与转向。
- 初次审查发现三项HIGH：合法gap仍会被100帧普通丢线保护停车；运行中MPU掉线未清理新状态；任意有效线2帧即可误重置entry yaw。已分别增加curve 180帧/gap 300帧独立watchdog、MPU 1到0原子退化、gap最短25帧+中心锁定+2帧确认。
- 自审另补：若弯末X4/X5恰好居中，最后误差为0，角度不足时必须继续使用曲率前馈，否则无法从提前全灭继续转到165度。最终复审确认角度符号、feedforward方向、状态进入退出和退化路径正确。
- 当前文件与`20260717_224008_v72_fixed_course_semicircle_gap_heading_fusion.c`SHA256一致：`AB38CAE4C2358CE8846ECC782821AC26C64A50641C5849EACB4F8A789ED68E0D`。clean/incremental build通过，未自动烧录。
## 2026-07-18 MPU掉线补回后的异常分析

- 用户反馈：此前MPU一直有一根线脱落，重新插好后，A点无线上电即出现左偏；进入循迹后无法正常贴线；顺时针右弯中仍持续左偏。
- 静态审计当前`empty.c`：A点`PHASE_START_BRIDGE`仅输出`566/566`后切换`429/429`，虽读取MPU但不使用MPU修正。因此A点偏航是接线/供电/电机侧变化或基础差速问题，不是当前MPU控制公式直接输出。
- 进入循迹时若MPU在线，`curve_start()`应设置`g_curve_active=1`；该状态会禁用中心锁定时的角速度阻尼，并加入正向右弯前馈。右弯持续左偏与设计不符，需用架空Watch确认`g_mpu_link_ok`、`g_gyro_z_mdps`方向、`g_curve_active`和`g_mpu_control_correction`，并核对烧录产物确为V72。
- 最小隔离顺序：完整断开MPU复测；仅接VCC/GND且断开SDA/SCL复测；最后按`PA0=SDA、PA1=SCL、3.3V=VCC、公共GND`完整接入。禁止在MPU未供电时单独连接SDA/SCL，防止信号线倒灌。
## 2026-07-18 V72始终停留启动桥接状态

- 用户静态Watch确认：模拟进入黑线后`g_phase`始终为8，未进入2；MPU身份、方向等其余检查正常。
- 因此此前“进入循迹后右弯持续左偏”的描述应重新解释为：车辆实际上一直执行`PHASE_START_BRIDGE`的固定左右PWM，从未执行灰度循迹、`curve_start()`或弯道前馈。
- V72唯一切换条件是`g_gray_line_valid`连续2帧有效。下一步需同时记录白底和仅压X4/X5黑线时的`g_gray_white_baseline_mask`、`g_gray_raw_high_mask`、`g_gray_changed_from_white_mask`、`g_gray_changed_count`、`g_gray_line_valid`、`g_line_entry_confirm_count`和`g_gray_unstable_mask`，确定故障发生在原始采样、白底异或、有效线宽过滤还是连续帧确认层。
## 2026-07-18 灰度输入被确认固定为低电平

- 用户反馈除`g_start_bridge_frame_count`持续增加外，灰度基准、原始mask、变化mask、有效线、入线计数和不稳定mask均为0。
- 结论：主循环和桥接计数正常，`PA7/GRAY_OUT`的9次采样稳定为低，不是入线确认条件过严。当前故障位于灰度板5V/GND、OUT到PA7、PB5/PB6/PB7选通线或新接MPU线路的物理冲突层。
- 正确引脚无软件复用：MPU为PA0/PA1，灰度为PB5/PB6/PB7/PA7。若完整断开MPU后灰度恢复，说明实物接错针位、共地/供电变化或线束短接；若仍全0，则优先检查灰度供电、OUT连续性和PA7输入。
## 2026-07-18 新灰度板接入前的硬件保护方案

- 用户怀疑旧板OUT输出级损坏，并确认此前OUT已分压，但AD0/AD1/AD2未串联电阻。
- 判断：正常供电条件下，3.3V GPIO直接驱动5V模块数字输入通常不是损坏原因；更应排查电源时序导致的输入倒灌、OUT分压节点误接、公共地缺失、5V过压/反接和带电插拔。
- 新板采用分阶段接入：模块单独5V测试；PB5/PB6/PB7各串1k~4.7k；OUT使用10k/20k分压并在PA7前再串1k；测得采样点最大电压低于3.6V后才连接PA7；先烧录电机禁用灰度测试，再接电机和MPU。
## 2026-07-18 新灰度板V50白底测试通过

- 用户更换新YB-MYX05-V1.0并烧录V50电机禁用测试。Watch显示`g_gray_white_baseline_mask=255`、`g_gray_raw_high_mask=255`、`g_gray_changed_from_white_mask=0`、`g_gray_unstable_mask=0`，X1~X8均为1且已显示通道高计数为9。
- 结论：新板在白底下8路输出稳定，PA7与分压链路已能读取高电平；旧板在同类条件下全0，进一步支持旧板OUT/选通输出级损坏。
- Watch中的MPU、phase、curve变量显示`identifier not found`属于正常现象，因为V50隔离测试源码未定义这些变量。下一步逐个压X1/X4/X5/X8黑线，核对raw mask位序和通道寻址。
## 2026-07-18 AD0倒灌导致3.3V轨升至4.1V

- 用户实测灰度板连接时LM2596 OUT由3.3V升至4.1V，拔掉AD0立即恢复，复接再次升高。该A/B证据将倒灌路径锁定在灰度板AD0到MSPM0 PB5。
- 原因候选按优先级：接口针序/正反面镜像导致AD0实际接到5V或OUT；模块AD0存在5V上拉；公共地异常。需在模块与MCU完全断开时，单独给模块5V供电并测AD0/AD1/AD2对模块GND的静态电压，同时核对接口丝印和线序。
- 安全动作：立即断开三根AD线，不再使用直接连接；OUT继续保持分压但也暂不接PA7，先确认MSPM0单独供电仍为3.3V并能运行。
## 2026-07-18 AD0倒灌后出现XDS110 SWD Error -615

- CCS报错`JTAG Communication Error: Error -615`，含义是XDS110已被电脑识别，但目标MSPM0没有正确响应SWD头；这不是C源码编译失败。
- 该错误紧随3.3V轨被AD0倒灌抬至4.1V发生，优先按目标供电过压、NRST状态和J101的GND/NRST/SWDIO/SWCLK跳帽排查，不先调整代码或反复烧录。
- 恢复测试必须完全隔离电池、LM2596、灰度板、MPU和TB6612，恢复J101默认的3V3/GND/NRST/SWDIO/SWCLK跳帽，仅用XDS110 USB给目标板供电；确认J11/3V3与NRST均约3.3V后再尝试低速SWD连接。
## 2026-07-20 重写灰度传感器简化Watch测试

- 用户要求重新写灰度测试程序并减少Watch变量。修改前读取到活动`empty.c`为V50，SHA256为`FA7AF7E50660956D28BFCFFA7B2AFA59E26BC7F880A32BC85DBAE3D5F2CFA8DB`，已先做时间戳备份。
- 新V73保留50帧白底自动基准和8路每路9次多数采样，新增`g_seen_black_mask`累计锁存曾经见过的通道、`g_last_black_mask`保留最后一次非零结果、`g_change_count`记录变化次数。
- 安全修正：旧V50把PB11拉低，会触发当前低有效蜂鸣器；V73改为PB11保持HIGH。电机方向、STBY、PWM、舵机均保持禁用，不初始化MPU I2C和OpenMV UART。
- 使用`C:\ti\ccs2050\ccs\utils\bin\gmake.exe`执行clean build，编译链接零报错；map中确认全部推荐Watch符号存在。活动源码和时间戳源码SHA256一致，以`B295ACA75430...`开头，未自动烧录。
## 2026-07-20 白底启动轻微右偏单变量修正

- 现场现象：有线循迹已正常，白底A点开环启动仍轻微右偏。代码核查确认V72桥接阶段不使用编码器或MPU：前20帧固定`563/566`，之后复用循迹基础`425/429`。
- 为保护已验证循迹，本轮新增桥接专用PWM并只扩大右轮相对左轮差值2 ticks，同时保持平均速度基本不变：增力`562/567`，普通桥接`424/430`。正常循迹仍严格使用`425/429`。
- 修改前备份：`20260720_152245_before_v74_start_bridge_trim_v72_backup.c`；新版本：`20260720_152245_v74_start_bridge_slight_left_trim.c`。CCS 20.5 clean build编译链接通过，活动文件与归档哈希一致，未自动烧录。
## 2026-07-20 出弯全灭瞬间左修正实现

- 静态审计确认原V74在首次全灭后的前4帧仍可能使用`g_line_last_nonzero_error`延续右转，第5帧才进入出弯制动；这与“出弯后仍向右偏”一致。
- 新增V75短状态：首次全灭且右侧历史成立时，连续4帧强制`g_line_correction=-18`，即左轮约407、右轮约447，随后交回原状态机。新增Watch：`g_turn_exit_left_nudge_active`、`g_turn_exit_left_nudge_remaining`、`g_turn_exit_left_nudge_trigger_count`，`g_turn_mode=4`表示脉冲正在执行。
- 补充清除`g_line_last_nonzero_error`，避免脉冲结束后旧右转误差复活；黑线重捕、curve start/finish和MPU运行时降级均清理脉冲状态。
- CCS 20.5编译链接通过；显式写入`g_firmware_version=75`并确认版本号与新增变量均进入最终map/out。活动文件与时间戳归档哈希一致，未自动烧录。
## 2026-07-20 V75一轮实测后的参数收敛

- 用户反馈V75方向正确：全灭瞬间出现预期左偏，但左偏角度需略减；白底启动仍右偏。
- 按单变量小步原则保留触发条件和持续时间，只将左脉冲幅度从18降到14；白底桥接左右差值各扩大2 ticks且平均速度基本不变，增力改为561/568，普通桥接改为423/431。
- CCS 20.5 clean build通过，map确认`g_firmware_version`和左脉冲触发计数符号存在；活动文件与V76时间戳归档一致，未自动烧录。
## 2026-07-20 V76实测后继续收敛左修正

- 用户反馈方向仍为左且角度过大，要求在当前基础上向右回一点。先核对活动V76与时间戳归档无差异，排除现场手调遗漏。
- 本轮只改一个参数：左脉冲10 ticks、4帧，输出约由V76的411/443变为415/439；未新增独立右转动作，避免重新引入持续右偏。
- CCS 20.5 clean build通过，版本号77与触发计数变量进入map；活动文件与V77归档哈希一致，未自动烧录。
## 2026-07-20 V78方向重新平衡

- 根据用户最新实车目标，不再继续把全灭动作理解成“减弱左修正”，而是明确改为小幅右修正；正correction通过`left=base+corr,right=base-corr`产生右转。
- 全灭脉冲使用+4 ticks、4帧，触发条件、旧误差清零和黑线重捕取消逻辑不变；白底左右差值再扩大2 ticks。正常循迹425/429、弯道前馈和MPU状态机不变。
- CCS 20.5 clean build通过；map确认版本号78与重命名后的`g_turn_exit_nudge_*`符号存在，活动源码与时间戳归档哈希一致，未自动烧录。
## 2026-07-20 回退V78

- 用户要求回退到上一版。使用已验证时间戳文件`20260720_155947_v77_reduce_exit_left_nudge.c`直接恢复活动源码，并新增本次恢复时间戳副本。
- 三份文件哈希一致，证明不是人工反向修改，而是完整恢复V77；CCS 20.5 clean build通过，未自动烧录。
## 2026-07-20 XDS110 Error -260只读枚举结果

- CCS报`CS_DAP_0 Error -260`，本机使用CCS 20.5自带`xdsdfu.exe -e`只读扫描，结果为`Found 0 devices`；Windows已连接设备枚举中也未发现XDS110、Texas Instruments、CMSIS-DAP或对应VID。
- 结论：当前问题发生在USB枚举/XDS110探针本体层，早于SWD目标连接；降低TCLK、修改程序、调整NRST或更新XDS固件目前均无效，因为DFU工具没有识别到设备。
- 下一步必须物理隔离目标侧后复测XDS：断开全部外部电源和模块，拔掉J101的3V3/GND/NRST/SWDIO/SWCLK使XDS110与目标完全隔离，只给XDS110插USB；更换确认可传数据的USB线和电脑USB口。若仍0设备，优先判断USB接口/数据线或板载XDS110硬件损坏。
## 2026-07-20 V73灰度测试再次固定全低

- 用户截图显示`g_test_state=2`、`g_scan_count=1716`持续增长，但`g_white_mask/g_raw_mask/g_black_mask/g_seen_black_mask/g_change_count`均为0，`g_unstable_mask=0`。
- 工具核对活动`empty.c`为V73且SHA256与原归档一致，排除烧错测试源码。该数据代表PA7每路9次采样均稳定为低；state2只表示8路一致，不表示硬件正常。
- 新板此前白底正常值为255，现在变0；结合AD0连接导致3.3V轨升至4.1V，判断跨5V/3.3V电源域倒灌、地址线隔离缺失、OUT分压/接触或PA7受损是“时好时坏”的主因。继续换灰度板而不修正接口会重复故障。
- 下一步用PA7独立强制高低测试分割故障：断开传感器OUT，PA7经10k接3.3V应使raw mask=255，接GND应为0；通过则查模块OUT/分压，失败则查PA7/引脚损坏。
## 2026-07-20 灰度板换电源后仍约一分钟卡高

- 用户先将灰度板从TB6612 5V改接现有LM2596，短暂恢复后再次无响应；随后用独立7.4V电池降压单独供电，约一分钟后再次故障。故障时模块侧OUT-GND稳定为4.93V。
- 该证据排除“灰度板单纯掉电”：OUT被稳定保持在5V高电平。结合AD0曾将MSPM0 3.3V轨抬到4.1V，根因仍指向5V灰度板地址输入与3.3V GPIO直接连接导致的电平阈值不兼容/倒灌，或复用输出级已受损。
- 若板上为HC/CD4051类5V复用器，3.3V高电平不一定满足保证的VIH，可能随温升、电源和噪声出现“先工作后失效”。正式修复必须用74HCT125/74AHCT125等3.3V输入、5V输出缓冲器驱动AD0/1/2，并保持公共地；不能继续靠更换电池验证。
- 还需核对供电域：单个LM2596不能同时为J11/3V3提供3.3V并为OpenMV/灰度5V端提供5V。MSPM0使用独立3.3V，OpenMV和灰度使用稳定5V，所有GND单点共地。
## 2026-07-20 共地后确认AD0与OUT两条独立倒灌路径

- 用户补接灰度5V降压OUT-与MSPM0 GND后，MSPM0外部3.3V降压输出由3.35V升至约4.35V。单独断AD0后降至约4.05V；单独断OUT基本不变；AD0与OUT同时断开才恢复3.35V。AD1/AD2当前未观察到贡献。
- 电路含义：AD0和OUT均在向MSPM0 3.3V轨注入电流，LM2596类降压不能吸收反向电流，所以输出被抬高。共地不是故障原因，而是建立正确参考后暴露了原有跨域倒灌；未共地时回流路径更不可控。
- OUT路径说明现有10k/20k分压实际接线、阻值或PA7接入点不符合预期，必须在PA7断开时测分压中点：模块OUT=4.93V时应约3.29V。AD0路径说明模块侧AD0存在5V电位/上拉、线序镜像或模块损伤，必须在PB5断开时测AD0对模块GND。
- 当前安全状态：AD0和OUT保持断开；AD1/AD2也暂不视为安全。外部3.3V供电J11时，J101的3V3跳帽必须断开，避免过压继续进入XDS110域。
## 2026-07-20 灰度板所谓AD0接GND出现大电流短路

- 用户在PB5断开、灰度板5V供电测试中，将模块标识/认为的AD0接GND后，全部传感器灯立即熄灭且导线快速升温。
- 该现象不可能来自正常高阻地址输入，说明实物针脚识别/正反面镜像/线束映射有误，所谓AD0实际接近5V电源；或模块内部AD0到5V已低阻损坏。当前按电源短路处理，禁止再次上电或继续跳线试探。
- 后续只允许断电电阻/连续性检查：确认5V-GND、所谓AD0-5V、所谓AD0-GND阻值，并拍摄模块丝印与接头同一视角的清晰近照。稳定低于约10欧姆的AD0-5V连接视为短路/错针；在针序确认前不接74HCT125、不接MSPM0。
## 2026-07-20 补充阅读官方《数据读取》资料

- 视觉核对两页接线图和表格。模块在丝印正面、X1至X8朝上时，六针排针从左到右明确标为`GND、OUT、AD0、AD1、AD2、5V`；地址真值表为`000->CH1`至`111->CH8`。
- 官方示例接线为`AD0->PA14`、`AD1->PA15`、`AD2->PA16`、`OUT->PA17`，说明正常未损坏模块的地址输入可由MSPM0 3.3V GPIO直接驱动。由此修正此前基于异常实测提出的“地址线必须74HCT125”结论：隔离器可作为增强保护，但不是官方接口成立的先决条件。
- 实物异常仍不能忽略：所谓AD0接地发热与官方电路完全矛盾，故当前不是选软件阈值或地址缓冲器的问题，而是必须先证明手中线束每根线实际对应哪个排针，并排除模块短路。
- 官方线束照片只展示XH2.54转6pin杜邦线，未给出可安全依赖的颜色对照；不得按红黑或其他颜色猜针序。
- OUT由5V域CD4051输出；鉴于现场高电平4.93V，保持MCU侧分压/电平转换硬门禁。PA7断开时先测OUT与分压中点，分别应约4.93V和3.29V。
- PDF第5至6页明确：某个Xn指示灯亮时，对应读取值为1；示例`Grayscale_Sensor_Read_All()`按0至7遍历，`_select_channel()`写AD0/1/2后延时50us再读取OUT，主循环示例间隔30ms。该时序将用于针序和电气链路修复后的测试程序复核。
## 2026-07-20 旧灰度板AD0-OUT短路事后分析

- 用户进一步测得旧板只有`AD0-OUT`短路，其他端子未见异常。该结果解释了此前把所谓AD0接地后全灯熄灭、导线发热：接地动作可能经内部短路同时拉低OUT及当前选中传感器输出。
- 下载并核对TI官方`CD4051B`和`MSPM0G3507`数据手册。CD4051B控制/信号端越轨不得超过电源轨0.5V，输入钳位电流限值±10mA；TI还专门警告分离电源驱动VDD和信号时，上下电必须避免持续钳位电流。MSPM0普通GPIO只允许至`VDD+0.3V`，单引脚支持二极管注入电流±2mA，VDD绝对最大4.1V。
- 现场已经观察到OUT=4.93V、接信号后3.3V轨升至4.35V，因此至少发生过超过MCU绝对额定值的倒灌。未共地阶段与多次带电改线又会让信号线承担电源回流，构成CD4051锁存/击穿条件。
- 根因置信度排序：1）OUT分压缺失、接错或失效造成5V注入；2）不同电源未共地及上电时序/热插拔造成保护结构持续导通；3）3.3V地址高电平低于CD4051B在5V下3.5V保证阈值，导致温升后选通不稳，但它更可能是功能故障的促进因素而非单独烧毁原因；4）线束或焊锡物理短路，需要与新板断电阻值对照排除。
- 后续换板采用分阶段验收：旧新板断电阻值对照；仅5V/GND上电；地址线串1k逐根接入并测端电压；OUT先离线验证10k/20k分压约3.29V；最后才连接PA7。所有插拔必须断电，所有电源必须先共地。
## 2026-07-20 V79起步桥接陀螺仪闭环修改前补充回忆

- 当前活动`C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`与`20260720_161523_restore_v77_after_v78_test.c`的SHA256一致，确认基底为V77，不是灰度隔离测试版。
- V77在`PHASE_START_BRIDGE`中虽然每20ms调用`mpu_update_sample()`，但电机只使用固定`561/568`起步增力和`423/431`桥接PWM，没有调用任何MPU修正；因此A点白底直行完全依赖静态左右差值。
- 本轮目标仅为A点起步桥接增加航向锁定：桥接开始记录当前yaw，叠加角速度阻尼与航向误差修正，设置独立限幅和每帧斜率限制；MPU失联时无条件退化为V77固定PWM。循迹、弯道、出弯、灰度和任务逻辑不修改。
- 用户既定要求继续保持：生成时间戳源码，只做CCS编译验证，不自动烧录，不生成readme。
## 2026-07-20 V79实施与验证结果

- 新增`start_bridge_compute_mpu_correction()`，单独计算A点桥接的角速度阻尼、航向误差和斜率受限输出，不复用弯道控制状态，避免改变V77已有循迹表现。
- 新增Watch变量：`g_start_bridge_heading_active`、`g_start_bridge_heading_target_mdeg`、`g_start_bridge_heading_error_mdeg`、`g_start_bridge_gyro_rate_correction`、`g_start_bridge_heading_correction`、`g_start_bridge_mpu_correction`、`g_start_bridge_left_cmd_ticks`、`g_start_bridge_right_cmd_ticks`。
- 桥接每帧先更新MPU，再根据当前处于增力阶段或普通阶段选择基础PWM，最后叠加闭环修正。修正总限幅40 ticks、每20ms最多改变4 ticks；不改变平均速度，只改变左右差速。
- 方向公式复审：安装方向下左转角速度/左偏航向为正，计算出正correction；`left+correction,right-correction`产生右向反修正。右偏时符号完全相反。
- 使用CCS 20.5生成的Debug makefile执行clean build，编译链接成功且无警告；map中存在`start_bridge_compute_mpu_correction`与新增Watch符号。活动源码和时间戳归档哈希一致，未烧录。
## 2026-07-20 V80修改前：用户手调V79的实车现象

- 用户在V79基础上只改了三项：桥接航向除数`500->400`、总限幅`40->80`、每帧斜率`4->8`。活动源码与用户附件除行尾外一致，确认后续应以手调版为基底。
- 实车结果：原保守参数持续右偏；手调后有概率直线很好，但偶尔突然向右修正。原因是`423/431`原本右轮快8 ticks，而`SLEW=8`允许正修正一帧直接变成`431/423`，左右差速在20ms内从-8翻到+8。
- V80目标不是简单退回保守参数，而是保留对长期右偏的强左修正能力，同时滤除角速度尖峰、限制右修正、要求右修正持续确认，并禁止控制量一帧跨零反向。循迹、弯道和出弯部分继续保持V77参数不变。
## 2026-07-20 V80实施与验证结果

- `start_bridge_compute_mpu_correction()`重构为四层：角速度一阶低通；带死区平移的角速度/航向P项；左右非对称目标限幅；禁止跨零的非对称斜率状态转换。
- 慢性右偏对应负correction，允许最大-65并以每帧6 ticks建立；偶发右甩对应正correction，限制+24、要求航向误差正向连续3帧并以每帧2 ticks建立。瞬时正角速度但航向误差未确认时不会启动右修正。
- 当当前修正与期望修正异号时，只按每帧6 ticks向0释放，本帧绝不进入另一侧。控制序列测试从-30切换到+20时得到`-24,-18,-12,-6,0,2,4...`，符合设计。
- CCS clean build成功且零警告，map确认新增Watch符号进入最终映像；活动源码与V80归档哈希一致。原灰度采样、循迹P控制、急弯、出弯、曲线前馈和MPU非桥接逻辑未改变。
## 2026-07-21 V81无MPU循迹修改前补充回忆

- 用户确认陀螺仪损坏，要求彻底不使用陀螺仪并继续改善循迹。当前活动V80含MPU初始化、起步桥接航向闭环、弯道角度门控和出弯航向保持，不能只拔线后直接宣称为完整无MPU算法。
- V81以V80为基底：增加`MPU_ENABLE=0`，跳过I2C初始化和MPU校准；A点桥接使用固定左右PWM；有效黑线由灰度误差连续达到阈值触发弯道状态，不在首次重捕线时无条件打开右弯前馈；出弯和无线段使用灰度历史、最后非零误差和无MPU固定帧超时。
- 保留所有既有灰度采样、普通/急弯P控制、丢线保护和V80桥接代码作为归档参考；本轮不接触OpenMV、舵机、任务点和电机接口。
## 2026-07-21 V81实施与验证结果

- 新增编译开关`MPU_ENABLE=0`。`i2c0_init_100khz()`、`mpu_init()`、`mpu_calibrate_gyro_z()`均不进入本次构建，`mpu_update_sample()`只清零角速度并返回；因此程序运行不依赖PA0/PA1及陀螺仪。
- 无MPU弯道入口由`curve_update_entry_without_mpu()`负责：有效灰度误差绝对值达到1200连续2帧才调用`curve_start()`。首次捕获黑线不再默认打开右弯前馈，避免直线阶段被`CURVE_RIGHT_FEEDFORWARD_TICKS=30`推偏。
- `curve_start()`和`curve_finish()`允许无MPU状态进入/退出固定弯道；出弯后使用灰度历史和无MPU固定8帧路径，不使用航向角。V80的灰度普通/急弯P、丢线处理、出弯候选逻辑保持。
- CCS clean build成功且无警告，map确认弯道入口函数存在且MPU初始化函数未进入最终映像；活动源码与V81归档SHA256一致，未烧录。
## 2026-07-21 V82修改前：用户10ms调参版

- 用户当前活动源码与附件除行尾外一致，已将`GRAY_MAIN_LOOP_MS`改为10ms，并手调基础PWM375/383、启动560/560、急弯除数6、急弯限幅300、右弯前馈12/步进2、出弯全灭确认2帧。
- 用户反馈同长度弯道的肉眼可见调整次数没有翻倍。确认原因包括：实际周期约11ms而非严格10ms；重复写相同PWM不等于新的方向调整；八路数字灰度空间分辨率不变；大量时间型帧数未按10ms换算；右弯前馈由30降到12也改变了实验变量。
- 本轮将未同步的时间型帧数按原物理时间翻倍，并新增只在`g_curve_active`且正灰度误差时生效的右向恢复项，用于右弯中车辆向左偏离黑线时更强地向右拉回。普通直线、负误差左修正和完全丢线后的历史找回不使用该增强项。
## 2026-07-21 V82实施与验证结果

- 完成10ms帧数换算，特别修正用户手调的`TURN_EXIT_CONFIRM_LOST_FRAMES=2`为10，恢复约100ms出弯防抖；`TURN_EXIT_BRAKE_NO_MPU_FRAMES=16`保持，最长帧由15增至30，避免无MPU正常结束条件永远晚于超时。
- 新增`CURVE_RIGHT_RECOVERY_ERROR_MIN=500`、`DIVISOR=10`、`CORR_LIMIT=80`及Watch变量。该修正只在右弯状态的正误差有效，叠加到普通/急弯正correction；方向公式仍为左轮加、右轮减，因此物理效果为更强右转。
- 普通直线`g_curve_active=0`、负误差以及lost recovery均不叠加该项。静态代表值验证为负误差0、500及以下0、1000为50、1300及以上80。
- CCS clean build成功且零警告，map中存在新Watch符号；活动源码与时间戳归档哈希一致。未烧录。
## 2026-07-21 V83修改前：V82弯道突然大幅左偏

- 用户实测V82在弯道会突然大幅向左、出线后无法恢复。代码审计确认V82新增右恢复本身只输出正修正，但它把右弯总正修正提高到“急弯最多300+恢复80+前馈12”；数字灰度误差一旦跨过中心变负，现有代码可在单个10ms帧中直接切换为负急弯修正。
- 更关键的是V82把用户原先调好的`TURN_EXIT_CONFIRM_LOST_FRAMES=2`改为10。全灭后最多约100ms继续使用最后非零误差；若最后误差为负，`LINE_SHARP_KP_DIVISOR=6`、限幅300会持续大幅左打。该帧数不能只按理论时间机械翻倍，应恢复实车已验证的2帧。
- V83计划：恢复2帧全灭确认；保留右弯正恢复，但把右弯总修正通过状态整形输出，负向最多80、每帧只建立15，正向每帧建立40，反向前先按每帧60释放到0且不允许同帧跨零。
## 2026-07-21 V83视频复核后修正诊断

- 两段视频逐帧结果一致：视频1约frame19-21、视频2约frame24-28仍接触黑线但右转量已不足；随后黑线从灰度板侧面/后方滑走，约frame23-24与frame31-32完全离线；离线后车辆沿原切线继续向外，没有出现先右转过量再猛烈左回。
- 因此撤回“主要由正负修正一帧翻转造成”的单一判断，也不再把全灭确认恢复为2帧。V82最直接的错误是右侧历史首次丢线时触发`TURN_EXIT_LEFT_NUDGE`：连续8帧负修正并清零最后正误差，使本应继续向右找线的车辆失去右转记忆。
- V83修订方案：右弯入口由正误差700连续2帧触发，不再使用绝对值1200连续4帧；禁用丢线左轻推；curve active或连续右侧历史丢线时把最后误差至少保持为+1200，继续右找线；有效线状态下若出现负误差，右弯中的左修正限幅60，避免数字灰度跳变造成大幅反向。
## 2026-07-21 V83实施与验证结果

- 最终将右弯丢线保持误差定为+1800而非计划中的+1200；在当前急弯除数6下对应+300线修正，加上既有右前馈后约+312，用于视频所示弯道外侧失线时继续右找线。
- `TURN_EXIT_CONFIRM_LOST_FRAMES`保持V82的10帧，未按修改前推断恢复2帧；视频证明当前需要保留约100ms右转找回窗口，真正错误是窗口内执行了左轻推并清空误差。
- 有效线负误差在curve active时普通/急弯均最多输出-60线修正；正误差仍保留V82恢复增强。右弯入口只接受正误差700连续2帧，防止负误差错误启动右弯状态。
- 代表输入静态验证通过，CCS clean build成功且零警告；活动源码与时间戳归档哈希一致，未烧录。
## 2026-07-21 陀螺仪损坏诊断记录

- 不能把“AD0接GND”判为损坏原因；对MPU I2C地址脚来说这是正常的`0x68`选择。更可能的失效链来自此前系统多电源异常：3.3V轨曾被抬高、信号线跨电源域、未共地/不同上电、带电插拔或SDA/SCL误接5V。
- 需要区分芯片损坏与总线被拉死：旧板断电测SDA/SCL对GND及VCC阻值，通电仅供3.3V时测空闲电平，再用最小I2C测试读取0x68的WHO_AM_I。VCC正确而SDA或SCL固定低、VCC异常高电流或始终无ACK，才支持模块硬件损坏。

## 2026-07-21 V84修改前：自然出弯被V83误判为弯中丢线

- 新视频`e7979551f9ae7a17fb04404ead43ea72.mp4`逐帧显示：车辆已经沿右弯跟到弧线末端，灰度板离开黑线后才突然继续向弯内侧猛打；这与上一轮“弯中右转不足、沿切线冲出”是两个不同问题。
- V83在`g_curve_active!=0`的任意丢线帧都把误差至少强制为`+1800`，当前急弯除数6下立即得到约`+300`线修正，再叠加右前馈约为`+312`。正常弧线终点同样会全灭，因此自然出弯被当成意外丢线处理，造成视频中的瞬间猛右转。
- V84拟增加弯道持续帧计数与成熟出弯候选：弯道前半段丢线继续使用V83强右找线；弯道运行达到最低时长且丢线前连续误差没有位于最外侧时，先按基础PWM直行确认，连续全灭后结束curve。确认期间若重新见线，立即取消候选并恢复循迹。

## 2026-07-21 V84实施与验证结果

- 新增`CURVE_MATURE_EXIT_MIN_FRAMES=280`、`CURVE_MATURE_EXIT_CONFIRM_FRAMES=4`和`CURVE_MATURE_EXIT_ERROR_MAX=1800`。只有curve已持续至少约3.1秒、丢线前连续两次有效误差均在该范围内，才把全灭识别为自然弧线终点。
- 自然出弯候选首帧立即清除线修正、右弯前馈和右恢复项，按基础PWM沿切线直行；连续全灭4帧后调用`curve_finish()`。确认期间重新检测到线会取消候选，继续原循迹。
- 较早丢线或丢线前误差仍位于外侧时，保留V83的`CURVE_RIGHT_LOST_HOLD_ERROR=1800`强右找线。原基础PWM380/383、10ms循环、普通/急弯P参数、右弯恢复参数均未改变。
- 新增Watch变量：`g_curve_active_frame_count`、`g_curve_exit_frame_snapshot`、`g_curve_previous_valid_error`、`g_curve_last_valid_error`、`g_curve_mature_exit_active`、`g_curve_mature_exit_confirmed_count`、`g_curve_lost_hold_active`等。
- CCS clean build成功且零警告，map确认新函数与Watch符号进入映像。活动源码与归档SHA256均为`D1D2196579B0CE8581F34147D27EC7A1D3CEB1E7F795CB4F8AB30CC9176D4520`；未自动烧录。

## 2026-07-21 V85修改前：V84成熟出弯条件未命中

- 用户实测V84仍表现为“出弯时猛地右拐一下，然后再走直线”，与V83一致。该时间顺序说明程序仍在全灭确认窗口内执行强右找线，随后才由出弯制动切为直行；V84新增的成熟出弯候选没有生效。
- 未命中的原因只能是弯道持续帧数未达到280，或丢线前两次误差至少一次超过1800。两者都说明依靠未经实测标定的时长/误差硬门槛不可靠，继续放宽只会把问题转移到弯中误退出。
- V85改为直接约束执行器过渡：全灭时不再把误差无条件抬到+1800，而是从丢线前实际总修正开始、只允许保持或衰减；全灭确认由10帧缩短为3帧。目标是保留极短的弯中闪断容错，同时消除额外最大右转脉冲。

## 2026-07-21 V85实施与验证结果

- 删除V84未命中的280帧/误差1800成熟出弯门控，避免两套全灭路径继续竞争。新增`g_curve_last_valid_total_correction`，在有效线控制完成后记录真正送往左右轮的总修正，而不是只记录灰度误差。
- curve全灭后不再生成`+1800`误差。第一次全灭从最后实际总修正取值并限幅到0~220；第二帧减半；第三帧进入零修正直行。代表序列：最后修正128时为`128->64->0`，最后修正392时为`220->110->0`。
- `TURN_EXIT_CONFIRM_LOST_FRAMES`由10改为3；无MPU且curve有效时，无论最后mask是否落在右侧历史集合，都建立出弯候选，确保第三帧一定切入直行制动，不会因X4/X5组合而长期留在curve。
- 原基础PWM380/383、10ms循环、普通/急弯P参数、有效线右弯恢复和左轻推禁用状态全部保持。CCS clean build成功且零警告，map确认新增Watch变量进入映像。
- 活动源码与归档`20260721_192204_v85_smooth_curve_exit_release.c`的SHA256均为`2A4E14D06F5FA4CB58779382FC1532E38793ED5E0BFDE013FEE17754D0D15DC2`；未自动烧录。

## 2026-07-21 V86修改前：带角度进弯产生左右摆动

- 用户确认V85已消除出弯瞬间猛右转。新问题只在带角度进入弯道时明显：近似平行入弯时循迹和出弯方向稳定，斜着入弯时会在弯内左右摆动，导致出口航向不确定。
- 当前有效线控制直接使用数字灰度的瞬时误差。带角度入弯时黑线会在相邻通道间快速跳动，急弯P、右恢复项和负向回正可逐帧改变总修正；虽然负向限幅为60，但正向可达约392，正负目标仍可快速反转。
- V86只修改curve有效线阶段：对灰度误差做低通，并对最终总修正做方向非对称斜率限制。右转逐步建立，左回修正更慢；正负反转必须经过0。直线、curve入口阈值、V85三帧出弯释放和基础PWM不变。

## 2026-07-21 V86实施与验证结果

- `CURVE_ERROR_FILTER_DIVISOR=3`：curve有效线误差使用`(2*历史+1*当前)/3`，降低数字灰度在相邻通道间跳动造成的误差符号突变；curve入口仍使用未经滤波的实时误差，避免延迟状态识别。
- 新增最终差速渐进器：右修正建立最多36 ticks/帧、右修正释放24、左修正建立14、左修正释放18。正负修正跨越时强制先到0，禁止同一帧直接反向。
- `curve_start()`从上一帧实际`g_total_control_correction`初始化，而不是从0初始化，避免原本平稳的近似平行入弯在状态切换时发生差速跌落。
- V85丢线释放和出弯制动会同步`g_curve_applied_correction`，短暂丢线后重捕线不会从已经衰减的PWM跳回旧修正。新增Watch：`g_curve_filtered_error`、`g_curve_desired_correction`、`g_curve_applied_correction`、`g_curve_correction_step`。
- 基础PWM380/383、10ms循环、急弯除数6/限幅300、3帧出弯确认和丢线最大保持220均未改变。代表目标序列模拟确认修正逐帧变化且不能跨零反转。
- CCS clean build与链接成功且零编译警告，map确认渐进函数和Watch变量进入映像。活动源码与归档SHA256均为`B064B94DE63A2CAE5FD6153E82706346854BE1D17E1DA190E45E667A4C5D02BC`；未自动烧录。

## 2026-07-21 补充召回：转入2021 F题智能送药小车可行性分析

- 用户提供赛题：`D:\软件\xwechat_files\wxid_gdp9f9sm7a9u22_4c08\temp\RWTemp\2026-07\7f9016cfc50d0e53a8466eab788203c5\2021_F题_智能送药小车.pdf`，并要求参考历史会话`019e7371-af12-7d03-a88c-240a230f2d96`及当前器材/算法，判断可实现功能、缺口和逐步实施流程。
- 原历史ID在当前Codex索引中不能直接读取；已读取承接任务`019f22a9-f880-7062-950c-25b5e741f86b`，其核心结论是早期七路循迹已形成“直线简单P控制 + 急弯/丢线状态机 + 模式迟滞”的融合思路。当前更可信的实车基线是今天的V86，不回退覆盖。
- 当前权威活动源码为`C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`，归档为`ccs_project\integration_test\20260721_195602_v86_progressive_curve_correction.c`。V86为10ms八路数字灰度、无MPU、右弯渐进控制；V85已由用户确认消除出弯瞬间猛右转，V86已clean build通过但尚无本条记录之后的实车结论。
- 赛题原文已用pypdf提取并将4页渲染到`tmp\pdfs\2021F_render\`核对。基本要求是单车识别1-8号、检测约200g药品装卸、沿红线寻径、进入5cm门口区域、红/绿灯提示并在送达/返回各20s内完成；发挥部分要求第二辆车、黄色灯和无线协同。
- 关键架构差异：当前八路灰度与V86针对黑线，F题导航线是红线且黑色数字边框距红线约2cm，红外反射阵列既无法可靠区分红/黑，也存在旧灰度板AD0-OUT短路及5V倒灌历史。因此不应把V86原样移植到F题。
- 当前优先方案：保留R3X底盘、TB6612、MSPM0 PWM/安全状态机和既有OpenMV UART经验；移除激光/二维云台负担，将OpenMV固定斜向下，用颜色视觉跟红线、黑框透视校正加1-8模板识别；MSPM0负责电机、编码器、载药开关、指示灯和拓扑状态机。灰度板仅在完成红/白/黑实测后考虑作为门口标记冗余，默认不作为主导航输入。
- 已知缺口：未发现载药/卸药检测传感器、数字识别代码、F题路口图导航代码、正在使用的编码器闭环、第二辆车及无线模块；题目随附的`数字字模.pdf`也不在用户提供目录中。已有红/绿LED和电阻采购记录，黄色LED未确认；MPU已损坏且本题不依赖。
- 下一动作：先更新`项目记忆.md`，再生成独立的F题可行性与实施路线文档。第一硬门禁是用真实白色亚光底、红胶带和黑色数字卡验证视觉/灰度对比；单车近端房间稳定完成前不投入双车发挥部分。

## 2026-07-21 V87修改前：弯道防摆状态机与白底上下文拆分

- 恢复状态核对：活动源码仍为V86，且与归档`20260721_195602_v86_progressive_curve_correction.c`逐行一致；活动文件与归档仅换行格式不同，不存在新的用户手调参数差异。
- 用户实测V86仍会在带角度进入右弯时连续左右摆动。主要原因是EMA带来相位滞后，且普通右弯仍允许单帧负误差形成真实左转，车辆越线后容易产生右转、左转反复切换。
- 用户已确认V87方案：删除EMA，改用三帧误差中位数；右弯拆分为捕获、稳定跟踪、内侧恢复三个状态。普通右弯修正最低保持`+8`，只有误差连续3帧不大于`-1800`才允许最大`-18`的小幅左回。
- 稳定跟踪阶段加入限幅D项，除数30、限幅正负35；修正变化率改为右建28、右释48、左建10、左释24 ticks/帧。V85三帧出弯平滑释放必须保留。
- 白底控制拆分为首次A点桥接和出弯后桥接。首次启动增力采用base 560、trim -6；后续首次桥接采用base 427、trim -10。计算为`left=base+trim`、`right=base-trim`，负trim用于抵消A点起步右偏。出弯白底继续保持用户已验证的`380/383`。
- 下一步：生成V87时间戳归档并执行CCS clean build，不自动烧录。

## 2026-07-21 V87实施与验证结果

- 删除V86 EMA，curve有效线阶段改为当前帧和前两帧误差的中位数。新增`g_curve_median_error`、`g_curve_error_delta`和`g_curve_derivative_correction`，避免单个灰度通道跳变直接改变控制方向。
- 新增三态弯道控制：`g_curve_control_state=1`为捕获、`2`为稳定跟踪、`3`为内侧恢复。捕获阶段误差绝对值不超过700连续4帧后进入跟踪；误差不大于-1800连续3帧才进入内侧恢复；恢复到不小于-700连续2帧后回到跟踪。
- 普通右弯的目标总修正最低限制为`+8`，负误差只减小右转量；只有内侧恢复允许最低`-18`。D项只在连续处于稳定跟踪时启用，除数30、限幅正负35，状态切换首帧不使用D项。
- 最终修正斜率改为右建28、右释48、左建10、左释24 ticks/帧，正负跨越仍必须先经过0。V85三帧丢线释放和出弯制动执行顺序保持不变。
- 新增`g_drive_context`：0为首次A点桥接、1为有线/弯道、2为出弯白底。首次增力输出由560/560改为554/566，后续首次桥接由423/431改为417/437，均提供独立左补偿；出弯白底仍为380/383。
- CCS clean build与链接成功且零编译警告，map确认新增Watch符号进入映像。活动源码与归档`20260721_201820_v87_curve_state_split_gap_trim.c`的SHA256均为`6D5C839DBD8A89EBFAC72B59F7B686B7C471C2351E39C51F42684F631DDB1F23`；未自动烧录，未生成readme。

## 2026-07-21 V87双起点视频复盘开始

- 新证据：`eca6e18b1cf375cd9c1e17c10e6cb8a9.mp4`为车身正对弯道入口测试；`4c0e33fbff566296851992d1fa8895a4.mp4`为A点启动时车头略偏左测试。
- 当前活动源码仍为V87。用户要求同时解决两种初始姿态下暴露的全部问题，并允许采用更细粒度的数学/物理控制算法。
- 分析原则：先逐帧标注入弯航向、灰度板相对黑线、控制振荡、首次全灭和出弯航向；再与`g_drive_context`、`g_curve_control_state`、中位数、D项及V85三帧出弯释放的执行顺序对应。不得仅凭单段视频继续放大P或增加固定偏置。
- 下一步：并行分析两段视频，本地核对V87路径，形成统一根因后生成新时间戳版本；只clean build，不自动烧录，不生成readme。

## 2026-07-21 2021 F题可行性路线输出完成

- 新增交付文档：`C:\Users\1\Desktop\电赛\2021F智能送药小车_可行性与实施路线.md`，包含赛题功能映射、现有能力、缺件、推荐架构、14步实施流程和逐阶段验收门槛。
- 文档在V87并发完成后已重新核对，当前基线引用更新为`20260721_201820_v87_curve_state_split_gap_trim.c`；V87新增右弯三态、中位数抑噪和上下文桥接，不改变F题的主结论：只复用底层运动和控制方法，红线/数字由固定OpenMV承担，另建F题导航状态机。
- 输出优先级依据评分和风险确定为：红线/数字可行性门禁 -> 编码器/载药/门口停车 -> 近端 -> 中部 -> 远端 -> 双车。双车在单车连续30次稳定前不投入。

## 2026-07-21 V87双视频逐帧结论与V88实现

- 视频一的确定失败链：下方U弯出弯航向偏右，丢线后仍保留顺时针角速度；约1.8秒后重新碰到同一U弯右支；V87的`GAP_REENTRY_MIN_FRAMES=50`只屏蔽约0.5秒，因此把旧弯道误判为下一弯入口并再次绕入同一U弯。主要问题是出口航向和固定赛道拓扑识别，不是单纯弯中P参数不足。
- 视频二中，A点约2.3~4.3秒持续左偏约7~9度，原因是人工初始左偏与V87的`START_BOOST_TRIM=-6`、`START_BRIDGE_TRIM=-10`同向叠加。第一圈两个弯基本成功；约17.1~20.4秒出弯白底逐渐右偏约9度，第二圈错过正确入口；随后从弧线中段侧向撞线，V87后续重捕要求中心锁定，未进入curve状态，普通对称循迹释放强左修正后出场。
- 可观测性边界：MPU不可用、编码器未使用硬件QEI时，八路灰度全白阶段不能观测绝对航向。高级算法不能从全白输入恢复不存在的信息；当前可做的最优软件补偿是利用末次控制量抑制残余角速度、利用固定赛道时间拓扑拒绝旧弯道、利用灰度误差变化率作为短时航向代理。
- V88取消首次A点固定trim，启动增力改为560/560，普通首次桥接改为427/427，避免继续放大人工左偏。首次看到有效线连续2帧后直接`curve_start()`，不再等待正误差门槛。
- V88弯道使用三帧中位数误差、三分之一IIR误差斜率和两帧前视预测：`predicted_error=median_error+2*filtered_delta`。捕获态D项除数20/限幅45，跟踪态除数30/限幅35；预测器首帧只初始化，不产生D脉冲。捕获稳定需`abs(error)<=700`、`abs(filtered_delta)<=350`连续4帧；预测误差连续2帧低于-1400才进入最大-18的内侧恢复。
- V88出弯消旋不使用固定反打。反向目标由最后有效转向量除以8得到，最大18 ticks，在16帧内线性衰减；实际输出必须经过既有渐进器跨越零点。离线序列验证最坏`+110`按`+62,+14,0,-10...`释放，不再单帧翻成负修正。
- V88固定赛道重入：前220帧拒绝黑线；提前遇线会撤销armed，之后必须连续8帧无线重新武装；合法窗口内任意有效线连续2帧即进入受限curve捕获，不再要求中心锁定。第280帧后若仍未找到新弯道，左搜修正以每帧最多2 ticks斜坡到-12。离线验证“旧线从180帧持续越过220帧”不会被接受，而“旧线穿越后重新无线、300帧遇新线”在301帧接受。
- 新增Watch包括`g_curve_filtered_error_delta`、`g_curve_predicted_error`、`g_curve_predictor_valid`、`g_turn_exit_dynamic_counter_correction`、`g_gap_search_active`、`g_gap_search_correction`、`g_gap_reentry_armed`、`g_gap_reentry_clear_count`、`g_gap_rejected_early_line_frames/events`。
- 独立代码复审首次发现并修复三项边界：持续旧线跨时间窗、反向脉冲单帧跨零、预测器首帧raw/median混用；修复后复审结论为APPROVE，无阻塞问题。
- 当前活动源码：`C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`；时间戳归档：`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260721_210618_v88_topology_predictive_capture.c`。两者SHA256为`FCB6ED30DA8E55CE622E2074D50A0D417EA20EF7834EEDA229D1749E65D379F8`，源码2339行。CCS clean build和链接成功，新增Watch符号已进入map；未自动烧录，未生成readme。

## 2026-07-21 V89控制变量实验启动

- 用户决定不直接使用V88整包改动，要求回到V87后将V88行为改动逐组加入，以实车控制变量法判断哪一项真正优化、哪一项引入副作用。
- 核对确认当前`C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`与归档`20260721_201820_v87_curve_state_split_gap_trim.c`逐行一致，仅换行格式不同；当前活动代码已经是V87，无需覆盖。
- V89阶段1只验证“首次A点白底固定左trim是否放大人工左偏”：`START_BOOST_TRIM_TICKS -6 -> 0`、`START_BRIDGE_TRIM_TICKS -10 -> 0`。这两个常量属于同一控制因素，即首次桥接左右轮静态偏置。
- 阶段1禁止修改V87的入线4帧确认、弯道三状态、原始D项、出弯固定0反打、中心锁定重捕和50帧gap窗口。后续只有完成同起点重复测试后才能依次加入入线策略、预测捕获、动态消旋、拓扑重捕。

## 2026-07-21 V89阶段1实现完成

- V89阶段1当前活动源码为`C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`；归档为`C:\Users\1\Desktop\电赛\ccs_project\integration_test\20260721_215425_v89_stage1_v87_startup_trim_neutral.c`。
- 相对V87唯一控制因素变化：启动增力输出由554/566改为560/560，普通首次桥接由417/437改为427/427。新增`g_v89_experiment_stage=1`仅用于Watch识别，不参与控制。
- `LINE_ENTRY_CONFIRM_FRAMES=4`、`GAP_REENTRY_MIN_FRAMES=50`、V87弯道状态阈值、D项、出弯和重捕代码逐行保持不变。差异审计及独立代码复审均确认无额外行为变化。
- CCS clean build和链接成功，map确认`g_firmware_version`与`g_v89_experiment_stage`进入最终映像；SHA256=`6FC411354268760EFF8F392D9636989A281CE100228760939FF0DCFEA52D9C8B`。未烧录、未生成readme。
- 阶段1实车必须用相同电量、相同摆放基准分别测试正对入口和略偏左起步，每种至少3次；只记录白底段横向偏移和碰到首弯的位置。若两个姿态都改善，阶段1保留；若正对姿态变差而略偏左改善，则静态trim不是鲁棒方案，应恢复V87值并等待可观测航向反馈。
## 2026-07-22 补充召回：进入第一步红线/数字验证

- 既有状态：V89阶段1仅验证整车启动trim；F题可行性分析已确认红线与数字不能直接复用V86/V87的黑线灰度算法。当前活动底盘 `ELECTRIC/ele-car/empty.c` 禁止改动。
- 已知材料：OpenMV红色检测脚本 `ccs_project/openmv_test/20260709_simple_red_color_sensitivity_test.py`、黑框脚本 `ccs_project/openmv_test/20260709_black_frame_center_test.py`、MPU V61独立测试 `ccs_project/mpu6050_test/20260717_112923_v61_mpu6050_mpu6500_compatible_motor_off_test.c`。
- 本轮决策：新建 `ccs_project/medicine_car_2021f/`；先用纯Python核心测试红线多ROI、Hamming模板和多帧投票，再提供OpenMV板端脚本和独立MSPM0 MPU验收程序。数字字模缺失时采用现场学习。
- 待完成：核对OpenMV API；写测试并确认RED；实现算法并确认GREEN/覆盖率；完成板端脚本、MPU源文件、接线及操作说明；完成代码审查。

## 2026-07-22 第一阶段程序完成与桌面验收

- 已完成独立目录 `ccs_project/medicine_car_2021f/`：OpenMV 红线/数字脚本、纯Python视觉核心、新MPU独立验收C程序、操作说明和回归测试。
- OpenMV API经现有H7脚本和官方v4.8.1资料核对：使用 `find_blobs` 多水平ROI、`find_rects` 黑框优先和黑blob兜底；数字由于字模缺失采用现场16×12网格模板，不依赖未知字体。
- TDD结果：测试先因缺少 `vision_core` 失败（RED），实现后5项测试全部通过（GREEN）；`vision_core.py`覆盖率95%。OpenMV脚本和核心已通过桌面 `py_compile`；MPU C文件括号/花括号配对检查通过，尚未在实际CCS副本中烧录。
- 关键修正：OpenMV输出帧定时器在三个模式均正确更新；数字识别在框丢失/当前帧不可靠时清空历史投票，避免旧数字延续到新卡片。
- 验收指标和接线已写入 `ccs_project/medicine_car_2021f/第一步_红线数字与MPU验证说明.md`。下一步必须先按说明完成红线100帧、1-8模板和MPU静止/手动旋转实测，再决定是否接入UART和整车状态机。
- 独立代码审查结论为 APPROVE。审查修复了一个 HIGH：OpenMV `get_pixel` 显式使用 `rgbtuple=True` 后再计算亮度，避免把RGB565编码整数误当作灰度值；修复后重新通过 `py_compile` 和5项回归测试。
- MPU程序已在纯ASCII临时副本 `C:\Users\1\tmp\medicine_car_mpu_build_20260722` 中执行 SysConfig、TI Arm编译和链接，`gmake clean all` 成功生成 `123.out`；map已确认 `g_mpu_state`、`g_mpu_who_am_i`、`g_gyro_z_mdps`、`g_yaw_mdeg`、`g_mpu_read_fail_total` 等Watch符号进入映像。未烧录、未连接实物。

## 2026-07-22 OpenMV 5.0.0 blob API兼容修复

- 实物OpenMV IDE截图确认板卡为OpenMV Cam H7、固件5.0.0；红线脚本在 `blob.pixels()` 处报 `TypeError: 'int' object isn't callable`。
- 根因是固件5.x把部分blob/rect字段从方法改为整数属性。脚本新增 `api_value()`，同时兼容 `pixels/cx/x/y/w/h` 的方法式与属性式访问，并覆盖黑框rect/blob路径，避免修完红线后在数字模式再次触发同类错误。
- 修复后桌面 `py_compile` 通过；用户需要把OpenMV IDE中当前的 `helloworld_1.py` 替换为本目录最新版或同步相同修改。
- 第二次实物报错为 `TypeError: object 'int' isn't a tuple or list`。最终以OpenMV 5.0.0标签底层 `modules/py_image.c` 为准：`draw_line`、`draw_rectangle`、`draw_cross`和`get_pixel`均从首个位置参数读取固定长度tuple/list；仅按网页展示签名改成分散整数是错误中间判断，已撤回。
- 最终5.0调用为 `draw_line((x0,y0,x1,y1),...)`、`draw_rectangle((x,y,w,h),...)`、`draw_cross((x,y),...)`、`get_pixel((x,y),rgbtuple=True)`。`find_blobs`的ROI继续使用四元组，`find_rects`使用整数threshold；所有blob/rect字段继续经过兼容层。

## 2026-07-22 恢复极简循迹：续接记录

-  prior status：活动源码为V89阶段1，灰度板与TB6612启用，MPU宏关闭；文件仍保留约76KB的历史桥接、弯道、MPU和丢线恢复逻辑。
- known files：活动源码 `C:\Users\1\Desktop\ELECTRIC\ele-car\empty.c`；既有时间戳归档位于 `ccs_project/integration_test/`；活动工程包含可直接运行的TI Arm编译产物与Makefile。
- decision：不改变硬件引脚、灰度8路扫描、白底基线识别、PWM极性和电机方向；直接重写为单比例循迹，明确移除桥接和弯道专用控制。
- unresolved：最终PWM、比例增益与短时丢线搜索力度仍需实车低速验证；本轮先采用当前已知可启动的基础PWM附近保守值。
- next action：备份V89，重写活动 `empty.c`，同步时间戳归档，执行Clean Build、静态关键词审计和最终代码自审。

## 2026-07-22 V90极简循迹实现结果

- RED：先新增`ccs_project/line_sensor_test/simple_line_control_test.c`，在控制头文件尚不存在时由GCC确认预期编译失败，失败点为缺少`simple_line_control.h`。
- GREEN：新增纯逻辑头文件并完成六类测试：居中、右偏、左偏、短时丢线方向保持、连续丢线停车、大误差限幅；6/6运行通过。
- 覆盖率在ASCII临时目录执行，避免MinGW gcov无法在中文路径写`.gcda`：控制头文件行覆盖率88.24%，整体行覆盖率95.18%。
- 活动`empty.c`由76456字节/约2178行收缩为19160字节/622行。桥接、弯道、MPU、航向、出弯、前馈和分级恢复函数及状态变量不再存在。
- 保留原V89已经验证的PB5/PB6/PB7地址扫描、PA7 OUT采样、白底自动极性识别、PA12/PA13 PWM、PB0-PB4 TB6612接口和PB11蜂鸣器静音。
- 活动工程在`C:\Users\1\Desktop\ELECTRIC\ele-car\Debug`执行TI `gmake clean all`成功，SysConfig生成、C编译和链接均完成且无警告；map确认固件版本、灰度误差、修正量、丢线计数及主控制函数进入映像。
- 活动主程序SHA256：`22642F7626BBD0DE581568A7AD838AE9CF9145BE1C4420371EB377CD5C256C12`；控制头文件SHA256：`DCF1560992513064D767205BE05CF45F3108621897C38BD6E81B94BE8436ABD9`；V89备份SHA256：`12304499E00C90D75F591C00186E3B95F2CC2CEECDAF9BAA435C9A37AFBAD7F0`。
- 归档：`20260722_v90_simple_line_follow.c`、`20260722_v90_simple_line_control.h`、`20260722_v90_simple_line_follow_readme.md`。未烧录，下一步必须先架空验证左右修正方向，再落地低速直线/缓弯测试。

## 2026-07-22 CCS编译报错：续接记录

- 用户反馈在CCS界面编译V90时报错，要求直接配置好CCS环境与工程。
- 已知命令行活动工程 `C:\Users\1\Desktop\ELECTRIC\ele-car` 曾用TI Arm Clang 4.0.4.LTS和MSPM0 SDK 2.10.00.04完成Clean Build，因此优先核查CCS当前打开的是否为同一`ele-car`工程、工程是否包含新增`simple_line_control.h`、IDE实际使用的build configuration及Problems/Console原始错误。
- 本轮计划：捕获CCS原始错误→列出并验证3个可证伪假设→修正工程元数据/源码依赖→在CCS界面和命令行分别Clean Build回归。

## 2026-07-22 单一英文模拟题工程：续接记录

- prior status：CCS 20.5当前磁盘中只有两个工程：`C:\Users\1\Desktop\ELECTRIC\ele-car` 与 `C:\Users\1\Desktop\ELECTRIC\empty_LP_MSPM0G3507_nortos_ticlang`；前者为V90极简循迹活动工程，后者为旧`empty`示例。
- reference：已回查会话`019e7371-af12-7d03-a88c-240a230f2d96`最早部分；已验证的环境为纯英文工程路径、LP-MSPM0G3507、MSPM0 SDK 2.10.00.04、SysConfig 1.26.2、TI Clang、No RTOS、XDS110。
- decision：停止CCS图形界面控制；新工程统一命名为`mock_problem`，路径为`C:\Users\1\Desktop\ELECTRIC\mock_problem`，以V90活动工程为源码与元数据模板，不复制旧`Debug`产物。
- deletion scope：只删除上述两个含`.project`的旧CCS工程目录，并清理只引用旧工程的Theia启动备份；保留`contest_pdf_pages`、`tmp_docs`、PDF等非工程资料。
- known issue：MSPM0 SDK安装目录的SysConfig CRC元数据不完整，同版本TI安装缓存中存在缺失资源；仅补复制目标中不存在的同版本文件，不覆盖现有文件。
- next action：核对SDK差集→创建并改名`mock_problem`→验证新工程完整性→解析并删除两个旧工程→命令行运行SysConfig及TI构建回归；不烧录、不启动Debug。

## 2026-07-22 单一英文模拟题工程：完成结果

- SDK修复：从同版本TI安装缓存向`C:\ti\mspm0_sdk_2_10_00_04\source\ti\driverlib\.meta\nonmain\crc`仅补复制54个目标中不存在的计算器资源；复核缺失数0、SHA256不一致数0，`cjs\calculators\crc32.js`已存在。
- 新工程：创建`C:\Users\1\Desktop\ELECTRIC\mock_problem`，保留V90的`empty.c`、`simple_line_control.h`、`empty.syscfg`、CCS工程元数据、`.settings`和`targetConfigs`，未复制旧`Debug`产物。
- 元数据：`.project`工程名、`.cproject`工程ID、`.ccsproject`模板ID以及`.theia\launch.json`均统一为`mock_problem`；XML/JSON解析通过，工程内不存在`ele-car`残余引用。
- 删除：已精确删除`C:\Users\1\Desktop\ELECTRIC\ele-car`和`C:\Users\1\Desktop\ELECTRIC\empty_LP_MSPM0G3507_nortos_ticlang`，同时删除两份只引用旧工程的launch备份；本轮未另做工程副本，删除内容不能从原工程目录恢复。
- 保留：`contest_pdf_pages`、`tmp_docs`、`contest_2026_track_problem.pdf`等非工程资料未改动。递归复核`.project`后只剩`C:\Users\1\Desktop\ELECTRIC\mock_problem`。
- 删除后独立构建：SysConfig 1.26.2生成通过；TI Arm Clang 4.0.4.LTS编译3个单元并链接通过，诊断数0，输出名`mock_problem.out`，临时构建产物SHA256为`DDD9FFA9BF04E71011F1A28D8C641E2E508B12A1E2CCCD371438D176986897FF`，map包含`main`和`g_firmware_version`。
- 临时构建目录已清理；未操作CCS图形界面、未启动Debug、未烧录。首次在CCS中只需打开`C:\Users\1\Desktop\ELECTRIC`工作区并导入/构建唯一的`mock_problem`工程。

## 2026-07-22 V90循迹大幅摆动：续接记录

- prior status：唯一活动工程为`C:\Users\1\Desktop\ELECTRIC\mock_problem`，控制代码仍是V90极简循迹，已通过命令行SysConfig、编译和链接，但尚未针对当前实车摆动现象调整。
- observed symptom：直线循迹时经常扩展到X1/X2/X7/X8点亮后才明显修正；用户期望正常直线主要保持X4/X5持续亮、X3/X6仅承担早期纠偏，避免大幅蛇形。
- diagnostic scope：核对8路位权、有效通道组合、误差到PWM的比例、修正方向、采样周期、白底极性和丢线分支；建立纯逻辑输入序列复现，不操作CCS界面、不烧录。
- next action：从活动`empty.c`与`simple_line_control.h`提取真实控制链，构造X1-X8单点/组合扫描表和直线偏移序列，再以可证伪假设定位是感知过宽、P增益不足、基础速度过高还是传感器几何/安装导致。

## 2026-07-22 V91中央提前纠偏：诊断与实现结果

- 灯态结论：YB-MYX05-V1.0已实测白底基准`0xFF`，因此物理灯亮和`g_gray_x*=1`表示白底，不表示检测到红线。原始mask`0xC3`即X1/X2/X7/X8亮、X3-X6灭，XOR白底后得到红线mask`0x3C`，实际代表红线位于中央X3-X6、误差0；不能要求物理灯只亮X4/X5。
- 摆动根因：V90删除急弯/分段控制后只剩`error/36`、限幅70。单点X3/X6仅41 ticks，X2/X7已69，X1/X8也只有70，中央纠偏偏弱且外侧没有继续增强余量；10ms循环无斜率限制，排除“修正建立慢”的软件延迟。
- V91保持基础PWM左380/右383、10ms循环、灰度采样、白底标定、丢线70 ticks/200帧停车及全部硬件引脚不变，只将单P改为`error/26`、限幅110。单点X4/X5、X3/X6、X2/X7、X1/X8修正依次约19、57、96、110；最大修正时内轮仍为270/273，不反转、不原地打转。
- 新增共享生产profile`simple_line_profile.h`，Host测试与固件使用同一参数；新增`simple_line_normalize_mask()`和`simple_line_error_from_mask()`，生产端mask到位权误差与测试共用实现。
- 新增`g_gray_line_x1...g_gray_line_x8`作为相对白底归一化后的红线命中观察量；原`g_gray_x1...g_gray_x8`继续表示原始高电平，避免破坏既有诊断语义。固件版本为91。
- TDD：首次RED因新profile不存在而失败；审查补测RED因mask误差helper不存在而失败。最终Host测试9/9，覆盖X1-X8、空mask、白底XOR、正负限幅和丢线；整体行覆盖率97.83%，控制头文件93.62%。
- 独立代码审查无CRITICAL/HIGH；指出的X2/X7/空mask测试缺口已全部补齐。SysConfig、TI Arm Clang 4.0.4.LTS三单元编译和链接通过，0 diagnostics，输出SHA256为`D769264BC3E6E6B0A859C911B22ACA3D1604B25106A398549F75CB6E2DFDB529`。
- 活动源码SHA256：`empty.c=32DD216DF8845CEF04B6596329B6A1D58B095917F135F5D7BB3D7F892E78C315`，`simple_line_control.h=3B8BC499053DCE5CDA73BE3864C49F6AC3D0EA474B66324E461964A252C61C53`，`simple_line_profile.h=1DD1993DBB5B31EC64F3927FAFBE67770449E5670FC091C9123C33E1705B3A7C`。
- 归档：`20260722_v91_early_center_correction.c`、`20260722_v91_simple_line_control.h`、`20260722_v91_simple_line_profile.h`、`20260722_v91_simple_line_control_test.c`及说明文档。未操作CCS界面、未烧录；实车先架空核对纠偏方向，再以`g_gray_line_x*`而非物理灯判断红线位置。

## 2026-07-22 全病房模拟赛道任务：续接记录

- objective：在暂不接入OpenMV的条件下，让小车按真实2021 F题赛道一次性经过全部病房、每个病房门口静止2秒并最终返回药房起点；路口需要完成直角转弯，运行中允许短暂停车进行陀螺仪零偏校正。
- current baseline：唯一活动工程为`C:\Users\1\Desktop\ELECTRIC\mock_problem`，V91已提供稳定的八路灰度归一化、提前单P纠偏、TB6612正向PWM和安全停车；当前未初始化MPU、编码器、OpenMV或任务状态机。
- hardware context：新陀螺仪与旧版本相同，既有验证方案按MPU6050/MPU6500兼容WHO_AM_I处理，I2C使用PA0/PA1；正式集成前必须重新核对活动SysConfig、验证程序和实物静止/转动数据。
- maneuver decision：原地旋转360°会回到原朝向，不能用于退出病房；计划采用门口停2秒后原地180°掉头，再以灰度循迹沿支线返回路口。只有在赛道空间不足以掉头时才改为倒退方案。
- next action：重新渲染并核对题目第2页拓扑→盘点当前MPU/编码器资源→定义可由灰度mask、陀螺仪和路口事件验证的完整路线表→先写Host状态机测试，再集成V91底层并命令行构建；不操作CCS界面、不烧录。
## 2026-07-22 V91 实车灯态与大幅摆动：补充召回

- prior status：唯一活动工程为 `C:\Users\1\Desktop\ELECTRIC\mock_problem`，当前为 V91；物理灰度灯/原始变量 `g_gray_x*` 的 1 表示白底高电平，归一化变量 `g_gray_line_x*` 的 1 才表示相对白底检测到红线。
- observed symptom：用户看到直线运行时常为 X1、X2、X7、X8 亮，并认为应保持 X4、X5 亮；同时实车仍表现为偏到较外侧后才显著修正、摆动幅度大。
- diagnostic decision：先分离“灯态极性误读”和“真实闭环摆动”两个问题；用现有纯逻辑测试构造 X1-X8/多点 mask 到误差、PWM 的确定性反馈表，再检查 10 ms 控制周期、P 增益、基础速度、安装高度/红线宽度与修正方向，不修改或烧录固件。
- next action：验证物理灯态 `X1/X2/X7/X8=1、X3-X6=0` 对应归一化红线 mask `0x3C` 与零误差；列出可证伪假设并决定是否需要用户提供直线 Watch 数据或视频后再进入修复。
## 2026-07-22 V91 灯态与摆动诊断结果

- feedback loop：使用活动工程 V91 头文件重新编译时间戳归档中的 Host 测试，结果 `simple_line_control_test: 9/9 passed`；活动文件 SHA256 与既有 V91 记录一致。
- confirmed：白底高电平时，物理灯 `X1/X2/X7/X8=亮、X3-X6=灭` 对应 raw mask `0xC3`，经白底 `0xFF` 归一化得到红线 mask `0x3C`，误差和修正均为 0；物理灯不是红线命中指示。
- confirmed：V91 对归一化单侧 X4/X5 已输出约 19 ticks，X3/X6 约 57，X2/X7 约 96，X1/X8 限幅 110；算法没有等待最外侧传感器才开始计算修正。
- unresolved physical loop：无法仅靠静态代码复现实车惯性、左右轮差与传感器安装几何。若归一化 `g_gray_line_x*` 确实持续跑到外侧，优先检查修正方向和当前基础 PWM 下 P-only 的控制能力；需要采集 `g_gray_line_mask/g_gray_line_error/g_line_correction/g_left_pwm_ticks/g_right_pwm_ticks/g_gray_unstable_mask` 区分。
## 2026-07-22 全病房路线V92：实施前补充召回

- authoritative state：`mock_problem`不是Git仓库；活动V91三份源码哈希与既有记录一致，当前`empty.c`仍明确未初始化MPU和mission logic。
- route：采用已核对的树形赛道与动作序列，访问次序为1、2、3、4、7、5、6、8，门口统一停2秒后原地180°掉头，最终从远端沿主干返回药房。
- design：新增可Host测试的纯逻辑模块，分别负责完整动作表、路口/端点去重检测和陀螺仪转向阶段；硬件层复用已验证的手写I2C0 100 kHz与MPU6050/6500兼容初始化。
- safety：MPU初始化/标定失败、连续读取失败、转向超时、路线越界或异常丢线均停车并保留Watch错误码；不启用OpenMV，不自动烧录，不操作CCS图形界面。
- TDD next：先创建路线/事件/转向/完整旅程测试并确认因缺少新头文件而RED，再写最小实现；Host覆盖率至少80%，随后TI SysConfig/编译/链接和独立审查。

## 2026-07-22 V92端点里程门控：补充召回

- prior status：唯一活动工程已是V92，固定路线为`1→2→3→4→7→5→6→8→药房`；53步动作表、17个路口事件、9个端点事件、90°/180°陀螺仪闭环转向和每病房非阻塞停留2000 ms均已进入固件，Host现有13/13与旧循迹9/9通过，TI命令行构建0诊断。
- review finding：`route_event_detector.h`当前只在armed后用连续8帧`active_count==0`确认端点；普通约80 ms脱线也会推进mission并造成剩余路线永久错位，是一次性跑完全程的HIGH风险。
- available hardware：双编码器既有接线为左A PB12、左B PB13、右A PB14、右B PB15；左轮前进原始计数为负、右轮前进为正。参考实现位于`ccs_project/encoder_test/encoder_motor_calibration.c`，旧估算`1471 edges/cm`尚待现场校准。
- decision：端点确认增加独立的分段前进里程门控；每个follow动作记录编码器起点，里程不足时全灭只能视为中途脱线，绝不推进mission；同时增加双轮方向/单轮失效健康检查与Watch量。
- next action：先写里程不足不产生endpoint、达标后才产生endpoint、中途脱线恢复不推进mission、单轮失效/方向异常停车的RED测试，再实现纯逻辑门控与PB12-PB15硬件计数，最后重跑Host覆盖率和TI clean build；不烧录、不操作CCS GUI。

## 2026-07-22 V93最终软件验收：补充召回

- prior status：唯一活动工程为`C:\Users\1\Desktop\ELECTRIC\mock_problem`；当前V93已经加入PB12-PB15双编码器里程门控、端点进入时距离锁存、单/双轮停转与方向/轮差fail-close，并把九个端点的最小/最大距离窗口移入`medicine_route_action_t`逐动作配置。
- known evidence：最近一次Host路线测试为18/18、旧循迹为9/9；在最后一次`medicine_route.h`结构调整之前TI构建为0 diagnostics。当前理论初值`736 rising edges/cm`和九段20～70 cm（药房20～100 cm）尚未实车标定，旧V93归档也早于最后结构调整。
- current decision：先对当前磁盘源码重新执行两套Host测试、覆盖率、TI clean build、map符号检查与SHA256，确认最终源码/产物一致后创建新的只读稳定归档；不烧录、不操作CCS图形界面。
- completion boundary：软件验证通过只允许进入实车验收；必须完成编码器方向与50 cm标定、MPU、单路口、单病房、逐段九窗口、半程和全程测试。只有一次性访问1→2→3→4→7→5→6→8并返回药房，才能完成总目标。

## 2026-07-22 V93最终软件验收与归档

- tests：活动头文件重新编译后，完整路线Host测试18/18、循迹9/9；路线模块总行覆盖率97.67%，循迹97.83%。端点测试新增九项精确预期，逐项锁定action index、ward和min/max窗口，避免生产表与测试共同写错仍自洽通过。
- build：发现`.cproject`指定4.0.4但旧Debug生成文件仍调用CCS20.2/tiarmclang4.0.3；已把makefile、subdir rules和clangd数据库统一到CCS20.5/4.0.4，旧活动clangd cache移到可恢复备份。使用4.0.4重新clean build，SysConfig/编译/链接0 diagnostics，最终out晚于全部活动源码。
- artifact：map确认`GROUP1_IRQHandler`、`SysTick_Handler`、左右编码器、路线故障、端点进入锁存、控制超时和停止原因符号存在；`mock_problem.out` SHA256=`429E3419AB318EE55BB8FCF1DD577F0365A1DEF97F3BE6F24D03863E5ABE0CC5`，map=`207843DAA34BB6E84BF659C9A005ED557939EF919B0223CCDE5E6A812C709B13`。
- review：最终独立增量复审0 CRITICAL/HIGH/MEDIUM；结论仅覆盖软件/构建配置，不覆盖编码器物理标定、PWM/STBY HIL、实时中断负载和整车路线。
- archive：新建`ccs_project/medicine_car_2021f/archive/V93_20260722_final_software_verified_4_0_4`，包含源码、CCS配置、4.0.4构建入口、out/map、两套Host测试和实车说明；`MANIFEST.sha256`共37项，逐文件校验0失败。旧归档未覆盖。
- next action：实车只从编码器架空方向和50 cm距离标定开始；标定结果回填`ENCODER_RISING_EDGES_PER_CM`后重新构建，再做MPU、单路口、单病房和九段窗口采样。当前未烧录，目标保持未完成。

## 2026-07-22 V93目标板完成性审计：补充召回

- prior status：V93最终软件快照、18/18路线测试、9/9循迹测试、TI Arm Clang 4.0.4 clean build、最终复审和37项归档清单均已通过；活动源码仍未烧录，编码器736 edges/cm与九段窗口仍无实测依据。
- completion gap：总目标要求一次性访问八病房、每处静止2秒、90°路口转弯、180°退出并返回药房；Host证据不能证明真实GPIO/PWM/STBY、I2C时序、中断负载、机械转向和场地端点。
- current audit：继续静态检查生产`empty.c`中的启动顺序、病房停留零偏更新、动作切换、转向完成/超时、编码器故障到`motors_stop()`以及最终完成状态，确认是否存在无需实车即可修复的阻断问题。
- next action：若代码层无新阻断，则冻结现有固件不再盲调；实车第一项仍为`g_motor_disabled=1`下的编码器方向与50 cm三次标定，并据结果更新唯一距离宏。

## 2026-07-22 V93目标板完成性审计结果

- startup：安全GPIO先以低电平初始化，PWM启动后立即执行`motors_stop()`；MPU身份/500样本标定、50帧白底和2秒准备均在任务电机启动前完成。启动失败统一停PWM并拉低STBY。
- ward bias：每个病房动作入口清零累加器，停留第500 ms后只接收`abs(gyro)<=3000 mdps`样本；2秒结束后在下一动作同步前要求至少50个样本才更新偏置。因此更新发生在180°掉头初始化之前，期望全程`g_ward_bias_update_count=8`。
- action transition：路口/端点、转向完成和病房停留均先停车再推进mission；下一动作最早下一10 ms帧同步。90°动作包含220 ms低速前移和100 ms静止，180°不前移但仍先静止100 ms再积分。
- fail close：编码器故障、端点硬上界、MPU连续3次失败、转向超时、240秒总超时和10 ms工作超时均调用`medicine_fail()`；该函数同帧执行PWM=0并清除AIN/BIN/STBY。物理PB4低电平仍必须HIL测量，Watch不能证明引脚电压。
- timing：灰度扫描约由8路×9次数字采样构成，MPU每帧读取6字节；代码有`g_mission_tick_work_ms`与`g_mission_tick_overrun_count`fail-close，但真实编码器中断负载只能在目标板验证。最终验收新增`g_i2c_error_count=0`、`g_turn_read_pause_count=0`、`g_ward_bias_update_count=8`和`g_mission_tick_overrun_count=0`。
- conclusion：无新的纯代码阻断，不修改V93稳定固件；剩余证据全部属于实车标定/HIL。下一步仍是编码器方向与三次50 cm数据，不能跳到全路线。

## 2026-07-22 V93进入实车数据阻断状态

- repeated blocker：连续多个目标回合没有新的实车输入；当前`736 rising edges/cm`、九段端点窗口、MPU转向符号/角度、PB4故障停车电平和完整路线均无法由Host或静态代码替代验证。
- exhausted software work：活动V93已完成18/18路线测试、9/9循迹测试、97%+覆盖率、TI Arm Clang 4.0.4 clean build、最终独立复审、稳定归档和目标板时序审计；未发现还能在无硬件证据下安全推进的代码级阻断。
- required external change：用户需提供至少三次50 cm的`Δleft_count/Δright_count`，且先确认左负右正；随后才能计算并回填真实edges/cm。之后仍需单路口、单病房、九段窗口、半程和全程HIL。
- status decision：不继续盲调、不烧录、不宣称完成；目标转为blocked，收到实车数据后按现有计划恢复。

## 2026-07-23 V93实车实验恢复：实验0

- resumed：用户要求开始逐步实验，阻断审计重新开始；仍以`C:\Users\1\Desktop\ELECTRIC\mock_problem`的V93稳定固件和既有分层验收说明为准，不更换算法、不接OpenMV。
- first gate：先做断电接线与输入电平确认，驱动轮架空、TB6612电机VM断开；确认编码器PB12-PB15输入不超过3.3V、MPU只接3.3V、灰度OUT经分压后PA7侧不超过3.3V，并核对公共地。
- next：用户回报电平与接线通过后，进入CCS加载V93并在`g_motor_disabled=1`条件下做编码器架空方向测试；本回合不运行完整路线。

## 2026-07-23 实验1：编码器架空方向开始

- assumption：用户指示进入下一步，按实验0接线、电平、共地与TB6612 VM断开均已通过处理。
- method：CCS加载活动V93，在`main`入口把`g_motor_disabled=1`后运行；等待初始化结束并确认`g_stop_reason=10`、左右PWM均为0。随后用“运行→手动正转车轮→暂停”的方式读取编码器差值。
- pass gate：左轮向前应`Δcount<0`、右轮向前应`Δcount>0`，两侧`Δedges>0`且每次同方向旋转计数连续变化；不在本阶段交换电机线或测试完整路线。

## 2026-07-23 实验1通过：编码器方向与隔离

- safe state：三张截图均为`g_motor_disabled=1`、`g_stop_reason=10`、左右PWM=0。
- left only：左轮前进手转后`left_count=-15258`、`left_edges=15388`，右侧count/edges均为0；方向为负且无串扰。
- right only：右轮前进手转后`right_count=17166`、`right_edges=17346`，左侧count/edges均为0；方向为正且无串扰。
- quality：净计数/总边沿约为左99.16%、右98.96%，少量差值符合手动起停回摆；左右方向和通道隔离通过，不交换A/B线、不修改ISR符号。
- next：进入实验2，保持`g_motor_disabled=1`和VM断开，落地手推三次准确50.0 cm，逐次清零并记录左右net count与total edges，计算真实上升沿每厘米。

## 2026-07-23 实验2三次50 cm：补充召回

- prior status：实验0接线/电平与实验1编码器方向已经通过；左轮前进净计数为负、右轮为正、两通道无串扰，活动工程仍为V93且临时比例为`736 edges/cm`。
- new evidence：用户按“第一次、第二次、第三次”提供三张50.0 cm截图；字段均可清晰读取，三次`left_count/right_count`分别为`-33152/33334`、`-33428/33334`、`-33003/33093`，对应total edges为`33272/33456`、`33428/33360`、`33055/33161`。
- decision：生产`route_progress_guard.h`从`g_encoder_left_count/g_encoder_right_count`建立局部快照并计算前进净里程，因此标定必须使用净count；total edges只能诊断回摆/噪声，不能拿其平均值替换生产参数。
- next action：计算三次净count比例并回填唯一生产宏，重跑Host路线/循迹测试、TI 4.0.4构建检查和独立复审，再创建不覆盖V93的V94归档；随后只进入MPU静止/方向验证。

## 2026-07-23 V94编码器标定、复审与归档

- calculation：三次结果为`(33152+33334)/100=664.86`、`(33428+33334)/100=667.62`、`(33003+33093)/100=660.96 edges/cm`；平均`664.48`，固件整数取`664U`。
- implementation：新增活动文件`encoder_calibration_profile.h`定义`ENCODER_RISING_EDGES_PER_CM=664U`，`empty.c`引用该profile并升级`FIRMWARE_VERSION=94`。病房20～70 cm对应`13280～46480`，药房20～100 cm对应`13280～66400`，5 cm端点确认缓冲为`3320 edges`。
- TDD：缺少新profile时路线Host按预期RED；实现后GREEN。复审发现完整路线仿真仍使用缩小的`100 edges/cm`和单元阈值，已把单元边界配置与生产仿真配置分离，完整仿真改为生产`664`及`1328/3320/3320`阈值。最终路线`19/19`、总行覆盖率`97.71%`，循迹`9/9`。
- build：活动`.cproject`、Debug makefile/subdir rules与linkInfo均确认CCS 20.5、TI Arm Clang 4.0.4.LTS；最终out/map晚于V94源码，map含`GROUP1_IRQHandler`、`SysTick_Handler`、`g_firmware_version`。out SHA256=`A335C6B1B7709B3133960B1252E08BD094828FC1A18A7065478523EC2315A3B4`，map=`1396A4DAF8EB592687AABC11913FF2677156E505A90C668939AA5A168E443DE5`。
- review：初次复审唯一MEDIUM为Host生产阈值保真度，已修复并重跑；生产固件无新CRITICAL/HIGH阻断。旧V93文档中的736仅是明确版本化历史记录，未改写。
- archive：新建`ccs_project/medicine_car_2021f/archive/V94_20260723_encoder_calibrated_4_0_4`，包含活动源码、profile、CCS配置、4.0.4 out/map/linkInfo、两套Host测试、MPU独立验证程序、说明和验证记录；`MANIFEST.sha256`39项复核0失败，V93归档未覆盖。
- operational warning：当前实际打开过的`C:\ti\ccs2020\ccs\theia\ccstudio.exe`会把Debug生成文件重写回TI Arm Clang 4.0.3；继续实验前必须结束当前Debug并关闭CCS 20.2，改用`C:\ti\ccs2050\ccs\theia\ccstudio.exe`。
- status：实验2通过，但总路线未完成。下一项严格限制为实验3A MPU静止/左右方向验证；之后仍需单路口90°、单病房2秒+180°、九端点各3次、半程与一次性完整路线。

## 2026-07-23 实验3A独立MPU镜像准备

- 为避免覆盖V94生产源码，直接以既有`20260722_mspm0_new_mpu_validation.c`、活动SysConfig生成物和启动文件构建独立镜像；程序将AIN/BIN/STBY、PWMA/PWMB和舵机输出全部保持安全电平，只运行I2C0、MPU采样与LED。
- build：TI Arm Clang 4.0.4.LTS编译/链接0诊断；linkInfo确认CCS20.5工具链。输出为`C:\Users\1\Desktop\ELECTRIC\mock_problem\Debug\mpu_validation.out`，SHA256=`B8B2A91B9DF6351C87303B10AFB1BBD2C1D754C896FA040A3D101ECC628318D4`；map含MPU状态、错误、WHO_AM_I、角速度、yaw和I2C错误变量。
- next：先只做3A-1静止60秒；通过WHO_AM_I、500样本标定、0读错和60秒yaw漂移后，再做左右手动旋转方向，不运行电机或完整路线。

## 2026-07-23 实验3A改为直接覆盖empty.c：补充召回

- user preference：用户不接受单独加载`mpu_validation.out`的操作形式，明确要求把测试源码直接覆盖活动工程`empty.c`。
- preservation：V94完整路线源码、profile、4.0.4 out/map、测试与说明已保存在`archive/V94_20260723_encoder_calibrated_4_0_4`且39项manifest通过，因此允许临时覆盖活动`empty.c`，不会丢失生产版本。
- implementation decision：使用已经单独编译通过的`20260722_mspm0_new_mpu_validation.c`作为活动`empty.c`内容；它只启用I2C0与LED，并把AIN/BIN/STBY、PWM和舵机引脚保持安全电平。覆盖后用CCS20.5/TI Arm Clang 4.0.4 clean build，不烧录、不操作CCS GUI。

## 2026-07-23 用户纠正后续工作流

- 用户指出自动清理环境、配置工程和构建过于耗时；以后只需要备份正确代码、给出备份绝对路径，由用户自行复制粘贴替换`empty.c`并Build/Debug。
- 已停止额外复核，不再继续环境操作。仅把当前已编译通过的V95测试源复制到`backup_code/20260723_v95_mpu_validation_empty.c`；源文件与备份SHA256均为`91C95EE7FC634E11BD2D191372ABF799C3899A62712947B7D428628651EE5C50`。
- 当前特殊状态：在用户提出新偏好前，上一条已明确要求直接覆盖，因此活动`C:\Users\1\Desktop\ELECTRIC\mock_problem\empty.c`目前已经是V95 MPU测试代码；不再自动恢复或继续修改。

## 2026-07-23 重新核对题目初始位置

- source：重新读取原题PDF第1页任务文字和第2页图1。原文为“手动将小车摆放在药房处（车头投影在门口区域内，面向病房）”；图中药房位于中央走廊最下方，向上至近端1/2号病房交叉口为60 cm。
- correction：撤回此前“先让灰度板看白底、在断点处再把车放到红线”的单路口测试步骤。正式测试每项只允许一次复位且计时后不得人工干预，应在药房门口原位启动。
- code implication：V94的运行时纯白基准采集会在中央红线起点学到非均匀mask并可能安全停机，因此必须先形成固定`0xFF`白底极性的备份源码；按用户偏好只提供备份路径和复制/Build/Debug步骤，不自动修改活动工程。

## 2026-07-23 V96药房红线起点备份完成

- RED：新增`tests/test_v96_pharmacy_start_source.py`，在目标备份不存在时2项按预期因FileNotFound失败。
- implementation：从只读V94归档复制到新备份，仅把版本升至96、定义`GRAY_FIXED_WHITE_BASELINE_MASK=0xFFU`、新增`gray_use_fixed_white_baseline()`并在main中替换50帧启动采白循环。未修改活动`ELECTRIC/mock_problem/empty.c`、工程配置或CCS环境。
- GREEN：V96源代码契约2/2；既有路线Host 19/19、循迹9/9。独立diff确认除上述四项外无变化；TI Arm Clang 4.0.4语法检查0诊断，复审0 CRITICAL/HIGH/MEDIUM。
- deliverable：`backup_code/20260723_v96_pharmacy_red_line_start_empty.c`，SHA256=`E60A38F4CEFEC6D4EB1601E5C2FF469EE7D8A7489983A385898A9DF43839D424`。

## 2026-07-23 V97首个90度转弯后自动停车：补充召回与完成

- supplemental recall：此前V96已经解决药房红线原位启动，固定白底极性为`0xFF`；编码器标定为`664 edges/cm`，MPU方向为左正右负。未解决项是避免CCS条件断点，让单路口实验只需Build/Debug/Run并自动结束。
- user constraint：只生成备份源码，不修改活动`empty.c`、不清理或配置CCS、不自动Build/烧录；用户自行复制覆盖。
- RED：新增`tests/test_v97_single_turn_auto_stop_source.py`。目标备份缺失时因FileNotFound按预期失败；随后测试又准确捕获完成标志误插入`medicine_follow_step()`的问题，以及故障优先级尚未实现的问题。
- implementation：从V96生成V97备份；首次90度左转进入时`g_turn_count=1`，仅在`medicine_turn_step()`报告完成且mission推进到action 2后设置`g_single_turn_test_complete=1`。主任务循环自动退出并在永久idle loop前执行`motors_stop()`。
- safety correction：完成帧仍可能触发control overrun；最终post-loop先处理`g_mission.fault`并保留已有stop reason，只有无故障时才写`STOP_REASON_TEST_COMPLETE`，避免掩盖真实故障。
- verification：V97源码契约2/2、完整路线Host 19/19、极简循迹9/9、TI Arm Clang 4.0.4 `-Wall -fsyntax-only` 0 diagnostics；最终只读复审0 CRITICAL/HIGH/MEDIUM。
- deliverable：`ccs_project/medicine_car_2021f/backup_code/20260723_v97_single_turn_auto_stop_empty.c`，SHA256=`68C1E77D083FA4D3FEB03F70FA870C0FF9B503D7662427F7DB9AE9A1EAC63BDB`。正常停车预期为`g_firmware_version=97`、完成标志1、action index 2、路口事件1、转弯计数1、route fault 0、stop reason 1、左右PWM 0。

## 2026-07-23 V98循迹负反馈方向修复：补充召回

- prior status：V97已实现药房红线直接启动和首个90度转弯后自动停车，但首次实车运行持续右偏直至完全离开红线。
- evidence：V97沿用V91的`left=base+correction/right=base-correction`；V90/V91记录明确说明纠偏方向尚未实车验证。此前已由用户验证可循迹的稳定基线使用相反的`left=base-correction/right=base+correction`，电机及PWM底层接线相同。
- decision：只在V98单文件调用`simple_line_step()`时反转当前灰度误差和最后有效误差，不改共享头文件、PWM、增益、灰度极性、路线、MPU或自动停车逻辑。
- delivery constraint：只生成并验证备份源码，用户自行复制覆盖`empty.c`；不修改活动工程、不配置CCS、不自动Build或烧录。

## 2026-07-23 V98循迹负反馈方向修复完成

- RED：新增`tests/test_v98_line_feedback_sign_source.py`，目标备份不存在时按预期因FileNotFound失败。
- implementation：由V97生成单文件V98；版本升至98，新增`LINE_SENSOR_ERROR_SIGN=(-1)`，仅在`simple_line_step()`入口对`g_gray_line_error`和`g_line_last_nonzero_error`同时反向。Gray原始Watch和路口/端点居中判断继续使用未翻转值。
- direction evidence：实际控制适配测试确认原始正误差输出`correction=-69、left=311、right=452`，原始负误差输出`correction=69、left=449、right=314`，与实车验证通过的稳定循迹差速方向一致。
- regression：V98源码契约3/3，其中完整文本变换测试保证V97→V98仅包含版本、符号宏和两个adapter输入；完整路线Host 19/19、极简循迹9/9、TI Arm Clang 4.0.4 `-Wall -fsyntax-only` 0 diagnostics。
- review：最终只读复审无CRITICAL/HIGH/MEDIUM/LOW；int16先提升为int32再乘-1，类型安全；PWM、增益、固定白底、编码器、MPU、路线和首转自动停车均未改变。
- deliverable：`ccs_project/medicine_car_2021f/backup_code/20260723_v98_line_feedback_sign_fix_single_turn_empty.c`，SHA256=`07AF75F620AEF28A6DB00B2955E83464FED6A8C06501D04AFE32FF381A03A7B9`。活动`C:\Users\1\Desktop\ELECTRIC\mock_problem\empty.c`未修改。

## 2026-07-23 V99首个路口提前触发修复完成

- supplemental recall：V98实车已不再持续右偏，但约10 cm后停车；代码显示生产路口检测在30个10 ms帧后即可重新武装，首个药房起点宽红线可能被误判为路口，并触发V98首转自动停车。
- RED：新增`tests/test_v99_first_junction_gate_source.py`，目标V99备份不存在时按预期FileNotFound失败；复审又发现初版180帧配置若全局使用会影响后续端点，因此补充了范围测试。
- implementation：V99由V98生成。新增首段专用`FIRST_JUNCTION_ROUTE_EVENT_CONFIG`，`min_travel_frames=180U`；仅当`g_route_action_index==0U`时使用，后续所有路口/端点仍使用默认30帧配置。PWM、反馈符号、增益、固定白底、编码器、MPU和首转自动停车均不变。
- verification：源码契约2/2、首段动态门控测试PASS、路线Host 19/19、循迹9/9、TI Arm Clang 4.0.4 `-Wall -fsyntax-only` 0 diagnostics；最终复审无CRITICAL/HIGH/MEDIUM。
- deliverable：`ccs_project/medicine_car_2021f/backup_code/20260723_v99_first_junction_gate_empty.c`，SHA256=`37CA49314849CFE689B1CA86CA61B7AE19CA8A1C5A7706F7C74ADA85D5427836`。

## 2026-07-23 V100路口转动中心几何补偿：补充召回

- prior status：V99已修复药房起点宽红线误判，保留首段180帧门控、V98负反馈方向、首个90度转弯后自动停车。
- hardware measurement：用户实测灰度传感器有效中心到左右驱动轮轴连线中点约`17.5 cm`；按`664 edges/cm`换算为`11620 edges`。
- decision：90度路口检测后，使用编码器控制继续前进17.5 cm，使驱动轮轴中点到达路口中心后再原地转弯；不把22 cm整车长度直接加到所有路线，不给病房端点后的180度掉头增加该补偿。
- constraints：只生成V100备份源码和验证测试，不修改活动工程`empty.c`，不配置CCS、不自动烧录。
- next action：先建立V100源码契约与距离门控测试并验证RED，再由V99生成V100，执行回归与TI Arm Clang语法检查。

## 2026-07-23 V100路口转动中心几何补偿完成

- RED：新增`tests/test_v100_encoder_turn_center_source.py`；备份不存在时4项测试均按预期FileNotFound失败。
- implementation：从V99重建完整V100；将灰度中心到轮轴中点的`175 mm`换算为`11620 edges`，90度路口使用编码器进度门控前进，达到目标后切换到陀螺仪SETTLE/ROTATE；180度掉头保持零前进补偿。
- safety：复用`route_progress_guard`检查前进方向、双轮无运动、轮差和超程；故障统一`STOP_REASON_ENCODER_FAULT`并停车；V99首段180帧门控、V98负反馈方向、固定白底、首转自动停车均保留。
- verification：V100源码契约及既有测试18/18；路线Host 19/19；极简循迹9/9；TI Arm Clang 4.0.4 `-fsyntax-only -Wall` 0 diagnostics；只读复审无CRITICAL/HIGH/MEDIUM代码缺陷。4秒转弯总超时仍需实车确认17.5 cm居中速度。
- deliverable：`ccs_project/medicine_car_2021f/backup_code/20260723_v100_encoder_turn_center_17p5cm_empty.c`，SHA256=`C930B1E1637D72AE1ED9B963A6359B6FFA174B3C2292B584CDB0AE1CF0F0E461`；活动工程未修改。

## 2026-07-23 V101路口触发与居中航向修复：补充召回

- prior status：V100以17.5 cm/11620 edges完成路口转动中心补偿，但实车多次转弯位置离散且转后中轴未对准病房支线。
- confirmed causes：路口宽线需2帧确认；事件发生后先停机、下一控制周期才记录居中编码器起点，滑行未计入；居中阶段PWM 300/303开环，只看双轮平均距离，没有航向保持。
- requested changes：宽线确认改为1帧；在路口事件瞬间锁存编码器并主动刹车，使滑行包含在11620-edge总距离内；居中阶段使用陀螺仪航向误差动态修正左右PWM。
- constraints：只生成下一版备份，不修改活动工程、不配置CCS、不烧录；保留17.5 cm、首段180帧门控和首转自动停车。

## 2026-07-23 V101路口锁存与航向保持完成

- RED：新增`tests/test_v101_junction_latch_heading_source.py`，目标备份缺失时5项源码测试按预期失败；后续补充滑行计入和trim方向模型，共7项V101专项测试。
- implementation：首段及后续路口`wide_confirm_frames`统一为1；检测到路口时先原子锁存左右编码器，再执行TB6612主动短刹，下一动作从事件快照计算11620 edges，使刹车/滑行边沿包含在总补偿内。
- heading control：CENTER阶段同时积分MPU Z偏航，并结合左右编码器里程差生成trim；正yaw或右轮超前时增加左PWM、减小右PWM，trim限幅±60，实际居中PWM范围安全。
- verification：全部Python测试25/25；路线Host19/19；循迹9/9；TI Arm Clang 4.0.4语法0 diagnostics；代码复审CRITICAL/HIGH/MEDIUM/LOW均为0。
- deliverable：`ccs_project/medicine_car_2021f/backup_code/20260723_v101_junction_latch_heading_hold_empty.c`，SHA256=`4784481B95D79303E75712AFB53275C48F31B9DD67B2BAAE7F1700FD0842B576`。活动工程仍为V100，未修改。

## 2026-07-25 V102路口阈值调整：补充召回

- prior status：V101已经实现路口单帧确认、事件瞬间编码器锁存、主动刹车和17.5 cm居中航向保持，活动工程未自动修改。
- requested change：将首路口与后续路口的`wide_active_min`统一从`7U`降为`4U`，即`g_gray_changed_count >= 4`即可进入宽线/路口判断。
- constraints：只生成新备份；不修改活动工程、不操作CCS、不改其他控制参数。
- RED：新增V102源码契约测试并对V101执行，因固件版本仍为101、两处阈值仍为7而按预期失败。
- implementation：从V101复制生成V102，仅更新文件头版本说明、`FIRMWARE_VERSION=102U`，以及首路口/后续路口两处`wide_active_min=4U`；直接比较仍为`g_gray_changed_count >= event_config->wide_active_min`。
- GREEN：V102专项源码契约2/2、路线Host19/19、极简循迹9/9、TI Arm Clang 4.0.4语法0 diagnostics；只读差异检查确认无其他控制参数变化。
- deliverable：`ccs_project/medicine_car_2021f/backup_code/20260725_v102_junction_threshold_4_empty.c`，SHA256=`AA6138CE01B954DA9B66671906AB20A23EABF67472654EA265587F79FC8BA2FA`。活动工程`empty.c`仍为V101，未修改。

## 2026-07-25 V102实车视频诊断与V103修复准备

- evidence：17.37秒视频中，小车约从第8秒开始沿红线前进并穿过十字路口，未出现持续单向脱线；主要异常是路口后继续直行，视频结束前未看到90度转弯。
- confirmed coupling：`route_event_step()`使用`wide_active_min`同时定义`normal_line`和路口宽线；`medicine_follow_step()`又用同一阈值决定是否跳过`simple_line_follow_update()`并固定PWM直行。V102从7降至4因此不仅降低路口触发门槛，也会使4至6路状态无法按原逻辑完成武装或循迹修正。
- decision：V103只在单文件中拆分阈值：未武装阶段和明确宽线旁路仍使用7，检测器已武装后使用4触发路口；不修改共享头文件和活动工程。
- RED/GREEN：V103首轮源码契约因V102未拆分阈值而失败；实现后4/4通过。复审补充端点边界测试，确认初版会让已武装端点使用4的风险后再次RED，修正为仅“已武装且目标为路口”才使用4，最终专项5/5通过。
- implementation：新增`JUNCTION_TRIGGER_ACTIVE_MIN=4U`和`LINE_WIDE_ACTIVE_MIN=7U`。未武装阶段、所有端点动作及明确宽线直行旁路使用7；只有已武装路口动作把检测阈值切换为4。4至6路且尚未触发事件时继续执行`simple_line_follow_update()`。
- verification：路线Host19/19、极简循迹9/9、TI Arm Clang 4.0.4语法0 diagnostics；只读复审未发现其他控制参数变化。
- deliverable：`ccs_project/medicine_car_2021f/backup_code/20260725_v103_junction_trigger_decoupled_empty.c`，SHA256=`97EF1457F69091FAD9F10FD18C78AA1CF3B4FDBD0B7E9D45392F5DB0C9B08939`。活动工程仍为V102，未修改。

## 2026-07-25 V103实车视频诊断与V104准备

- evidence：新视频时长16.72秒；小车从药房起点出发约1.5秒到达首个十字路口，之后继续沿直线到达线路末端，末端原地转向。该末端转向是丢线找回动作，不是首路口任务90度转弯。
- confirmed cause：V103的`FIRST_JUNCTION_MIN_TRAVEL_FRAMES=180`对应至少1.8秒，而首路口更早到达；首路口检测器未武装，8路宽线被按未武装宽线直行处理，离开路口后才完成武装。
- decision：V104在`medicine_follow_step()`更新编码器进度后，若首段平均前进边数达到25 cm、且`normal_frames>=5`，直接武装首路口；保留V103的4路已武装路口触发、7路正常线/端点界限和其他参数。
- RED：新增V104首路口距离门控契约测试，V103按预期因仍为固件104缺失、无距离门控而失败。
- implementation：新增`FIRST_JUNCTION_ARM_DISTANCE_CM=25U`及对应`664 edges/cm`距离阈值；`route_arm_first_junction_by_distance()`仅在首段、未武装、编码器达到25 cm且正常线帧达到5帧时置`armed=true`。原180帧配置保留为距离门控未满足时的安全后备，不再阻挡已满足距离门控的首路口。
- GREEN：固件源代码契约30/30、V104专项3/3、路线Host19/19、极简循迹9/9、TI Arm Clang 4.0.4语法0 diagnostics。活动工程当前为V103，未修改。
- deliverable：`ccs_project/medicine_car_2021f/backup_code/20260725_v104_first_junction_encoder_arm_empty.c`，SHA256=`9C39B718C272DBE252A11C103F2D251F35A9564F88864D26B35A908E3C825F12`。

## 2026-07-25 V105视频诊断修复与仓库合并

- evidence：两段实车录像的可见左转发生在首路口检测之后约17.5 cm，正常直线阶段蓝灯多为中央2路、偶尔3路；摄像机30 fps不能排除MCU 10 ms控制帧之间的4路瞬态。首路口附近还可见黑色病房边框，绿光灰度板不能单独可靠区分黑线和红线。
- confirmed cause：V104首段25 cm距离武装过早，且首路口使用`>=4`单次确认；外侧黑框或瞬态信号可在首路口前触发，触发后继续17.5 cm补偿使视频中的提前左转位置被掩盖。
- implementation：V105只改首路口门控：`FIRST_JUNCTION_ARM_DISTANCE_CM=50U`、`JUNCTION_WIDE_CONFIRM_FRAMES=3U`，并要求中央`X4/X5`同时有效；其他路口、17.5 cm转动中心补偿、事件瞬间编码器锁存、航向保持、V98负反馈方向、固定白底、MPU转向和首转自动停车保持不变。
- deliverable：`ccs_project/medicine_car_2021f/backup_code/20260725_v105_video_gated_first_junction_empty.c`，活动工作区快照为`ccs_project/medicine_car_2021f/active_project/`中的V104，未覆盖活动`empty.c`。

## 2026-07-25 GitHub项目合并

- 将远端`origin/master`的功能汇总提交与`origin/codex/add-openmv-conversation`的OpenMV会话记录快进合并到本地`master`。
- 新增`PROJECTS.md`和各项目`PROJECT_README.md`，明确2025 E题旧循迹/视觉工程、2021 F题智能送药小车和OpenMV/通用硬件验证的边界。
- 从`C:\Users\1\Desktop\ELECTRIC\mock_problem`复制源码和CCS元数据到2021 F题的`active_project/`快照；编译产物只保留本地，不进入提交。
