# OpenMV 靶心识别与矩形识别对话记录

- 日期：2026-07-16 至 2026-07-17
- 仓库：`jlg006728/electronic-design-contest`
- 主题：MSPM0G3507 流水灯、OpenMV 靶心识别、K230 矩形中心识别

> 本记录仅整理用户与 Codex 的可见对话和实际交付结果，不包含系统指令、内部工具日志、认证信息或本机私密路径。

## 1. CCS 创建 MSPM0G3507 流水灯工程

### 用户

> 打开 CCS 创建一个 MSPM0G3507 开发板实现流水灯的项目。

### Codex

在 CCS 21.0.0 中选择 `LP-MSPM0G3507 LaunchPad`，导入 `gpio_toggle_output` RGB LED 示例，并创建工程：

```text
running_led_mspm0g3507
```

选用 TI Arm Clang 4.0.2 编译器，将主程序修改为 LED1、LED2、LED3 依次点亮，最后在 CCS 中完成编译，输出：

```text
**** Build Finished ****
```

工程已创建和编译，未在该次对话中执行烧录。

## 2. 整合 OpenMV 颜色追踪 API 生成靶心识别代码

### 用户

> 参考仓库中的 `OpenMV颜色追踪API.md`，整合参考代码生成识别靶心的代码。

### Codex

读取了：

- `知识库/视觉模块/OpenMV颜色追踪API.md`
- `知识库/视觉模块/OpenMV_MSPM0_串口通信协议.md`
- `ccs_project/e_car/openmv/openmv_e_target.py`

在参考代码基础上设计了两阶段识别算法：

1. 通过 LAB 红色阈值和 `find_blobs()` 快速获取红色候选区域。
2. 使用面积、长宽比、密度和与画面中心的距离进行粗筛。
3. 仅在排名靠前的候选 ROI 中执行 `find_circles()`，减少计算量。
4. 用局部霍夫圆检测的圆心修正红色弧或红色中心块的坐标。
5. 圆边缘不清晰时，如果红色区域足够接近圆形，回退到色块质心。

串口继续使用 MSPM0 工程中的请求—响应协议：

```text
MSPM0 -> OpenMV: 'X' (0x58)
OpenMV -> MSPM0: [0x2C, detected, cx_l, cx_h, cy_l, cy_h, xor, 0x5B]
```

未识别到目标时：

```text
detected = 0
cx = 0xFFFF
cy = 0xFFFF
```

生成的脚本名为：

```text
openmv_bullseye_target.py
```

脚本已通过 Python 语法检查。实机使用前，仍需在 OpenMV IDE 中根据现场光照标定 `RED_THRESHOLDS` 和 `CIRCLE_THRESHOLD`。

## 3. K230 矩形代码的识别逻辑

### 用户

> 这个代码的识别逻辑是什么？

附件：`矩形8.3.1.py`

### Codex

该脚本不是通过颜色或圆形特征直接识别靶心，而是：

> 先识别靶纸外部矩形，再计算矩形两条对角线的交点，将其作为靶心。

主要流程：

1. 摄像头输出 480×320 RGB888 图像。
2. 调用 `cv_lite.rgb888_find_rectangles_with_corners()` 执行高斯模糊、Canny 边缘检测、轮廓提取和四边形拟合。
3. 以外接框的 `w × h` 为依据，选择面积最大的候选矩形。
4. 读取四个角点，计算四条边的长度和方向角。
5. 使用面积、最小边长、对边长度差、对边平行和邻边垂直条件过滤。
6. 连接 `c[0] -> c[2]` 和 `c[1] -> c[3]`，求两条对角线交点。
7. 用图像中心 `(240, 160)` 减去矩形中心，生成水平和垂直偏差。

偏差定义：

```python
wucha_xcenter = 240 - intersection[0]
wucha_ycenter = 160 - intersection[1]
```

识别成功时输出类似：

```text
[+012-034*]
```

识别失败时原代码输出：

```text
(x=999,y=999)
```

指出的主要问题：

- 只选择面积最大的矩形，容易将屏幕、窗框或其他纸张误识别为靶纸。
- 矩形面积使用外接框面积，不是实际四边形面积。
- 平行和垂直允许±30°误差，过滤较宽松。
- 没有验证矩形中心是否真的存在红点、圆环或十字标记。
- 对角线按无限直线求交，没有检查交点是否落在线段内。
- 成功和失败使用了两种不同的串口帧格式，接收端不易统一解析。

因此，这份脚本更准确的定位是“最大矩形几何中心识别”，而不是基于靶心特征的严格靶心识别。

## 4. GitHub 上传要求

### 用户

> 将对话上传至 GitHub 仓库 `jlg006728/electronic-design-contest`，不要覆盖已有文件。

### Codex

采用以下保护措施：

- 先同步远程 `master`。
- 在独立的 `codex/add-openmv-conversation` 分支操作。
- 使用带日期和主题的新文件名。
- 仅暂存和提交本对话记录，不使用 `git add -A`。
- 不删除、改名或修改仓库中已有文件。
