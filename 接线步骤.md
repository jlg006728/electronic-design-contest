# 接线步骤 — 按已购器件逐步验证

> 目标：每接完一个模块就验证一次，避免整车一次性接完后无法定位问题。
> 所有接线、换线、插拔模块操作都在**断电状态**下进行。

---

## 2026-07-14 当前整车权威接线

> 本节优先级最高。旧的 7 个独立 TCRT5000 已退出正式方案；后文保留的历史验证说明如与本节冲突，以本节为准。

### 电源分路

```text
2S 电池 7.4V+ -> 总开关 -> TB6612 扩展板电源输入/VM
2S 电池 7.4V- -> TB6612 GND -> 全系统公共 GND

2S 电池 7.4V+ -> LM2596 IN+
2S 电池 7.4V- -> LM2596 IN-
LM2596调至3.30V：OUT+ -> LaunchPad J11/3V3，OUT- -> J11/GND
TB6612 扩展板稳定 5V OUT -> 八路灰度板 5V
LaunchPad 3.3V -> 低电平触发蜂鸣器模块 VCC

独立稳定 5V 支路 -> OpenMV VIN、两个舵机红线
独立稳定 5V 支路 GND -> 公共 GND
```

- 你们当前若仍用 L298N 板载 5V 给 OpenMV/舵机供电，只能作为已实测稳定的临时支路；双舵机动作时必须保持约 `4.8V~5.2V`。最终优先换成足够电流的 5V 降压/UBEC。
- 两个 5V 电源的正极不要并联，只连接公共 GND。
- TI官方指定外部电源从 `J11/3V3` 直接给目标板供电；不要再用 `J10/5V` 作为脱离USB后的目标MCU供电入口。
- 外部3.3V与USB调试同时存在时，拔掉J101隔离排针中的 `3V3` 跳帽，保留GND、NRST、SWDIO、SWCLK；否则XDS110的3.3V与外部3.3V会并联。
- LM2596必须在断开LaunchPad时先空载调至 `3.30V`，接入后带载保持 `3.25V~3.35V`。
- TB6612 `3V3 OUT` 不再连接LaunchPad，避免此前约2.6V掉压再次影响目标MCU。
- 灰度板必须接 `5V`，不能接 LaunchPad `3V3`。

### MSPM0 引脚总表

| MSPM0 | 当前连接 |
|---|---|
| `PB0` | TB6612 `AIN1`，左电机方向 |
| `PB1` | TB6612 `AIN2`，左电机方向 |
| `PB2` | TB6612 `BIN1`，右电机方向 |
| `PB3` | TB6612 `BIN2`，右电机方向 |
| `PB4` | TB6612 `STBY` |
| `PA12` | TB6612 `PWMA`，左电机 PWM |
| `PA13` | TB6612 `PWMB`，右电机 PWM |
| `PB12/PB13` | 左编码器 `A/B` |
| `PB14/PB15` | 右编码器 `A/B` |
| `PB5/PB6/PB7` | 八路灰度板 `AD0/AD1/AD2` |
| `PA7` | 八路灰度板 `OUT` |
| `PA10` | MSPM0 TX -> OpenMV `P5/RX` |
| `PA11` | MSPM0 RX <- OpenMV `P4/TX` |
| `PA14` | 水平舵机信号 |
| `PA17` | 俯仰舵机信号 |
| `PB11` | 低电平触发蜂鸣器 `I/O`；LOW响、HIGH关闭 |
| `PA15` | 状态 LED |
| `PA8/PB8/PB9/PB10` | 当前释放，暂不接 |

`PA2~PA6` 继续禁用；`PA20` 是 SWCLK，也不作为普通外设脚。

### 八路灰度板六针接口

按 PCB 丝印接线，不按照片方向或杜邦线颜色猜测：

| 灰度板 | MSPM0/电源 |
|---|---|
| `5V` | TB6612 扩展板稳定 `5V OUT` |
| `AD2` | `PB7` |
| `AD1` | `PB6` |
| `AD0` | `PB5` |
| `OUT` | `PA7`，数字输入 |
| `GND` | 公共 `GND` |

地址关系：`AD2 AD1 AD0 = 000~111`，依次选择 `X1~X8/CH1~CH8`。安装时优先让 `X1` 位于车头左侧、`X8` 位于车头右侧；如果机械上装反，代码中反转通道顺序即可。

首次接 `OUT` 前，先让灰度板单独接 5V/GND，用万用表测 `OUT-GND` 的最高电压：

- 最高不超过约 `3.3V`：可直接接 `PA7`。
- 接近 `5V`：禁止直连；使用 `OUT -> 10kΩ -> PA7`，并在 `PA7 -> 20kΩ -> GND` 分压。

当前 V49 程序仍是七路直接读取，不能直接驱动这块复用灰度板；接线完成后先烧录“电机禁用的八通道扫描测试程序”。

---

## 0. 当前器件边界

本文只使用 `器件采购清单.md` 里已有/计划购买的器件，不假设额外 MOSFET、三极管、专用电源板或示波器。

已购/计划相关器件包括：

- LP-MSPM0G3507 LaunchPad
- WHEELTEC R3X 三轮底盘，含 2 个编码电机和 1 个 TB6612 驱动模块
- 2S 18650 电池盒，输出约 7.4V
- Micro-USB 数据线
- 杜邦线、万用表
- 5V 充电宝
- AMS1117-3.3V 模块
- MP1584 DC-DC 可调降压模块
- 亚博/Yahboom `YB-MYX05-V1.0` 八路灰度巡线板，接口 `5V/AD2/AD1/AD0/OUT/GND`
- 原 TCRT5000 独立小板仅保留作备件，不再接入正式整车
- MG90S 舵机 ×2
- OpenMV H7
- 405nm 可调焦激光头
- 5mm LED + 330Ω 电阻，红/蓝电源 LED + 1kΩ 电阻
- 按键、洞洞板、电容、扎带、热缩管等

---

## 1. 总原则

### 1.1 先分模块，后整车

推荐顺序：

```text
LaunchPad 单独烧录
→ TB6612 + 电机
→ 编码器
→ 巡线传感器
→ 舵机
→ OpenMV
→ 激光
→ 按键/状态 LED
→ 整机联调
```

不要一开始就把所有模块接上。

### 1.2 全系统必须共地

只要两个模块之间有信号线，就必须共地。

```text
LaunchPad GND
TB6612 GND
电池负极
MP1584 GND
5V 充电宝 GND
OpenMV GND
MG90S GND
八路灰度板 GND
编码器 GND
```

最终都要连通。

### 1.3 任何 7.4V 都不能直接进 MSPM0 GPIO

MSPM0 GPIO 是 3.3V 逻辑。  
禁止把电池 7.4V 接到任何 GPIO、`3.3V`、`5V`、传感器信号脚。

---

## 2. 当前阶段：电机与 TB6612 验证

你们目前已经连接了：

```text
MSPM0G3507
TB6612
电机
2S 18650 电池
```

先完成这一节，再接别的模块。

### 2.1 电机验证接线

| LaunchPad | TB6612 | 说明 |
|---|---|---|
| `PB0` | `AIN1` | 左电机方向 1 |
| `PB1` | `AIN2` | 左电机方向 2 |
| `PB2` | `BIN1` | 右电机方向 1 |
| `PB3` | `BIN2` | 右电机方向 2 |
| `PB4` | `STBY` | TB6612 使能，HIGH=工作 |
| `PA12` | `PWMA` | 左电机 PWM |
| `PA13` | `PWMB` | 右电机 PWM |
| `LaunchPad GND` | `TB6612 GND` | 共地 |
| `LaunchPad J11/3V3` | `LM2596 OUT+ = 3.30V` | 独立运行时直接给目标MCU供电 |
| `电池 7.4V+` | `TB6612 VM` | 电机电源 |
| `电池 7.4V-` | `TB6612 GND` | 电机电源地 |

> LM2596外部3.3V接J11时，USB调试必须断开J101中的3V3跳帽，不能让两个3.3V稳压输出硬并联。

电机输出：

```text
TB6612 AO1/AO2 → 左电机
TB6612 BO1/BO2 → 右电机
```

如果方向反了，交换该电机的两根线。

### 2.2 上电前检查

断电状态下检查：

- [ ] 电池正负没有接反
- [ ] `TB6612 VM` 没有接到 LaunchPad
- [ ] `TB6612 5V OUT` 空载及电机运行时都稳定在约 5V
- [ ] `LM2596 OUT` 及 `LaunchPad J11/3V3` 为约3.25V-3.35V
- [ ] `LaunchPad GND`、`TB6612 GND`、`电池负极` 连通
- [ ] 电机悬空，轮子不接触桌面

上电后用万用表测：

| 测量点 | 正常值 |
|---|---|
| `TB6612 VM-GND` | 约 7.0V-8.4V |
| `TB6612 5V OUT-GND` | 约 4.8V-5.2V |
| `LM2596 OUT+-OUT-` | 约 3.25V-3.35V |
| `LaunchPad J11/3V3-GND`（USB断开） | 约 3.25V-3.35V |
| `PB4/STBY-GND` | 程序运行后约 3.3V |
| `PA12/PWMA-GND` | 有高电平或 PWM |
| `PA13/PWMB-GND` | 有高电平或 PWM |

### 2.3 手动硬件验证：不用 PWM

这一步用于确认电池、TB6612、电机本体没有问题。

断电后接线：

```text
TB6612 STBY → 3.3V
TB6612 PWMA → 3.3V
TB6612 PWMB → 3.3V
```

左电机正转：

```text
AIN1 → 3.3V
AIN2 → GND
```

右电机正转：

```text
BIN1 → 3.3V
BIN2 → GND
```

如果这样电机能转，说明：

```text
电池 OK
TB6612 OK
电机 OK
```

如果这样也不转，先不要查代码，优先查 `VM/VCC/GND/STBY/PWMA/PWMB`。

### 2.4 GPIO 满速验证

这一步用于确认 MSPM0 的 GPIO 能控制 TB6612。

使用已保存的测试文件：

```text
C:\Users\1\Desktop\motor_gpio_fullspeed_test.c
```

现象：

```text
PA12/PA13 被当作 GPIO 高电平
PWMA/PWMB 得到 3.3V
双电机满速正转
```

如果这一步能转，说明 `PA12/PA13 → PWMA/PWMB` 接线没有问题。

### 2.5 PWM 调速验证

PWM 版需要注意三个关键点：

```text
PA12 = IOMUX_PINCM34_PF_TIMG0_CCP0
PA13 = IOMUX_PINCM35_PF_TIMG0_CCP1
TIMG0 配置完成后必须调用 DL_TimerG_startCounter(TIMG0)
```

常见错误：

```text
只配置了 TIMG0 PWM，但没有 start counter
→ PA12/PA13 没有 PWM
→ PWMA/PWMB 等价于 0%
→ 电机不转
```

验证通过的现象：

```text
30% 占空比转 3 秒
60% 占空比转 3 秒
90% 占空比转 3 秒
停止 2 秒
循环
```

---

## 3. 电源规划

### 3.1 调试阶段推荐供电

调试时最稳的供电方式：

```text
LaunchPad → 电脑 USB 供电
TB6612 VM → 2S 18650 电池 7.4V
所有 GND 共地
```

这适合下载程序、单独验证电机、编码器、传感器。此时若J101的3V3跳帽安装着，不要再把TB6612 3V3 OUT接到LaunchPad 3V3。

### 3.2 后续整车供电

根据已购器件，推荐：

```text
控制回路:
2S 18650 → 开关1 → TB6612 VM
2S 18650 → LM2596 IN+/IN-
LM2596 OUT调至3.30V → LaunchPad J11/3V3和J11/GND
TB6612稳定5V OUT → 八路灰度板5V
LaunchPad GND、TB6612 GND、灰度板GND共地

同时连接USB调试时:
拔掉J101隔离排针中的3V3跳帽
保留J101的GND、NRST、SWDIO、SWCLK

瞄准回路:
5V 充电宝 → 开关2 → 舵机/OpenMV
```

LM2596只负责LaunchPad 3.3V控制电源，不与TB6612 3V3 OUT并联，也不同时给5V舵机/OpenMV供电。

MP1584 的定位：

```text
有 5V 充电宝时，优先用充电宝给舵机/OpenMV 供电。
不用充电宝时，才用 MP1584 将 7.4V 降到 5.0V。
```

### 3.3 MP1584 预调

如果使用 MP1584：

```text
电池 7.4V+ → MP1584 IN+
电池 7.4V- → MP1584 IN-
```

先不接舵机和 OpenMV，只接万用表。

- 调节电位器到 `5.0V ±0.1V`
- 调好后断电
- 再接负载

### 3.4 AMS1117 注意事项

AMS1117-3.3V 可用于给 3.3V 小负载供电，但不要把它当作大电流电源。

禁止：

```text
7.4V → LaunchPad 5V
7.4V → LaunchPad 3.3V
7.4V → 任意 GPIO
```

---

## 4. 编码器接线

### 4.1 接线表

| 编码器 | LaunchPad |
|---|---|
| 左轮 A 相 | `PB12` |
| 左轮 B 相 | `PB13` |
| 右轮 A 相 | `PB14` |
| 右轮 B 相 | `PB15` |
| 编码器 VCC | `3.3V` |
| 编码器 GND | `GND` |

颜色可能随厂家变化，不要只按颜色接，优先看卖家说明或线标。

> 当前代码不使用 `PC0-PC3`。本地 MSPM0G3507 SDK 头文件未提供 `GPIOC` 端口对象，写 `PC0-PC3` 会直接编译失败，所以编码器改接 `PB12-PB15`。

### 4.2 验证

先不让电机自动转，手动慢转轮子：

- A/B 相对 GND 应在 `0V` 和 `3.3V` 之间变化
- 用程序计数时，输出轴一圈理论边沿数约：

```text
500线 × 30减速比 × 4倍频 = 60000 边沿/轮圈
```

如果只用单 A 相双沿，计数会低于 4 倍频结果，按代码定义确认。

---

## 5. 八路灰度巡线板接线

### 5.1 接口与选通

本车正式使用 `YB-MYX05-V1.0` 无 MCU 八路数字灰度板。板上只有一根复用数字输出 `OUT`，由三根地址线选择当前通道：

| `AD2` | `AD1` | `AD0` | `OUT` 对应 |
|---:|---:|---:|---|
| 0 | 0 | 0 | `X1/CH1` |
| 0 | 0 | 1 | `X2/CH2` |
| 0 | 1 | 0 | `X3/CH3` |
| 0 | 1 | 1 | `X4/CH4` |
| 1 | 0 | 0 | `X5/CH5` |
| 1 | 0 | 1 | `X6/CH6` |
| 1 | 1 | 0 | `X7/CH7` |
| 1 | 1 | 1 | `X8/CH8` |

正式接线：

```text
灰度板 5V  -> 稳定 5V
灰度板 AD2 -> PB7
灰度板 AD1 -> PB6
灰度板 AD0 -> PB5
灰度板 OUT -> PA7（确认电平后直连或分压）
灰度板 GND -> 公共 GND
```

原独立传感器的 `PA8/PB9/PB10` 不再使用。不要同时保留旧七路模块和新灰度板。

### 5.2 安装

- 探头朝下，PCB 与地面平行。
- `X1` 作为最左，`X8` 作为最右；板中心 `X4/X5` 之间对准车体中心线。
- 白光款首次把探头离地调到约 `16~20mm`，再在比赛白底与黑线上微调。
- 使用板上长孔、M2.5/M3 螺钉和尼龙隔柱固定，不在 PCB 上另行钻孔。

### 5.3 上电前验证

先禁用电机，逐个选择 `X1~X8`，记录白底、黑线和悬空时 `OUT`：

- 八路必须都能随黑白变化。
- 先根据实测确定 `OUT` 极性，代码不预设“黑=0”或“黑=1”。
- 电机运行后再复测一次，排除供电噪声和线束接触问题。

---

## 6. 舵机云台接线

### 6.1 供电

MG90S 不从 LaunchPad 5V 取电。

推荐：

```text
MG90S 红线 → 5V 充电宝输出 或 MP1584 5V 输出
MG90S 棕线 → 公共 GND
MG90S 橙线 → MSPM0 PWM 信号
```

### 6.2 信号线

`PA3/PA4` 舵机方案已经作废。TI 官方 `LP-MSPM0G3507` 原理图显示 `PA3-/PA4-` 到 MCU 真实引脚之间默认有 `DNC` 隔离电阻，且 MCU 端连接 32.768kHz 晶振。

当前状态：

| 用途 | 引脚 | 结论 |
|---|---|---|
| 单舵机临时验证 | `PA12` | 已验证舵机可以正常动作，但 `PA12` 正式被 TB6612 `PWMA` 占用 |
| 正式水平 X 舵机 | `PA14` | 2026-07-03 已验证可驱动舵机 |
| 正式俯仰 Y 舵机 | `PA17` | 2026-07-03 已验证可驱动舵机 |

正式整合前不要再把舵机信号接到 `PA2/PA3/PA4/PA5/PA6`。MSPM0 的 3.3V PWM 通常可以被 MG90S 识别，但前提是信号脚确实从 LaunchPad 排针输出到舵机。

### 6.3 验证

先只接一个舵机：

```text
1500us → 中位
1000us/2000us → 左右或上下偏转
```

确认不抖、不重启、不发热后，再接第二个。

---

## 7. OpenMV 接线

### 7.1 供电

```text
OpenMV VIN → 5V 充电宝 或 MP1584 5V
OpenMV GND → 公共 GND
```

不要用 LaunchPad 3.3V 直接供 OpenMV。

### 7.2 UART

```text
OpenMV P5 (RX) ← PA10 (MSPM0 TX)
OpenMV P4 (TX) → PA11 (MSPM0 RX)
```

注意 TX/RX 交叉：

```text
MSPM0 TX 接 OpenMV RX
MSPM0 RX 接 OpenMV TX
```

如果使用 UART0 和 OpenMV 通信，调试 printf 不要同时占用 UART0。

---

## 8. 激光模块接线

### 8.1 先确认激光头类型

已购 405nm 激光头大概率是两线模块：

```text
VCC
GND
```

如果只有两根线，**不能直接用 PA20 给激光供电**。  
MSPM0 GPIO 不能承担激光电流，直接接有烧 GPIO 风险。

### 8.2 当前已购器件下的安全接法

没有额外 MOSFET/三极管时，先用手动供电验证：

```text
激光 VCC → 对应规格电源
激光 GND → GND
```

用开关或插拔供电控制亮灭。

### 8.3 只有带 TTL/EN 信号脚的激光模块才接 MCU 控制脚

如果激光模块明确有：

```text
VCC
GND
TTL/EN/S
```

才可以接 MCU 控制脚，但不要接 `PA20`：

```text
TTL/EN/S → 重新选定的非 SWD、已验证可达 GPIO
VCC → 模块规格要求的 3.3V 或 5V
GND → 公共 GND
```

`PA20` 是 `SWCLK` 调试脚，后续不作为激光正式控制脚。否则 `GPIO → 激光` 这条线暂时不要接。

---

## 9. 蜂鸣器、按键与状态 LED

### 9.1 低电平触发有源蜂鸣器模块

当前程序使用 `PB11`，因此 `PB11` 不能再接 START 按键。

按模块丝印接线：

```text
模块 VCC -> LaunchPad 3.3V
模块 I/O -> PB11
模块 GND -> 公共 GND
可选：I/O 与 3.3V 之间加 10kΩ 上拉，避免复位瞬间误响
```

模块已经带三极管和输入电阻，`I/O` 不再串 330Ω，也不再使用旧的两针蜂鸣器直驱接法。控制逻辑固定为：

```text
PB11 = LOW  -> 响
PB11 = HIGH -> 关闭
```

先用 3.3V 供电验证。若改用 5V 供电，必须确认 MSPM0 的 3.3V 高电平能让模块完全关闭；否则保持 3.3V，不能把 5V 接入 PB11。

### 9.2 按键

当前集成代码没有启用外接 MODE/START 按键，暂不接。`PB8` 保留；后续增加按键时重新分配，不能占用 `PB11`。

### 9.3 状态 LED

BOM 里状态 LED 配的是 330Ω 电阻，接法：

```text
PA15 → 330Ω → LED 长脚(+)
LED 短脚(-) → GND
```

电源指示 LED：

```text
控制回路电源 LED → 1kΩ → GND
瞄准回路电源 LED → 1kΩ → GND
```

---

## 10. 最终检查清单

### 10.1 电源

- [ ] `TB6612 VM-GND` 为 7.0V-8.4V
- [ ] `TB6612 VCC-GND` 为 3.3V
- [ ] `LaunchPad 3.3V-GND` 为 3.3V
- [ ] `5V 充电宝/MP1584 输出` 为 5.0V 左右
- [ ] 所有 GND 连通

### 10.2 电机

- [ ] `PB4 → STBY`
- [ ] `PA12 → PWMA`
- [ ] `PA13 → PWMB`
- [ ] `PWMA/PWMB` 有高电平或 PWM
- [ ] 电机悬空测试完成后再装轮

### 10.3 信号线

- [ ] `PB0/PB1/PB2/PB3` 接 TB6612 方向脚
- [ ] `PB12-PB15` 接编码器
- [ ] 灰度板 `AD0/AD1/AD2 -> PB5/PB6/PB7`
- [ ] 灰度板 `OUT -> PA7`，且已确认高电平不超过 3.3V；否则已分压
- [ ] 原独立巡线模块和 `PA8/PB9/PB10` 已断开
- [ ] 舵机正式信号脚使用 `PA14/PA17`；不要使用 `PA3/PA4`
- [ ] `PA10/PA11` 接 OpenMV UART
- [ ] `PB11` 接低电平触发蜂鸣器 `I/O`，空闲时为 HIGH，未同时接按键
- [ ] `PA15` 接状态 LED
- [ ] 激光未确认 TTL/EN 前，不接 MCU GPIO；正式控制脚不要用 PA20

### 10.4 短路

断电后测：

- [ ] `7.4V-GND` 无短路
- [ ] `5V-GND` 无短路
- [ ] `3.3V-GND` 无短路

---

## 11. 当前推荐下一步

新八路灰度板接好后，下一步按顺序做：

```text
1. 断开或禁用电机
2. 测量灰度板 OUT 最高电压
3. 烧录八通道复用扫描测试程序
4. 逐个验证 X1~X8 的黑白响应和物理左右顺序
5. 再合入电机、编码器和 OpenMV 联调
```
---

## 2026-07-02 最新修正：暂时不要把舵机信号接到 PA3/PA4

官方 `LP-MSPM0G3507` 原理图显示：`PA3-/PA4-` 排针侧到 MCU 的 `PA3_LFXIN/PA4_LFXOUT` 之间默认有 `DNC` 隔离电阻，且 MCU 端连接 32.768kHz 晶振。因此此前“舵机信号接 `PA3/PA4`”的步骤作废。

当前现场规则：

- 舵机红线仍接稳定 5V。
- 舵机黑/棕线接公共 GND。
- 舵机信号线暂时不要接 `PA2/PA3/PA4/PA5/PA6`。
- 下一版测试应先选一个新的候选 GPIO，先烧录“常高/常低诊断程序”确认排针确实能输出 3.3V，再写舵机 PWM。
